# Fleets Subsystem — Developer Reference

**What it does:** Groups ships into named fleets so the player gives orders to the fleet, not to individual ships. A fleet is just an entity with a `FleetDB` blob — ships are assigned to it by entity ID. The fleet can have sub-fleets (tree structure), a flagship, and standing conditional orders.

**Why it matters for ground combat:** Ground forces need transport ships. Transport ships are in fleets. The invasion order ("land troops") is issued by a fleet in orbit over a target colony. Ground forces that travel between systems ride inside fleet-managed transport ships. **Phase 4 wires directly into this subsystem — read this before building the landing order.**

---

## Files

| File | Role |
|------|------|
| `FleetDB.cs` | The DataBlob. Holds flagship ID, parent fleet ID (tree hierarchy via `TreeHierarchyDB`), `StandingOrders` list, and `InheritOrders` flag. |
| `FleetFactory.cs` | Creates fleet entities. `FleetFactory.Create(manager, factionID, name)` — creates the entity with `NameDB`, `FleetDB`, `OrderableDB`, and sets `entity.FactionOwnerID` directly (no `OwnedDB` blob). |
| `FleetOrder.cs` | All player-issued fleet commands — `Create`, `Disband`, `ChangeParent`, `AssignShip`, `UnassignShip`, `SetFlagShip`, `ToggleInheritOrders`. Each is an `EntityCommand` with `ActionLaneTypes.InstantOrder`. These are executed by the generic `OrderableProcessor` (the order-execution path), which calls each command's `Execute()` — that's where ship membership actually changes (`AssignShip`/`UnassignShip`). **CYCLE GUARD (2026-07-16 freeze fix):** `ChangeParent` now refuses to reparent a fleet under itself or one of its own descendants (`WouldFormCycle`, a bounded parent-chain walk). Without it, the client fleet drag-drop (`FleetWindow.DisplayDropTarget`) let a player drop a parent fleet onto its own sub-fleet, forming a 2-node parent cycle — the malformation the combat fleet-tree walk (`FleetCombat.Collect`, inside `CombatEngagement.Tick`) recursed forever on (the "StarInfoDB TRUE WEDGE"). This is the SOURCE fix; the walk-side seen-set (see `Combat/CLAUDE.md`) is the backstop. `Disband` also got a `RemoveChild`-before-`AddChild` so a disbanding fleet's ship is never momentarily in two parents' lists. Gauge: `CombatFleetTreeSafetyTests.ChangeParent_UnderOwnDescendant_Refused_NoCycleFormed`. |
| `FleetOrderProcessor.cs` | An `IHotloopProcessor` (`RunFrequency` = 1 hr) keyed to `FleetDB`. It does NOT execute `FleetOrder` commands — it evaluates a fleet's `StandingOrders` (conditional orders): when a standing order's condition is true and the fleet has no pending actions, it clones that order's actions onto the fleet's `OrderableDB` action list. |
| `FleetTools.cs` | Read-only UI helpers so the MAP can draw a fleet as ONE icon (matching how the engine already treats it as one unit). `CollapsedFleetMemberShipIds(manager, factionId)` = the ship ids to hide (every ship in a 2+ ship fleet except its flagship/first representative); `FleetShipCountFor(...)` backs a "Fleet (N)" label. Pure queries, defensive (render-thread safe). Gauge: `FleetCollapseTests`. Client wiring: `Pulsar4X.Client/CLAUDE.md` → "Fleet-as-one-icon". **B-2c1: `AllShipsRecursive(fleet)`** — every ship under a fleet walking DOWN through sub-fleets (recursion depth-capped at 8). The move orders (`WarpMoveCommand`, `MoveToSystemBodyOrder`) now enumerate ships through this instead of `Children.Where(is-ship)`, so a fleet decomposed into role sub-fleets still moves as ONE (nested ships come along). Byte-identical for a flat fleet (same ships, same order). |
| `FleetRoleComposer.cs` | **Phase B-2a/b (sub-fleet roles, Q7)** — sorts a fleet's ships into the four fighting jobs `FleetRole { Screen, Line, Artillery, Support }` by reading each ship's `Combat.ShipCombatValueDB`. `ClassifyRole(ship)` decides in priority order: utility hull (RoleWeight < 1.0) → Support; nimble (Evasion ≥ 0.5) → Screen; long-reach (`MaxWeaponRange` ≥ 400 km, i.e. railgun/missile class) → Artillery; else the gunline → Line; an unrated hull → Support. `PlanRoleSubFleets(fleet)` buckets `FleetCombat.Ships` into a Dict with all four roles keyed (pure read). **B-2b: `FormRoleSubFleets(fleet)`** actually FORMS the structure — one child `FleetDB` per non-empty role among the parent's DIRECT ship children, ships moved in (`SetParent`/`RemoveChild`/`AddChild` — the internal engine-side tree mutators, the same way `ColonyFactory` builds the start fleet, NOT through orders), the sub-fleet's `FlagShipID` set on its first ship, and a `FleetRoleDB` tag attached. Returns the role→sub-fleet map. Sorts direct ship children only (skips nested sub-fleet nodes, so a detach can't miss and leave a ship in two lists); co-locates each sub-fleet in the ships' `StarSystem` manager; leaves the parent's `FlagShipID` pointing at its now-nested flagship (nulling it would trip `MoveToSystemBodyOrder`'s `FlagShipID == -1` guard). **Wired but default-off (2026-08-09 correction — the earlier "no game-loop caller" line was stale): the caller EXISTS — `NPCDecisionProcessor.cs:819` forms role sub-fleets (and `FleetAssembly` uses the same recipe), gated behind the default-off `EnableOrderEmission`, so a STOCK game is still byte-identical.** B-2c wires it into AI battle-prep + gives each sub-fleet its own doctrine (and MUST add the `Parent == null` top-level-only guard at `NPCDecisionProcessor.RunFleetDoctrinePolicy` + the combat-enrol `Parent == null` guard in `CombatEngagement.Tick` — deferred to the wiring slice per the byte-identity discipline). Reads `[JsonIgnore] ShipCombatValueDB.MaxWeaponRange` (max `WeaponProfile.Range_m`, 0-range ignored). Gauge: `FleetRoleCompositionTests` (classification + `FormRoleSubFleets` conservation/discoverability/re-run/combat-seam/save-load). |
| `FleetComposition.cs` | **Fleet-composition ladder (2026-07-16, slice 1)** — `FleetCompositionTier { Forming, Deployable, Ideal, Perfect }` + `FleetCompositionTemplate` (per-faction: `MinToDeploy`/`IdealSize`/`PerfectSize` + `TierFor(shipCount)` + `TargetCountFor(aspiration)`). The growth ladder a **HOLISTIC** fleet climbs as the AI feeds it ships and resources allow — a fleet is never single-role; it aims to hold all the fighting jobs as sub-fleet formations via `FleetRoleComposer`. Tiers are SIZE/DEPTH (deploy at the min core → grow to ideal + reorganise into role sub-fleets → perfect + sub-sub-fleets), NOT role quotas; role split is emergent, EMPHASIS rides the per-fleet doctrine. Default `DefaultStrikeFleet` = 3/8/18. Pure data; the AI wiring (build-toward-composition, deploy-at-min, resource-gated escalation) is slices 2–3. Gauge: `FleetCompositionTests`. |
| `FleetCompositionDB.cs` | **Slice 2 (2026-07-16)** — the passive MARKER blob the AI tags a FORMING fleet with (the twin of `FleetRoleDB`): stores which ladder it climbs (`TemplateName`/`MinToDeploy`/`IdealSize`/`PerfectSize` — the template's numbers, so a per-faction ladder survives save/load; the plain `FleetCompositionTemplate` isn't serialisable) + a `Deployed` latch (flips ONCE at the deploy floor). **+ Slice 3:** an `Aspiration` tier (how big the AI is currently aiming to grow this fleet — set from treasury+war each cycle, read back by the massing rung as the CAP). `Template` rebuilds the template; the current tier is computed from the live ship count, never stored. No processor keyed to it → inert until the AI forms a fleet. A hand-made player fleet never carries it — so it's how the AI finds its own forming strike fleets. Name LOCKED (L3, save-embedded). Real `Clone()`. |
| `FleetAssembly.cs` | **Slice 2 (2026-07-16) — THE MISSING LINK: the AI assembles its BUILT warships into ONE holistic FORMING fleet.** `AssembleBuiltWarships(faction)` sweeps the faction's loose ARMED ships (the ones `ShipDesign.OnConstructionComplete` parks flat under the faction ROOT fleet — which lives in GlobalManager, so `FactionState.OwnedFleets()` never saw them → the strike fleet never massed) and folds them into a real, faction-parented, flagship-bearing IN-SYSTEM `FleetDB` per system, tagged `FleetCompositionDB`. GROWS an under-strength forming fleet before starting a new one (concentrate force: the most-ships fleet below its ASPIRATION target, skipping any mid-battle); flips `Deployed` at the min core. Uses the exact `FleetRoleComposer.FormRoleSubFleets` engine-side tree recipe (`GetChildren` snapshot → `RemoveChild`/`SetParent`/`AddChild` + `FlagShipID` + `Transfer`). Only ARMED hulls (Firepower>0) are swept — transports/freighters stay loose for their own logic. **+ Slice 3 — the RESOURCE-GATED ASPIRATION + the RESERVE SEAM:** `AspirationFor(balance, atWar)` is the pure decision (broke→Deployable / `IdealWealth` 50k→Ideal / `PerfectWealth` 150k→Perfect; war bumps ONE tier — "the situation calls for it"); `SetAspiration(faction, tier)` stamps it on every forming fleet. `FindGrowableFormingFleet` now caps a fleet's growth at its **aspiration target** (not PerfectSize) — so once fleet #1 is full, the next hulls **overflow into a SECOND fleet** (the home-defense RESERVE), instead of one ever-growing blob that gets committed whole. So a poor faction sizes its strike group at the 3-ship core + keeps a reserve; a wealthy/at-war one grows toward 18 + a reserve. Called by `NPCDecisionProcessor.RunFleetAssemblyPolicy` behind the default-off `EnableOrderEmission` gate (byte-identical off), and read back by `ConquerResolver` (Rung 3 `ShouldStopMassing` stops only once there's a complete strike fleet AND a reserve; Rung 1 `HasHomeReserve` won't sail the LAST organized fleet — the developer's "don't pull all fleets at once," a placeholder for the system military-commander delegate). **Deferred to a later slice: the standing PATROL order; the "Perfect→sub-sub-fleets" nesting** (role sub-fleets already form via B-2c2; a real nested squadron split needs a new mechanism, not recursive `FormRoleSubFleets` which is degenerate on a single-role fleet); **and the BATTALION/ground analog** (the same ladder + reserve rule applies to planetary units — the developer's call). **+ Slice B — PER-FACTION ladders:** `TemplateFor(faction)` reads the faction's OWN composition numbers off `Factions.FactionInfoDB` (`FleetMinToDeploy`/`IdealSize`/`PerfectSize`/`FleetTemplateName`, authored via the scenario `"fleetComposition"` JSON node), falling back to `DefaultTemplate` (3/8/18) for a faction with none — byte-identical for the engine tests (which build factions at the defaults). Fleet creation now stamps `TemplateFor(faction)` (not the shared `DefaultTemplate`) onto the fleet's `FleetCompositionDB`, so a UMF **Battle Line** (4/10/20) and a Kithrin **Raid Swarm** (3/6/12) grow to different sizes off ONE choke point (the reserve + massing logic already reads the per-fleet numbers, so nothing else changed). **+ SUPPORT/UTILITY HULLS (2026-07-17) — the AI now FIELDS the unarmed TENDERS it used to build and never use.** `AssembleSupportHulls(faction)` is the tender twin of `AssembleBuiltWarships`: it sweeps the faction's loose SUPPORT hulls off the root fleet and folds each into the faction's strongest FORMING fleet in the same system (via `FindEscortableFleet`), so a tender travels WITH the strike group and the monthly role/doctrine pass sorts it into the **Support sub-fleet** (unarmed → `FleetRole.Support`, hold-fire). A tender ESCORTS a fleet — it never forms one of its own, so with no strike fleet yet it's left loose. `IsSupportHull(ship)` = UNARMED (`ShipCombatValueDB.Firepower ≤ 0`) + a field cargo-transfer ability (`Storage.CargoTransferAtb` — a fleet oiler/collier, e.g. the base-mod Freighter's shuttlebay) + NOT a troop transport (`GroundCombat.GroundBayAtb` — those stay loose for the ConquerResolver invasion rungs); the transfer ability is what tells a real tender from a plain cargo hauler (storage-only) or a surveyor. A tender is NEVER counted a warship (`MilitaryComposition.WarshipCount`/`ArmedShipCount` see Firepower>0 only), so the strike threshold, Deployed latch and reserve logic are unchanged. Called by `NPCDecisionProcessor.RunFleetAssemblyPolicy` right after `AssembleBuiltWarships` (so a fleet exists to escort), behind the same default-off `EnableOrderEmission` gate → byte-identical off / with no tender built. **Deferred:** the actual in-field refuel/resupply (a tender only travels with the fleet today — `RefuelAction`/`ResupplyAction` are still stubs); a dedicated tender component marker if NPC logistics AI later wants to claim freighters for cargo routes; and folding tenders built in a DIFFERENT system than the strike fleet. Gauges: `FleetAssemblyTests` (slice 2), `FleetCompositionEscalationTests` (slice 3), `FleetCompositionPerFactionTests` (slice B), `FleetSupportHullTests` (support hulls). |
| `FleetRoleDB.cs` | **Phase B-2b** — the marker DataBlob that tags a fleet entity as an auto-formed ROLE sub-fleet, carrying its `FleetRole`. A hand-made player fleet never carries it, so it's how B-2c tells a role sub-fleet from a player fleet (`HasDataBlob<FleetRoleDB>()`) and reads `.Role` to pick its doctrine. Passive: no processor keyed to it, nothing reads it yet → inert until a sub-fleet is formed. Real `Clone()` copies `Role` (NOT `FleetDB.Clone()`'s blank-return). Name is **locked** (L3: `TypeNameHandling.Objects` embeds it in saves). |
| `RefuelAction.cs` | `EntityCommand` subclass — fleet-level refueling action. **DE-FANGED (A4, 2026-08-13):** behavior still PARKED (what's a supply source + transfer range? — ADJUDICATION QUEUE), but `Execute` now completes the order (`_isFinished = true`) so it can no longer WEDGE the fleet's blocking order lane (the old empty `Execute` + never-finishing `IsFinished` jammed it forever). `IsValidCommand` resolves the fleet via `CommandHelpers`. To finish: call `CargoTransferOrder.CreateRefuelFleetCommand(supply, fleet)`. Gauge `OrderStubSafetyTests`. |
| `ResupplyAction.cs` | `EntityCommand` subclass — fleet-level resupply action. **DE-FANGED (A4):** de-wedged like RefuelAction; behavior PARKED ("resupply" is under-specified — ordnance/magazine reload vs an Aurora MSP resource that doesn't exist here; no `CreateResupplyFleetCommand` helper yet). Gauge `OrderStubSafetyTests`. |
| `ServeyAnomalyAction.cs` | `EntityCommand` subclass — **BUILT (OPERATION BLUEPRINT-TO-STEEL B6, 2026-08-17): "survey the nearest anomaly."** An "anomaly" IS a `JPSurveyableDB` (a gravitational survey point — every generated system spawns ~30), so this order does NOT duplicate the working survey machinery — `Execute` FINDS the nearest un-surveyed anomaly in the fleet's own system (`FindNearestUnsurveyedAnomaly`, internal — flagship-nearest, per-faction `IsSurveyComplete`) and dispatches the SAME two orders the client's JP-survey menu issues: `WarpFleetTowardsTargetOrder.CreateCommand(fleet, target)` then `JPSurveyOrder.CreateCommand(faction, fleet, target)`. It's the combined "go survey the nearest anomaly" convenience neither existing order gives on its own (`MoveToNearestAnomalyAction` only MOVES; `JPSurveyOrder` only surveys a picked target), and the same primitive a player and the AI can both issue (one verb, both seats). `IsValidCommand` = the requesting faction owns the fleet AND the fleet is survey-capable (`HasJPSurveyAbililty`) — it does NOT require a live anomaly (a standing order persists; `Execute` is a safe no-op when nothing un-surveyed is reachable). **Save-safe: the target is resolved LAZILY at Execute (nothing entity-referencing stored → no serialized-target gap** that `JPSurveyOrder`/`GeoSurveyOrder` carry). Client-reachable via `OrderRegistry` ("Survey Nearest Anomaly" standing order). Misspelled class name kept (L3, save-embedded). A bare/fleet-less instance is still a safe no-op (the A4 safety property). Gauges: `ServeyAnomalyActionTests` (finds an un-surveyed anomaly / capability gate / dispatch no-throw) + `OrderStubSafetyTests` (bare no-op + Clone). |

---

## Data Model

```
FleetDB extends TreeHierarchyDB
  FlagShipID         int        — entity ID of the flagship ship
  InheritOrders      bool       — if true, uses parent fleet's orders
  StandingOrders     SafeList<ConditionalOrder>
```

A fleet entity has these DataBlobs: `FleetDB`, `OrderableDB`, `NameDB`. (No `OwnedDB` — ownership is carried by `entity.FactionOwnerID`, set directly in `FleetFactory.Create`.)

Ships are assigned to fleets through `FleetOrder.AssignShip()`. The membership change happens inside `FleetOrder.Execute()` (the `AssignShip`/`UnassignShip` cases call `navyDB.AddChild`/`RemoveChild` on the fleet's `FleetDB`) — NOT in `FleetOrderProcessor`, which only handles standing orders. The tree hierarchy (`TreeHierarchyDB`) tracks parent/child, so membership is expressed as the fleet's children rather than a separate member list.

---

## How Orders Work

All fleet commands go through `FleetOrder` (an `EntityCommand`). The pattern:

```csharp
var order = FleetOrder.AssignShip(factionId, fleetEntity, shipEntity);
StaticRefLib.OrderHandler.HandleOrder(order);
```

`FleetOrder` commands are executed by the generic `OrderableProcessor` (the order-execution path) — each command's `Execute()` runs when the order is processed. Orders are instant (`ActionLaneTypes.InstantOrder`), so they fire immediately. `FleetOrderProcessor` is a separate thing: an `IHotloopProcessor` (fires every game-hour) that evaluates a fleet's *standing/conditional* orders, not the one-shot `FleetOrder` commands above.

---

## Sub-Fleets (Tree Hierarchy)

`FleetDB` extends `TreeHierarchyDB`, which means fleets nest. A transport group can be a sub-fleet of the invasion fleet. When the parent fleet moves, sub-fleets with `InheritOrders = true` follow. Sub-fleets can be detached into independent fleets via `ChangeParent`.

This is the mechanism ground combat needs: the assault echelon (transports + troops) starts as a sub-fleet of the invasion fleet, detaches to land, then the escort holds orbit.

---

## Fleet Actions (Refuel / Resupply / Survey)

`RefuelAction`, `ResupplyAction`, and `ServeyAnomalyAction` are `EntityCommand` subclasses (NOT `INavAction`). They were all **stubs**; A4 (2026-08-13) DE-FANGED them (no wedge / no crash). **B6 (OPERATION BLUEPRINT-TO-STEEL, 2026-08-17) then resolved each against ENGINE REALITY:**

- **`ServeyAnomalyAction` — BUILT** (real "survey the nearest anomaly" behaviour — see its file-map row above + `ServeyAnomalyActionTests`).
- **`RefuelAction` — stays a de-fanged no-op, deliberately.** The developer's intent was "top the ship's own tank from its own cargo," but in this engine **a ship's burnable fuel IS its fuel cargo** — `NewtonThrustAbilityDB.TotalFuel_kg` is a cache reconciled from `CargoStorageDB` by `CargoTransferProcessor.UpdateMassFuelAndDeltaV` (there is no separate "tank"), so a self-refuel-from-cargo is a **no-op**. The only real refuel — pulling fuel from an EXTERNAL colony/station — is `CargoTransferOrder.CreateRefuelFleetCommand(source, fleet)`, already wired as the client's **"Refuel at ⟨colony⟩"** button. Wiring this stub to that would DUPLICATE the button. *(A physical fuel-tank-vs-cargo separation — Aurora-style — is a new mechanic awaiting a developer ruling, not a B6 order-wire.)*
- **`ResupplyAction` — stays a de-fanged no-op, deliberately.** Same shape: there is **no physical per-launcher magazine** to top up — `MissileProcessor.LaunchMissile` pulls each round straight from the ship's `CargoStorageDB` ("ordnance-storage") at fire time, and `GenericFiringWeaponsDB.InternalMagQty` is a free-reloading cooldown counter, not a store of rounds. The real rearm (ordnance from a base into the ship's hold) already exists as the client's **"Rearm at a base"** button. *(A true per-launcher magazine is a new mechanic awaiting a developer ruling.)*

So the two cargo-supply stubs are safe placeholders whose stated intent turned out to be a no-op or an already-shipped button; the landing action for ground combat will be a **new command** — `LandTroopsAction` or similar — that triggers when the fleet is in orbit over a target colony and the player issues the invasion order.

See `Movement/CLAUDE.md` for the movement/action pattern.

---

## Pulsar Status

Fleet management is **fully functional**. Ships can be created, assigned to fleets, given movement orders, and the fleet tree hierarchy works.

**What does NOT exist yet (Phase 4 will add):**
- Any ground-force-related fleet action (there is no "load troops" or "land troops" action)
- Any cargo-loading order that specifically tracks ground units as cargo (though the cargo system exists — `Storage/`)
- Fleet composition checks for "orbital dominance" (no concept of controlling the space over a colony)

---

## Phase 4 Hook Points

| Ground combat need | Hook here | Notes |
|-------------------|-----------|-------|
| Load ground units into transport | `FleetOrder` + cargo system | Ground unit entities go into the transport ship's `CargoStorageDB` |
| Move to target system | Fleet movement (already works) | No new code — fleet moves like any other fleet |
| Establish orbital dominance | New check in invasion order | Query enemy ships in same orbit zone over target colony |
| Issue landing order | New `LandTroopsAction : EntityCommand` (same shape as `RefuelAction`) | Fires when fleet in orbit, checks dominance, creates `GroundCombatDB` on colony |
| Detach escort sub-fleet | `FleetOrder.ChangeParent()` (already works) | Transports detach, escorts remain in orbit |

---

## Proposed Future Work — Fleet Combat Doctrine

A design (not built) splits a fleet into named **components** (Front Line / Flank / Rear Guard / Artillery) — most cheaply as **sub-fleets of this tree hierarchy** — each running a switchable **combat doctrine** (Offensive / Defensive / Utilitarian) with situational trade-offs, a game-time switch cooldown, and optional commander "operational discretion." Full design (integrated with combat System 4 — Fleet Doctrine — and System 6 — Commander Discretion): **`docs/combat/COMBAT-DESIGN.md`** → "System 4 (detailed design) — Fleet Components & Switchable Doctrine." Read it before adding any component/posture concept to `FleetDB`.
