# Ground Combat — Subsystem Reference

The planet-surface war layer: units you build, station in regions, move, and fight with — the ground echo of the space fleet/combat systems. Lives in `GameEngine/GroundCombat/`. **New 2026-07-04 (slice 5a).**

> **Read `docs/GROUND-COMBAT-MAP-DESIGN.md` first** — the locked design + the sub-slice roadmap (5a–5i). v1 target is **full tactical** (unit types + terrain + base-coverage + formations + a navigable click-to-place map), shipped as CI-gated sub-slices. This subsystem sits ON the region map (`Galaxy/PlanetRegionsDB` — the 4-slice ring per body).

---

## LOCKED PRINCIPLE — every buildable is a real building on the ground (2026-07-04)

The developer's rule: *"everything I build on a planet that's selectable in space is represented by an actual building on the planet itself."* The colony economy (`ComponentInstancesDB` installations) and this ground map are **two views of the SAME physical things** — nothing is abstract-only. So any new planet buildable must be a component (`CONVENTIONS.md` §6) AND carry a **region location** so it draws on the planet view. The reconciliation is **done (#5, 2026-07-04):** `GroundInstallations.LocateColonyInstallations` (hooked in `ColonyFactory.CreateFromBlueprint`) drops a colony's installations into its capital region's `Region.InstallationIds`, so the planet view draws them and fortification counts them. (Follow-up: locate DYNAMICALLY-built installations — those added post-creation via the colony economy — at their install site; v1 covers the start colony's buildings.) Full rationale + slice mapping: `docs/GROUND-COMBAT-MAP-DESIGN.md` → "LOCKED PRINCIPLE." This is *why* the infrastructure system exists: a located building can be bombarded, invaded, defended, and lost region-by-region; an abstract one can't be fought over.

## The model in one line

A planet body carries a **`GroundForcesDB`** (a roster of `GroundUnit`s). Each `GroundUnit` knows its **region**, its **owner faction**, and its **combat stats** — so one roster holds both sides of a contested world. Units are **built** at a colony through the normal industry rails (`GroundUnitDesign : IConstructableDesign`), **moved** region→region on the region graph's crossing-time edges (5b), **fought** by a strength-math resolver mirroring the space `AutoResolve` (5c), and **captured** by flipping `FactionOwnerID` (5d) — the same primitive as a ship/fleet.

## File Map

| File | Role | Status |
|------|------|--------|
| `GroundForcesDB.cs` | `enum GroundUnitType` (Infantry/Armor/Artillery), the `GroundUnit` **data object** (DesignId · Name · FactionOwnerID · RegionIndex · **HexQ/HexR** (hex position within the region, H2a) · **Domain** (Land/Water/Air movement, H2a) · **MovementSpeed** (V1 — multiplier on the base march pace, transit ÷= this so armour 2.0 crosses in half infantry's time; base pace tuned to ~1 day/region) · UnitType · Attack · Defense · MaxHealth · Health — a build-time snapshot, like a ship's cached `ShipCombatValueDB`), the **`GroundForcesDB`** blob on the planet body (`List<GroundUnit>`, deep-cloned + `[JsonProperty]` = save-safe from day one), and **`GroundForces.RaiseUnit(body, design, factionId, regionIndex)`** — the "place a unit on the surface" primitive (creates the roster on demand). **+ FORMATIONS (5h):** the `GroundFormation` data object + `GroundForcesDB.Formations` + `GroundUnit.UnitId`/`FormationId` + the `GroundForces` formation API (`CreateFormation`/`AssignUnit`/`UnassignUnit`/`SetLeader`/`DisbandFormation`/`OrderFormationMove`) + `GroundFormationTools` reads — the ground echo of a fleet (see "Formations" below). | ✅ 5a, 5h formations |
| `GroundDefenseAtb.cs` | The **ground-defence component attribute** (`IComponentDesignAttribute`) that makes a buildable installation a FORTIFICATION — the "depending on base design" rung. `LocalFortify` (hardens its region) + `AdjacentProjection` (shields adjacent friendly regions). Summed on demand (install/uninstall are no-ops). A Bunker carries it; a solar panel doesn't. | ✅ 5h (attribute) |
| `GroundStartGarrison.cs` | **The default HOME GARRISON** — `RaiseForFactionColonies(game, faction)` raises a modest combined-arms garrison (3 Infantry / 2 Armor / 1 Artillery, tunable `Composition`) in each of a faction's colony bodies' capital region, so a fresh New Game has ground units on the tactical map without DevTools. Called from `NewGameMenu.CreateGameCore` (the New-Game path, gated `AutoRaiseHomeGarrison`) — **NOT** from `ColonyFactory`, so the CI harness (`CreateWithColony`) stays a clean slate (same split as the space combat scenario). Idempotent (skips an already-garrisoned body). Gauge: `GroundForcesTests.StartGarrison_RaisedOnFactionHomeColony`. | ✅ default garrison |
| `GroundInstallations.cs` | **The LOCKED-principle reconciliation (#5):** `LocateColonyInstallations(colony)` drops a colony's un-placed installations into its **capital region** (region 0) `Region.InstallationIds`, so normal-economy buildings (not just map-placed ones) draw on the planet view + count for fortification. Idempotent (a located id is skipped, so map-placed buildings keep their chosen region). Hooked in `ColonyFactory.CreateFromBlueprint` after installations are added. Gauge: `GroundForcesTests.Installations_LocatedInCapitalRegion_AtColonyCreation`. | ✅ #5 |
| `GroundFortification.cs` | Turns a region's buildings into the defender's fortification multiplier (`DefenseMult`) — local + adjacent-friendly `GroundDefenseAtb`, capped at `Cap` (+100%). Resolves installation ids → the attribute via a `resolve` func so the MATH is unit-testable without JSON; `BuildResolver(body)` builds the real one from the body's colonies' components. Gauge: `GroundForcesTests.Fortification_DesignDriven_*`. | ✅ 5h (engine) |
| `GroundFormationDoctrine.cs` | The ground echo of `Combat/FleetDoctrine.cs` — read/set a formation's active STANCE from the moddable `GroundStanceBlueprint` catalog (`ModDataStore.GroundStances`). `TrySetStance` (honours the switch cooldown) + `AttackMult`/`DamageTakenMult` per-unit reads (via its formation) that `ResolveRegionCombat` consumes. ±25% trade-off catalog in `groundStances.json`. Gauge: `GroundForcesTests.FormationStance_*`. | ✅ 5h stance |
| `GroundUnitDesign.cs` | `GroundUnitDesign : IConstructableDesign` — a **buildable** ground unit. Rides the existing industry rails for free (research→queue→consume materials→complete). Its `OnConstructionComplete` mirrors `ComponentDesign`'s batch bookkeeping but PLACES a unit on the building colony's planet (via `RaiseUnit`) instead of installing a component. Defensive (a colony with no planet / no region layer / missing production line skips that part; never throws in the daily industry hotloop). Plus **`GroundForces.OrderMove(body, unit, toRegion)`** (in `GroundForcesDB.cs`) — the march order: sets the transit clock to the region's `CrossingTimeSeconds`, one adjacent hop at a time (5b). **+ HEX MOVEMENT (H2b):** hex-target **overloads** `OrderMove(…, toRegion, toQ, toR)` / `OrderFormationMove(…, toRegion, toQ, toR)` plot an A* route (`HexPathfinder`, per the unit's `Domain`) into the unit's new `GroundUnit.Path` (`List<HexWaypoint>`); the region-index overloads stay as the coarse fallback (client still uses them). | ✅ 5a/5b/H2b |
| `GroundForcesProcessor.cs` | The ground layer's **one** hotloop (`IHotloopProcessor` on `GroundForcesDB`, hourly). Does every surface job each tick — landmine L9 forbids multiple processors on one blob: **MOVE** (5b region-hop + **H2b HEX-PATH walk** — `AdvanceHexPath` steps a unit along its `Path` hex-by-hex, carrying overshoot so any tick length works and a full march totals the derived crossing time: the London→Paris transit), **ENVIRONMENTAL ATTRITION** (E3 — a unit standing in a region with a damaging hazard bleeds health, the ground twin of a ship in a space hazard; **E4 — the unit's environmental GEAR (`GroundUnit.ResistanceTo`) negates a fraction of that attrition**, so a heat-shielded unit survives fire that kills an unprotected one), **FIGHT** (`ResolveRegionCombat` — TERRAIN + TYPE + FORTIFICATION aware: the triangle × terrain-affinity × the defender's cover × the defender's **fortification** (`GroundFortification.DefenseMult` — DESIGN-DRIVEN: only buildings whose design carries a `GroundDefenseAtb` fortify, local + adjacent-friendly projection, capped, so a Bunker is a fortress and a solar panel isn't, 5h); mirrors the space `AutoResolve` salvo math over `GroundUnit`s, deterministic, 5c/5f/5g/5h), **CAPTURE** (region `OwnerFactionID` flip; all-regions → `TryCapturePlanet` flips the colony, 5d). Trivial ctor + `try/catch` body (never throws in a hotloop, L4). `SalvoScale` is the combat-pace dial. Gauge: `GroundForcesTests`. | ✅ 5b–5g, E3, E4, H2b |
| `HexMovement.cs` | **NEW (Hex ground H2a, 2026-07-04)** The `MovementDomain` enum (Land/Water/Air) + the pure `TerrainCost(domain, terrain)` — movement cost is UNIT-dependent (the developer's steer: a tank bogs in rough / can't cross ocean; an aircraft flies flat). Developer-approved numbers: LAND open ×1 / vegetated ×1.6 / elevated ×2.5 / ice ×2 / ocean ∞; WATER ocean+coast only; AIR flat (per-hex hazard cost is H3). Today's units are all Land; water/air *rules* exist so the pathfinder is domain-ready before the *units* are built. Gauge: `GroundForcesTests.HexMovement_TerrainCost_*`. | ✅ H2a |
| `HexPathfinder.cs` | **NEW (Hex ground H2a, 2026-07-04)** `FindPath(regionsDB, from(region,q,r), to(region,q,r), domain)` — pure A* over `Region.Hexes`, terrain-weighted by `HexMovement`, crossing region borders at **edge gates** (a region's east-edge hex bridges to the neighbour's west-edge hex, seam-free around the ring). Base step-time is **derived** from `Region.CrossingTimeSeconds ÷ patch diameter` (no new distance constant — a full march ≈ the coarse crossing time). Operates on `PlanetRegionsDB` (no entity) → unit-testable with hand-built terrain; deterministic. Also holds **`HexWaypoint`** (H2b) — the save-safe (`[JsonProperty]`+copy-ctor) stored twin of a `HexStep`, the element type of a unit's `Path`. Gauges: `GroundForcesTests.FindPath_*`. | ✅ H2a/H2b |
| `GroundTerrain.cs` | The ground twin of `SpaceHazardTools` — classifies a region's geography into Open/Cover/Rough and provides the combat dials: the classic **triangle** (Armor▸Infantry▸Artillery▸Armor), **terrain-type affinity** (`TerrainAttackMult` — armour bad in rough, artillery loves high ground), and the **cover** defence multiplier (`CoverDefenseMult` — a defender in rough takes less). Shares the hazard effect VOCABULARY in naming/docs without depending on the green hazard engine. Generation-agnostic: consumes whatever a region holds. Gauge: `GroundForcesTests` (triangle cycle · terrain math · rough-favours-defender). | ✅ 5f/5g |
| `PlanetEnvironmentsDB.cs` | The planet's **dynamic environmental hazards** (`RegionEnvironment` list on the body) — the ground twin of the space-hazard layer, REUSING the shared `HazardEffectType` vocabulary hosted on a region (by index). Fire tornadoes = `HeatDamage`, corrosive superstorm = `CorrosiveDamage`, dust/lightning = `SensorJam`. Persistent (`Clone`). `ForRegion(i)` yields a region's active hazards. Design: `docs/ENVIRONMENTS-DESIGN.md`. | ✅ E1 |
| `PlanetEnvironmentFactory.cs` | The **physics→hazard generator** (the intelligence). `GenerateForSystem(system)` reads a world's `AtmosphereDB` (temp/pressure/hydrosphere/composition) + `SystemBodyInfoDB` (body type/tectonics) and emits ONLY environments that make sense there (scorching→fire, sulphur air→corrosive, dry→dust, thick→lightning, tectonic→ash), **guaranteeing a qualifying menace in ≥1 region**. **★ The load-bearing gate: a gas/ice giant has no surface → NO surface environments** (`HasSurface`). Defensive/idempotent/RNG-seeded, mirrors `PlanetRegionsFactory`; hooked at all four `StarSystemFactory` gen paths (after regions). Gauges: `GroundForcesTests` (gas-giant gate · scorching→fire). | ✅ E2 |

## Formations — the ground echo of a fleet (5h, 2026-07-04)

A **`GroundFormation`** is a named grouping of units that move and fight as one — the ground echo of a `FleetDB`, mirroring its **shape** within the data-object model (the developer's call: *"mirror the fleet as much as possible"*; units aren't entities in v1, so a formation is a serializable data object too). The parity map:

| Fleet | Formation |
|-------|-----------|
| `FleetDB` (entity + `TreeHierarchyDB`) | `GroundFormation` (data object in `GroundForcesDB.Formations`) |
| ship's parent-fleet id (membership on the ship) | `GroundUnit.FormationId` (membership on the unit) |
| `FleetDB.FlagShipID` | `GroundFormation.LeaderUnitId` (references a `GroundUnit.UnitId` — a new stable id, the ground echo of an entity id) |
| `FleetFactory.Create` / `FleetOrder.AssignShip` / `UnassignShip` / `SetFlagShip` / `Disband` | `GroundForces.CreateFormation` / `AssignUnit` / `UnassignUnit` / `SetLeader` / `DisbandFormation` |
| fleet move order (moves as one) | `GroundForces.OrderFormationMove` (marches every member with the leader, one hop) |
| `FleetTools` (read helpers for the map) | `GroundFormationTools` (`MembersOf` / `MemberCount` / `Leader` / `FormationsFor`) |
| `FleetDoctrineDB` + `CombatDoctrineBlueprint` catalog (`FleetDoctrine.TrySetDoctrine`) | `GroundFormation` stance fields + `GroundStanceBlueprint` catalog (`GroundFormationDoctrine.TrySetStance`) |

**Leader loss = reassign, no penalty** (the locked decision — exactly what a fleet does): `GroundForcesProcessor.MaintainFormations` (runs after casualties are removed each tick) passes leadership to a surviving member, or -1 if the formation was wiped. Gauge: `GroundForcesTests.Formation_MirrorsFleet_CreateAssignMoveAndLeaderReassign`.

**Stance/doctrine ✅ built (2026-07-04) — a full moddable catalog mirroring the fleet's (`GroundFormationDoctrine.cs`).** A formation carries an active stance (id/family + cached `AttackMult`/`DamageTakenMult` + `SwitchableAfter` cooldown), set from the **`GroundStanceBlueprint` catalog** (`ModDataStore.GroundStances`, loaded from `GameData/basemod/TemplateFiles/groundStances.json`) via `GroundFormationDoctrine.TrySetStance` (honours the cooldown, like `FleetDoctrine.TrySetDoctrine`). The **±25% trade-off** (the developer's call): **Offensive** deals +25% / takes +25%, **Defensive** (Dig In) takes −25% / deals −25%, **Balanced** neutral. `ResolveRegionCombat` reads the mults **per unit** (via its formation): the attack pool × `AttackMult`, and each unit's incoming damage scaled by `DamageTakenMult` (the ground echo of the space resolver's per-ship `ToughnessMult`) — stacks with terrain cover + fortification. Units in no formation read the neutral 1.0. **Wiring = the full 6-point mod chain** (gotcha #10 class): blueprint (`Engine/Blueprints/GroundStanceBlueprint.cs`) + `ModDataStore.GroundStances` dict + `ModInstruction.DataType.GroundStance` (enum + ReadJson case) + `ModLoader` case + `modInfo.json` entry + the JSON file. Gauge: `GroundForcesTests.FormationStance_Catalog_TrySet_AndDefensiveSoaksLess` (catalog loads, TrySet applies + cooldown blocks, dig-in defender soaks less). **Still NOT built (follow-ups):** sub-formation nesting (the fleet tree), and the **client stance selector** (the Fleet-window-doctrine echo on the tactical map — the second half of list item #2).

## Why units are DATA objects, not entities (v1)

**Environmental gear (E4) — the cradle-to-grave hazard counter.** A `GroundUnitDesign` carries an `EnvironmentalResistance` map (`HazardEffectType` → 0..1 fraction negated); `RaiseUnit` snapshots it onto the `GroundUnit.EnvResistance` (like the combat stats), and the attrition step reduces incoming hazard damage by `unit.ResistanceTo(effect)`. This is the ground echo of a ship's `HazardResistanceAtb` — a heat-shielded design (`{HeatDamage:0.8}`) outlasts fire that kills an unprotected unit. **v1 folds the "gear" into the design** (its cost rides `ResourceCosts`); making gear a swappable, researched, *installable/strippable* component (the true grave rung) is the v2 promotion, alongside units-as-entities. Gauge: `GroundForcesTests.EnvironmentalGear_ReducesHazardAttrition` (0.8 → 20% attrition, 1.0 → immune, survives a clone).

A `GroundUnit` is a plain serializable object inside `GroundForcesDB`, not a full `Entity` — like `Galaxy.RegionFeature` inside `PlanetRegionsDB`. So a garrison of many units stays cheap and save-safe, and the combat resolver (5c) will read stats directly off the object. The trade-off: it can't reuse the Entity-based `AutoResolve.Resolve` verbatim — 5c **mirrors** that salvo-loop math over `GroundUnit`s instead. (If per-unit entities are ever needed — commanders, per-unit orders — that's a v2 promotion.)

## Connections (Prime Directive)

- **Galaxy / `PlanetRegionsDB`** — the surface `GroundForcesDB` sits on (regions = where units stand/move). A unit's `RegionIndex` indexes into `Regions`.
- **Industry / `IConstructableDesign`** — `GroundUnitDesign` builds on the existing rails (`IndustryTools.ConstructStuff` → `OnConstructionComplete`). A base-mod JSON template (six-point registration, gotcha #10) is the follow-up that makes it player-buildable in a New Game.
- **Colonies / `ColonyInfoDB.PlanetEntity`** — the build hook resolves colony → its planet → the roster.
- **Combat / `AutoResolve`** — the strength-math **shape** slice 5c mirrors for ground battles.
- **Capture** — clearing a region's garrison flips `FactionOwnerID` (5d), the "take a planet" moment; reuses the fleet-capture primitive and ties into the live colony-damage path (orbital bombardment softens a garrison).
- **Client / `PlanetViewWindow`** — 5e (built 2026-07-04) draws units in their region (faction+type tokens with health), click-to-move (`GroundForces.OrderMove`), click-to-place a base (`PlaceInstallationInRegionOrder`), and shows terrain class + `PlanetEnvironmentsDB` hazards — the map's upgrade from readout to navigable surface. Runtime is the developer's local build (CI-blind).

## Gotchas

1. **`GroundForcesDB` is on the PLANET BODY, not the colony** — parallel to `PlanetRegionsDB`. Forces are *of the planet* so an unowned world can hold a defending garrison. Don't attach it to the colony entity.
2. **Unit stats are a build-time SNAPSHOT on the `GroundUnit`** (not looked up from the design each time) — the same choice `ShipCombatValueDB` makes. Changing a design later does not retroactively change units already raised.
3. **`OnConstructionComplete` must never throw** — it runs in the daily industry hotloop (a throw there crashes the sim, GameEngine gotcha #1). It's guarded on colony/planet/region-layer/production-line presence.
4. **v1 units are data objects, not entities** — so `Entity.Destroy()` / the Entity-based `AutoResolve` don't apply; the 5c resolver operates on `GroundUnit`s. Don't reach for the ship resolver directly.
5. **The Bunker is a real base-mod installation — the SIX-point registration (gotcha #10) applies.** `GroundDefenseAtb` binds through the JSON chain: `bunker` ComponentTemplate in `installations.json` (`AtbConstrArgs(PropertyValue('LocalFortify'), PropertyValue('AdjacentProjection'))` → `Pulsar4X.GroundCombat.GroundDefenseAtb`, ctor-arg ORDER must match `GroundDefenseAtb(localFortify, adjacentProjection)`; `MountType: PlanetInstallation`) → `default-design-bunker` in `componentDesigns.json` → `bunker` in `earth.json` `StartingItems` **and** `default-design-bunker` in `earth.json` `ComponentDesigns`. Miss any and New Game / `CreateWithColony` crashes. Gauges: `BaseModIntegrityTests` (builds) + `Fortification_BaseModBunker_BuildsAndWinsTheFight` (the atb binds + wins). Add new ground-defence buildings the same way; new fortification VALUES are per-design data (moddable), not engine constants.
