# Sites — the Site Engine (subsystem reference)

**What it is, in one line:** the **one** data-driven engine every mid-game *episode* is a row in — a ruin, an anomaly, an outbreak, a derelict, a resource node, a first-contact, a late-game crisis. Design: `docs/SITE-ENGINE-DESIGN.md` (parent doc; `docs/EXPLORATION-CONTENT-DESIGN.md` is its catalog chapter).

The whole mid-game reduces to one shape:

> **a LOCATED thing → a berth-seated LEADER works it → it can be FOUGHT OVER at that location → it RESOLVES down one of several branches → a yield, or nothing.**

Build it once and exploration, ground combat, diplomacy, research, economy, and the crisis system all become **consumers of one engine** instead of six islands (the Prime Directive's "connect" made into architecture).

---

## Build sequence (anomaly-first — the engine, then content)

The space anomaly is the engine minus the surface bits, so it's the cheapest first build.

| Slice | What it builds | Status |
|-------|----------------|--------|
| **SE-1** | the engine on a space anomaly — `FieldSiteDB` + state machine + presence detection + accrue + resolve + a research yield | ✅ done (SE-1a + SE-1b + SE-1c) |
| SE-2 | the Command Berth as a real component (Role/Grade/Support/Survivability/Span) + the posting-danger incident roll | ✅ done (SE-2a gear + SE-2b seat/work-rate + SE-2c incident roll) |
| SE-3 | surface sites — locate ruins on a hex, the unit-worker path, Load/Land orders + hex landing, the guardian hook | ✅ core done (SE-3a location + SE-3b ground-worker + SE-3c Load/Land orders + SE-3d guardian gate). Deferred: hex-exact landing (SE-3c lands at region-centre) + discovery wiring (spawn a real ruin, SE-3e). SE-3c orders live in `GroundCombat/` (`LoadTroopsOrder`/`LandTroopsOrder`; gauge `TroopOrderTests`). SE-3d = `SiteWorkProcessor.RegionIsClearFor` gates `ProcessSurfaceWork` on the region being clear of foreign units (gauge `SurfaceGuardianTests`). |
| SE-4 | the incident shape (Europa) — bleeds-you pressure + hold-while-you-work + a menace faction + the spawn event | ✅ done (SE-4a state + SE-4b menace + SE-4c pressure + SE-4d spawn/spread + SE-4e discovery wire). SE-4a: `FieldSiteDB` incident dials + `SiteMachine.IsIncidentLive`/`CurrentPressure` (`IncidentMachineTests`). SE-4b: `MenaceFactory.RaiseMenaceAt` (`CreateBasicFaction`+`RaiseUnit`) stands up a hostile menace faction on the site's region → trips the SE-3d guardian gate (`MenaceFactoryTests`). SE-4c: `SiteWorkProcessor.ApplyIncidentPressure` (in `ProcessEntity`, gated on `SiteMachine.IsIncidentLive`) bleeds `CurrentPressure × days` health off every NON-menace unit in the incident's region each tick (the menace is spared — it's the source); gauge `IncidentPressureTests`. SE-4d: `SiteIncidentProcessor : IInstanceProcessor` (reschedules via `AddEntityInterupt`, `NavalAcademyProcessor` pattern) — each `SpawnIntervalDays` it raises a fresh menace unit at the region + `OrderMove`s one to an adjacent region + fires `EventType.NewHostileContact`; STOPS (no reschedule) once contained (grave rung). `Schedule(site)` arms the first fire. `MenaceFactory.RaiseMenaceUnit` reinforces without a new faction. Gauge `SiteIncidentSpawnTests`. **SE-4e: `IncidentScenario` — the discovery/authoring wire.** `SpawnIncidentAt(game, system, body, region, …)` composes the SE-4 parts into ONE live incident (Shape.Incident surface site + menace holding the region + pressure/spread dials + armed spread via `Schedule`). `MaybeSpawnForNewGame(game)` is the New-Game hook, gated behind the default-OFF `AutoSpawnIncident` flag (the `AutoRaiseHomeGarrison` pattern) → New Game byte-identical until flipped on; wired in `NewGameMenu.CreateGameCore` + a DevTools checkbox. Gauge `IncidentScenarioTests`. |
| SE-5 | branches + the ruptured edge (the crisis) — composable multi-path resolve, Diplomat/Intelligence roles, persistent→crisis | 🔨 building (SE-5a data structure done). **Plan (each CI-gated, byte-identical-first):** SE-5a `SiteBranch` record + `FieldSiteDB.Branches`/`CommittedBranchIndex` (pure data, empty on every existing site → byte-identical) ✅ · SE-5b branch-aware `SiteMachine` — `UnlockedBranchIndices`/`IsBranchUnlocked`/`AnyBranchUnlocked` (each branch unlocks independently by its own `UnderstandingRequired`) + `ResolveBranch(site, i)` (resolves to the chosen branch's `ResultStatus`); all NEW methods, the single-path `Resolve`/`BranchUnlocked` untouched → a branchless site is byte-identical. Gauge `SiteBranchMachineTests`. ✅ · SE-5c the `CommitSiteBranchOrder` (mirror `LoadTroopsOrder`) + `OrderableDB` on the site entity + stop the auto-resolve for branched sites — a branched site keeps accruing understanding but `TryResolveAndDeliver` holds it at Worked until the player commits a branch (which resolves + pays via the now-public `DeliverSiteYield`); a branchless site auto-resolves exactly as before → byte-identical. Gauge `SiteCommitBranchTests`. ✅ · SE-5d the RUPTURE edge (a persistent/Cursed site rolls a rupture chance → `Status=Ruptured` + spawns a new crisis site via the SE-4 menace machinery; a `RuptureChancePerDay` dial defaults 0 → byte-identical) · SE-5e the Diplomat/Intelligence yield routes (`DeliverYield` cases → `RelationshipState.AdjustScore` / the intel ledger; the Command Berth is already role-agnostic so berth-carried Diplomatic/Intelligence sites work today). NOTE: the NetworkRoute/jump-gate yield is SE-6 CONTENT per the design doc (§7 consumer), not core SE-5. A separate galaxy-level crisis (`Factions/GalaxyCrisis.cs`, faction ascension) already exists + is live — SE-5's rupture is the site-level cousin, not that system. |
| SE-6+ | content — author catalog rows across the dials as data | planned |

Each slice is CI-gated and byte-identical-first (new code neutral/unattached until wired).

---

## File map

| File | Role |
|------|------|
| `FieldSiteDB.cs` | **SE-1a (2026-07-12)** The SITE RECORD + the enums. The §3 dials — `SiteRole` (Science/Tactical/Diplomatic/Intelligence/Engineering), `SiteShape` (OneShot/Persistent/Incident), `SiteHook` (Benign/Guardian/Cursed/…), `SiteYield` (Research/Blueprint/…) — plus the live §4 state: `SiteStatus` (Discovered→Worked→Depleted/Persistent/Ruptured), `Progress` (banked yield magnitude), `Understanding` (the branch gate), `UnderstandingToResolve`, `YieldDelivered`. A DataBlob with `[JsonProperty]` fields + deep-copy `Clone()`. **SE-3a added the surface location** — `SurfaceBodyEntityId`/`SurfaceRegionIndex`/`SurfaceGlobalQ`/`SurfaceGlobalR` (a site on a planet's ground, located like a `GroundUnit`) + `IsSurfaceSite` (true when `SurfaceBodyEntityId ≥ 0`); location-neutral -1 defaults keep the space-anomaly path byte-identical. **SE-5a added the composable branches** — `List<SiteBranch> Branches` (empty = single-path SE-1 site) + `CommittedBranchIndex` (-1 = none) + `HasBranches`; deep-copied in Clone. Empty on every existing site → byte-identical. **Not attached to any live entity → byte-identical.** |
| `SiteMachine.cs` | **SE-1a (2026-07-12)** The PURE state machine (§4, agency-preserving, no timers). `Accrue(site, work, understanding)` — first work begins the study (Discovered→Worked), banks progress + understanding; a resolved site ignores further work. `BranchUnlocked(site)` — Understanding ≥ threshold (knowledge unlocks, never a timer). `Resolve(site)` — a worked+unlocked site transitions to Depleted (OneShot) / Persistent by Shape. Pure/static/deterministic (no clock/RNG/game state) so SE-1b's processor is a thin driver over it. **SE-5b added the composable multi-branch resolve** — `UnlockedBranchIndices`/`IsBranchUnlocked`/`AnyBranchUnlocked` (each `SiteBranch` unlocks independently at its own `UnderstandingRequired`) + `ResolveBranch(site, i)` (a Worked+branched site → the chosen branch's `ResultStatus`, recording `CommittedBranchIndex`). All NEW; the single-path `Resolve`/`BranchUnlocked` are untouched → a branchless site is byte-identical. Gauge: `FieldSiteTests` + `SiteBranchMachineTests`. |
| `FieldSiteFactory.cs` | **SE-1b (2026-07-12)** Puts a `FieldSiteDB` into the world as a NEUTRAL, point-in-space anomaly — NameDB + a fixed `PositionDB` (`MoveType.None`) + the site record, added via `system.AddEntity` (mirrors `JumpPoints.JPFactory`). `CreateAnomalySite(system, atPosition, name, …dials)`. **SE-3a added `CreateSurfaceSite(system, body, regionIndex, q, r, …dials)`** — a site on a planet's ground (records the body+region+hex; carries NO PositionDB, so the space work path skips it until SE-3b's ground-worker branch). **Not called in the live New-Game path → byte-identical** (no site exists until an exploration-discovery slice spawns one). |
| `SiteWorkProcessor.cs` | **SE-1b + SE-1c + SE-2b (2026-07-13)** The DRIVER: a daily `IHotloopProcessor` keyed to `FieldSiteDB`. For each site, if a worker is present (`TryFindWorker` = the nearest non-neutral **ship** within `PresenceRadius_m` = 1000 km), it feeds `WorkPerDay`/`UnderstandingPerDay × multiplier` into `SiteMachine.Accrue` and records `WorkedByFactionId`. **SE-2b:** `BerthWorkMultiplier(worker, role)` = the worker's best MANNED, Role-matching Command Berth → `Grade × (1 + leaderSkill + Support/100)`; no manned matching berth → 1.0 (the SE-1b flat rate, additive/byte-identical). **SE-2c:** after accruing, a seated leader at a DANGEROUS site rolls `RollPostingIncident` (via `SiteHazard`, using the SEEDED system RNG); a hit vacates the berth + `DestroyCommander`s the leader (the grave rung). A Benign site never rolls → RNG untouched → byte-identical. **SE-1c:** once understanding fills, it `Resolve`s the single-branch anomaly and calls `DeliverYield` **once** (`YieldDelivered` guard) to route the banked Progress into the Yield's consumer. No worker → no advance (pressure, not a timer). **SE-3b:** `ProcessEntity` now branches — a SURFACE site (`IsSurfaceSite`) is worked by `ProcessSurfaceWork` (`TryFindGroundWorker` = the first alive non-neutral `GroundUnit` in the site's region on its body, flat `SurfaceWorkMultiplier` rate), a space anomaly by `ProcessSpaceWork` (the ship+berth path, unchanged); both share `TryResolveAndDeliver`. Sleeps on an empty system → byte-identical live. Gauge: `SiteWorkTests` + `SiteYieldTests` + `BerthWorkTests` + `SitePostingDangerTests` + `SurfaceWorkTests`. |
| `SiteYields.cs` | **SE-1c (2026-07-13)** The YIELD ROUTER (§3 Yield dial) — the "connect" that pays a resolved site's Progress into an existing consumer system. `DeliverResearch(game, factionId, points)` adds research points to the working faction's NEAREST BREAKTHROUGH (most-progressed researchable tech, ties by id) via `FactionDataStore.AddTechPoints`. Pure over game state. The other yields (Blueprint/Resource/Population/Leader/StrategicAsset/NetworkRoute) route into their own systems in later slices. Gauge: `SiteYieldTests`. |
| `CommandBerthDB.cs` | **SE-2a (2026-07-13)** The berth GEAR — one host's roster of installed Command Berths (`List<CommandBerth>`; a host can carry several, like an Enterprise-D). Each `CommandBerth` carries the §5 dials — `Role` (which `SiteRole` it works), `Grade` (work-rate tier), `Support` (leader competence boost), `Survivability` (incident-risk reduction), `Span` (force size) — plus its `ComponentName` identity. **SE-2b added occupancy** — `CommandBerth.CommanderID` (-1 = empty) + `BestOccupiedBerthFor`/`BestEmptyBerthFor`/`FindBerthOfCommander` (the reads seating + the work-rate use). `[JsonProperty]` + deep-copy `Clone()`. Byte-identical (nothing seats a leader until an order/NPC does). Gauge: `CommandBerthBaseModTests` + `BerthWorkTests`. |
| `SiteHazard.cs` | **SE-2c (2026-07-13)** The posting-danger model (§5). `BaseIncidentChancePerDay(hook)` — a per-Hook daily incident chance (Benign 0 → the base anomaly is safe; Guardian/Cursed/… are lethal). `IncidentChance(hook, survivability, days)` = base × (1 − Survivability/100) × days, clamped [0,1] — the berth's Survivability buys the risk down (≥100 = immune). Pure/static (no clock/RNG) so the roll stays in the processor. Gauge: `SitePostingDangerTests`. |
| `BerthOps.cs` | **SE-2b (2026-07-13)** Seating + competence read. `SeatLeader(host, commander, role)` fills the best empty matching berth (`CommandBerth.CommanderID`) AND sets the leader's `CommanderDB.AssignedTo` back-reference (the shared convention the admin/scientist assignments use); `VacateBerth(host, commanderId)` frees both (reassignment / the grave rung, wired to death in SE-2c). `LeaderSkill01(manager, commanderId)` = the seated leader's `Experience / MaxLeaderExperience` (200), clamped 0..1 — the SE-2b site-work skill proxy (a dedicated Site-work BonusCategory is a later refinement). Gauge: `BerthWorkTests`. |
| `MenaceFactory.cs` | **SE-4b/4d (2026-07-13)** The MENACE — a hostile "monster" force for an incident. `RaiseMenaceAt(game, body, region, name, unitCount)` = `FactionFactory.CreateBasicFaction` + a loop of `RaiseMenaceUnit` (its own faction, so it trips the guardian gate — not a normal empire); returns the menace faction. `RaiseMenaceUnit(body, menaceFactionId, region, name)` raises one Infantry menace unit (Attack 120 / Def 12 / HP 600) via `GroundForces.RaiseUnit` without a new faction (the SE-4d reinforcement). Byte-identical (nothing raises a menace until an incident is authored). Gauge: `MenaceFactoryTests`. |
| `SiteIncidentProcessor.cs` | **SE-4d (2026-07-13)** The incident SPAWN/SPREAD engine — an `IInstanceProcessor` that, while a Shape.Incident site is live, each `SpawnIntervalDays` reinforces the menace at its region + creeps one unit into an adjacent region + fires `EventType.NewHostileContact`, then reschedules itself; STOPS (no reschedule) once contained (the grave rung). `Schedule(site)` arms the first fire. Inert until a live incident is scheduled → byte-identical. Gauge: `SiteIncidentSpawnTests`. |
| `CommitSiteBranchOrder.cs` | **SE-5c (2026-07-13)** The player order that COMMITS a resolution branch — an `EntityCommand` (mirrors `LoadTroopsOrder`) issued to the site entity (which now carries an `OrderableDB` from `FieldSiteFactory`). `Execute` calls `SiteMachine.ResolveBranch` + `SiteWorkProcessor.DeliverSiteYield` (the committed branch's Yield × Progress×YieldScale), fires an `AnomalyDiscovered` event, and finishes; a still-locked branch leaves the order pending to retry (understanding is still accruing), a resolved/branchless/out-of-range target finishes as a no-op. `IsValidCommand` = a real branch on a branched, unresolved site. Additive — nothing issues it yet (a client picker is the UI slice) → byte-identical. Gauge: `SiteCommitBranchTests`. |
| `SiteBranch.cs` | **SE-5a (2026-07-13)** ONE composable RESOLUTION BRANCH (§3 Branch set) — a plain serializable record (the moddable-catalog shape) carrying `Name`, `UnderstandingRequired` (the per-branch unlock gate — branches compose, unlocked by knowledge not consumed by each other), `Yield` + `YieldScale` (what/how-much it pays), `ResultStatus` (Depleted/Persistent terminal), and `Ruptures` (whether committing it spawns a crisis — SE-5d). Deep-copy `Clone()`. Carried in `FieldSiteDB.Branches`. SE-5a only adds the type + the list (empty on every existing site → single-path resolve untouched → byte-identical); SE-5b/5c read/commit it. Gauge: `SiteBranchDataTests`. |
| `IncidentScenario.cs` | **SE-4e (2026-07-13)** The DISCOVERY/authoring wire — the switch that makes SE-4 reachable in a real game. `SpawnIncidentAt(game, system, body, region, …)` composes a full live incident (surface site + menace + dials + armed spread). `MaybeSpawnForNewGame(game)` seeds one home-world incident per player faction, gated behind the default-OFF `AutoSpawnIncident` static flag (the `AutoRaiseHomeGarrison` pattern), so New Game stays byte-identical until flipped on (client New-Game hook in `NewGameMenu.CreateGameCore` + a DevTools checkbox). Gauge: `IncidentScenarioTests`. |
| `CommandBerthAtb.cs` | **SE-2a (2026-07-13)** The component that MAKES a berth buildable (the Site-Engine twin of `ResearchAcademyAtb`/`IntelDirectorateAtb`). On install adds a `CommandBerth` to the host's `CommandBerthDB` (seeding the blob if absent); on uninstall removes THIS component's berth by name (save/load-robust) and drops the roster when the last berth goes (the grave rung). `[JsonProperty]` dials + parameterless ctor (save/load). Six-point base-mod registration: `command-berth` template (`installations.json`, dials Role/Grade/Support/Survivability/Span, Role authored as a `GuiEnumSelectionList` over `Pulsar4X.Sites.SiteRole` like the ship bridge's Admin Level) + `default-design-command-berth` (`componentDesigns.json`) + Earth's start colony lists both (StartingItems + ComponentDesigns), so it's buildable from turn one. AttributeType FQN `Pulsar4X.Sites.CommandBerthAtb`. Gauge: `CommandBerthBaseModTests` (JSON→atb binding + install/seed/drop); `BaseModIntegrityTests` covers the material/unlock end. |

---

## Key locked decisions (design)

- **One engine, data-driven** — every episode is a row across ~7 dials (Location · Discovery · Worker · Role · Shape · Hook · Yield). Not parallel systems.
- **No timers** — an unresolved incident/persistent site applies steady PRESSURE; the player chooses when/how to act. The pressure is the clock (protects agency).
- **Branches compose** — accrued knowledge UNLOCKS branches, never closes them; understand → *then* choose (seal vs. ally), never railroaded; different rewards *or none*.
- **The persistent site can RUPTURE** into a crisis (the reward carries the risk) — the small-scale seed of the late-game crisis (SE-5).
- **Where a leader is posted sets their incident/death risk**, mitigated by the Command Berth's Survivability dial — one incident roll across all pillars (SE-2).
- **The Command Berth IS the delegate seat** (`docs/GOVERNANCE-AND-DELEGATION-DESIGN.md`) — built on the one `AdminSpaceAtb` seat mechanism; a ship fits several (Enterprise-D).

---

## Connections (Prime Directive)

- **Exploration/survey** (`GeoSurveyProcessor`, fog) — discovers sites.
- **People/commanders + academies** (X.0) — fill the berths; the grave rung (incident roll) is theirs.
- **Ground combat + the hex grid** (`GroundForcesProcessor`, `SurfaceGrid`, `RaiseUnit`, conquest) — the located-combat hook + surface substrate (SE-3/4).
- **Ships/fleets/movement** (`MoveToSystemBodyOrder`, `GroundTransport`) — reach + deploy.
- **Research / economy / population / jump-network / diplomacy / espionage** — the yield routes + branch consumers.
- **Delegation/governance** (`AdminSpaceAtb`, `AssignAdministratorOrder`) — the berth is the seat.
