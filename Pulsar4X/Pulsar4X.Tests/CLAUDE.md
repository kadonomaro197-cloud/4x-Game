# Pulsar4X.Tests — Testing Reference

> ### ⚠ TWO CORRECTIONS, verified 2026-07-27 (OPERATION GROUND TRUTH A2)
>
> **1. THIS IS NOT A FULL TEST INVENTORY, despite being indexed as one.** `docs/DOCS-INDEX.md` describes this
> file as *"Test harness + **full test inventory**"*. Measured: the project holds **343 `*Tests.cs` fixtures**;
> this doc names **142**. **201 are undocumented (59%).** Every fixture it *does* name exists (checked all 142 —
> zero missing), so nothing here is a false claim; the defect is **coverage**, and it bites in a specific way:
> a session asking *"is there already a gauge for X?"* consults this list, finds nothing, and writes a duplicate.
> Concrete example — **`BattleLogTests` exists and is not listed here**, and it is directly relevant to the
> planned ground battle-log slice (`docs/ground/PLANETARY-FUNCTIONAL-PLAN-2026-07-27.md` S1).
> **Before concluding a gauge does not exist, run the real list:**
> `ls Pulsar4X/Pulsar4X.Tests/*Tests.cs` (or `grep -rl "class .*Tests" Pulsar4X/Pulsar4X.Tests/`).
>
> **2. THE "~13 min" SHARDING FIGURE BELOW IS STALE — the `rest` shard has crept back to ~33 min.** Measured on
> run `30290497412` (2026-07-27): `campaign-clock` 2 · `selfsufficiency` 4 · `economy-readout` 8 · `economy` 11 ·
> `stations` 13 · **`rest` 33** · `build-client` 0.5 (minutes). So wall-clock is **~33 min, back to the
> pre-sharding figure**. The paragraph below already states the remedy and its trigger — *"if a new fixture
> becomes a heavy outlier, rebalance the shard filters in `ci.yml`"* — **and that trigger is now met.** Plan
> against ~33 min, and note the complement filter puts every NEW fixture into that slowest shard by default.


NUnit 3 test project. Runs in CI (`.github/workflows/ci.yml`) on every push/PR, and locally via `dotnet test`.

> **CI builds the engine + tests only — NOT the SDL client.** Client (UI) bugs are invisible here; they surface only in the developer's local build / the `launch.bat` console loop. See root `CLAUDE.md` → "The Visibility Gate" and the CI note.

> **CI is SHARDED across 6 parallel jobs (2026-07-03; 4th 2026-07-10; 5th+6th 2026-07-18)** — wall-clock ≈ the slowest single shard, not the sum (~33 min → ~13 min). The suite **cannot** run in-process-parallel: several tests share static counters (`SensorScan.ScanCount`, `CombatEngagement.TickCount`, `NPCDecisionProcessor.TickCount`), so only process-level sharding is safe. Split by measured fixture time: **stations** = `StationFactoryTests` (the ~11-min bottleneck, alone), **economy** = `ScenarioHarnessTests`+`ProductionBuildTests`+`MoraleTests`, **economy-readout** = `EconomyReadoutTests` alone (a ~7.5-min game-year sim, its own shard so it can't push `rest` past stations), **campaign-clock** = `CampaignClockReadoutTests` alone (a ~10-min multithreaded transit sim — Operation Earthfall P0), **selfsufficiency** = `FactionSelfSufficiencyReadoutTests` alone (a ~12-min 120-day 3-faction sim — Operation Earthfall P0; these two DevTest sims fell into `rest` and doubled it to ~28 min, so each got its own shard to restore ~12-min turnaround), **rest** = the complement filter (a NEW fixture lands here by default). A red on any shard reds the run; each shard publishes its own TRX report. The root cause of the slowness is that `CreateWithColony()` re-parses ALL the mod JSON every call and the heavy fixtures call it per-test — the deferred deeper fix is to cache that parse (needs `GameFactory.CreateGame` verified read-only on the `ModDataStore`). If a new fixture becomes a heavy outlier, rebalance the shard filters in `ci.yml` (read the TRX duration column). **`EconomyReadoutTests.Economy_BaselineReadout_OverOneYear` is RE-ENABLED (2026-07-10)** — the storage-cap that quarantined it is fixed. Root cause was the `warehouse-facility` template clamping Storage Volume to 10,000 m³ (`storage.json` `MaxFormula`), so 10 warehouses held only 100,000 m³ and the 50M×17-goods stockpile overflowed — most minerals clamped to 0, jamming mining + refining. Fix: dropped the start stockpiles to 1M each (verified 62% of the 100k m³ cap, fits with headroom). Detail: `GameEngine/Industry/CLAUDE.md` → ALPHA stockpiles.

---

## How to run

```powershell
dotnet test Pulsar4X/Pulsar4X.Tests/Pulsar4X.Tests.csproj
dotnet test Pulsar4X/Pulsar4X.Tests/Pulsar4X.Tests.csproj --logger "console;verbosity=detailed"   # per-test output
```
CI runs the full suite automatically and publishes a per-test TRX report (inline table + downloadable artifact), so a red run names the exact failing test.

**Mod data at runtime:** the `CopyJsonData` target in the `.csproj` copies `..\GameData\**` to `bin\...\Data\`, so tests load the real game data via relative paths like `"Data/basemod/modInfo.json"` and `"Data/testingmod/modInfo.json"`.

---

## The Scenario Harness — `TestScenario` (build feature tests on this)

`TestScenario` is the reusable engine-level mid-game state. It stands up a REAL faction + colony the way the live game does, so feature tests (economy, combat, ground) don't re-derive a start each time. **Engine-only → runs in CI.** It is the engine's *gauge board*: build a known state, advance the clock, read/assert.

### Use it
```csharp
var s = TestScenario.CreateWithColony();        // base mod, New Game wizard defaults
// handles: s.Game, s.Faction, s.Species, s.StartingSystem, s.StartingBody, s.Colony
s.QueueProductionJob("space-crete", repeat: true); // optional: give the factory/refinery work to do
s.AdvanceTime(TimeSpan.FromDays(365));           // step granularity defaults to 1 game-day
```

`QueueProductionJob(designId, count, repeat)` is the **job lever**: the factory/refinery do nothing until a job is queued. It looks the design up in the faction's unlocked `IndustryDesigns` (refined materials like `space-crete`/`stainless-steel`, and component/installation designs), builds an `IndustryJob`, and adds it to the production line that handles its industry type. A "refining" material needs its mineral inputs present (mined or in starting cargo) or the job sits at `MissingResources` — check the job Status in the industry readout.

### Feature-test pattern (read → advance → read → assert the delta)
```csharp
var s = TestScenario.CreateWithColony();
// read STARTING gauges off s.Colony (cargo, installed components, deposits) ...
s.AdvanceTime(TimeSpan.FromDays(365));
// read ENDING gauges and assert/report the deltas (X mined, Y refined, Z built) ...
```
*(The exact cargo / mineral read API is established by the first economy readout test — link it here when it lands.)*

### How it's built (mirrors `NewGameMenu.CreateGameCore`)
load mods → pick New Game defaults (first `CanStartHere` body / first species / first colony) → `GameFactory.CreateGame` → `StarSystemFactory.LoadFromBlueprint` per enabled system → find the body by matching `GetDefaultName()` to the blueprint → `FactionFactory.CreateBasicFaction` → `SpeciesFactory.CreateFromBlueprint` → `ColonyFactory.CreateFromBlueprint` → **`startingSystem.IncrementExternalObserver(priority: true)`** (promote out of Stasis — see gotcha 6). A build failure logs the full exception (`ex.ToString()`) to the test output, then rethrows.

### Extend it
- **Expose another entity:** capture it in `CreateWithColony` and add a `public ... { get; private set; }` handle.
- **Add a precondition helper:** e.g. an `AddMine(s.Colony)` that attaches a mine component before `AdvanceTime`, so the economy actually does work to measure.
- **Use the verified namespace map below** when referencing engine types — this is where blind edits go wrong.

### Verified API + namespaces (the harness's dependencies)
Namespace drift between branches is the #1 compile trap here (it bit `PositionDB` and `MassVolumeDB`). Verify a type is **imported**, not just that it exists:

| Type / API | Namespace |
|---|---|
| `Game`, `StarSystem`, `Entity`, `NewGameSettings`, `GameFactory.CreateGame(ModDataStore, NewGameSettings)` | `Pulsar4X.Engine` |
| `ModDataStore`, `ModLoader.LoadModManifest(path, store)` | `Pulsar4X.Modding` |
| `FactionFactory.CreateBasicFaction(game, name, abbr, funds)`, `FactionInfoDB` | `Pulsar4X.Factions` |
| `SpeciesFactory.CreateFromBlueprint(system, speciesBlueprint)` | `Pulsar4X.People` |
| `ColonyFactory.CreateFromBlueprint(game, faction, species, system, body, colonyBlueprint)` → `Entity` | `Pulsar4X.Colonies` |
| `StarSystemFactory.LoadFromBlueprint(game, systemBlueprint)` → `StarSystem`; `SystemBodyInfoDB`; `MassVolumeDB` | `Pulsar4X.Galaxy` |
| `PositionDB` (**active class is in Movement, not Datablobs**) | `Pulsar4X.Movement` |
| `GetDefaultName()` (Entity extension) | `Pulsar4X.Extensions` |
| `CargoStorageDB` | `Pulsar4X.Storage` |
| `Mineral`, `ProcessedMaterial` | `Pulsar4X.Industry` |
| `ComponentInstancesDB` (**namespace ≠ folder** — file is in `Engine/Components/`, namespace is Datablobs) | `Pulsar4X.Datablobs` |
| `ComponentInstance`, `ComponentDesign`, `TryGetComponentsByAttribute`, `GetAttribute` | `Pulsar4X.Components` |
| `IndustryAbilityDB`, `IndustryJob`, `IndustryTools`, `MiningDB`, `InfrastructureDB`, `MineResourcesAtbDB` | `Pulsar4X.Industry` |
| `ShipFactory`, `ShipDesign`, `ShipInfoDB` | `Pulsar4X.Ships` · `OrbitDB` → `Pulsar4X.Orbits` |
| `IConstructableDesign` | `Pulsar4X.Interfaces` |
| `DamageProcessor` (**internal** — reachable via `[assembly: InternalsVisibleTo("Pulsar4X.Tests")]` in `Engine/Game.cs`), `DamageFragment`, `DamageResult`, `EntityDamageProfileDB`, `DamageTools` | `Pulsar4X.Damage` |
| `Vector2` — `DamageFragment.Velocity`; **must be non-zero** or `DealDamageEnergyBeamSim` does `1/\|v\|` → NaN → `Convert.ToInt32` throws | `Pulsar4X.Orbital` |
| `BeamWeaponProcessor.FireBeamWeapon(launcher, target, hitsTarget, energy, wavelen, beamVel, beamLenSec, baseHitChance, optimalRange_m)` (the real fire entry point — for the firing-path gauge) | `Pulsar4X.Weapons` |

---

## Gotchas (hard-won)

1. **`CreateWithColony(base + testing mod)` throws** — the testing mod ships incomplete Armor/Theme data and NREs in colony build (it adds no species/colony). That variant is `[Ignore]`'d in `NewGameStartSmokeTests`. **Base mod alone is the working baseline.** Re-enable once the testing-mod data is fixed.
2. **`AdvanceTime` is single-threaded and sets `Ticklength`.** It sets `Game.Settings.EnforceSingleThread = true` (so a processor throw surfaces on the test thread, not the thread pool) and `Game.TimePulse.Ticklength` (default 1 day; the per-system scheduler catches up sub-daily processors within each step). Pass a smaller `step` for fine-grained needs (e.g. combat closing).
3. **Faction entities live in the `GlobalManager` — which `MasterTimePulse` NOW iterates (keystone fixed 2026-06-30).** `SimulateTimeUntil` processes `GlobalManager.ManagerSubpulses` after the per-system block, so `AdvanceTime` fires **faction-level** processors (NPCDecisionProcessor, and the future politics/diplomacy loops) as well as the colony ones. Gauge: `FactionEconomyTests.FactionLevelProcessors_FireOnceGlobalManagerIsIterated` (asserts `NPCDecisionProcessor.TickCount` climbs). *(Historical note: this used to be the "GlobalManager not iterated" trap — faction processors never fired, so things like `ColonyEconomyProcessor` were deliberately run colony-side. That workaround still works; new faction-level work can now run faction-side.)*
4. **A large chunk of the integration suite is commented out** (`/* ... */`): `SystemGenTests`, `SerializationManagerTests`, `FactoryTests`, `MiningTests`, `SavingAndLoadingTests`, parts of `PathfindingTests`. So the "all green" count is mostly unit-level. Re-enable these one at a time, CI-verified — they likely rotted (like the in-code `DefaultHumans` start).
5. **CI cannot test the client.** Engine-only. UI verification is the developer's local build.
6. **A star system defaults to `Stasis` and Stasis systems are NOT processed.** `StarSystem.ActivityState` starts at `Stasis`; `MasterTimePulse.SimulateTimeUntil` filters `Stasis` systems out of the processing loop, so a colony in a Stasis system does **zero** mining/population/industry no matter how far you advance the clock — and nothing throws, so a naive smoke test passes while measuring a frozen universe (this is exactly what hid "the mine does nothing" — the rates were correct; the system was asleep). The live game promotes a system via faction-entity detection (`Game` setup loop → `UpdateActivityState` → Background) and via the client observing it (→ Foreground). `CreateWithColony` reproduces the "player is here" case with `IncrementExternalObserver(priority: true)` → Foreground (full speed; Background throttles hotloop frequency 10×). The economy readout prints `s.StartingSystem.ActivityState` as its first gauge for this reason. **If a feature test advances time and sees no state change, check ActivityState first.**

7. **Gauging a BUILD→raise/install through the industry queue is flaky in `CreateWithColony` — drive `OnConstructionComplete` directly (2026-07-06).** Two traps bit the slice-A ground-unit fielding gauge in a row: (a) zeroing a design's `ResourceCosts` to "isolate the mechanic" throws `resources can't cost 0` — use a **minimal non-zero** cost of a bulk-stocked material (`{ "stainless-steel": 1 }`) instead; (b) even then, `QueueProductionJob` + `AdvanceTime` did **not** reliably complete an `installation-construction` job in-harness (no green test drives that path to completion), so the raise never happened and `HasDataBlob<GroundForcesDB>` was False. **Fix pattern:** assert the design is a real buildable (registered on the faction, valid `IndustryTypeID`, emergent stats), then call the design's `OnConstructionComplete(colonyEntity, storage, "line", new IndustryJob(faction, id), design)` directly — the exact call `IndustryTools.cs:214` makes at completion — for a deterministic raise. The generic queue itself is covered by `ProductionBuildTests`; don't re-litigate it in every feature gauge. (See `Industry/CLAUDE.md` gotchas 6-7.)

---

## Test inventory

**Operation Earthfall (landed 2026-07-21)** added the fixtures below (all in the `rest` CI shard unless noted). Two existing fixtures also gained Earthfall cases: `AIDecisionRecorderTests` (P0.4 — the AI tape now records a station count) and `NpcFleetReadyToSailTests` (P4.2 — `BuiltShips_ChargedForNpc_NotForPlayer_PerProvisioningPolicy`, the industry-path built-hull charge).

| Test | What it guards |
|------|----------------|
| `SafeDictionaryEventLockTests` | **Earthfall P0.5** — `SafeDictionary` raises its change events AFTER releasing its lock (copy-then-notify); a concurrent reader from inside a notification completes within a bounded Join |
| `CampaignClockReadoutTests` | **Earthfall P0.1** — the campaign-clock freeze repro: drives the DevTest master clock 40 game-days with the AI gates + combat interrupt armed; FineStepCount flat during transit, wall bounded, zero tick errors (the freeze is NOT a sim crawl) |
| `FactionSelfSufficiencyReadoutTests` | **Earthfall P0.2** — the per-faction/per-host economy self-sufficiency board over 120 DevTest days (structural truths; would have caught the Kithrin station-upkeep drain) |
| `EfUmfGovernmentAndCeresFactoryTests` | **Earthfall P3.1** — the UMF authored `"government"` node lands Militarism/Authority High (war term +10 not −5) + Ceres now hosts a manufacturing line |
| `LegitimacyStaleEchoDebounceTests` | **Earthfall P3.2** — a rebellion needs 2 consecutive collapsing reads (no one-sample revolt); legitimacy reads THIS cycle's morale (no stale echo); flags-off byte-identical |
| `EfHysteresisBreakGlassTests` | **Earthfall P3.3** — the three crisis break-glasses release a passed crisis commit (shorter dwell / trigger-cleared / contradiction-debounce) while a genuine sustained crisis holds Defend; pins the Survive-floor unreachability bug |
| `EfOperationContinuityTests` | **Earthfall P3.4** — a winning in-flight Conquer survives a transient internal wobble; a genuine Defend recalls in-flight offensive fleets home |
| `EfSealiftQueueGuardTests` | **Earthfall P4.1** — Rung 2 builds ONE troop transport then the already-queued guard makes the next cycle fall through (kills the 4× redundant queue that strangled Mars) |
| `EfLaunchFuelStockTests` | **Earthfall P4.3** — every host owning a `LaunchComplexDB` also stocks its launch fuel (the two `TryDeductFuel` preconditions — Mars had a pad but no fuel) |
| `EfSealiftEndToEndTests` | **Earthfall P4.4** — the full BUILD→LAUNCH→LOAD→SAIL sealift chain through the real resolver/industry/launch/order paths on DevTest Mars (charged, fuelled, loaded, sail-ready) |
| `EfConquerGroundRungsTests` | **Earthfall PW.1** — the ConquerResolver ground rungs: beachhead parts-haul · landed-unit form-up · infra-destroy stance-gate (an Offensive battalion is tasked, a Defensive one is not) |
| `EfPwInfraButtonContractTests` | **Earthfall PW.2** — the engine surface the client battalion-rename + infra Destroy/Capture buttons draw against (rename setter, order factories incl. hex (0,0), the QUEUE-path resolve) |
| `OperationEarthfallTests` | **Earthfall P8.1a** — the whole AI conquest arc as 8 ordered milestone tests through the real engine paths, composed into one narrative tape (`TestResults/earthfall-readout.txt`); each milestone mirrors a proven-green sibling |
| `PlayerGroundChainRailsTests` | **Earthfall P8.1b** — the GENERIC player unit-creation→battalion→defend/invade RAILS (no hard-coded marine, dev decision #2): assemble→build→FormUp→stance/ROE→defeat a landed force + the UMF brain reacts→embark→land offensively |
| `MidCampaignSaveLoadTests` | **Earthfall P8.1c** — a mid-invasion snapshot (landed battalion, brain posture, a brain-issued order + issuer, beachhead building + surface parts, a captured hex, the P3 crisis state) round-trips Save/Load and the campaign continues on the next brain-on tick |
| `EfGroundConstructorTests` | **Earthfall G1.1** — the combat-engineer atb (`GroundConstructorAtb`) JSON-binds (six-point) + surface parts haulage lands/reads/deep-copies + `LandPartsFromShip` check-then-consumes from ship cargo |
| `EfBeachheadBuildTests` | **Earthfall G1.2** — an engineer erects + fortifies a colony-free beachhead building over ticks (grave rung: it can be bombed); the three build gates (held ground / enemy-free / has parts) |
| `EfGroundFormUpTests` | **Earthfall G2.1** — `FormUpLoose` sweeps loose units into battalions (AI formation parity) + the manager micro-helpers (rename / cross-body registry / radar reach) |
| `EfGroundTacticalBrainTests` | **Earthfall G2.2** — the ground tactical brain's six §4 posture gauges + fog-honesty (undetected enemy = 0) + order-ownership (a player order suppresses the brain); flag off ⇒ byte-identical |
| `EfGroundSustainmentTests` | **Earthfall G2.3** — ammo drain/silence in combat + depot resupply + upkeep-value wiring (garrison HP-scaled, assembled mass-scaled) |
| `EfGroundInfraCombatTests` | **Earthfall G3** — per-hex destroy/capture-infrastructure orders (range-gated) + the fortification consumer (a captured bunker stops fortifying the defender) |
| `EfGroundSealedComponentTests` | **Earthfall G4** — the buildable `sealed-systems` component JSON-binds + the assembler→`EnvironmentalResistance` wire (a sealed unit survives an airless+toxic world an unsealed twin dies on) |
| `EfClientSensorAgeOutTests` | **Earthfall C2.1** — sensor track lost→memory flip (LAGGED→FROZEN, still held) + `ContactStaleSeconds` age-out actually removes a stale track (the engine gauges A2 named as missing) |
| `EfC3BattalionRegistryTests` | **Earthfall C3.1** — the cross-body battalion registry the Force-Management Battalions tab draws (two formations on two bodies collected + aggregated; enemy excluded) |
| `EfC5TroopLiftOrderTests` | **Earthfall C5.1** — the FleetWindow embark/land button contract (CreateCommand arities, per-class bay-capacity readout, the region-picker RegionIndex wire) |
| `EfKithrinSurveyChainTests` | **Earthfall D1.1** — `ExpandResolver` emits a real `GeoSurveyOrder` for an idle surveyor, else queues ONE surveyor build behind an in-production guard (the Kithrin survey→found chain) |
| `EfStationIncomeTests` | **Earthfall D2.1** — a populated station's monthly income exceeds its operating cost (ends the A6 structural bankruptcy); an unmanned station yields zero (byte-identical); booked to the `StationIncome` ledger category |
| `EfConsolidateStationTests` | **Earthfall D2.1** — a station-only faction's `ConsolidateResolver` emits a real executable action (GrowEconomy fall-through) instead of a guaranteed None; a content colony faction still returns None |
| `EfKithrinExpandArcTests` | **Earthfall D3.1** — the whole Kithrin expand arc through the real paths (survey emit→complete→found→in `Colonies`); a brain-driven companion; M5 "pays tax" `[Ignore]`d (an AI-founded colony is born empty+untaxed) |
| `Resolver2DJointsSpecTests` | **Earthfall T0.1** — the executable spec for the two 2D-resolver joints: conserved target-weighted fire-allocation (kills the double-count trap) + combined-theater cadence (fixed-5s ground step, fast-forward==watch) |
| `GroupPlaneTests` | **Earthfall T1.1 / S0** — the pure `GroupPlane` battle-plane math (frame determinism, orthonormal axes, `RoleOffset` trig, joiner-uses-frozen-frame, `EnemyDirection` tie-breaks, `PairDistance`); no live caller |
| `EfGroupPlaneAnchorTests` | **Earthfall T2.1 / S1** — the group-plane wiring behind `EnableGroupPlane`: flag-on seeding matches the pure math, joiner copies the frozen board, the anchor slides by the scalar-gap delta; flag-off byte-identical |
| `EfGroupPlaneRangeGateTests` | **Earthfall T3.1 / S2** — the 2D range gate reads the anchor pair-distance (a long fleet fires a short one at range even with the scalar gap forced small); flag-off reverts to the scalar/3D read; the closing flag is the master switch |
| `ScenarioHarnessTests` | the harness itself — builds a colony start; advances a real colony a game-year without throwing (**the colony/economy loop coverage the suite never had**) |
| `NewGameStartSmokeTests` | the real New Game colony path (rides the harness) |
| `EconomyReadoutTests` | the economy board — mining (asserts deposits deplete), refining (asserts Space-Crete produced via the job lever), infra/fuel readouts |
| `ShipSpawnTests` | engine ship-spawn — `ShipFactory.CreateShip` (the DevTools spawn path) lands a ship in the system with its parts, and it survives a tick |
| `ShipHullBaseModTests` | **§0b mass-budget cap — Slices D1 + D2a** (dossier ⚙11) — the hull is a real base-mod component (`ShipHullAtb`, the space echo of the ground `GroundChassisAtb`), now split into **three graduated tiers** (light/medium/heavy) off one `ship-hull` template via per-design dial overrides. Asserts all three load, each binds a `ShipHullAtb` with its overridden `Mass Budget` (25 t / 90 t / 180 t) and its own frame **Hull Mass** (decoupled from the budget — 0.5 t / 10 t / 25 t, the weight a hulled ship carries; light trimmed to 0.5 t so fighters keep their evasion edge), climbing on both axes, ship-component mount. The gotcha-#10 JSON→atb sensor for the hull + proof the input-dial overrides propagate. `ShipDesign.Recalculate` sources the budget from a mounted hull; no ship mounts one yet (D2b), so ship combat is byte-identical |
| `ShipMassBudgetTests` | **§0b mass-budget cap — Slice A** (dossier ⚙11, the Chassis keystone) — `ShipDesign` now COMPUTES `MassBudget`/`OverMassBudget` in `Recalculate` (the space echo of the live ground `GroundUnitAssembly` carry cap), sourced from the design's own mass at 1.0 headroom → byte-identical (nothing over budget, `IsValid` untouched, no construction gated). Asserts every base-mod ship gets a finite budget ≥ its mass and none reads over-budget, AND prints every ship's mass — the `[mass-budget]` readout is the **calibration deliverable** the enforcement slice needs to size a real hull ceiling above the heaviest ship |
| `ShipMassBudgetEnforcementTests` | **§0b mass-budget cap — ENFORCEMENT (dossier ⚙11 Hull)** — the slice Slice-A's comment anticipated ("a later slice … lets `IsValid` bite"). `ShipDesign.EnforceMassBudget` (static flag, default OFF, client-ON) makes an over-budget design (mass > its mounted hull's Mass Budget) read `IsValid = false`, so the client production list refuses it — the §0b "physical budget forces the build" gate made to BITE. Asserts (1) byte-identity — every base-mod ship stays `IsValid` on OR off (all under budget), and (2) the gate bites — a LIGHT hull (25 t budget) carrying six lasers (~60 t) reads `OverMassBudget` and, with enforcement on, `IsValid = false` (off → still valid; only the client gate reads it). Constructs the overloaded `ShipDesign` in-test (light hull + 6 lasers + `faction.Data.Armor`) |
| `StartFleetTests` | the New Game **start fleet** — asserts `CreateFromBlueprint` builds the colony blueprint's fleets (CI-proven `[start] fleets=3, ships=5` from `colony-earth`). The engine-checkable half of "fleets aren't working" (the client half needs the dev's local build + DevTools "Dump State") |
| `FleetAssemblyTests` | **Fleet-composition slice 2 — the AI assembles built warships into ONE forming fleet.** Drives `FleetAssembly.AssembleBuiltWarships` directly (no sim advance): loose warships parked under the faction ROOT fleet (mimicking `ShipDesign.OnConstructionComplete`) fold into a real in-system fleet tagged `FleetCompositionDB` (flagship set, `Deployed` at the 3-ship core), a second sweep GROWS that fleet rather than starting a new one, and with no loose warships it's a no-op. Proves THE UNBLOCK: after assembly `MilitaryComposition.ReadyStrikeFleet` finds the massed group that was invisible while loose (the strike-never-masses fix) |
| `FleetSupportHullTests` | **Support/utility hulls — the AI FIELDS the unarmed TENDERS it used to build and never use (2026-07-17).** Drives `FleetAssembly.AssembleSupportHulls` directly (no sim advance): a built tender (the base-mod Freighter — unarmed + a `Storage.CargoTransferAtb` shuttlebay, so `IsSupportHull` reads true) parked loose under the faction ROOT fleet folds into the strike fleet as a **Support** member (travels with it, `FleetRoleComposer.ClassifyRole` = `FleetRole.Support`) WITHOUT changing the armed count (a tender is never a warship) — so the strike-threshold / Deployed / reserve logic are unchanged and `ReadyStrikeFleet` still reads the group ready. Also: with no strike fleet formed the tender is left loose, and with no tender the pass is a no-op (the byte-safety tripwire keeping the ConquerResolver/MilitaryComposition path unchanged) |
| `OrderStubSafetyTests` | **OPERATION BLUEPRINT-TO-STEEL A4 — the four order stubs DE-FANGED (no wedge, no crash).** `RefuelAction`/`ResupplyAction` had an empty `Execute` + a never-finishing `IsFinished`, so once fired onto a fleet's BLOCKING order lane they jammed it forever; `ServeyAnomalyAction`/`ShipLogisticsOrders` threw `NotImplementedException` in `Clone` (a clock-killing [FATAL] since `FleetOrderProcessor` clones standing actions on the sim thread). Asserts Refuel/Resupply now COMPLETE after `Execute` (IsFinished flips true → the order leaves the lane); `ServeyAnomalyAction`/`ShipLogisticsOrders` `Clone()` no longer throws; the survey order is a safe inert shell. Their real BEHAVIOR is parked (campaign log ADJUDICATION QUEUE) — this pins only the landmine removal. Engine-only, no JSON drift, no save-break |
| `ShipRoleToolsTests` | **OPERATION BLUEPRINT-TO-STEEL A3 — the ship ROLE classifier (`Ships.ShipRoleTools`).** The ONE place a warship is told from a freighter (the space twin of `GroundRoleComposer.ClassifyRole`), derived from mounted components: `ShipRole` (Warship/Survey/Freighter/Transport/Tender/Hauler/Utility). Asserts every AI-warship design classifies Warship+Military and every other design civilian — the **byte-identity tripwire** for `ConquerResolver.IsWarship`/`DefendResolver.IsWarship`, which now DELEGATE to `ShipRoleTools.IsWarship` (one verb, both seats); a BUILT ship classifies the same role as its design (window built-ship path == AI design path, via `ShipCombatValueDB.Firepower`); Mil/Civ maps only Warship→Military; null-safe. Runs on the real base-mod start faction (no hard-coded ids) |
| `DevTestFleetRoleReadoutTests` | **New AI-usable units, cradle-to-grave (2026-07-16).** Loads the whole DevTest sandbox (UEF+UMF+Kithrin) and builds EVERY faction ship design — so a scenario-faction registration slip (missing material/template/component) fails CI instead of a player's New Game (**closes the `BaseModIntegrityTests` blind spot**, which only checks the colony blueprint, not the `umf.json`/`kithrin.json` faction files). Prints each design's `FleetRoleComposer.ClassifyRole` + combat spec (the holistic role readout), and asserts the new gap-filler units are armed warships (Firepower>0 → AI-fielded) landing in their designed role: **Kithrin Harbinger → Artillery** (heavy disruptor/plasma standoff), **UMF Legionnaire → Line** (heavy chain-flak brawler), and the light **UMF Skirmisher** out-evades the heavy Legionnaire (the Screen candidate — the 0.5 bar is read off the printout + tuned next). The armed-unit acceptance test for "the AI knows how to use it" |
| `GroundGarrisonPerFactionTests` | **Per-faction ground garrison (the ground echo of per-faction fleet composition).** `GroundStartGarrison.CompositionFor` reads a faction's authored garrison mix off `FactionInfoDB.GarrisonComposition` (default 3/2/1 = 6 when unset); the DevTest UMF raises its authored heavier **Martian legion** (4 Inf / 3 Armor / 2 Arty = 9) on its worlds from the `"garrison"` JSON node while the player's Earth keeps the default light watch (6). A militarist aggressor's worlds start more heavily defended |
| `FleetCompositionPerFactionTests` | **Per-faction fleet composition (slice B).** `FleetAssembly.TemplateFor` reads the faction's own ladder off `FactionInfoDB` (default 3/8/18 when unset/null); the DevTest UMF loads a **Martian Battle Line** (4/10/20) and the Kithrin a **Kithrin Raid Swarm** (3/6/12) from their `fleetComposition` JSON nodes (player keeps the default); and `AssembleBuiltWarships` stamps the faction's ladder onto the forming fleet's `FleetCompositionDB`. The fleet-size half of each faction's identity |
| `FleetCompositionEscalationTests` | **Fleet-composition slice 3 — the AI sizes its fleet to its resources AND keeps a reserve.** The pure aspiration decision (`FleetAssembly.AspirationFor`) scales with treasury (broke→Deployable / 50k→Ideal / 150k→Perfect) and bumps a tier at war; the `ConquerResolver` massing rung keeps building PAST one fleet (a single complete fleet with no reserve → still `QueueWarship`) and STOPS only once a strike fleet + a reserve fleet both exist (→ not `QueueWarship`); and the reserve guard `ConquerResolver.HasHomeReserve` won't let the AI commit its LAST organized fleet (false with one fleet, true once a reserve exists, true for a non-organized sandbox fleet). The developer's "don't pull all fleets at once" — the military-commander reserve doctrine |
| `ProductionBuildTests` | the build-to-product link — the factory consumes stored minerals and installs a new Refinery on the colony (`InstallOn` path); proves "resources → installed product", the rails a built unit rides |
| `GroundBombardmentTests` | **the space→ground link for "take a planet"** — orbital bombardment of a colony (`DamageProcessor.OnTakingDamage` → `OnColonyDamage` → `ApplyGroundBombardment`) now softens its DEFENDING ground garrison: a heavy strike drains the defenders' health and kills some units, while a non-defender (a landed invader) is left untouched (the friendly-fire guard); AND the strike is reduced by a unit's defences via `GroundDamageMatrix` — a SHIELDED defender resists better than an identical unshielded one. A garrison-less colony is byte-identical (additive) |
| `TakeAPlanetIntegrationTests` | **the MVP capstone — "you can take a planet" as a CONNECTED chain** (Prime Directive: test the cross-system stack). End-to-end through the real engine paths: orbital bombardment (`OnColonyDamage`→garrison softening) clears a colony's defending garrison, a landed invader is left holding region 0, and `GroundForcesProcessor` CAPTURES it (region owner flips). Its complement (an invader that CAN'T take a full un-bombarded garrison) is `RegionCombat_StrongerGarrisonWins`, so together they prove the bombardment is what enabled the invasion |
| `CombatReadoutTests` | **MVP Stage 1** — the space-combat *damage* gauge (the path was 🔴 DARK / no tests). Calls `DamageProcessor.OnTakingDamage` directly on a real ship with a beam-shaped fragment and prints `[combat]` (health drop / components removed / destroyed?). Reading: the per-pixel sim deposits ~0 damage — which is why the auto-resolve engine below routes around it. |
| `GroundResolverComparisonReadout` | **resolver merge, slice 3b-ii PREVIEW** — the CI-certified BEFORE/AFTER table for the ground-resolver swap (the developer's "show me the numbers" gate). Computes per-attacker damage the OLD way (triangle × matchup × armour) vs the NEW kernel path (dodge + shield-pool + armour, no triangle), holding terrain/stance/fort equal so only the merge's change shows. Asserts the exact values, so green = the table is right. Headline: the Armor▸Inf▸Arty triangle dissolves (Armor→Inf 258→167, Inf→Armor 44.5→77.5, same-type unchanged); dodge is preserved; shield goes %→depleting-pool (stronger early, collapses). Additive/no live change — the swap is the next slice, after sign-off |
| `GroundKernelBridgeTests` | **resolver merge, slice 3b-i (the bridge)** — proves a `GroundUnit` presented as a `CombatKernel.Combatant` reproduces the ground combat semantics THROUGH THE SHARED KERNEL: an aimed Ballistic weapon is dodged ≈(1−evasion), Artillery/Melee are undodgeable, Energy bleeds through shields where Kinetic is soaked, and flat armour bounces a swarm — the Armor▸Inf▸Arty triangle falling out of weapon-nature × evasion/shield/armour, not a bolted-on multiplier. The verification that de-risks the 3b-ii resolver swap; the mapper is additive/unwired so live ground combat is byte-identical |
| `CombatKernelTests` | **resolver merge, slice 1** — the pinning + byte-identity gauge for the new shared `CombatKernel` (the domain-neutral salvo math a ship AND a ground unit will run through). Pins `HitFraction`/`ArmourSoak`/`ResolveShield`/`SoakFractionOf` to hand-computed values AND cross-checks the kernel against the live `CombatEngagement` (ship) + `GroundDamageMatrix` (ground) functions over an input sweep, so the slice-1 duplication can't silently drift before slice 2 removes it. Pure math, no colony harness |
| `CombatFleetTreeSafetyTests` | **the FROZEN-CLOCK / PERF-FREEZE regression gauges** (`[Timeout]`-guarded, so a real hang fails fast instead of wedging a CI shard). Builds MALFORMED fleet trees (a diamond, a cycle, a 25-level STACKED diamond = 2^25 unguarded descents) and asserts every fleet-tree walk terminates + counts each ship once: the already-guarded `GetFleetShips`/`GetCombatShips`/`AllShipsRecursive`/`Root`, AND (2026-07-16 round 2) the previously-UNGUARDED `FleetCombat.Ships`/`FirepowerAtRange`/`SensorReach`/`WarpSpeedFloor` + `FleetAssembly.ArmedShipCount` (the walker run inside `CombatEngagement.Tick` — the actual "StarInfoDB TRUE WEDGE"). Plus **the SOURCE guard** (`ChangeParent_UnderOwnDescendant_Refused_NoCycleFormed` — the client drag-drop that built the cycle is refused) and **the PERF thrash bound** (`StandingHostilePair_DoesNotFineStepUnboundedlyAcrossAdvances` — a standing imminent-but-never-engaging pair must not re-arm the 24-fine-step tax every game-hour; reads the new `MasterTimePulse.FineStepCount`). The gauge board for the whole freeze class |
| **Auto-resolve combat engine** (8 fixtures, `GameEngine/Combat/`) | the v1 space-combat spine — all CI-green: |
| &nbsp;&nbsp;`ShipCombatValueTests` | every starting design gets a `ShipCombatValueDB` (toughness > 0; firepower > 0 for beam-carriers) at build |
| &nbsp;&nbsp;`AutoResolveTests` | the salvo loop — stronger fleet wins & wipes the weaker; zero-firepower = stalemate; combatants die before utility hulls |
| &nbsp;&nbsp;`BattleTriggerTests` | `CombatEngagement.Tick` detects two hostile fleets in range, engages, and resolves; same-faction fleets never engage; **+ the combat interrupt** — a new engagement with `InterruptTimeOnNewEngagement` on sets `TimePulse.CombatInterruptPending` (clock halts at first contact); asserts it defaults off + try/finally so the static flag never leaks. **+ `NewEngagementImminent_*`** — the time-loop's fine-step gate reads **true** for two hostile un-engaged fleets in range and for one-engaged-one-joining (round 2), **false** for BOTH-already-engaged (the ongoing fight runs at the player's set speed, not a forced 5 s), for friendly-only, and for no-fleets. The *clock-stops-early* integration is **live-only** — this colony harness doesn't reliably auto-fire the battle-trigger hotloop on `TimeStep`, which is also why the fixtures drive `Tick` directly. **+ `Tick_RequireDetection_NoBattleUntilDetected`** — fog of war (detection slice 2): with `RequireDetectionToEngage` on, two hostile sensor-capable fleets in range do NOT engage until a sensor scan populates the track tables (no scan → no battle; scan → battle). Flag defaults off; try/finally so it never leaks. **+ `FirstStrike_SeerWipesBlindEnemy_Unscathed`** — first-strike (detection slice 5): two EQUAL armed fleets, the enemy BLINDED via the grave-rung path (its `SensorReceiverAtb` components removed + `ReCalcAbilities`), fog on → the player detects it and wipes it taking **zero** losses (the directed-fire resolver lets the blind fleet take fire without returning it; equal forces both firing would be mutual destruction, so player-intact + enemy-wiped proves the asymmetry — composes detection × grave-rung × weapons). (Clears the colony's own start fleets first — see the fixture's `ClearExistingFleets` note) |
| &nbsp;&nbsp;`FleetDoctrineTests` | the moddable doctrine catalog loads; `TrySetDoctrine` applies mults + honours the switch cooldown; an aggressive (×2) fleet beats the identical enemy |
| &nbsp;&nbsp;`FleetComponentTests` | per-component doctrine — a ship in an offensive sub-fleet reads ×2 while a ship directly in the fleet reads ×1; a component's ×2 flips a battle the raw hull would lose |
| &nbsp;&nbsp;`FleetRetreatTests` | retreat — a `fighting-withdrawal` fleet breaks off intact (posture); a 4-ship fleet that loses half retreats with survivors (threshold); both record a `FleetRetreatDB` |
| &nbsp;&nbsp;`EngagementLockTests` | an engaged fleet (`FleetCombatStateDB`) refuses an AssignShip order; a `TrySetDoctrine` on it still applies; clearing the combat state lets the order through |
| &nbsp;&nbsp;`WeaponRangeTriggerTests` | **the weapon-range battle trigger** (the developer's rule, 2026-07-02) — a fight auto-starts only within actual WEAPON range, not the 1 Gm proximity bubble. Pure `WithinWeaponRange(sep, reachA, reachB)` math; through the real `Tick`: two 500 km-gun fleets 752 km apart DON'T engage (the live "not actually in battle" report), close to 400 km and they DO, flag-off still engages at 752 km; `NewEngagementImminent` agrees (weapon-range + weapons-release gates, so the combat-interrupt fine-step can't crawl) |
| &nbsp;&nbsp;`CombatTestShipsTests` | the example ships (Aegis warship / Picket corvette) load + rate strong-vs-weak; a 3v3 auto-resolve is a decisive `SideAVictory`; **+ `ChargeReactors_FillsStoredEnergy_SoASpawnedShipCanWarp`** — a freshly built Leviathan boots with 0 stored energy (< warp bubble cost), `ShipFactory.ChargeReactors` tops it to `EnergyStoreMax` ≥ bubble cost (the "spawned ship won't move" fix) |
| **Combat depth — weapon flavor + dodge** (`docs/combat/WEAPONS-DESIGN.md`) | all CI-green: |
| &nbsp;&nbsp;`ShipEvasionTests` | Evasion (maneuverability) — a small/light/high-thrust fighter dodges far better than a heavy sluggish battleship; no engine = no dodge |
| &nbsp;&nbsp;`WeaponProfileTests` | per-weapon flavor profiles — a laser warship has Beam profiles at ~light-speed with hit-chance + rate-of-fire; profiles sum to Firepower |
| &nbsp;&nbsp;`DodgeResolveTests` | the dodge — `HitFraction` curve (beams ignore evasion, slugs are dodged, flak floors it); and through the resolve, slug fire kills the un-evasive battleship while the same-toughness fighter survives |
| &nbsp;&nbsp;`CombatPerformanceTests` | 200 real warships resolve in milliseconds — the dodge resolve is O(ships) (fire aggregated by weapon class), the tripwire against an O(ships²) regression |
| **Combat — multi-party engagements** (`GameEngine/Combat/`) | CI-green: |
| &nbsp;&nbsp;`MultiPartyEngagementTests` | any number of fleets / either side / join mid-fight (`StepEngagementGroup`) — **assist** (two fleets beat a lone equal enemy on combined fire), **same-faction side** (a friendly reinforcement shares a side: no friendly fire — sized so an ally would die if it regressed), **join** (a reinforcement pulled into a 1-v-1 through the real `Tick` tips the fight), **fire-split** (one fleet vs two divides its fire — can't kill both in the step it could kill one, but reaches both). The 2-fleet path stays the existing fixtures' n=2 special case |
| &nbsp;&nbsp;`RailgunWeaponTests` | **P3 railgun, through the REAL data path** — builds the base-mod `Lancer` railgun cruiser (JSON `railgun-weapon` template → `RailgunWeaponAtb` via reflection → `ShipCombatValueDB`) and asserts the railgun profiles rate as finite-velocity, ballistic (low-tracking) kinetic fire that's **dodgeable** (lands on a sluggish hull, juked by a nimble one). This + `BaseModIntegrityTests` gauge the gotcha-10 JSON→Atb binding in CI |
| &nbsp;&nbsp;`FlakWeaponTests` | **P4 flak, through the REAL data path** — builds the base-mod `Bulwark` flak escort (JSON `flak-weapon` template → `FlakWeaponAtb` → `ShipCombatValueDB`) and asserts **saturation = rounds/sec × pellets/shot** (high) FLOORS the dodge: flak lands heavily on a nimble (ev 0.9) target where a low-saturation slug is juked — the fighter/missile killer. Same gotcha-10 gauge as railgun |
| &nbsp;&nbsp;`WeaponTriangleTests` | **P6 the triangle, on REAL built ships** — builds the `Wasp` fighter (tiny/agile = evasive) + `Leviathan` capital (big/sluggish/armoured) and asserts the dodge edges off their real combat values: FIGHTER ▸ railgun (fighter dodges slugs the capital eats), BEAM ▸ fighter (light-speed ignores the dodge), FLAK ▸ fighter (saturation floors it). All four corner designs (Wasp/Leviathan/Aegis/Bulwark) spawnable from DevTools |
| &nbsp;&nbsp;`WeaponTriangleBattleTests` | the triangle as ACTUAL fleet battles (not just hit-fractions): identical fighter screens take equal firepower — survive the railgun, fall to beams/flak; and an evasive Wasp swarm out-survives an evasion-zeroed swarm vs the same capital. Isolates evasion so it's calibration-robust |
| &nbsp;&nbsp;`CombatStressLab` | **10 extreme weapon-design + fleet-scale stress sims** (post-rebalance, `SalvoDamageScale` 0.1), assertions encode measured DIRECTION + real numbers: rate-of-fire (S01: 38 vs 5 /100) and velocity (S03: 39 vs 5) each defeat evasion like saturation does; slow flak is useless (S02: 34 vs 7); nothing is untouchable (S04: 39 vs 3 of ev0.95); alpha beats attrition (S05); 100v100 mirror is **exactly even** (S06: 50–50); a big-enough swarm overwhelms a super-capital (S07: ~400 now, since the slower pace lets the swarm's 50% retreat trigger first) at ~25–50 fighters/capital (S10); doctrine x2 swings a fight ~1.6:1 (S08: 40 vs 25); dodge scales to fleets (S09: railguns leave 85/100, beams 49/100) |
| &nbsp;&nbsp;`ShieldTests` | **the space SHIELD layer** (option B, depleting/regen pool) — Phase A (`ShieldAtb` pool + `ShipCombatValueDB` sums it, 0 if none) + Phase B (the resolver wiring): pure `SoakFractionOf` (nature matchup — kinetic 1.0 / energy 0.5 / exotic 0.0) + `ResolveShield` (drain/regen/bypass) math, AND end-to-end through the real `StepEngagement` — a shielded hull takes LESS kinetic fire than an unshielded one (the pool soaks it) while an EXOTIC anti-shield attacker bypasses (same fire reaches the hull as unshielded). Unshielded = byte-identical |
| &nbsp;&nbsp;`DisruptorWeaponTests` | **Phase D** — the base-mod **Ion Disruptor** (anti-shield exotic) through the REAL data path (the exotic twin of `RailgunWeaponTests`): the Ravager frigate builds from JSON, its `disruptor-weapon` binds `Weapons.DisruptorWeaponAtb` and rates as a light-speed (undodgeable), `WeaponNature.Exotic` profile, and end-to-end through `StepEngagement` it BYPASSES a shield (a shielded hull takes the same disruptor fire as an unshielded one) while an equal-power kinetic gun is soaked by that same shield — the anti-shield payoff |
| &nbsp;&nbsp;`ShieldBaseModTests` | **Phase C + the fleet readout** — the base-mod **Deflector Array** through the REAL data path (the shield twin of `RailgunWeaponTests`): the base-mod Bastion cruiser builds from JSON, its `deflector-array` components bind `Combat.ShieldAtb` and project a shield pool into `ShipCombatValueDB` (JSON template → NCalc → atb → combat value), and an unshielded Aegis reads a 0 pool (additive). The gotcha-10 JSON→atb sensor for the space shield |
| &nbsp;&nbsp;`WeaponAxesTests` | the two-axis weapon foundation — `WeaponProfile` carries Nature × Delivery independently (phaser Energy/Beam vs blaster Energy/Bolt), copy-safe, defaults Kinetic/Slug |
| &nbsp;&nbsp;`PlasmaBoltWeaponTests` | **the two-axis payoff on real data** — the base-mod Plasma Repeater (Vanguard) builds from JSON, its `plasma-repeater` binds `Weapons.PlasmaBoltWeaponAtb` and rates as a finite-velocity, dodgeable (**Railgun-class**) but **`WeaponNature.Energy`** bolt: a nimble ship jukes it like a railgun (`HitFraction` falls with evasion) yet a shield only HALF-soaks it (`SoakFractionOf` = 0.5) where a same-class kinetic railgun is fully soaked (1.0). Same dodge-class, different nature — the axes are independent |
| &nbsp;&nbsp;`WeaponClassifierTests` | **weapon-unification** — `WeaponClassifier.Classify(delivery, vel, trk, sat)` derives the triangle corner from the axes+specs (each corner + the disambiguating edges); every real base-mod weapon's computed `WeaponProfile.Class` is the EXPECTED corner (laser→Beam/railgun→Railgun/flak→Flak/ion-disruptor→Beam); and the corner survives `BuildFireMix` **aggregation** (the mix buckets by class+nature+delivery, so a real Vanguard's plasma keeps Energy/Bolt, not the Slug default). `Class` is now a pure computed read-out — no authored type field |
| &nbsp;&nbsp;`CombatBattleSims` | **the "10 more" whole-BATTLE sims at the rebalanced pace** — B01 duration (a 50v50 mirror lasts 38 salvos = 190 game-seconds, not 2–4); B02 duration scales ~linearly with toughness (38/150/599 at ×1/×4/×16); B03 saturation frontier + B04 evasion frontier as smooth curves; B05 combined-arms (railgun+flak) clears a mixed enemy better than mono; B06 quality endures over quantity at equal totals; B07 symmetric 3-way FFA (5/5/5); B08 a joining reinforcement flips a losing 10-v-20; B09 a mid-fight doctrine switch turns an even mirror into a 22–15 win; B10 1 dreadnought vs **1000** gnats resolves in ~9 ms (the bucketed-resolve tripwire at extreme scale) |
| `WarpArrivalNaNGuardTests` | **the warp-arrival "Speed Result is NaN" [FATAL] guard (2026-07-23).** A fleet "move to body" warp arrives co-located with its target (`ExitPointrelative == (0,0,0)`), which collapsed the derived orbit's SMA to +0 and made vis-viva compute `|2/0 − 1/0| = NaN` → a throw on the background sim thread = an unobserved `[FATAL]` that killed the clock. Pure math (no harness): asserts `OrbitalMath.InstantaneousOrbitalSpeed(sgp, 0, 0)` no longer throws + returns finite (the defensive backstop), a zero-position `KeplerFromPositionAndVelocity` reads as a degenerate orbit (the geometry the warp fallback diverts), and `GetStateVectors` on it doesn't throw. Red-before/green-after. See `GameEngine/Movement/CLAUDE.md` gotcha #6 |
| `GameLoopSmokeTests` | core sim loop advances on a generated (colony-less) universe |
| `SaveLoadSmokeTests` | `Game.Save → Load` round-trips (colony-less universe; doesn't check designs) |
| `SaveLoadDesignRoundTripTests` | **the DESIGN save sensor (US-2, 2026-07-14)** — the guardrail the universal-designer/Entity-Assembler work rides on: builds a real faction+colony (`TestScenario.CreateWithColony`), `Game.Save`→`Game.Load`, and asserts every `ComponentDesign` survives identical — same count + key set, and per design the same `UniqueID`/`Name`/**`AttributesByType` type-key set** (the `*Atb` type names `TypeNameHandling.Objects` embeds). So a later rename/drop of any design/`*Atb` class fails CI instead of silently corrupting a player's save. The complement to `SaveLoadSmokeTests` (which never checks designs) |
| `ChassisAtbTests` | **the shared chassis-budget abstraction (US-3, 2026-07-14)** — asserts `ShipHullAtb`/`GroundChassisAtb` implement the additive `IChassisAtb` (StructuralBudget/BudgetKind/PartMount) with the right budget currency (Mass vs Carry) and mount, and that `ComponentMountType.Station` exists. The interface members are `[JsonIgnore]` computed getters → no save-JSON change (the `SaveLoadDesignRoundTripTests` round-trip is the proof); the live gates (`ShipDesign.Recalculate`/`GroundUnitAssembly.Compute`) are untouched → byte-identical combat/construction |
| `StateIntegritySmokeTests` | entity positions stay finite across a clock advance (catches silent NaN) |
| `PerformanceReadoutSmokeTests` | reads the engine's per-processor stopwatch; prints timing |
| `BaseModIntegrityTests` | base-mod JSON data (starting designs buildable; zero skipped entries) |
| **Component-designer re-derivation** (3 fixtures, `docs/economy/DESIGNER-NORTH-STAR.md`) | the fixes falling out of the door-by-door designer derivation. All byte-identical where they touch a shipped design: |
| &nbsp;&nbsp;`FireControlMassLeakTests` | **Sensors S2 (§34.4)** — the beam director's two phantom dials (`Size vs Range`, `Size vs TrackingSpeed`) appeared in **exactly one place**, the mass formula, and never reached the attribute: both at their 0.25 minimum divided a director's mass by **sixteen with no loss of range or tracking speed**, and mass is the currency the Chassis door budgets on. Asserts the dials are gone from the **template** (structural — no future design can re-open the discount), that the shipped Beam Fire Control still weighs exactly **150 kg** and binds Range 100 / Tracking 5000 (with the PD director as the untouched control), and that mass is now a pure function of capability. The design's own `"Size vs Range": 1` override went in the same change — `ComponentDesignFromJson:34` indexes `ComponentDesignProperties[key]` **unguarded**, so an override pointing at a deleted template property throws `KeyNotFoundException` on New Game (gotcha #10 — check the OTHER end) |
| &nbsp;&nbsp;`PowerFuelGateTests` | **Power P0 (§39.7a)** — the fuel chain's last rung. `Lifetime` now reaches `EnergyGenerationAtb` in **seconds** (asserted as the exact 8760 × 3600, with carried fuel == charged fuel so the books balance): the reactor authored hours and the RTG years, so their real endurance was **2.43 hours** and **5 seconds** — only the turbine was dimensionally correct, and it is the only one with a shipped design, which is why nobody had noticed. `Output vs Economy` is asserted to buy power with fuel-per-kilowatt (exact ½ power / ¼ fuel), the reactor's specific fuel rate is asserted **as a ratio** against the turbine's so a future turbine re-tune fails loudly instead of drifting, and `EnergyGenProcessor.EnableFuelExhaustion` (static flag, default **off**, reset in `finally`) makes a dry generator produce nothing — installed on `s.Colony` with `dateTimeLastProcess` pinned so a negative span can't refund fuel |
| &nbsp;&nbsp;`PowerJustificationTests` | **Power §39.8 — the developer's rule made executable:** *every option behind a door must win at least one axis outright, or it is clutter.* Six tests over the five power templates on the axes the sim actually reads (kW/kg · kW/m³ · crew · signature · fuel · mounts): the **reactor** wins power-per-kilogram **and its crew bill is no longer a kilogram count** (`CrewReq` was `[Mass]` — **1500 crew for a 1500 kg reactor**, which made the densest generator in the game unusable, since component crew sums into a ship's `CrewReq` and a colony's infrastructure demand); the **turbine** wins power-per-cubic-metre by >100×; **solar** is the ONLY silent source (all three fuel-burners carry a 1700 K `SensorSignatureAtb`) and now mounts `PlanetInstallation`/`Station` so a colony can finally build a power plant while keeping the ship mount five ship designs depend on; the **RTG**'s zero-crew axis is asserted on the **template** (it ships no design) — it was ~22,000× worse per kilogram than a turbine to save two crew, a strictly dominated recipe; the **battery** is asserted to be a different job, not a worse generator. **`NoPowerType_IsDominatedOnEveryAxis` is the rule itself** — one representative per TYPE, so the next dead recipe fails CI instead of shipping. ⚠ **It went red on first CI run and the GAUGE was wrong, not the data** (§39.8c): a solar array has no stored kilowatt number (the engine recomputes it per tick from the star's attenuated light), so the test did the physics by hand — and `BestEfficiency` is a **percent** (`8.0`), which the engine converts with an explicit `* 0.01` at `EnergySolarGenProcessor.cs:106`. Reading it as a fraction made a 20 kg panel out-perform a 1500 kg fission core per kilogram. It now calls the engine's own **`AbsorbedPower`** at a stated 1 AU illumination instead of re-deriving it — the campaign's own rule (*read what the sim reads*) applied to its own instrument |
| &nbsp;&nbsp;`CargoCompartmentTests` | **Logistical L1 (§46a) — WHAT A SHIP CAN CARRY.** Six cargo classes were declared in `cargoTypes.json`; **three had no component that provided them.** `passenger-storage` and `cryogenic-storage` were declared and provided by **nothing**, while `TeamObject` (every `Scientist`) has always declared `CargoTypeID = "passenger-storage"` — so **a research team could never be loaded onto anything**, and `CargoMath.GetFreeVolume` reported that as a **silent 0**, not an error (the worst failure shape in the codebase: "cannot carry that" and "no room right now" read identically). Landed four compartment templates on the existing 2-arg `CargoStorageAtb` (**no ctor overload → no L13 save risk**): `passenger-cabin` · `cryo-bay` · `refrigerated-hold` · `containment-hold`, plus the two classes the taxonomy lacked (`perishable-storage` for food and anything alive, `contained-storage` for cargo dangerous to its own carrier). **Five tests:** ① 🔒 **the structural rule that cannot rot** — every declared cargo class must be providable by something, with an allow-list where each entry states *why* (`battery-storage`: energy lives in the parallel `EnergyStoreAtb`); ② the bug **red-before/green-after** (a team reads 0 free volume on a general-storage-only host, real room once a cabin is installed); ③ **each class wins an axis** (§39.8 — general 0.01 kg/m³ < refrigerated 0.02 < passenger 0.04 < containment 0.06, and cryo carries 5× the people on half the crew); ④ the **person-volume** pinned — `TeamObject` reported `0.065 × teamSize`, **65 litres a head = the volume of a human body**, which would berth 7,000 people in a 500 m³ cabin; now `PassengerPacking` (10 m³ a berth, 2 m³ a pod), **byte-identical because nothing provided passenger storage so it was never read**; ⑤ the two shipped cargo holds asserted **unchanged** (same type/volume/mass) against the armed mass-budget gate. ⚠ The fixture's own readout warns that the **absolute** masses are ~1000× light across the whole hold family — an inherited unit bug (the `Size Efficiency` property describes itself as *"tonnage"* and the field is kg); the new four are costed on the same law deliberately. ✅ **The developer then ruled *"make the field kgs no tonnes"*** and the shipped design NAMES pinned the factor at exactly **100** (not the ~1000× estimated from the *"tonnage"* wording): `Cargo Hold 1t` and `Cargo Hold 5t` now mass exactly **1,000 kg** and **5,000 kg**, so the names became true — a **unit** fix, not a balance change, and the one deliberately non-byte-identical part (the **Freighter** gains 4,950 kg against a 90,000 kg budget; `ShipMassBudgetTests` adjudicates). ⚠ **Then the developer asked where the fixes for the cargo VARIETY were, and it landed:** 37 goods existed and **30 were in one box (81%)** — eight compartments and nothing reclassified into them. Two more tests followed. **`EveryGood_NamesACompartmentSomethingProvides`** is the MIRROR of the class-side rule and the one that should have been written first: walking **goods → providers** (not just classes → providers) found `electricity` and **`lithium-battery`** declaring `battery-storage`, which nothing provides — and **the class-side allow-list had excused it**, valid for a charge and **plainly wrong for a manufactured object you can build and then not store**. 🔒 *An allow-list entry that reasons about the CLASS can hide a bug about a GOOD.* `lithium-battery` → `general-storage`. **`Food_IsAShippableGood_AndAnImportedStockpileFeedsAColony`** pins the whole new chain: the `food` good exists at last (`SustenanceProcessor.cs:14` had said *"food from the — not-yet-existing — food cargo good, so 0 for now"*), rides `perishable-storage` so **a bare hold refuses it**, is refinable at the start colony, and `SustenanceProcessor.DrawStoredFood` **consumes** it to cover a shortfall the farms cannot — so **a colony that cannot farm can be supplied by one that can**. Unit-careful (demand and farm output are per-DAY, a stockpile is a QUANTITY, the processor is MONTHLY — the same trap as the reactor `Lifetime` bug) and **byte-identical at the stock `PerCapitaFoodDemand` of 0**, which the test asserts on both sides. 🔒 **Then "get the last 12%": three goods moved to their physically-correct compartment** — `water` and `hydrocarbons` are **liquids** → `fuel-storage`, raw `fissionables` is **radioactive** → `contained-storage` (and it already had a real consumer: it is the input to `fissile-fuels`). **82% → 74% general**, which is the HONEST FLOOR: the remaining 28 are ore, metal, plating and parts, which genuinely ARE bulk, and `TheThreeMisfiledGoods_…` asserts they **stay** there so the move can never become a sweep. 🔑 **`EveryMineralThisWorldHas_HasSomewhereToGoOnTheColony` is the gauge that had to exist FIRST** — reading `MineResourcesProcessor` corrected the severity I had assumed: `AddCargoByUnit` returns **what actually fitted** and only that is subtracted from the deposit, so **you do not lose ore — the colony silently STOPS MINING**, no error and no log, the same signature as the Stasis bug. Measured before moving anything: fuel storage is **1,000,000 m³ at 5% use** (my "a few thousand m³" caution was the right shape and the wrong size), while **containment and refrigerated were ZERO** — and Earth *refines food*, so it had nowhere cold to put it. Fixed by registering **and installing** a `containment-vault` + `cold-store` (10,000 m³ each). Blast radius **verified not assumed**: a sweep of every scenario file shows **Earth is the only colony blueprint in the base mod**. 🔒 **Then the developer's rule — *"if we're going to add all these resources we need to justify their existence"* — made executable as `EveryResource_IsConsumedBySomething` (§46f), the goods-side twin of §39.8.** Three tests for a GOOD: a **producer** (true of all 38 by construction), a **consumer**, and being **distinguishable**. ⚠ **The audit corrected itself and the correction is the lesson:** a first pass scanning only `ResourceCost` reported **14 dead goods** — but **six were armour materials referenced through `ArmorBlueprint.ResourceID`**, a field the scan never looked at, dropping it to **five**. *An audit is only as good as the reference forms it knows about* — and a data scan **cannot see a consumer written in C#** (which is why `food` reads unreferenced despite being eaten by `SustenanceProcessor`), so the allow-list must **NAME the code consumer**. 🔑 **And none of the five was clutter:** four are a **MATERIAL GRADE LADDER** (`stainless-steel-d/-a` at credit 12/80 around the standard 25; `electronics-d/-a` at 80/1000 around 250 — distinct inputs, clean 3-tier spread) which is the structural twin of the **already-wired fuel-grade system**, missing only its READER: nothing lets you build a component out of premium steel. They sit on a **stated waiting list** with a developer ruling owed (build the reader, or delete the four). ✅ The fifth, `lithium-battery`, needed no mechanic — just a line: the battery bank was built from **raw lithium** while the refined cell sat unreferenced, and is now `lithium (mined) → lithium-battery (refined) → battery-bank (built)`. The gauge also asserts the ladder is a real ladder and not three reskins (each tier differs in credit value; the premium tier differs in what it is made of). 🔒 **Then the scope correction — *"I just wanted what wasn't on the list you made initially justified in game"*** — §46f had audited the **38 goods that already exist**, which was the opposite direction: the ask is to WORK IN the taxonomy items the game **lacks**, each built cradle-to-grave. First one landed: **`Antimatter_ExistsCradleToGrave_AndMakesContainmentLoadBearing`**. It needed **zero C#**, because `ExhaustVelocityLookup`/`FuelGradeLookup` (`ChainedExpression.cs:537-560`) read `Formulas["ExhaustVelocity"]`/`["FuelGrade"]` **off the material blueprint** — so a new fuel is a material row plus an engine template. Chain: `fissionables` (mined) → **`antimatter`** (500 fissionables + 50 REE + 20 tungsten → **1** unit at 5,000 industry points) → **`antimatter-engine`** → `default-design-antimatter-drive`, all six registration points at Earth. 🔑 **The joint that makes CONTAINMENT load-bearing rather than decorative:** `GetFuelTypes` (`ComponentDesignDisplay.cs:939`) walks the fuel dial's `DataDict` **keys as CARGO CLASSES** and offers only materials in that class whose `Formulas["FuelType"]` matches — antimatter rides `contained-storage`, so **a ship with this drive and no containment hold has a drive it cannot feed.** ⚠ **And the calibration correction, which is the durable lesson: the FIRST version of this test PASSED a broken number.** Authored at grade 0.35 and asserted with `Grade(antimatter) < Grade(ntp)` — worthless, because **thrust = EV × mass flow and mass flow scales with grade**, so an 8.6× EV rise swamps a 2.1× grade fall: antimatter had **4× the nuclear drive's thrust AND 8.6× its range**, strictly better on every axis but price — the exact dominance §26.3 exists to remove, shipped behind a green test. 🔒 ***Assert the PRODUCT the sim computes, not the input dial*** — both templates share the same `[Mass] * 0.017 * grade` coefficient, so `EV × grade` **is** thrust per kg of engine; at the corrected grade **0.05** that reads **51.0 N/kg vs the nuclear drive's 89.2**. *(Second time this campaign a green test hid a real defect — the first was the 100× solar-unit error in `PowerJustificationTests`; both gauges re-derived a number instead of computing what the engine computes.)* The resulting position is a real one: antimatter is **not a fleet fuel** — it is for the deep-range probe that cannot afford propellant mass, and that sentence is in the material's own player-facing description |
| &nbsp;&nbsp;`DockBayTests` | **THE DOCK — its own area (§46d), the one cargo class the compartment taxonomy could not express.** A compartment measures cubic metres poured in; a **vessel is discrete and leaves under its own power**, so a dock answers *how many and how big*. Buildable `docking-bay` template (its own `TemplateFiles/docking.json`) with a **SPLIT dial**: `Berth Tonnage` carved into `Berths`, so **total capacity is invariant** (the §34.7a honest shape) while each extra door narrows the rest **and costs structure + deck crew** — so "many small" is flexibility you buy, not get. Two shipped designs prove it: `docking-bay` (60 t / 4 doors) and `heavy-berth` (the same 60 t / 1 door). **Five tests, all calibration-independent** — they measure real spawned ships and real installed bays and assert RELATIONSHIPS, so a re-tune cannot make the fixture lie: the JSON→atb bind + the split invariant + its costs; **the DOOR gate** (a ship too heavy for the widest berth is refused however much tonnage is free, and the refusal names the door — proven against measured hull masses, with a guard that fails loudly if no shipped design is big enough to exercise it); **the BUDGET gate separately** (identical ships dock until the tonnage runs out, refusal names the budget); the **position wire** (a docked ship is re-parented via `PositionDB.SetParent` so it travels with its carrier) and the **grave rung** (`UndockAll` releases everything **alive** — losing a hangar must never delete the ships inside); and the additive case (no bay ⇒ zero capacity, readable refusal, and **asking never attaches the blob** — a read must not mutate). ⚠ Found while writing it: **there are TWO `PositionDB` classes** and the one in `Engine/Datablobs/PositionDB.cs` is **entirely commented out**, so importing `Pulsar4X.Datablobs` for it silently gets you nothing — the live one is `Pulsar4X.Movement` |
| `GroundUnitDesignerTests` | **shared-designer A1** — installing a ground-unit component (a `GroundUnitAtb`) raises a `GroundUnit` on the planet from the atb's stats and removes the transient component (the build→deploy hook); + a null-parent defensive check (never throws in the industry path, L4) |
| `GroundUnitBaseModTests` | **shared-designer A2** — the three base-mod ground-unit designs (infantry/armor/artillery) load onto the start faction, bind a `GroundUnitAtb` from JSON with the garrison's stats, and mount as `PlanetInstallation` (so building one auto-installs → raises the unit). The gotcha-10 JSON→atb sensor for ground units (the `RailgunWeaponTests` equivalent) |
| `WeaponGroundMountTests` | **weapon-unification P1** — the five base-mod direct-fire weapon templates (laser/railgun/flak/disruptor/plasma) carry `ComponentMountType.GroundUnit` alongside their ship mount (the ONE designer now offers them on a ground chassis), while the ordnance `missile-launcher` and defensive `deflector-array` deliberately do NOT (weapons-only slice). The gotcha-10 JSON sensor for the mount flag — purely additive, no resolver change yet (P2/P3 wire the supply gate + profile read) |
| `PowerPlantGroundMountTests` | **weapon-unification P2a** — the SUPPLY twin of P1: the three fuel-burning reactors (`reactor`/`rtg`/`steam-turbine-reactor`) carry `ComponentMountType.GroundUnit` (power is a mounted part, same as a ship — the developer's cradle-to-grave call), while energy `battery-bank` (storage) and `solarArray` deliberately don't. The gotcha-10 JSON sensor for the reactor mount flag; purely additive until P2b wires the assembler's power gate |
| `GroundPowerGateTests` | **weapon-unification P2b** — the SUPPLY GATE ("a Titan can, infantry can't"): `GroundUnitAssembly` refuses a ground-unit design whose energy weapons draw more watts than its mounted reactors supply (hard gate). Uses the real faction laser/flak/reactor; calibration-INDEPENDENT (derives "enough reactors" from the new `EnergyDemand_W`/`ReactorSupply_W` gauges). Proves supply mode has teeth: a laser (Energy) needs a reactor, flak (Ammo) doesn't, a railgun is Both. The engine gauge for `WeaponSupply` + the assembler power pass |
| `GroundAmmoTests` | **weapon-unification B (ammo depletion) foundation** — the durable, resolver-agnostic ammo pool: a raised unit snapshots its magazine capacity into `MaxAmmo_kg`/`CurrentAmmo_kg`; `GroundAmmo.Consume` drains, over-consume floors + goes `IsDry` (→ ammo weapons silent), `Refill` tops it; a unit with no magazine never runs dry. **+ resupply (P2c-d v1):** `GroundForces.ResupplyUnit` rearms a dry unit to full on FRIENDLY-held ground, refuses on enemy/neutral ground. The in-combat drain/silence rides the resolver merge (next branch) |
| `GroundUnitFieldingTests` | **slice A — THE CONNECT** — an assembled unit design (frame+parts) via `GroundUnitAssembly.RegisterAssembledDesign` is buildable through the real industry queue and, when the build completes, FIELDS a unit on the colony's planet with the assembly's emergent stats (attack 40 from the rifle, HP 350 from frame+plating). Proves design→build→fielded end-to-end (uses existing planetary parts, so combat-real today; universal-weapon fidelity lands with the resolver merge next branch) |
| `GroundWeaponLoadoutTests` | **W-track W1 (closing-fight fidelity)** — a ground unit no longer COLLAPSES its weapons into one Attack+Range: `GroundUnitAssembly` now also builds a per-weapon `GroundWeaponMount` loadout (each with its own range/mode). Asserts a rifle(range 1)+cannon(range 3) unit carries two mounts with distinct ranges, the byte-identity invariant (Σ mount.Attack == unit.Attack, Max(mount.RangeHexes) == unit.Range) reproduces the old collapse, a single-weapon unit → one mount, and the `GroundUnit` clone deep-copies the loadout. Additive/unread by the resolver → live combat byte-identical |
| `GroundWeaponBandingTests` | **W-track W2 (closing-fight fidelity)** — the payoff of W1: `ResolveRegionCombat` fires EACH weapon in its OWN hex range band. Asserts a rifle(range 1)+cannon(range 3) unit damages an enemy 2 hexes away via the cannon ALONE (the long gun reaches while the short rifle is silent) and MORE when co-located (the rifle bands in). Byte-identical for every pre-W2 fight (empty-loadout & single-weapon units reproduce the collapsed fire); only multi-weapon assembled units change. Mirrors the closing-fight idiom of `GroundForcesTests.RangeCombat_OutRangerHitsCloserUnitFirst_CloneVsZerg` |
| `GroundWeaponPenetrationAssemblyTests` | **OPERATION BLUEPRINT-TO-STEEL A2 — armour-crack carry-through (ENGINE-WIRING-BACKLOG TIER 1 #1).** The assembler weapon PART (`GroundWeaponAtb`) now carries `Penetration` + `PerShotEnergy` (its new 6th/7th ctor args, the K1 pattern), so a *player-designed* AP weapon cracks plate a small-arms weapon bounces off (before, only the monolithic base-mod units did). Asserts a cannon (authored 20/140 in JSON) assembles a unit whose `GroundUnitDesign` + `GroundWeaponMount` + resolver `WeaponProfile` all carry 20/140 (JSON→atb→assembler→design→mount→profile — the gotcha-10 sensor for the new 7-arg ctor); a rifle stays 0/10; on a mixed rifle+cannon unit each mount keeps its OWN pen through `GroundCombatant.ToWeaponProfile(unit, mount)` (per-mount honesty — cracks plate only with the cannon); and `GroundDamageMatrix.ArmourSoak` lands MORE with pen than without at equal damage (the bite). Byte-identity tripwires stay green: `GroundWeaponAttackCostTests` (pen/per-shot NOT in the Mass formula) + `BaseModIntegrityTests` (the 7-arg bind). Real base-mod designs, no hard-coded stats |
| `GroundRoleManeuverTests` | **W-track W3 (closing-fight fidelity)** — sub-formation ROLE maneuver: a unit's role emerges from its stats and each role moves differently as it closes ("fighters move differently than a line ship"). Two pure gauges pin `GroundRoleComposer.ClassifyRole` (no-attack→Support / evasive→Screen / long-range→Artillery / else Line) + `RoleMoveAway` (screen leads · line closes-then-holds · artillery kites-to-standoff · support keeps-away); one wired gauge (flag `EnableGroundRoleManeuver` ON) puts an artillery + a line unit in ONE Close-to-Engage formation facing the same enemy and asserts they move in OPPOSITE directions (artillery kites away, line closes). Flag OFF → byte-identical (resets the static flag in `finally`) |
| `GroundSquadSizeTests` | **Entity Assembler "units per build" dial** — a `GroundUnitDesign.UnitsPerBuild` (squad size) set in the assembler: ONE build of a squad-of-5 design raises 5 units into the muster region (a default design raises 1), and the design's build cost + build-time are exactly 5× the single-unit design's — the dial scales cost (no free multiplication). Drives `OnConstructionComplete` directly (gotcha 7). Default 1 → byte-identical |
| `GroundSpaceWeaponTests` | **W-track W1b (closing-fight fidelity)** — a UNIFIED space weapon gives GROUND firepower once mounted ("if you can power/feed/carry it, you use it"). Pins `SpaceWeaponGround` (laser→Energy / railgun+flak→Ballistic; a higher-dps railgun out-hits a laser on the ground; a native rifle→null) and, through the assembler, a laser on a walker frame contributes Attack = its ship firepower × the flagged `AttackPerDps` scale (calibration-independent assertion), fires as ENERGY, is a loadout entry (so W2/W3 apply), reads under-powered with no reactor and clears the P2 gate once powered WITHOUT changing firepower. A native rifle unit is byte-identical (the space-weapon branch never fires for a `GroundWeaponAtb`) |
| `GroundAmmoGateTests` | **weapon-unification P2c-a** — the AMMO GATE (the ammo-axis twin of P2b): `GroundUnitAssembly` refuses an Ammo/Both weapon (flak, railgun) with no `GroundMagazineAtb` mounted (hard gate; exposes `AmmoCapacity_kg`). Isolates it with FLAK (pure Ammo, draws no power) — flak needs a magazine, a laser (Energy) doesn't. **+ P2c-b `Magazine_SatisfiesTheAmmoGate`** — the buildable base-mod `ground-magazine` binds `GroundMagazineAtb` from JSON (gotcha-10) and flak + magazine clears the gate. The engine gauge for the ammo half of `WeaponSupply` + the magazine registration |
| `SensorDetectionTests` | **first gauge on the (previously 🔴 DARK) sensor/contact layer** — M1 detection lever, slice 1. Two hostile ships at a body; fire `SensorScan` by hand (the harness never schedules it — `PostNewGameInitialization` does, and the colony harness skips it), then assert the player faction holds a `SensorContact` for the enemy ship. Reaches `ProcessorManager.GetInstanceProcessor` via `InternalsVisibleTo`. *Foundation for the fog-of-war seam (slice 2).* |
| `ShipLongRangeLaserTests` | **Weapons ⚙1 ▸ Energy RANGE dial, cradle-to-grave** — the base-mod `default-design-long-range-laser` turns the laser's Range knob to the `tech-beam-range` starting ceiling (10,000 m, Focal Length 5,000 m) on the new Longbow Standoff Cruiser; asserts the ship's beam reach — read the SAME way the firing path enforces it (`WeaponUtils.GetMaxBeamRange_m` → `GenericBeamWeaponAtb.MaxRange`) — comes out at 10,000 m, double a default-laser Aegis's 5,000 m. Proves a JSON design's Range override propagates to the gun (design → NCalc `genericBeamWpnAtbArgs` → atb); default laser untouched → byte-identical. **+ S9 the range COSTS mass** (`TheRangeDial_CostsMass_DefaultUntouched`): the `laser-weapon` Mass formula now adds `Max(0, Range-5000) * 0.5`, so the long-range laser weighs exactly +2,500 (25%) more than the default — long range is *earned*, not free (the cost flows to crew/research/credits/materials via `[Mass]`), and the default laser (anchored at Range 5,000 → zero extra) is byte-identical |
| `ShipHighVelocityRailgunTests` | **Weapons ⚙1 ▸ Ballistic MUZZLE-VELOCITY dial, cradle-to-grave** — the base-mod `default-design-high-velocity-railgun` dials Muzzle Velocity to 200,000 m/s (4× the 50,000 baseline) on the new Culverin cruiser. Asserts (1) COMBAT PAYOFF — its railgun `WeaponProfile.Velocity` reads 200,000 and lands MORE fire on an evasive (0.9) target than a default-railgun Lancer via the real `CombatEngagement.HitFraction` (the velocity-beats-dodge weapon-triangle edge), and (2) THE COST — the railgun Mass formula now adds `Max(0, MuzzleVelocity-50000)/1000`, so the high-velocity design weighs exactly +150 more than the default (which, anchored at 50,000 → zero extra, is byte-identical). Fast is earned; JSON → RailgunWeaponAtb → WeaponProfile → dodge resolver |
| `ShipHighVelocityPlasmaTests` | **Weapons ⚙1 ▸ plasma BOLT-VELOCITY dial, cradle-to-grave** (3rd of the free-velocity-dial trio) — the base-mod `default-design-high-velocity-plasma` dials Bolt Velocity to 600,000 m/s (3× the 200,000 baseline) on the new Tempest Plasma Lancer. Asserts (1) COMBAT PAYOFF — its plasma `WeaponProfile.Velocity` reads 600,000 and lands MORE fire on an evasive (0.9) target than a default-plasma Vanguard via `CombatEngagement.HitFraction`, and (2) THE COST — the plasma Mass formula now adds `Max(0, Bolt Velocity-200000)/10000`, so the high-velocity design weighs exactly +40 more than the default (anchored at 200,000 → byte-identical). JSON → PlasmaBoltWeaponAtb → WeaponProfile → dodge resolver |
| `ShipHeavyFlakTests` | **Weapons ⚙1 ▸ flak DAMAGE-PER-PELLET dial, cradle-to-grave** (a consistency fix — the last free DAMAGE dial) — the base-mod `default-design-heavy-flak` dials Damage Per Pellet to 5,000 J (5× the 1,000 baseline) on the new Redoubt Heavy Flak Escort. Asserts (1) FIREPOWER PAYOFF — its flak `WeaponProfile.DamagePerSecond` exceeds a default-flak Bulwark's at the SAME saturation, and (2) THE COST — the flak Mass formula now adds `Max(0, Damage Per Pellet-1000)/100`, so the heavy design weighs exactly +40 more than the default (anchored at 1,000 → byte-identical). Brings flak in line with the railgun (whose damage dial already costs mass) |
| `ShipHighThrustDriveTests` | **Propulsion ⚙2 ▸ EXOTIC reactionless THRUST dial, cradle-to-grave** (the "settable mass-dial" pattern resolved by a mass FLOOR) — the base-mod `default-design-high-thrust-drive` dials Thrust to 600,000 N (3× the 200,000 baseline) on the new Sprint High-Thrust Cruiser. Asserts (1) AGILITY PAYOFF — the Sprint's `NewtonThrustAbilityDB.ThrustInNewtons` reads 600,000 and it out-dodges an otherwise-identical default-drive Nomad (higher `ShipCombatValueDB.Evasion`), and (2) THE COST — the drive Mass = `Max(Drive Mass, 5000 + Max(0, Thrust-200000)/200)`, so the high-thrust design weighs exactly +2,000 more (un-bypassable floor), default byte-identical. Proves the free-dial fix extends past weapons to propulsion |
| `GroundWeaponAttackCostTests` | **Weapons ⚙1 (GROUND) ▸ the Attack dial now costs (S15)** — resolves the flagged free-Attack dial byte-identically on BOTH axes: BUILD cost (each ground-weapon JSON Mass formula `+ Max(0, Attack-baseline)*1` → the Heavy Battle Rifle at Attack 200 costs +160 build-mass) AND CARRY-WEIGHT (Option A — `GroundUnitAssembly.Compute` floors a weapon's carry-mass at `max(CarryMass, Attack*0.1)`, so the heavy rifle eats 20 carry vs the stock 10). `AttackCarryFactor 0.1` = the tightest stock ratio → every base-mod weapon byte-identical; the stock Guardsman/autocannon gates (`GroundUnitAssemblyTests`) are the tripwire |
| `ShipHighVelocitySiegeTests` | **Weapons ⚙1 ▸ Ballistic SIEGE Muzzle-Velocity cost (S14)** — completes "velocity is earned" for ALL ballistic mass-drivers (railgun S10 + siege S14). The `siege-railgun` Mass formula now adds `Max(0, MuzzleVelocity-50000)/1000`; asserts the base-mod `default-design-high-velocity-siege` (250,000 m/s) weighs exactly +200 more than the stock siege railgun, which (anchored at the 50,000 baseline) is byte-identical. Product of the full mass-chain-resolved free-dial audit (`docs/archive/economy/COMPONENT-DESIGNER-DIALS.md §0d′`) |
| `ShipCaliberTests` | **Enhancers ⚙6.2 — UNIT CALIBER, the first wired dial in the (previously all-dark) Enhancers category.** A per-hull elite stamp: the base-mod `unit-caliber` "Veteran Cadre" (`Combat.UnitCaliberAtb`) multiplies a hull's Firepower + Toughness at build (`ShipCombatValueDB.Calculate`, health-scaled). Asserts the Praetorian Veteran Cruiser (an exact Aegis + a cadre) reads caliber ×1.30/×1.20 from JSON and out-guns the identical stock Aegis by exactly ×1.30 firepower, while an un-calibered ship reads 1.0 (byte-identical). Non-redundant with doctrine/commander (fleet-level) — the only per-hull-at-build combat differentiator. JSON → atb → combat-value gotcha-10 sensor |
| `RangeReadoutTests` | **the "what the player can SEE" gauges** (`docs/combat/INFORMATION-DELTA-DESIGN.md`) — the engine half of making engagement range + detection range visible. Beam-range aggregation on the Aegis (`WeaponUtils.GetMaxBeamRange_m` / `GetBeamWeaponRanges`); the attenuation round-trip (`RangeForSignal` inverts `AttenuationCalc`); loudest-band + activity scaling (`DetectionRange_m`); and `SelfDetectionRange_m` shrinking on Silent end-to-end through the real `FleetEmcon` posture path. Client rings/readouts ride these (CI-blind — local build only). |
| `LedgerTests` | the (economy-disconnected) `Ledger` math |
| **Morale / population / economy / politics** (M-ECON cluster) | the space-economy + political-substrate gauges (all CI-green): |
| &nbsp;&nbsp;`MoraleTests` | morale math (conditions/crowding/employment/comfort/tax/shortage two-sidedness + caps), migration sign/bounds, and the real start colony born neutral; **+ housing data** (`StartingColony_Infrastructure_ProvidesHousingComfort` — the JSON `HousingAtbDB` binds on the start infra) |
| &nbsp;&nbsp;`EmploymentMoraleTests` | **OPERATION BLUEPRINT-TO-STEEL A1 — the employment→morale PRODUCER (ENGINE-WIRING-BACKLOG TIER 2 #2).** The ±40 employment term never fired because no template declared a job (`GetTotalJobs` summed to 0 forever). A1 wires `GetTotalJobs` to source each building's operating-crew (`ComponentDesign.CrewReq`) as its employment (civic-door design; `EmploymentAtbDB.Jobs` overrides where declared). Asserts (1) the PRODUCER: a staffed start colony now reads a non-zero jobs total (was 0); (2) the CONSUMER GATE: with `PopulationProcessor.EnableEmploymentMorale` OFF (default) the employment morale factor is neutral 0 (byte-identical — the `MoraleTests` homeworld baseline stays green), with it ON the term fires. Flag reset in finally. **The on-switch is a parked developer CALIBRATION** (CrewReq-as-jobs reads heavy unemployment vs a billions-pop workforce) — see the campaign log ADJUDICATION QUEUE |
| &nbsp;&nbsp;`ManpowerTests` | M3 pools (workforce/talent, available=pool−committed, commit/release) + the crew-shortage decision; **+ M3-2b enforcement** (`ManpowerTools.ResolveBuild` honours the host pool + government `CrewPolicy` — Block vs conscript — end-to-end, and is inert on a pool-less host) |
| &nbsp;&nbsp;`SustenanceTests` | M5b power/food shortage math, starvation rate, and the factory-attached `ColonySustenanceDB` inert-by-default (0 demand → 0 shortage) until calibrated |
| &nbsp;&nbsp;`FoodProductionTests` | **M5c food SUPPLY, cradle-to-grave** — the `FoodProductionAtbDB` ctor clamp + the morale food-QUALITY bonus math (above-baseline lift, capped, scaled to 0 by shortage); the gotcha-10 JSON→atb sensor (the base-mod `food-production` template + `agri-complex`/`hydroponics-arcology` designs bind the atb with the authored output/quality dials + PlanetInstallation mount); the supply END-TO-END (an installed arcology reports 5000/day + quality 2.5 and ENDS a food shortage the same demand leaves at 100% without it — the fix for the hostile-world −40 morale floor); **the whole-pipeline INTEGRATION through the real monthly processors** (`FoodPipeline_EndToEnd_MoraleTracksFoodOverTime` — advance the clock: no farm → a negative food morale factor; install an arcology + advance → shortage cleared, quality bonus added, morale measurably rises, via SustenanceProcessor→PopulationProcessor→ColonyMoraleDB); and **the GRAVE rung** (`FoodProduction_GraveRung_DestroyingTheFarmReturnsStarvation` — removing the installed farm drops supply to 0 and total shortage returns, so food is a losable capability) |
| &nbsp;&nbsp;`GovernmentTests` · `GovernmentWiringTests` | the dials/derivation/classifier; and attach + `GovernmentTools` lookup + the `TaxCeiling` cap billed end-to-end |
| &nbsp;&nbsp;`LegitimacyTests` · `LegitimacyProcessorTests` · `RebellionTests` | legitimacy math + the per-province recompute (incl. `War_TaxesLegitimacy_ByMilitarism`) + the rebellion begin/quell reaction-window |
| &nbsp;&nbsp;`FactionEconomyTests` | GlobalManager-iterated faction processors fire; colony tax flows to the faction Ledger; the SocietyReadout colony line |
| &nbsp;&nbsp;`SocietyReadoutTests` | the instrument-panel text (sustenance shown, diplomacy ledger stance/score/treaties, no-contacts case) — the observability gauge the client's Dump Society prints |
| **Diplomacy cluster** (`DiplomacyTests`, `DiplomacyIffTests`, `DiplomacyFirstContactTests`, `DiplomacyLogisticsAccessTests`, `DiplomacyTreatyTests`, `DiplomacyCasusBelliTests`, `DiplomacyWarTests`, `DiplomacyReactiveTests`, `ExchangeCatalogTests`, `DiplomacyDriftTests`) | relationship track + IFF (signed-pact suppression) + first-contact + logistics gate + treaty trust-ladder + casus-belli militarism gate + declare-war/peace latch + the reactive table + exchange catalog + **the live monthly reactive DRIFT** (`DiplomacyDriftTests`: militarist cools, treaty warms, inert without contacts) |
| `GameStageTests` | the **staged game-state generator** (task #39) — `GameStageFactory.AgeTo` builds a real galaxy-at-a-stage: Early (player multi-world) / Mid (rival EMPIRES with their own colonies + a treaty) / Late (mature: several player colonies, multiple rival empires, an active war, a rebelling colony); asserts each stage's content, that rivals own colonies, and that aging is cumulative & convergent (re-aging doesn't duplicate). The rig that lets the late-triggering political cluster be seen/asserted without playing for hours |
| orbit math / vectors / EntityManager / serialization / modding | unit-level |

---

## Adding a new test

1. New `*.cs` in `Pulsar4X.Tests/` with `[TestFixture]` + `[Test]`.
2. For a feature test, start from `TestScenario.CreateWithColony()` and follow the read → advance → read pattern.
3. **If the test reproduces a known-broken path, ship it `[Ignore("reason")]` in the SAME commit** — never land a deliberately-red test on the integration branch (root `CLAUDE.md` lessons / `SESSION_STATE.md` 2026-06-22).
4. Push. CI runs it; the TRX report names any failure.
