# R3 — DOCS-MINED DIRECTIVES (62 entries; the full agent output is authoritative — this is the action digest)

## Reshapes existing plan slices
- MVP §5 Stage 4: THE one remaining v1 gap = the invade-from-orbit UI panel (embark/land has NO client button; MVP §3 row D). Fold into the Battalion/manager lane as a first-class slice.
- Beachhead: SURFACE-FOG-AND-RECON-DESIGN.md "Beachhead/FOB" = "a ROLE not a new component — AI lands at easiest region, plants an EXISTING installation (bunker/radar), region becomes staging anchor; almost no new buildables, the new part is AI logic." RECONCILE with A5 (colony-free building impossible today): the developer's explicit ask is a construction UNIT with components — keep the engineer path but reuse the FOB doc's role/anchor framing + its EASIEST-LANDING SCORE spec (Ownership → garrison strength → fortification → terrain(Ocean disqualified) → hazard → proximity → resource hex, all fog-read) for the landing-region choice.
- Ground orders: GROUND-ORDERS-CATALOG-DESIGN.md — O1a formation order QUEUE built (MoveToHex/MoveToRegion/HoldFor/SetStance/SetEngagement); client order-queue UI = the explicit next O1 slice; O1 directive: data-driven order REGISTRY (do NOT extend the hardcoded Issue-Orders switch; ~51 EntityCommand classes exist, ~5 reachable; formations don't ride the EntityCommand pipeline — FormationOrderableDB idea); O3 batch: move-to-hex/attack-hex/dig-in/garrison/bombard-support/load-to-transport (attack-hex ≈ the infra destroy/capture orders).
- Planetary range display: GROUND-SURFACE-MAP-DESIGN H4/G5 follow-ons flag "in-range-hex SHADING" un-built; H3 RealReachKm/HexPitchKm = the readout; INFORMATION-DELTA Failure A/B framing; space precedent = Fleet Combat tab "Show range rings" toggle reusing SimpleCircle/UIWidgets (no new SDL code); ground detection display = the ground-radar component's reach (recon-is-a-component); STO weapons = ground-to-orbit ring case (park).
- Client battalion UI mirrors FleetWindow anatomy (Client CLAUDE "Fleet Combat tab"): tabs Summary/Combat/Issue Orders; stance selector = the un-built "follow-up half" (dir 16); ground-combat readout tab owed (5e "Still to finish"); Aurora §4 card model: render POSITIONS as cards not units; positions Front-Attack/Front-Defence/Support/Rear + fork's Reserve; Support/Rear untargetable until front collapses.
- Marine flow: sealed/environmental component = "the next slice" un-built (GroundCombat CLAUDE PlanetEnvironmentFactory; Hardening = HazardResistanceAtb pattern, DIALS §5.3); caliber/training + SCARCE TALENT pool wired ("you can't mass-produce Space Marines" — ManpowerTools.HasTalentToBuild/CommitTalent); GroundAugmentAtb power-armor/reflex-booster base-mod exists; carry/power/ammo gates exist; ONE designer LOCKED (UNIVERSAL-ASSEMBLY §0 — Component Designer + Entity Assembler, never a bespoke window/type).
- Embark: CARRIER-DESIGN C1 GroundBayAtb→BayAtb generalization (BayClass enum ordinal-stable, Personnel=0/Vehicle=1) is the locked shape — do NOT invent a second bay; a Formation must fit one ship (Aurora §6). Flag C1 as adjacent-prerequisite; don't absorb the carrier build.
- Aurora capture depth (spec to mirror, flag as options): intact installations PRODUCE FOR THE CONQUEROR; required garrison ≈ Pop(M)×(Determination/100)×(Militancy/100); occupation strength ≈ sqrt(Size×Units×Morale)/10000; police infantry suppress cheap. Aurora invasion pipeline: orbital superiority → bombard → combat-drop → reinforce; load/unload only at colony-marked body.
- Aurora combat math reference: ToHit → Pen=(AP/Armor)² → Destroy=(Dmg/HP)²; GSP=AP×Dmg×Shots; moving/loading resets fortification (re-fort 30 days); "weapon specialisation must be VISIBLE (strong-vs/weak-vs labels + composition warnings)".
- Universal weapon dedup (GroundWeaponAtb vs space weapon atbs = one energy weapon exists twice) — "needs a design doc + phased plan before code" (UNIVERSAL-ASSEMBLY §2a). DEFER; note only.
- Command seats: Battalion Commander seat #13 WIRE status (GOVERNANCE roster); mission-command MANDATE/REPORT 2-slot protocol; command components = seats; "blow up the flag bridge and delegated command falls apart". Park for later; cite in ops doc.
- 2D resolver: directives 41-46 confirm the locked design + S-slices + the 2 joints + Station-Lattice fallback + "never feed a variable dt into a group step".
- Fog: per-faction ground fog slices 1-4 BUILT; slice 5 (fog-limited enemy garrison read — the easiest-landing score needs it) + slice 6 open. Manager/rings must be fog-honest.
- Colonization: CreateColonyOrder exists; System Governor #7 owns "found at body X" (GOVERNANCE Gap #3); survey-fog "you know the ground where you settle"; FACTION-CREATION-GUIDE landmine: off-world colonies use space-habitat not infrastructure.
- Working agreement for parallel branches: one-slice-per-CI-green PER BRANCH (root CLAUDE pre-flight 6); doc-update-same-commit 3 owners (DOCS-INDEX/TESTING-TRACKER/SYSTEM-CONNECTION-MAP); six-point registration + BaseModIntegrityTests sensor; DataBlob rename breaks saves (L3 — relevant to BayAtb rename); one hotloop per blob L9; repo scope gotcha #9.
- M-ECON tie: "you take a world by breaking its morale, not just its buildings" (MVP §1); ground upkeep mechanism = the economic pressure (value-source deferred).
