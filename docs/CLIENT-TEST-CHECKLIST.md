# Client-Side Test Checklist — what only YOUR local build can verify

**Why this exists:** CI now *compiles* the client (the `build-client` job, added 2026-06-28), so compile breaks are caught automatically. But CI still **cannot run** the client — it's display-coupled, headless CI can't open a window. So **runtime behavior, rendering, and clicks are verified only by your local build** + the `game_logs/` pages. This is the running list of client things awaiting that local check. Tick them off; tell me what's broken (send the relevant `game_logs/` page) and I'll fix.

> **How to test:** pull the branch → `launch.bat` (captures `console_output.txt` + rolls `game_logs/` pages) → play → for anything weird, send me the page. Close the game fully before reading `console_output.txt` (it's buffered until exit).

---

## ⚠ FIRST — confirm the build config change didn't break your local build
- [ ] **The game still builds AND launches locally.** I changed `Pulsar4X.Client.csproj` (fixed two `HintPath`s: `Libs\ImGui.NET.dll` and `Libs\SDL3-CS.dll` — they were pointing at non-existent subfolders, so a clean checkout couldn't build). CI proved it *compiles* on Linux; you confirm it *runs* on Windows. If the build or launch breaks, that's the first thing to fix.

## ⭐ THE UNIVERSAL DESIGNER — `claude/devtest-faction-design-xpfnhe` (added 2026-07-14)

The Component Designer makes the PIECES; the **Entity Assembler** (renamed from "Ship Design") assembles them into anything buildable. These are the runtime checks for that work (engine + client-compile are CI-green).

### Component Designer — doors-only (slice 1)
- [ ] **The left tree shows DOORS, not named example designs.** Open the Component Designer (toolbar). Expand a category (e.g. **Weapons**) — the clickable leaves should be the **doors** (Energy / Ballistic / Guided / Melee / Exotic) — **NOT** a pile of "＋ New Rending Claws / ＋ New Point Defense Laser" entries. Every category reads the same way (Defense → Armor / Shields / Hardening / Fortification, Chassis → Personnel / Vehicle / Hull / …).
- [ ] **Click a door → the dials appear.** Click a door (e.g. **Defense ▸ Armor**) → the design panel opens with its **dials** (sliders), a name field, and Save. Tweak, name, Save → the design appears in the middle "Current Component Designs" list.
- [ ] **Multi-type door shows a "Type" dropdown; single-type door shows none.** Click **Weapons ▸ Ballistic** → the panel's top shows a **Type** dropdown (Railgun / Flak / Autocannon / …); pick one → the dials rebuild for that type. A door with only one type (e.g. Chassis ▸ Hull) shows **no** Type dropdown.
- [ ] **Every door has working dials** (the "ALL the Doors" pass) — spot-check a door in each category (Power ▸ Generation, Industrial ▸ Extraction, Civic ▸ Development, Chassis ▸ Personnel): clicking it should show adjustable sliders, not read-only text.

### Entity Assembler (was "Ship Design")
- [ ] **The window + toolbar read "Entity Assembler."** The toolbar button tooltip and the window title should say **Entity Assembler**, not "Ship Design". Assembling a ship still works exactly as before (pick hull + parts + armor → Save).
- [ ] *(later slices)* pick a **ground chassis** → the parts list shows ground parts, the budget bar tracks carry-weight, Save registers a buildable unit. (Stations / buildings follow.)

## ⭐ THIS BRANCH — `claude/4x-space-stations-design-t0a4b0` (space stations + the ground map) — added 2026-07-03

Everything below is engine-CI-green; these are the *runtime/render* checks only your local build can do. Full seven-field detail is in `docs/TESTING-TRACKER.md` (rows **G3**, **G4**). Work top to bottom — the ground-map items are the headline.

### Ground map — the planet surface (slices 3 + 4, the new work)
- [ ] **G3 — the Planet View window opens and draws.** Start a New Game → click **Earth** on the system map → right-click it → context menu → **"Planet view (regions)"**. A window should open showing **three region columns** (the centre region plus its two ring neighbours), each painted as **stacked terrain bands** (blue ocean, green forest, grey mountains, etc.) with a label per band. Below is a detail strip: area, crossing-time, installations. *What right looks like:* three coloured columns, sensible terrain, no blank window. *If it's wrong:* send me the `game_logs/` page — a draw fault logs `[RenderError] PlanetViewWindow` once instead of blanking the UI.
- [ ] **G3 — the ring rotates (no seam).** In that window, click **◀ West** / **East ▶** (or click a side column). The centre should swing to the next region, and rotating past the last region should **wrap back to the first** — no dead end, no gap. This is the "Pacific survives" topology: the four regions are a ring, and you're always looking at three of them.
- [ ] **G3 — Earth known, LUNA is fog (the corrected model).** Earth (your home colony) should show real terrain. **Now open the Planet view of Luna** — it should show grey **"? UNSURVEYED"** fog. (Fixed 2026-07-04: nothing is pre-surveyed except the world you colonise, and moons now get a region layer, so Luna is a real survey target. Before, all of Sol was surveyed and there was nothing to do.)
- [ ] **G4 — surveying Luna lifts the fog LIVE (the money shot).** You already start with a **"Surveyor I"** ship in your **Science Fleet** (it carries the geo-survey component — no need to build one). Open **Luna's** Planet view (fog) and leave it open → order the Surveyor I to Luna and let the geological survey finish → Luna's fog columns should turn into **real terrain the moment the survey completes**, in the open window, no reopen. (Engine reveal is CI-proven; this confirms the window re-reads it live.) *Snag to report:* if a body can't be surveyed at all it's missing `GeoSurveyableDB` — tell me and I'll add it.
### Ground map — the TACTICAL layer (slice 5e, 2026-07-04 — the "map I can navigate" you were waiting on)
The Planet View is no longer just a readout. **Earth now has a starting garrison by default** (3 Infantry / 2 Armor / 1 Artillery in the capital region — raised on every New Game), so open Earth's Planet view and you should see your cyan unit tokens with no setup. To add MORE (or an ENEMY to fight): **DevTools (SM mode) → "[ Raise Ground Unit ]"** — pick the body in the "Orbit around" picker, choose type + count + region, **"Raise (your faction)"** / **"Raise (enemy)"** (same region to test the fight + capture). Report what you see either way.
- [ ] **G5 — units draw in their region.** Open the Planet view of a world that has ground units. Each region column should show **unit tokens** along the bottom — a coloured box reading e.g. `I x3` (I=Infantry, A=Armor, R=Artillery) with a little **health bar**. **Cyan = yours, red = hostile.** *What right looks like:* tokens sit in the right region, colour matches owner, count/health look sane. *If wrong:* `game_logs/` — a fault logs `[RenderError] PlanetViewWindow` once.
- [ ] **G5 — click-to-move a unit.** Click one of **your** (cyan) tokens — it should highlight (yellow border) and a selection bar appears below with **"March to Region N"** buttons. Click a March button (or click an **adjacent** region column). The status line should read "marched N× … to Region N", and after time advances the units should **arrive** in the new region (a `»` marks them in transit). Non-adjacent / not-your-units should refuse. 
- [ ] **G5 — click-to-place a base.** On a world where you have a **colony**, the Build panel (below the map) should list your installation designs. Pick one → **"Build here"** → the status line confirms it and the centre region's **⚙ building count** ticks up. (This is the "what I build in space is a real building on the ground" rule — the building is placed at that region.) *If no panel:* you have no colony there, or no `PlanetInstallation` design yet — expected.
- [ ] **G5 — terrain + hazards read.** Each region shows a **terrain-class chip** (OPEN / COVER / ROUGH, top-right) and, on hostile worlds, **hazard chips** (e.g. red "Fire Tornadoes", green "Corrosive Superstorm", amber "Dust Storm"). Confirm they match the world (a scorching world shows fire, a calm one shows none).
- [ ] **G5b — the start colony's buildings draw on the map (#5).** Open Earth's Planet view on a fresh game — the **capital region** should show a **⚙ building count** for the start colony's existing installations (they're auto-located there at colony creation). Build a **Bunker** via the Build panel in a region → that region's ⚙ count ticks up and it fortifies (a Dig-In / defended fight there is visibly tougher). *Still owed:* buildings added *later* via the colony economy UI (not the map Build panel) don't yet auto-locate — that's the next reconciliation step.
- [ ] **G6 — formations (command a group as one, the fleet echo).** With some of your units in a region, open the Planet View → the **Formations** panel (under the Build panel). Click **"Form up N unit(s) in Region X"** → a formation appears in the list (name · count · rally region). Select it → **March formation ◀/▶** should move the *whole* group one region as one order (watch them all go into transit, then arrive), and **Disband** should break it up (units remain). Selecting a formation should also re-centre the map on its rally region. *What right looks like:* one order moves the whole block; membership/leader are stable. Engine is CI-proven (`Formation_MirrorsFleet_*`); this confirms the client wiring.
- [ ] **G6b — formation stance (the ground echo of fleet doctrine).** With a formation selected, a **Stance** row shows its current stance + attack/damage-taken mults and a dropdown (Offensive Push / Dig In / Balanced) + **Set stance**. Pick **Dig In** → Set → the mults should read `atk ×0.75, dmg-taken ×0.75`, and the button greys out with a "switch in Ns" cooldown. In a fight, a Dig-In formation should visibly outlast a no-stance one (engine-proven `FormationStance_*`). Try **Offensive Push** on an attacking formation — it should kill faster but melt faster.

### Space stations (slices A / A2 / D / E / F — earlier this branch)
- [ ] **Deploy a station from a construction ship.** Right-click one of your **cargo/construction ships** → **"Deploy Station Here"**. A station should anchor at the ship's current spot (works even at a star or belt, nowhere you'd colonise), and the ship survives to do it again. Confirm a station entity appears.
- [ ] **Manage the deployed station.** Right-click the new station → **"Manage Station"**. The window should show its host body / structural-integrity (durability) / population / operating cost, and — if it has a constructor module — let you queue+install modules in place.
- [ ] **Materials are pooled from the fleet on deploy.** Deploying consumes frame material (stainless-steel) from the *fleet's* cargo holds, not just the one ship. With enough steel in the fleet it deploys; with too little it should **refuse** (no station, a message). (Engine `StationFactoryTests` prove the math; this is the live feel.)
- [ ] **(Optional) Lagrange markers + listening outpost.** L4/L5 Trojan points should appear as stable named points near a planet's orbit (a deploy can anchor to them). A listening-outpost station flavor runs a sensor scan after deploy.

## Space economy / morale / politics (branch `claude/space-economy-morale`, added 2026-07-02)
All engine-green; these are the *runtime/feel* checks. Full detail + what-right-looks-like is in `docs/TESTING-TRACKER.md` (rows T0, A2, C1, C2, C3, D0–D3).
- [ ] **T0 — New Game boots + clock runs** (the gate): New Game → press play → advance several months → close; `console_output.txt` clean, no exception. Confirms the new blobs/processors (legitimacy, sustenance, manpower, government, diplomacy) don't crash boot or the tick.
- [ ] **Dump Society reads sensibly.** DevTools → **Dump Society (log)** → close → read a `game_logs/` page: each colony shows pop / morale (+factors) / legitimacy / workforce+talent / **pwr-short/food-short** / tax; plus the **government** name and the **diplomacy** ledger line.
- [ ] **Government dials bite (C3).** DevTools → **Government (test regimes)** → *Totalitarian War-State* → Dump Society (name changes) → advance time and watch the effects (tax ceiling higher, research slower under low openness, crew conscripted for a build a consent regime would block). *Liberal Democracy* is the mirror; *Federal Republic* resets.
- [ ] **Morale moves population (A2).** DevTools → Create Colony on Venus/Mercury (hostile) → Dump Society (lower morale) → advance months → Dump again (population emigrated).
- [ ] **Crew gate (C1).** Build a large fleet (or drain the pool) → a ship build **blocks** under the default regime; flip to Totalitarian → it **conscripts** (builds understaffed).
- [ ] **Reactive diplomacy drift (D3).** Spawn a hostile fleet (first contact) → set that faction militarist → advance several months → Dump Society: your view of them cools toward Hostile on its own.

## Fleet UX — ✅ PASSED 2026-07-03 (after the fleet-menu freeze fix)
- [x] **Left-click a fleet selects it immediately** — no menu, no dead-click, no "click elsewhere first." **PASSED.**
- [x] **Right-click shows the context menu** (right-click only). **PASSED.**
- [x] **The Fleet Management window is USABLE.** It used to hard-freeze the instant its list rendered a fleet — a native ImGui assert in `BeginPopupContextItem` (its internal mouse-button query), which reads as a `[HANG]` because the assert's modal blocks the main thread. Fixed by converting all three fleet-window context menus to the explicit `IsItemClicked(Right)+OpenPopup(id)+BeginPopup(id)` pattern. Confirmed live ("it worked").

## ⭐ Branch `claude/4x-game-testing-strategy-19xw8q` — confirmed live 2026-07-03
- [x] **T0 — New Game boots + clock runs** with the full auto-spawn scenario (43 ships, 4 rival factions), no `[FATAL]`/`⚠ TELEPORT`.
- [x] **New Game auto-spawns the combat scenario by default** — 2 player task forces + 4 rival factions (capital-led) at Luna/Venus/Mercury/Mars, no SM button. Toggle off in DevTools › Detection/Fog for a clean start.
- [x] **Large Earth stockpiles** — building isn't resource-gated (50 M minerals + refined materials, warehouse ×10).
- [x] **Visual pass** — planets deeper shades, space darker.
- [ ] **Save/load a PLAYED game** (D1) — the one remaining "survives a session" risk. Play a bit → Save → Load → confirm no exception + state persists. (Engine `SaveLoadWithJobTests` covers the queued-job NRE that was fixed; the full played-game round-trip is the live check.)
- [ ] **Range-ring hover tooltips render** — hover a weapons/sensor/EMCON ring line → a label names the unit + which ring. CI-green; live render unconfirmed.

## Hazards — the headline (the whole cradle-to-grave loop)
- [ ] **Hazards render on the system map.** Corona = faint red-orange ring at the star; solar flare = bright orange (transient). *Note:* gas cloud, debris field, ion storm, and gravimetric anomaly currently **all render the same green** — distinct colors per type is a flagged follow-on, not built yet. So you'll see green blobs; that's expected for now.
- [ ] **Fly a ship into the corona → discovery fires.** You should get an "Environmental hazard discovered: thermal (extreme heat)…" notification, and the counter-research opens.
- [ ] **The research loop pays off.** Research the unlocked counter (Stellar Science → Thermal Shielding) → build **nickel-steel armor** → re-enter the corona → it takes noticeably **less** damage than an unarmored hull.
- [ ] **Normal orbit = ZERO hazard damage.** Park in a normal planetary orbit (Earth, Mercury) → no hazard damage accumulates. Only a genuine close dive toward the star should cook you (inverse-square calibration).
- [ ] **The new environments actually appear in generated systems** — debris field (~25% of systems), ion storm (~15%), gravimetric anomaly (~8%). Explore a few systems and confirm they show up. (Gas clouds are now *corrosive*, not heat.)
- [ ] **(Optional) Supplemental effects feel right** — inside a gas cloud your sensor range should shrink and movement/warp drag; inside an ion storm sensors shrink; a solar flare blinds. (The engine asserts these in CI now via the diorama test; this is just the live feel.)

## ⚠ M-ECON branch (`claude/space-economy-morale`) — the new economy/people/government systems
- [ ] **BOOT TEST (do this first — the highest-value check).** Pull the branch, launch, start a **New Game**, let the clock run a few months, then close and read `console_output.txt`. Confirms the one thing CI can't: that adding three new DataBlobs to every colony (`ColonyMoraleDB` / `ColonyManpowerDB` / `ColonyEconomyDB`) and a new auto-discovered processor (`ColonyEconomyProcessor`) didn't break **startup or the sim**. If it boots and the clock advances with no exception, the foundations are runtime-safe. (Risk it catches: a startup crash from processor auto-discovery, or a blob breaking the New Game JSON path.)
- [ ] **The instrument panel — DevTools › "Dump Society (log)".** Enable SM mode → open Dev Tools → click **Dump Society (log)**. Close the game and read `console_output.txt` for `[DevTools] SOCIETY DUMP` lines: each colony's **pop · morale (+ factor breakdown) · workforce/talent pools · tax → income/mo**, plus the player's **government**. This is how you SEE everything this branch built (most of it has no other UI yet). On the default Earth start the numbers are *neutral* (morale ~50, tax 0%, no government) — expected; it proves the readout works.
- [ ] **Watch a lever move.** DevTools › Create Colony on a HOSTILE world (e.g. Venus/Mercury) → Dump Society → that colony should read **lower morale** (conditions penalty) than Earth, and (after advancing time) **lose population** to emigration. The morale valve, made visible.

## Performance — the number I need
- [ ] **The `⏱ map breakdown` perf number.** Run a busy scenario (a combat with a few dozen ships, or a dense system), and when a frame is slow watch `console_output.txt` / `game_logs/` for a line like `⏱ map breakdown ms — orbits u../d.. (N) | …`. **Send me that line** — it tells me which icon list eats the frame so I can make the targeted render fix and give you the real "how many entities can the lemon PC handle" budget.

## Event Logger (2026-07-16, slice 2 — client)
- [ ] **The top-centre event strip appears.** New Game (or Load) → a compact **Events** strip sits at the top-centre of the screen, showing `(0)` / `(no events)` at first. Click **"> Events"** to expand it downward (up to 20 rows), click **"v Events"** to collapse back to 5.
- [ ] **Enemy detection pauses + drops to 1-hour steps.** Set the time step to **months or years** and fast-forward toward a known enemy fleet (DevTest UMF/Kithrin). When your sensors first pick it up: the clock should **auto-pause**, a row **"Enemy Fleet detected at [nearest planet]"** appears in the strip, and the **step size should read 1 hour** (not the months you set). Un-pause → you advance carefully at 1-hour steps. *(This is the whole point — verify the step actually reset.)*
- [ ] **Tune (send me feedback):** position/width of the strip, whether "large body" picks the right landmark, whether the alert should fire on *unknown* foreign ships or only *at-war* ones, and whether a multi-ship fleet firing several rows is annoying (I can dedupe to one alert per incursion).

## 🧊 FREEZE FIX — verify the SIM-STALL is gone (2026-07-16, the headline)
Your committed `game_logs`/`console_output` `[SIM-STALL]` was two things, both now fixed engine-side (CI-green gauges in `CombatFleetTreeSafetyTests`); only your local run can confirm the *live* freeze is gone.
- [ ] **The clock no longer crawls/pauses-repeatedly near hostiles (the PERF freeze).** Load the DevTest game (UEF + UMF + Kithrin, at war), set a big time step (months/years), and press play. It should advance smoothly — NOT crawl at ~5 game-seconds with the sensor-scan count exploding, and NOT throw a phantom Battle Report pause every few seconds. Root cause was combat seeding the wrong range so a fight instantly "froze" and re-triggered forever; fixed. If it still crawls, send the new `game_logs/` page (the `[SIM-STALL]` line names the stuck processor).
- [ ] **No hard wedge (the "TRUE WEDGE").** Play through first contact / a real battle; game-time must keep advancing, no `[SIM-STALL] … TRUE WEDGE`. (The combat fleet-tree walk that could infinite-loop is now loop-proof, and the drag-drop that could build the bad tree is refused — see next item.)
- [ ] **Fleet drag-drop can't build a bad tree.** In the Fleet window, try dragging a parent fleet ONTO its own sub-fleet. It should refuse (no reparent) rather than create a loop — before, this was the drag that could wedge combat. Normal reparents (a fleet onto an unrelated fleet) still work.

## 🧊 OPERATION EARTHFALL — C1.1: the CONTACT-BLIP freeze (the 2050-04-22 stop)
The real session froze at game-date **2050-04-22** with a UMF strike fleet mid-transit toward Earth. Root cause (findings/A1-freeze.md H1): the fog-of-war **contact blips** had no finite/on-screen cull (orbit icons got theirs in gotcha #15, blips were missed), so a degenerate/off-screen last-known contact coordinate reached `SDL.RenderLine` — a native rasteriser hang, no exception, CI-invisible. Fixed CLIENT-side (cull + finer breadcrumb + tighter watchdog); only your local run confirms the *live* freeze is gone. Engine has no gauge for this — the findings say no CI fixture can reproduce a native client freeze, so **this checklist IS the test.**
- [ ] **Play PAST 2050-04-22 with an enemy fleet mid-transit — NO freeze.** Load the DevTest game (UEF + UMF + Kithrin), make sure **fog of war is ON** (DevTools › Detection / Fog of War — it's on by default; the blips only exist with fog on), get a hostile fleet moving toward you (or Age→Late / spawn one), set a moderate time step and play across **2050-04-22** while that contact is in flight and detected. The client must keep running smoothly — no stop, no white window, no force-kill. *(Before the fix this is the exact frame it died on.)*
- [ ] **A degenerate contact no longer freezes — it just doesn't draw.** If a tracked contact ever drifts to a bad/very-far last-known position (fogLag drift, a lost track anchor), its blip should silently **vanish for that frame** and reappear when the position is valid/on-screen again — the map keeps drawing, the game keeps running. You should NOT see the map lock up or the frame time climb toward a freeze. (The diamond blip is culled before its coordinate can reach SDL.)
- [ ] **If it DOES still hang, the breadcrumb now NAMES the stage.** Send me the latest `game_logs/` page. A `[HANG]` line should now read `wedged in stage: 'GalacticMap.Draw/contacts'` (or `/orbits`, `/ships`, `/labels`, …) instead of the old coarse `'GalacticMap.Draw'` — that pinpoints which icon list wedged so the next fix is precise. The watchdog also now fires at **3.5 s** of a frozen frame (was 5 s), so a freeze is named ~1.5 s sooner.
- [ ] **Normal play is unchanged.** A New Game / DevTest with fog on should look exactly as before — contact blips still draw as red diamonds with names at detected enemy positions, fade to "(last known)" when a track is lost. The cull only ever removes a blip that was off-screen or degenerate anyway. (If a blip you SHOULD see is missing, that's a false cull — tell me and send the page.)

## 🛰 OPERATION EARTHFALL — C2.1: sensor truth (the honest ring + held-vs-fresh)
The real session read "saw the enemy fleet before sensor range." Diagnosis (findings/A2-ghost-contacts.md): detection was HONEST (the homeworld's deliberate 200 Gm early-warning horizon), but the green detection RING around Earth was sized against ONE arbitrary reference ship, so a LOUDER enemy fleet was genuinely detected while drawn OUTSIDE the ring. C2.1 makes the readouts honest — no engine/sim value changed. CI compiles the client but can't run it, so this checklist is the test. Design call for the horizon itself: `docs/combat/HOMEWORLD-SENSOR-HORIZON-MEMO.md`.
- [ ] **A detected enemy is NEVER drawn outside Earth's green detection ring.** Load the DevTest game (fog ON), get a hostile fleet detected by the homeworld's early-warning bubble (Age→Late or spawn one at Venus/Mercury/Mars). The colony's green ring should now be a **band**: an outer (faint) ring against the loudest contact present and an inner (brighter) ring against the quietest. Every red contact blip you're detecting must sit **inside** the outer ring. *(Before the fix a genuinely-detected fleet could render outside the single ring — the "saw them before sensor range" report.)*
- [ ] **The band tracks EMCON.** If an enemy runs hot vs dark (or you have several enemies of different loudness), the outer ring should size to the loudest and the inner to the quietest — the band widening/narrowing as the mix changes. Hover a ring line for its label ("Max/Min detection reach…").
- [ ] **The band never over-promises past 200 Gm.** The rings are clamped to the true horizon — you should never see the colony ring reach past the belt/Ceres (~265 Gm), and a contact out there should genuinely NOT be detected (no blip). If a blip appears with no ring covering it, tell me + send the `game_logs/` page.
- [ ] **(Optional) The held-vs-fresh `[DETECT]` split.** Set `SessionLog.SplitHeldVsFresh = true` (a default-off diagnostic switch) and read a `game_logs/` page: the `[DETECT]` line should split "holds N contact(s)" into "(F fresh / S held-stale)" — fresh = tracked at the last scan, stale = coasting on a last-known fix. Default OFF is byte-identical to before. This is a log readout only; it changes no gameplay (the hostile-engage gate still counts stale contacts, on purpose).

## Living Opponents — fleet composition + new units (2026-07-16)
Engine + client-compile are CI-green (`FleetAssemblyTests`, `FleetCompositionEscalationTests`, `FleetCompositionPerFactionTests`, `DevTestFleetRoleReadoutTests`, `GroundGarrisonPerFactionTests`). These are the *runtime* checks only your local build can do — the point is to confirm the AI now **masses what it builds** and the new hulls actually spawn + get used. Enemy AI order-emission is **off by default** (byte-identical New Game); turn it on in **DevTools** to watch the AI fight. Full seven-field detail: `docs/TESTING-TRACKER.md` (LIVING OPPONENTS row) + `SESSION_STATE.md` (2026-07-16).
- [ ] **T0 still boots** — the whole point-of-no-regression check: New Game boots, clock runs, `console_output.txt` clean. The new `FleetCompositionDB`, `FleetAssembly`, per-faction JSON nodes, and three new ship designs must not break startup or the New-Game JSON path.
- [ ] **The new hulls spawn (no New-Game / scenario crash).** With the DevTest scenario (UEF + UMF + Kithrin) loaded, the UMF should field **Legionnaire** (heavy line: 6× chain-flak + PD) and **Skirmisher** (light screen: 1× chain-flak), and the Kithrin a **Harbinger** (disruptor + plasma + shields). If any is mis-registered the scenario throws on load — send me the `game_logs/` page. *(This is the gap `BaseModIntegrityTests` can't see — it covers colony blueprints, not scenario faction files; `DevTestFleetRoleReadoutTests` now guards it in CI, but a live scenario load is the final proof.)*
- [ ] **The AI MASSES what it builds (the headline — the "builds but never attacks" fix).** Enable AI order-emission in DevTools → let a militarist NPC (UMF) build for a while at a fast time-step. Its built armed hulls should get **swept out of its home stockpile into organized fleets** (before this fix they parked flat in the faction root fleet, invisible to the AI, so it built ships and never used them). Watch for the NPC fleets growing, then moving to engage.
- [ ] **The AI keeps a HOME RESERVE — it does NOT commit its whole navy (the developer's correction).** As the NPC masses, it should **hold back a reserve fleet** for home defence rather than send everything at once. You should see it launch a strike only once it has both a complete strike fleet AND ≥2 deployable fleets, and never empty its last organized fleet. *(Report if it goes all-in — that's the military-commander reserve rule failing.)*
- [ ] **Per-faction flavour reads through.** The UMF's fleets should aim for a **heavier line** (its authored `fleetComposition`: min 4 / ideal 10 / perfect 20) and its worlds start with a **heavier 4/3/2 legion** garrison; the Kithrin aim for a **lighter swarm** (min 3 / ideal 6 / perfect 12). The player (UEF) keeps the default light 3/2/1 garrison. (Ground garrison is visible on each world's Planet view.)

## 🪖 OPERATION EARTHFALL — C3.1: the Battalions tab (battalions beside fleets)
The Fleet window is now **"Force Management"** with a top-level **Fleets / Battalions** tab bar. The Battalions tab is the ground echo of the fleet manager — every ground formation you own, across every world, in one list, with the same march/queue/stance/ROE orders the planet view gives. Engine reads + order paths are CI-tested (`EfC3BattalionRegistryTests` pins the cross-body registry); CI compiles the client but can't run it, so this checklist is the live test. Class stays `FleetWindow` (title only changed).
- [ ] **The window opens as "Force Management" with two tabs.** Open the Fleet window (hotkey / toolbar / a fleet's context menu). The title reads **Force Management**; a tab bar shows **Fleets** and **Battalions**. The **Fleets** tab must look and behave EXACTLY as before (select a fleet → ships/orders/Combat tab all work — this content was moved verbatim). *If the fleet layout looks shifted/overlapping, tell me — the fleet content's absolute cursor offsets (27px) may want a nudge now that it's inside a tab.*
- [ ] **Battalions lists your formations across ALL worlds.** Raise ground units on TWO different worlds (DevTools › "Raise Ground Unit" for a couple, then form them up on each world's Planet view), then open **Force Management › Battalions**. Both formations should appear in the table — even though they're on different bodies — with columns Name / World / Region / Strength / Health / Reach / Stance / ROE. *(An empty list with no formations reads "No battalions yet …" — expected before you form any up.)*
- [ ] **Filters work.** With battalions on ≥2 worlds in ≥1 system, the **System** and **World** combos should narrow the list; **"With orders only"** should hide battalions that have no queued plan. Nothing crashes when you switch filters.
- [ ] **Select → command.** Click a battalion row. The order surface below should show: **March to Region N** buttons (adjacent regions of its rally region), a **queue** panel (+ March / + Hold 6h / + ROE buttons, and a Clear plan button once a plan exists), a **stance** selector (Offensive/Dig In/Balanced + Set stance, greying out on cooldown), and an **ROE** selector (Hold / Close / Stand-off). Each should apply — watch the green status line ("marches N unit(s) …", "queued → region N", "stance: …", "ROE: …") and confirm on the Planet view that the battalion actually moves/changes.
- [ ] **"Open planet view" jumps to the world.** With a battalion selected, click **Open planet view** — the surface tactical map for THAT world should open (or come forward), centred on the battalion's world. This is the manager → map bridge.
- [ ] **Nothing hard-crashes on an enemy/other-faction world.** Only YOUR formations should ever appear (the enemy garrison on a contested world must NOT show in your Battalions list). If you ever see a foreign formation here, tell me.
- [ ] **Rename button (LANDED in PW.2).** A selected battalion now has a text field + **Rename** button (below the strength line). Type a new name, click Rename → the green status reads "renamed to '…'" and the table row + panel header update. A blank name is ignored ("rename ignored (blank name)"). See the PW.2 section below.

## 🎯 OPERATION EARTHFALL — C4.1: planetary range overlay + assembler rows
Two client-only additions to the ground UI (no engine/sim value changed): (a) the **planet surface tactical map** (`PlanetViewWindow`, the globe view) now TINTS the hexes a selected ground unit can reach — red = weapon reach (`GroundUnit.Range` in hexes), green = radar reach (its best `GroundSensorAtb.Range_km` ÷ the region's hex pitch) — the ground echo of the space map's range rings; and (b) the **Entity Assembler** (`ShipDesignWindow`, ground-unit branch) readout gains three rows — **Training** (the cadre veterancy multiplier, previously computed but invisible), and **always-on Power (draw / supply)** + **Ammo Capacity** rows (were shown only as red "Problems" text on violation). CI compiles the client but can't run it, so this checklist is the test.
- [ ] **A selected unit's reach lights up the map.** Load the DevTest game, open a defended world's **Planet view (regions)** (context menu on the body), and click one of YOUR unit tokens on the globe. Hexes within that unit's **weapon reach** should tint faint **red**, and (if the unit carries a radar) hexes within its **radar reach** tint faint **green** — a rough hexagonal disk centred on the unit, wrapping correctly across the map seam. The caption should read "Range overlay: red = weapon reach, green = radar reach …". Deselect (click the token again) → the tint clears.
- [ ] **Radar green only appears for a unit that HAS a radar.** A stock garrison / DevTools-raised unit (no component backing) shows **no green** — only red weapon reach. Design a ground unit in the Entity Assembler with a **ground-radar** component, build + field it, select it → it should now show a green radar disk (larger the higher the radar's Range_km). *(This is the honest bit: only a designed-with-a-radar unit reveals.)*
- [ ] **Artillery reaches farther than infantry.** Select an Artillery group (default Range 3 hex) vs an Infantry group (Range 1) — the red disk should visibly be bigger for artillery. The disk is the odd-r hex distance the map is drawn in (a proper hex disk, not a sheared rhombus).
- [ ] **The overlay honours the range-ring toggle + fog.** Toggle **DevTools › Detection / Fog of War → "Show all range rings"** OFF → the planet overlay disappears (same switch as the space map's rings). Turn it back on → it returns. You should NEVER see a range overlay for an **enemy** unit (select is own-faction-only; you don't know their ranges — same rule as the space rings).
- [ ] **No clutter / no perf hit.** With a large garrison selected the tint should render cheaply (it only tints the visible window's hexes for the selected group). No slow-frame `[PERF]` spike from opening the planet view with a selection.
- [ ] **Entity Assembler shows Training + Power + Ammo rows.** Open the Entity Assembler (retitled "Ship Design" toolbar/window), set **Assembling: Ground Unit**, add a ground chassis + parts. The stats table should now include a **Training** row (e.g. `1.00×`, rising when you add a **training-cadre** part), a **Power (draw / supply)** row (e.g. `0 / 0 W`, going red **UNDER** when energy weapons out-draw the mounted reactors), and an **Ammo Capacity** row (e.g. `0 kg`, rising when you add a magazine). These are always visible now — not only when a rule is violated.
- [ ] **The red "Problems" list still matches the rows.** Build an under-powered design (a big energy weapon, no/weak reactor) — the Power row goes red **UNDER** AND the "under-powered" Problem line still appears. Build an ammo weapon with no magazine — Ammo Capacity reads `0 kg` AND the "no magazine" Problem appears. (The rows are the always-on gauge; the Problems list is the buildable/not verdict.)

## 🚀 OPERATION EARTHFALL — C5.1: embark + land troops (the invasion control panel)
The player half of the invasion **lift** step (MVP Stage 4) — `LoadTroopsOrder`/`LandTroopsOrder` had no player button until now. A new **"Embark / land troops ..."** order in **Force Management ▸ Fleets ▸ Issue Orders** loads ground units onto a troop-bay ship at a garrisoned world, and (once the ship holds the orbit over a target) lands them on a chosen region. Engine order + capacity helpers are CI-tested (`EfC5TroopLiftOrderTests` pins the button contract); CI compiles the client but can't run it, so this checklist is the live test. **The north-star click-path: embark Earth marines → fly to Mars → land on Mars.**
- [ ] **The order only shows when the fleet can lift troops.** Select a fleet with NO troop-bay ship → the Issue Orders list has NO "Embark / land troops ..." entry. Design/spawn a ship with a **Troop Bay** (or **Vehicle Bay**) component, put it in a fleet, select that fleet → the entry appears. Click it.
- [ ] **Bay-capacity readout is right.** Each troop-bay ship shows as a collapsing header with its bay lines — **Troop bay (infantry): used/cap** and/or **Vehicle bay (armour/artillery): used/cap** — matching the bays actually installed. A ship with only a troop bay shows only the Troop-bay row.
- [ ] **Embark loads a garrison unit.** Park the transport in orbit of a world where you have ground units (Earth with its garrison / DevTools › "Raise Ground Unit" for a couple). Under **Embark** each of your units on that body lists a **Load \<name\> (type, size N, Personnel/Vehicle bay)** button. Click one → green status "Loading …", the unit leaves the ground roster (check Planet view) and the bay's "used" count goes up. A unit too big for the remaining room (or wrong class — a tank vs a troop-only bay) is **greyed** with a "needs N room, M free" note.
- [ ] **Fly the transport to the target.** Issue a normal **Move to …** order to send the loaded transport to an enemy/other world (e.g. Earth → Mars). It should warp and arrive in orbit like any ship.
- [ ] **Land is gated on holding the orbit.** With the transport over the target, open the Troops order again → the **Land** section shows the loaded units, a **region picker** combo, and an orbital-control line. If a FOREIGN ship is present over the body, the line reads "A foreign ship holds the orbit …" and the **Land** buttons are **disabled**. Clear the space (destroy/repel the enemy ship) → the line reads "Holding the orbit …" and the buttons enable.
- [ ] **Land drops the unit into the picked region.** Pick a region in the combo, click **Land \<name\> in Region N** → green status "Landing …", and on the target's **Planet view** the unit appears in **that** region under your faction (a hostile drop lands it straight into the region's fight). The bay frees up.
- [ ] **Full north-star run.** Embark a marine battalion on Earth → move the transport to Mars → win/hold the Mars orbit → land the marines on a Mars region. That's MVP Stage 4 end-to-end. Report anything that stalls (a button that does nothing should log a `[troops]` line — if it doesn't, tell me).

## 🏚 OPERATION EARTHFALL — PW.2: battalion rename + infrastructure destroy/capture buttons
The post-merge cross-lane buttons: (a) **rename a battalion** (both Force Management ▸ Battalions AND the Planet-view formation panel), and (b) **raze / capture infrastructure** — the client front door onto GROUND's G3 `DestroyInfrastructure`/`CaptureInfrastructure` orders, in the battalion order surface (both windows) AND the Planet-view **city-zoom**. Footprint buildings sit on a region's centre hex; a battalion razes/seizes the infra in the region it STANDS in (the order's range gate needs a unit there). Engine surface is CI-tested (`EfPwInfraButtonContractTests`); CI can't run the client, so this is the live test.
- [ ] **Rename a battalion in Force Management.** Open **Force Management ▸ Battalions**, select a battalion → below the strength line there's a text field pre-filled with its name + a **Rename** button. Change it, click Rename → green status "renamed to '…'", and the table row's name updates. A blank/whitespace name is rejected ("rename ignored (blank name)"). Select a DIFFERENT battalion → the field reseeds with THAT one's name (no stale text).
- [ ] **Rename a formation on the Planet view.** Open a world's **Planet view (regions)**, select one of your formations in the Formations panel → the same rename field + button appears. Rename works the same way; the formation list entry updates.
- [ ] **Infra buttons appear only where there's infrastructure to hit.** With a battalion selected (either window), an **"Infrastructure — Region N (where this battalion stands)"** line shows. On a region with NO footprint buildings it reads "(no footprint buildings here …)" and shows no buttons. Move the battalion onto a region that HAS footprint buildings (a defended enemy capital region) → the line shows a count + **Raze infrastructure** / **Capture infrastructure** buttons.
- [ ] **Raze razes.** With a battalion standing on an enemy region that holds a bunker/fort, click **Raze infrastructure** → green status "queued: raze infra in Region N". Let time run (the order is staged over ground ticks) → the footprint building(s) disappear from that region (check the ⚙ count / city zoom), and are gone from the enemy colony's economy (a real loss). The queued order clears when the hex is razed.
- [ ] **Capture flips the hex.** Instead click **Capture infrastructure** → "queued: seize Region N hex". Next ground tick the hex owner flips to you — the captured buildings STOP fortifying the defender (their region's defence multiplier drops; the invasion gets easier). *(v1 capture is instant; the building keeps producing for its original colony until the whole planet flips — that's expected.)*
- [ ] **City-zoom infra section.** On the Planet view, double-click an operational hex to enter the **city view**. If that hex's region band holds footprint buildings, an **"Infrastructure combat — Region N"** section appears at the bottom. If one of YOUR battalions is standing in that region it offers **Raze with '\<battalion\>'** / **Capture with '\<battalion\>'**; if none is, it tells you to "move one of your battalions into this region." Confirm the raze/capture works via the named battalion.
- [ ] **Out-of-reach is a safe no-op.** Queue a raze, then march the battalion OUT of the region before it fires → the order should pop harmlessly (nothing razed, no wedge, no crash). Razing your OWN infrastructure is allowed (scorched earth) — the buttons don't block it, so double-check you're on the enemy's region.

---

*Maintenance: when a client feature ships that CI can't runtime-verify, add a line here. Remove a line once you've confirmed it live. This is the standing "runtime gauge is the developer" list — the companion to CI's compile gate.*
