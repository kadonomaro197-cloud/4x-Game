# Pulsar4X — Docs Index & Status Dashboard (LIVING — keep commit-current)

**What this is:** ONE place that lists *every* doc in the repo, what it's *for*, and its *status right now* — so anyone (a new session, a different model, the developer weeks later) can open this file and know where everything stands without spelunking. It is the doc-level twin of `docs/archive/SYSTEMS-STATUS-AND-TEST-PLAN.md` (which tracks *systems*) and `docs/TESTING-TRACKER.md` (which tracks *tests*).

**THE RULE (not optional):** when a commit changes a doc — or changes code that makes a doc newly-true or newly-stale — update that doc's row here **in the same commit**. A row's *status* is the maintained truth (git already knows the last-touch sha; what git can't tell you is whether the content still reflects reality). Keep the "As of" stamp current on any substantive pass.

**Layout (reorganized 2026-07-13):** `docs/` is filed **by subject** — `combat/ ground/ economy/ society/ ai/ environment/ explore/` for design docs, `aurora/` (external spec), `docs/archive/DESIGNER-AUDIT-2026-07-08/`, `archive/` (superseded, bannered), and the top level for vision + these dashboards. **A new design doc goes in its subject subfolder, not flat `docs/`.** This file owns *doc currency*; `TESTING-TRACKER.md` owns *tests*; `SYSTEM-CONNECTION-MAP.md` owns *system connections* — update the right owner, don't merge them back together. The repeatable audit method is `DOCS-AUDIT-PROCESS.md`.

**As of:** 2026-07-29 · branch `claude/operation-ground-truth-prompt-l9729h` (open): **OPERATION GROUND TRUTH — the two consolidated combat records.** The designer + resolver-audit campaign collapsed into `docs/COMBAT-DESIGNER-GROUND-TRUTH-2026-07-28.md` (replaced 4 docs), and the **eight** auto-resolver design docs collapsed into `docs/AUTO-RESOLVER-GROUND-TRUTH-2026-07-29.md` (the resolver anatomy + merge · the teardown field manual · unified-resolver + battle-stats · real-distance · fleet-combat closing · the 2D group plane · the two-joints memo · the ground W-track). Both carry a **deletion map** so nothing was lost, and both were re-verified against source — which surfaced **14 stale claims** now recorded rather than dropped, headed by a **locked "no 2D, ever" decision that shipped code overturned** and a **"slice unbuilt" claim for a slice built six days earlier.** Every reference was repointed in the same commit: **~46 C# doc-pointers + ~70 markdown references, verified zero dangling.** Two live-source fixes rode along: a stale `GroundWeaponMount` docstring claiming the resolver ignores its own field, and a retired-doc pointer that resolved silently and misled. — *(Prior stamp: 2026-07-24 · branch `claude/faction-design-audit-bb3tqz`: **STAR-SYSTEM GENERATION & PERSISTENCE design LOCKED** (`docs/environment/SYSTEM-GENERATION-AND-PERSISTENCE-DESIGN.md`) — a generated system produces everything from a grassland resource node to the Oort cloud, then writes a **hand-editable spec file**; **hybrid** recipe-for-untouched / **frozen-detail-for-observed** (so improving the generator can't rewrite ground the player already fought over), written the first time, surfaces on demand, per-save-vs-shared-library chosen at galaxy setup. NOT built: **no system writer exists**, and **procedural systems get no asteroid belts**. Also: **SURFACE-MAP DOC CONSOLIDATION** — `docs/ground/GROUND-SURFACE-MAP-DESIGN.md` is now THE single home for all three surface zooms: `MINI-HEX-TACTICAL-GRID-DESIGN` was merged in as **Layer 5** and DELETED (all 8 refs swept, incl. 5 C# files), the tile-naming spec was pulled out of the AI DevTest doc into **Layer 6** alongside the developer's new display requirements, and a **live contradiction between two locked docs was resolved** (the 2026-07-04 "battles never per-mini-tile" line was superseded by the 2026-07-22 tactical model — units DO fight on mini hexes). Also fixed the 2026-07-13 link-sweep damage that had rewritten merged-away source names into the surviving doc's own path (destroyed provenance; 3 sites). Remaining mini-hex gaps: **M4 per-mini-tile terrain** + **tile naming** + a client runtime pass. — *(Prior stamp: 2026-07-24: **HOLISTIC PLANETARY-GAMEPLAY AUDIT complete** (`docs/ground/PLANETARY-GAMEPLAY-AUDIT-2026-07-24.md`) — 8-subsystem design-vs-code pass + adversarial verify. Finding: the planetary cradle-to-grave loop is BUILT + CI-path-proven but **unreachable from the menu front door**, **closes by bypassing bombardment**, and is **runtime-unverified**; a P1–P7 roadmap prioritises reachability + a live play-test over more depth. Flags PLAY-TO-MARS / MVP / GROUND-UNIT-VARIABLES as stale. Also this branch: the developer's live-play fixes — warp-arrival NaN [FATAL] (co-located exit → clean circular settle), Kithrin debt (over-crowded Titan hive → dialled-up life-support) + expansion (survey speed 1→10), belt-rock minerals, assembler instrumentation, city-zoom deposit render. — *(Prior stamp: 2026-07-22: **REAL-DISTANCE COMBAT design LOCKED** (`docs/AUTO-RESOLVER-GROUND-TRUTH-2026-07-29.md §12`) — the developer's rule that real km/km-h on the weapon are the truth and hexes are only a display ruler; ~80% of the machinery already exists, so it's a CONNECT job in 5 CI-gated slices (Slice 1 = the additive km↔hex translation + gauge, byte-identical, landed). Prior on this branch: **EARTHFALL IMPLEMENTATION AUDIT complete** — a plan-vs-code line-by-line pass over the whole merged window (`docs/earthfall/IMPLEMENTATION-AUDIT-2026-07-22.md`) found the build faithful to plan: **0 critical, 0 high, 4 medium, 15 low**; crash-risk surface clean. Fixes are ordered in `docs/earthfall/AUDIT-RESOLUTION-PLAN.md` (the pre-test-critical one: activate the P3 legitimacy flags in menu games). — *(Prior stamp: 2026-07-21 · branch `claude/devtest-faction-design-xpfnhe` (open): **OPERATION EARTHFALL LANDED** — the parallel-lane fix/build campaign (`docs/earthfall/CAMPAIGN-PLAN.md`) executed end to end and merged into CORE, every slice CI-green. The five real-playthrough failures are fixed (freeze / ghost-contacts / AI abandoning its invasion / the transport that never sailed / the missing ground campaign); the invasion machinery is BUILT cradle-to-grave (mass → sail → one-shot bombard → land → **ground tactical brain** picks posture → beachhead FOB → per-hex infrastructure raze/capture → second wave → capture), for the AI *and* the player (embark/land UI, Force-Management battalions tab, planet range overlay). NPC factions now develop (Kithrin survey chain; station income ends the structural bankruptcy). The 2D group-plane resolver landed S0-S2 (default-off). The invasion on-switch (`EnableGroundTacticalAI` + `AutoFormUp`) is flipped ON for menu games (one-line revert). What shipped: `docs/earthfall/CAMPAIGN-REPORT.md`; the as-built step-and-click: `docs/ground/EARTHFALL-CAMPAIGN-OPS.md`; the developer's answers: `docs/earthfall/EARTHFALL-DECISIONS.md`. — *(Prior stamp: 2026-07-16 · **LIVING OPPONENTS** — the AI now MASSES what it BUILDS; full detail below.)*

**Prior (2026-07-16):** **LIVING OPPONENTS** — the AI now MASSES what it BUILDS. `Fleets/FleetAssembly` sweeps built armed hulls out of the faction ROOT fleet (where NPC-built ships parked invisibly to `OwnedFleets()` — the "builds but never attacks" bug) into in-system fleets; `FleetCompositionDB` adds an aspiration ladder (Deployable→Ideal→Perfect, war bumps a tier) with overflow spilling into a home-defence RESERVE; `ConquerResolver.HasHomeReserve`/`ShouldStopMassing` stop the AI committing its whole navy (the developer's correction — the military-commander's job; same logic applies to ground battalions). Per-faction fleet numbers + ground garrison read off `FactionInfoDB` (`fleetComposition`/`garrison` JSON nodes → militarist UMF a heavier line + 4/3/2 legion, Kithrin a light swarm). Three new armed hulls (UMF Legionnaire/Skirmisher, Kithrin Harbinger) spawn cradle-to-grave + are AI-usable with zero new code (armed hull → `IsWarship` → folded + massed). Client event ticker + `DevTestFleetRoleReadoutTests` (guards the scenario-load path `BaseModIntegrityTests` misses). All CI-green. Full narrative: `SESSION_STATE.md` ⏩ 2026-07-16. — *(Prior stamp: 2026-07-11 · branch `claude/game-versions-ai-testing-f1yqpq`: **THE AI DESIGN SUITE** — a full "quark→supercluster" design of the faction/NPC AI (the brain that lives in `NPCDecisionProcessor`, today a working socket with an empty `Tick`). Eight `docs/AI-*.md` docs, entered through `docs/ai/AI-BRAIN-BUILD-TRACKER.md` (design→code bridge + **§4 SOCKET VERIFICATION**: every designed element mapped to a code home — no core design is homeless; 2 load-bearing NO-SOCKETs = fleet situational-read + crisis seed; 1 keystone = seat durability, the "paramount" first build). Covers: 12-trait `PersonalityDB` + shared `DecisionScorer` · mission-command (destination vs. how) · fractal needs-ladder objective engine · 21-leader org chart · emergent inter-faction ecosystem · emergent late-game crisis · scenario-authoring layer. Companion: `docs/explore/EXPLORATION-CONTENT-DESIGN.md` gained the incident-response shape + the "fill Sol for 250 years / four seasons of Star Trek" density bar. The **root `CLAUDE.md` reference table now lists the suite** so a cold session discovers it. Also removed the main-menu Discord link. — *(Prior stamp: 2026-07-08 · branch `claude/sol-playtest-earth-map-8r59j6`: ground units-as-entities (radar-reveal / parametric locomotion / terrain-handling / base-mod scout parts) **and the DESIGNER AUDIT** (`docs/archive/DESIGNER-AUDIT-2026-07-08/`); prior 2026-07-06 · branch `claude/4x-hex-movement-pathfinding-o7qeaa` (CLOSED, CI-green):* **weapon unification** (ONE shared designer — weapons + reactors + magazines ground-mountable; the power supply gate + ammo gate) and the **planetary-unit designer wired to the battlefield** (slice A: assemble → build → field a unit; slice B: mass-based ammo pool + manual resupply). The **combat-resolver MERGE is the next branch** (one damage kernel both ships and planetary units call — `WEAPON-UNIFICATION-DESIGN.md` §0 pt 6). Full session narrative + the three reds-and-fixes + lessons: `SESSION_STATE.md` ⏩ Session 2026-07-06. — *(Prior stamp: 2026-07-03 testing branch; live-run results + scorecard in `docs/TESTING-TRACKER.md`.)*

**Current focus (what's being worked on):** runtime hardening of the merged M-ECON/politics/combat layer is essentially done at the foundation level (boot, sim, movement, combat trigger, fleet UI all 🟢). **Design development can now begin in parallel** — next BUILD targets are ground combat (MVP M2) and stations (the space-economy pillar). Remaining test debt is a short PC queue (played-game save/load, Society-tab + economy-UI render, a real in-range fight, calibration re-dumps) — see `docs/TESTING-TRACKER.md` → the scorecard + ranked "what's genuinely LEFT". Latest landings: DevTools test levers, New-Game auto-scenario (4 rival factions), alpha stockpiles, a visual pass, and the fleet-menu crash fix.

---

## Status legend

| Mark | Meaning |
|------|---------|
| 🟢 CURRENT | content reflects HEAD; trustworthy as-is |
| 🟡 STALE | behind the code/plan — note says since-when + what changed |
| ♻️ LIVING | a ledger meant to be updated continuously (never "done") |
| 🔒 DESIGN-LOCKED | the design is agreed; **build state is tracked separately** in the Build column |
| 📚 REFERENCE | stable external/spec material; not "built," doesn't go stale from code |
| 🗄 SUPERSEDED | kept for history; a newer doc replaced it (pointer in notes) |

Build column (for design docs): ✅ built · 🏗 building · ⚫ design-only (not started) · ➕ partial.

---

## 1. Operational / process (the "how we work" + code-truth docs)

| Doc | Purpose | Status | Notes |
|-----|---------|--------|-------|
| `CLAUDE.md` (root) | Master dev reference: layout, conventions index, gotchas/Landmine Index, Prime Directive, pre-flight checklist, design-doc index | 🟢 ♻️ | Update the design-reference table + add a gotcha whenever a subsystem/landmine changes |
| `CONVENTIONS.md` | Pulsar's actual coding idioms (DataBlob/Clone, `*Atb`, processor auto-discovery, JSON discipline) | 🟢 📚 | Idioms are stable; touch only when a real convention changes |
| `ARCHITECTURE.md` | Hybrid-ECS data-flow diagram (Entity→DataBlob→Processor→…) | 🟢 | Pattern-level, still accurate; add a note if a new *kind* of data flow is introduced |
| `Pulsar4X/GameEngine/CLAUDE.md` | Engine-core subsystem ref (ECS infra, modding, save/load) | 🟢 | |
| `Pulsar4X/GameEngine/Colonies/CLAUDE.md` | Colonies/population/morale/manpower/economy/legitimacy | 🟢 | Updated this session (ManpowerTools, crew gate) |
| `Pulsar4X/GameEngine/Factions/CLAUDE.md` | Factions, government, diplomacy substrate + teeth + reactive drift | 🟢 | Updated this session (drift live, CrewPolicy wired) |
| `Pulsar4X/GameEngine/Combat/CLAUDE.md` | Auto-resolve combat engine + doctrine + IFF | 🟢 | |
| `Pulsar4X/GameEngine/Damage/CLAUDE.md` | Damage application (Complex is live; Simple dead) | 🟢 | |
| `Pulsar4X/GameEngine/Weapons/CLAUDE.md` | Beams, missiles, flak/railgun, weapon flavors | 🟢 | |
| `Pulsar4X/GameEngine/Sensors/CLAUDE.md` | EM signatures, scanning, contacts, first-contact hook | 🟢 | |
| `Pulsar4X/GameEngine/Movement/CLAUDE.md` | Newton thrust, warp, jump, pathfinding | 🟢 | |
| `Pulsar4X/GameEngine/Industry/CLAUDE.md` | Production, mining, materials, infrastructure efficiency | 🟢 | |
| `Pulsar4X/GameEngine/Fleets/CLAUDE.md` | Fleet grouping, fleet-as-one-icon | 🟢 | |
| `Pulsar4X/GameEngine/Logistics/CLAUDE.md` | Automated cargo routes + diplomacy access gate | 🟢 | |
| `Pulsar4X/GameEngine/Stations/CLAUDE.md` | Space stations (parallel host) | 🟢 | |
| `Pulsar4X/GameEngine/Hazards/CLAUDE.md` | Gas clouds / solar flares / space environments | 🟢 | |
| `Pulsar4X/GameEngine/Galaxy/CLAUDE.md` | System/body gen, atmosphere | 🟢 | System not changed recently — still current |
| `Pulsar4X/GameEngine/Tech/CLAUDE.md` | Research, scientists, tech unlocks | 🟢 | + `ResearchMultiplier` gov wire (small) |
| `Pulsar4X/GameEngine/People/CLAUDE.md` | Commanders, scientists | 🟢 | Officer/scientist talent-draw still a future slice |
| `Pulsar4X/Pulsar4X.Client/CLAUDE.md` | UI reference + the RUNTIME-CI-blind discipline + DevTools inventory | 🟢 | Updated this session (DevTools government lever + diplomacy readout) |
| `Pulsar4X/Pulsar4X.Tests/CLAUDE.md` | Test harness + a **PARTIAL** test inventory (~~full~~) | 🟡 | **Corrected 2026-07-27:** this row called it a *full* inventory. It names **142 of 343** fixtures — **201 undocumented (59%)**, including `BattleLogTests`, which the planned ground battle-log slice needs. Every fixture it names **does** exist (all 142 checked, zero missing), so the defect is coverage, not falsehood — but a session that trusts it as complete will write duplicate gauges. **Real list: `ls Pulsar4X/Pulsar4X.Tests/*Tests.cs`.** Also corrected there: the "~13 min" sharded-CI figure is stale — `rest` measured **33 min**, so wall-clock is back to the pre-sharding number and the doc's own rebalance trigger is met. |
| `docs/DOCS-AUDIT-PROCESS.md` | **Repeatable playbook (SOP)** for auditing all docs vs. code, deciding utility, and reorganizing — re-runnable cold by a future session. Root `CLAUDE.md` points at it | 🟢 ♻️ 📚 | The permanent *how-to*; each run's findings go in a dated `DOCS-AUDIT-YYYY-MM-DD.md`. NEW 2026-07-13 |
| `docs/DOCS-AUDIT-2026-07-13.md` | Findings of the 2026-07-13 audit run (per-doc verdicts + reorg plan): staleness+duplication, 0 delete / 3 archive / ~60 stale "not built" claims | 🟢 | Point-in-time record; superseded by the next dated run. Produced via `DOCS-AUDIT-PROCESS.md` |
| `docs/ground/PLANETARY-FUNCTIONAL-PLAN-2026-07-27.md` | **NEW 2026-07-27 — THE PLAN.** The ordered, gauged route from "a deep ground engine nobody can watch" to "a planetary war you can play and see." Holds the **delta ledger** — every designed planetary capability × **functional? / accessible? / observable?**, three-state with evidence, and each row marked **[V]** verified-this-pass / **[A24]** inherited-unverified / **[?]** unverified so an inherited guess can never be quoted later as fact. Then the slices: **S0** scale-by-measurement · **S1 the ground battle LOG (start here — the clock already halts, so this is the missing half, and space's `CombatLog`+`BattleLog` pattern makes it cheap)** · **S2** flags into the save · **S3** promote the existing scenario start · **S4** unstick the designer door · **M1 the live cradle-to-grave sitting** · **S5** ONE build queue (+#9 *must* ship with #10 or ground defence breaks) · **S6** movement rework · **S7→D2/D3a/D3b** doctrine becomes the steering wheel · **S8** shorten the tick then calculate fire rate · **S9** the bombardment joint · **S10** the designer chain in the forced order #2→#4→#1 · **S11** depth · **S12 BLOCKED on ruling #21**. Sequencing principle (observable→accessible→steerable→deep) is argued, not asserted. Every balance number FLAGGED, none chosen silently. **Five developer questions in prose at the top** (#21 capture transfer; the scenario start; the tick VALUE; mid-tick overkill; per-weapon rates). | 🟢 ♻️ | The build authority for planetary work. Executable half = `.claude/workflows/close-planetary-delta.js`. Awaiting the developer's go + rulings. | **Completed 2026-07-28 (Phase D audit): new §4a carries REACH + SEE for all 19 slices** — the click-path requirement had been met on only 6, and a blank reads as "nobody thought about it". Where a slice has no player path it now says so and names what the player encounters instead. Three slices re-sized DOWN by Phase B — ruling #25 medium→cheap-wire, **S6** large→cheap-wire+retirement, **S8** from "build the rate model" to "connect the one that already exists" (`RoundsPerSecond` is authored in `weapons.json` and `GroundCombat/SpaceWeaponGround.cs` already computes damage-per-second from it). S1e re-shaped with a proven root cause (`SignalStrength_kW` is a detection MARGIN, not loudness). Q5 narrowed to the flat-`Attack` residue.
| `.claude/workflows/close-planetary-delta.js` | **NEW 2026-07-27 — the executable half of THE PLAN.** A saved, re-runnable workflow, parameterized by slice (`{slice:"S1"}`), mirroring the `earthfall-campaign.js` shape. Per slice: **implement → adversarial verify (default to refuted) → repair → a docs agent that flips the dashboard rows**, then a **completeness critic** ("what's missing — a wall not covered, a ruling not landed, a gauge not written?") whose findings become the next slice's list. Carries the landmines in-prompt (atb exact-arity, six-point registration, L9 one-hotloop, save type-names, doctrine reciprocal, determinism, byte-identity, three-state honesty) and the corrected CI reality (~33 min; new fixtures land in the `rest` shard). `S12` refuses to run and emits the ruling-#21 decision aid instead. | 🟢 📚 | **VALIDATED STATICALLY ONLY AND NEVER INVOKED** (`node --check`, meta-shape diff vs the precedent, phases/slice-key audit) — running it starts the build, which is the developer's call. |
| `docs/DOCS-AUDIT-2026-07-27.md` | **NEW 2026-07-27 — OPERATION GROUND TRUTH run, 🏗 IN PROGRESS (checkpointed).** The planetary/ground doc-vs-code reconciliation launched from `OPERATION-GROUND-TRUTH-PROMPT.md`. A 19-agent evidence fan-out **died on the account usage limit**, so this file is the checkpoint (briefs preserved on disk; §5 Resume). What it DID establish first-hand, file:line: **a new ground battle already HALTS the clock** (`GroundForcesProcessor.cs:329-330`) while the whole 1,078-line file emits **zero** log lines/events/records and then deletes the dead (`:334`) — the interrupt actively misleads, which is why ruling #24's log is the missing half of a halt that already fires; the **space narration + `BattleLog` pattern to copy** (with 3 honest cautions: ship-shaped fields, runtime-only/not-saved, `MaxEvents=250`); **the "DevTools-only" premise is REFUTED** — an ungated **"DevTest" main-menu button** (`MainMenuItems.cs:51`) already boots a three-faction at-war scenario with all five ground flags ON, which is how ruling #27b and the audit's P1 reconcile without new machinery; **#27a's real defect is LOAD** (the flags are set on the *normal* New Game path too, `NewGameMenu.cs:562-581`); the ground-design **reopen wall** (`ShipDesignWindow.cs:166/223/592`); **CI is ~33 min not ~13** (the `rest` shard, which every new fixture joins by default); and the **surface-scale contradiction diagnosed** — 37 km (pitch÷13, what the code measures) vs 47 km (an unused area-equivalent), with the Earth coarse figure left to a **gauge** rather than guessed. | 🏗 | Point-in-time record + resume plan. **Supersedes nothing yet** — finish Phases A–E before flipping the seed-list rows it verdicts. |
| `docs/POSTMORTEM-SENSORSCAN-FREEZE-2026-07.md` | **Postmortem of the multi-session SensorScan freeze hunt** — symptom, every attempt (incl. the misses: fixed combat first, hung a CI shard), the root cause (reschedule inside a per-receiver loop → exponential), and the 6 permanent rules (read the gauge before fixing; reschedule once per invocation; a hang-repro must be hang-proof; exponential bugs hide on short tests; build the culprit gauge first). Read before touching the sensor scan / any self-scheduling processor | 🟢 📚 | Point-in-time record + lasting rules; the developer's "never get to that point again". NEW 2026-07-17 |
| `.claude/commands/build-check.md` · `damage-audit.md` · `phase-status.md` | Slash-command / skill definitions | 🟢 📚 | Tooling; touch only when the workflow changes |

## 2. Vision & the two firewalls (why + what-not-to-do)

| Doc | Purpose | Status | Notes |
|-----|---------|--------|-------|
| `docs/NORTH-STAR-VISION.md` | The ceiling: stage aspects of the great sci-fi universes; the extracted-mechanics pillars | 🟢 📚 | Raises the ceiling; does not lower the bar |
| `docs/LIVING-GALAXY-DESIGN.md` | **NEW 2026-07-07 — an OBJECTIVE / BAR, not a system.** The commitment that the game is a rich detailed story SOLO (250 years without alien contact still full of rebellions/discoveries/disasters/hard-choices) — as the **emergent result of all systems running together, nothing forced.** Explicitly NOT a storyteller/director/event-engine/injector (corrected after two wrong drafts). Achieved by building every *other* system deep + connected + observable (visibility is each system's OWN job, e.g. finishing rebellion's surface — not a central layer). Imposes a lens on every system ("does this contribute to a galaxy that lives on its own without being forced?"); balancing = per-system rate/coupling calibration, done last by gauge-watching. | 🟢 📚 ♻️ | A bar like NORTH-STAR/MVP — nothing to build here; measured against, and refreshed as systems get deep+observable |
| `OBJECTIVE.md` | The concrete end-state spec (what "done" looks like, build order) | 🟢 📚 | Goal-level; stable |
| `docs/MVP.md` | **Scope firewall** — the v1 finish line ("you can take a planet") + parking lot | 🟢 ♻️ | The live plan (superseded `docs/archive/PLAN.md`); read before adding any feature. **Refreshed 2026-07-27** — the As-of stamp had flagged it stale while this row read current (a self-disagreement, now resolved): its "no invade-from-orbit control panel — the one true remaining v1 gap" claim was **false** (built 2026-07-19, Earthfall C5.1, `FleetWindow.cs:1756,1814`) and Stage 4 is **re-pointed** at the three real gaps — a *supported* front-door enemy, one live cradle-to-grave sitting, and the two client designer walls. |
| `docs/PLAY-TO-MARS-WALKTHROUGH.md` | Button-by-button trace of a full New Game → invade Mars in the NORMAL UI, with every gap + proposed fixes | 🟢 | NEW 2026-07-07. **Refreshed 2026-07-27** — the As-of stamp had flagged it stale while this row read current (self-disagreement, resolved). Its §L (*"the invasion bridge — no button, no order… only callers are the unit tests"*) was **the most misleading passage in the doc tree** and is **REFUTED**: `LoadTroopsOrder`/`LandTroopsOrder` are issued by the player (`FleetWindow.cs:1756,1814`) and by the AI (`ConquerResolver.cs:63`). §L now reads ✅ with the live gaps re-pointed: a supported front-door enemy · the runtime pass · the two designer walls. Still-true blockers: no first-class bombardment order. | **Phase-B corrected 2026-07-27:** table **row L** flipped ❌→✅ (the invade panel IS built — §L had been rewritten but its own index row still read "no button, no order", a self-contradiction 95 lines apart) and the header gap count corrected **three → two** (K bombardment, I no-enemy-on-a-normal-start).
| ~~`docs/ground/GROUND-SURFACE-MAP-DESIGN.md`~~ | **DELETED 2026-07-08 — superseded.** Its "author separate ground parts (ground-radar / ground-locomotion) and merge with the space parallel" approach is replaced by the universal parametric designer (`docs/economy/COMPONENT-DESIGNER-CATEGORIES.md`): there ARE no separate ground parts — universal components mount on a Chassis ▸ Personnel/Vehicle. The still-valid core (a ground unit must be an entity carrying components) is folded into the universal-assembly model. Removed so it stops steering work toward the deleted parallel-parts pattern. | — | ⛔ | Superseded by `docs/economy/COMPONENT-DESIGNER-CATEGORIES.md`. |
| `docs/REALISM-VS-GAMEPLAY-AUDIT.md` | **Weight firewall** — every system must be the source of a stacking player DECISION; the cheap-wiring list | 🟢 ♻️ | Framework still governs; the verdict board is the thing to refresh as systems get wired |
| `docs/LITMUS-BLOOD-ANGELS-SQUAD.md` | **NEW 2026-07-17 — a cradle-to-grave LITMUS TEST + gap ledger.** Can a player build a 6-man Blood Angels Space Marine squad end-to-end (mineral→component→assemble→field→upkeep→move→lose)? An 11-agent workflow (WH40k research × code survey, adversarially checked) verdict: the FRONT half is strong (mine/refine/design-most-kit/assemble/build/field/march — file:line-confirmed), but the chain breaks on the signature bits — **no 6-model squad granularity, no standing upkeep/consumption, no population/gene-seed scarcity, no jump-pack, no dual ranged+melee, no per-unit psychology, no armour condition**. The two features the developer named (upkeep-as-it-exists · a 6-man squad) are exactly the two the game CANNOT do today. 13 ranked gaps, each with a nearest build-from hook + effort. | 🟢 📸 | Point-in-time audit at HEAD `6e7549a`; the gap ledger is a work backlog for ground-unit depth |
| `docs/ground/GROUND-UNIT-VARIABLES.md` | **NEW 2026-07-17 — the litmus FOLLOW-UP: the generalizable variable set (any unit, not just marines).** Answers the developer's probes against the code (file:line): the litmus report OVERSTATED four "CANNOT"s — a 6-model squad (2 one-line resolver hooks, `ModelCount` neutral=1), multi-weapon-per-profile (mounting already works; the gap is the resolver blending + the singular snapshot), jump-pack (only flight/ignore-terrain missing = 1 dial), and power-armour (`GroundArmorAtb` IS complete armour; report conflated it with the strength-augment; only the sealed/life-support dial is missing). Designs a shared **Training** dial (build-time + scarce TALENT + credits → veterancy, militia→elite = one slider) and **accurate upkeep** (copy `StationUpkeepProcessor` — credit-upkeep S/closed, ammo-drain S-code/M-design/open, rations L). Cheapest-first build order + a variable-set table (capability · status · hook · effort · generalizes-to). | 🟡 📸 | Design backlog; every dial serves ALL units. Verdict: developer's instinct right — 6 of 8 items S/S–M, only 3 real builds **Refreshed 2026-07-27:** the As-of stamp had flagged this stale while the row read current — resolved. Two corrections landed as a header banner: **every `GroundForcesProcessor.cs:NNN` line reference in it is stale** (that file is now 1,078 lines — a drift table gives the current anchors), and its **"multi-weapon plurality is missing" finding is REFUTED — the W-track built it** (`ba983a8`/`f8a2ee5`/`4cb7c0d`; the resolver loops `WeaponLoadout` at `:472` and range-gates each weapon separately). Design content (Training dial, upkeep axes, variable-set table) still governs. |
| `docs/COMBAT-DESIGNER-GROUND-TRUTH-2026-07-28.md` | ⭐ **THE CONSOLIDATED RECORD of the 2026-07-28 verification campaign — READ THIS BEFORE TOUCHING THE DESIGNER OR THE RESOLVER.** Replaces four documents (the 17-pass resolver audit, the 7-pass designer audit, the 7-agent retrofit survey, and the compliance record); **§16 maps where every deleted section went — nothing was lost.** Holds: **§2 six gauges that cannot rot** (template count 96 vs a locked target of ~37 · **shared dials per door = 0** · templates that construct without throwing 92/96 · `WeaponProfile` fields fed from the design · `CombatKernel.Combatant` production consumers = 0 · doctrine dials that reach the fight 9/14); **§3 the SEVEN open developer decisions** (Q-A aggregate-vs-individuate · Q-B which armour model wins · Q-C bucketing-vs-targeting · Q-D the station build model, where a doc-rejected alternative shipped anyway · Q-E the Prebuilt-Units door · #21 capture transfer · three smaller ones); **§4 the 36 LOCKED decisions** that bound any work; **§5–§7 the designer as-built, its intent, and the gap** (headline: *the taxonomy was built, the collapse was not* — the design's own bar, "build a phaser and a Death-Star beam from the same sliders," **fails literally**, while three ground weapons with **identical** dial sets sit uncollapsed); **§8 all 40 designer findings · §9 all 49 resolver findings in 5 root causes · §10 the cross-audit reconciliation** (where the two audits disagreed, including one blocker neither could find alone); **§11 the six CORRECTIONS made** — including a retracted doc self-correction that would have crashed New Game; **§12 the save-compatibility risk register** (adding a ctor overload breaks load on ~12 `*Atb` classes); **§13 verified-good, do not rebuild**; **§14 the A/B/C/D plan.** | 🏗 | **The single source of truth for the designer + resolver retrofit.** The 2026-07-08 `docs/archive/DESIGNER-AUDIT-2026-07-08/` remains the separate *universality* survey; canon rulings live in `docs/ground/GROUND-GAMEPLAY-DECISIONS-2026-07-24.md` M19. |
| `docs/AUTO-RESOLVER-GROUND-TRUTH-2026-07-29.md` | ⭐ **THE CONSOLIDATED RECORD OF THE AUTO-RESOLVER — READ THIS BEFORE TOUCHING ANY COMBAT RESOLVER (space, ground, or the shared kernel).** Replaces **eight** documents (the resolver anatomy + ship↔ground merge · the teardown field manual · the unified-resolver + battle-stats design · the real-distance design · the fleet-combat closing spine · the 2D group-plane design · the two-joints memo · the ground closing-fight W-track); **§20 maps every section of every one — nothing was lost.** Holds: **§8 the 14-row scenario-correctness matrix** ("does it handle X?" — the fastest answer in the repo, with the honest agent disagreements preserved); **§9 the six-man-squad case** + Fix A/B/C (and the ordering ruling, now partly overtaken because Fix C shipped first as W2); **§10 the ten-row ship-vs-ground divergence table** — range · fog · multi-weapon · **firepower conservation (the live ground double-count bug)** · retreat · stats · hostility · ⛔ **the per-tick-vs-per-second `dt` trap that re-baselines every ground gauge** · two space paths · perf; **§11 THE NORTH STAR** (*"ground units work the same as ships as far as fleets or battalions are concerned"*) + the locked Fleet▸Group / Battalion▸Formation vocabulary + "compute one, distribute across N"; **§12 the ground closing fight** (real distance + the W-track, incl. the two hazards on the metres↔hex layer); **§13 the 2D group plane + the two PINNED joints** (residual-exact conserved fire-allocation · the fixed-5 s combined-theater cadence, with an executable-spec gauge); **§14 the build ledger across all six tracks**; **§15 battle statistics** (per-weapon attribution: NONE, anywhere); **§16 the nine open developer decisions + six structural questions**; **§17 the parking lot**; **§18 the Prime-Directive connection map**; **§19 the 14-row stale-claim register + the line-drift warning.** Headline: **two resolvers sharing ONE damage kernel, and a ship or a ground unit is "whole-or-dead."** | 🏗 | **The single source of truth for the resolver.** Companion: `docs/COMBAT-DESIGNER-GROUND-TRUTH-2026-07-28.md` (the designer + the 17-pass resolver audit's findings). Canon: `docs/ground/GROUND-GAMEPLAY-DECISIONS-2026-07-24.md` M1–M19. |
| `docs/BEYOND-PROTOCOL-REFERENCE.md` | Second design north-star (the strategic/human layer alongside Aurora's physics) | 🟢 📚 | |

## 3. Living status ledgers (the dashboards)

| Doc | Purpose | Status | Notes |
|-----|---------|--------|-------|
| `docs/DOCS-INDEX.md` | **This file** — every doc's purpose + status | ♻️ 🟢 | Keep commit-current (the rule at the top). **Owner of doc build-status** (the three-owner split: connections → SYSTEM-CONNECTION-MAP; tests → TESTING-TRACKER) |
| `docs/SYSTEM-CONNECTION-MAP.md` | **The connection graph** — one row per system, what it feeds/reads/shares/triggers (the Prime-Directive "map connections first" tool). **The ONE owner of system-to-system wiring** | ♻️ 🟢 | NEW 2026-07-13 — extracted from `SYSTEMS-STATUS-AND-TEST-PLAN.md` so the graph outlives that doc's stale status columns. Carries NO build/test marks (can't rot). Add a connection here in the same commit that reveals it | **Refreshed 2026-07-27 (OPERATION GROUND TRUTH Phase C) — the first content change since the file was created.** Gained a **⚡ BROKEN AND MISSING EDGES** section: 10 rows of system pairs the design requires to be connected where the wire is missing, one-way, or reads a value that is always zero — capture never updating the faction colony registry (8 AI readers consume it, no removal path exists), ground combat halting the clock while emitting nothing, the AI threat read summing a field that is always 0, doctrine assignment dropping 4 of its own fields, the costed queue writing hexes that can never fortify, the AI garrison rebuild producing cargo instead of soldiers, base-mod units structurally unable to bill upkeep, ground damage being per-tick while space is per-second, and orbital bombardment never firing on a colony. Rationale recorded in the header: **a cut wire is a connection fact, not a status mark**, so it does not breach this file's no-status rule. Also corrected three rows that were stale or incomplete: **Diplomacy** (said "design only" — it is wired into space-combat hostility both ways), **Ground combat** (was missing its edge INTO the time loop — a ground battle stops the player's clock), and **Sensors** (was missing its two real consumers, the AI threat read and survey-reveal accuracy, which share one struct).
| `docs/TESTING-TRACKER.md` | Standing global test ledger (CI layers + the Layer-3 PC-test queue), 7 fields/row | ♻️ 🟢 | CURRENT — **refreshed 2026-07-27** (OPERATION GROUND TRUTH A5): gained an **⏸ OFF-BUT-REAL** board — the **six `[Ignore]`d gauges**, three of which park a live defect (a player-REACHABLE testing-mod New-Game NRE; an AI-founded colony that starts 0-pop/0-tax so it never pays; the 2D anchors unproven through save/load), replacing the old "no deliberately-red gaps remain" claim that was true about **reds** and silent about switched-off tests; a **⚠ shard-pressure** section with the measured heavy-path load (`rest` = 665 `CreateWithColony` calls / 234 fixtures vs **18** in the isolated ~11-min `stations` shard; `GroundForcesTests` alone is 48) and the **hand-maintained-complement trap** (a new shard without a matching `!~` in `rest` runs TWICE); and a Layer-3 **⛳ M1** row for the planetary plan's live cradle-to-grave sitting, which previously had no home in any doc that owns Layer 3. The same sweep verified all **97** fixture-name and **7** `Fixture.Method` claims in this doc resolve — **zero phantom tests**. Prior **refresh 2026-07-11**: Layer-1 CI table now lists the component-designer dial suites (⚙ ship + ground) + the resolver-merge kernel; **C5 ground combat moved ⚫→🔵 engine-built + CI-green**; a five-agent audit confirmed the 83-commit build clean. Also holds the 2026-07-03 LIVE-RUN RESULTS + test-completion SCORECARD + fleet-menu diagnostic lessons |
| ~~`docs/archive/SYSTEMS-STATUS-AND-TEST-PLAN.md`~~ | **🗄 RETIRED 2026-07-27 — retirement COMPLETED** (OPERATION GROUND TRUTH Phase C). Its three jobs went to single owners: connections → `SYSTEM-CONNECTION-MAP.md`, tests/gauges → `TESTING-TRACKER.md`, doc currency → this file. **Root `CLAUDE.md` no longer sends anyone here** — the 4 places that mandated opening it (its design-reference row, the Prime-Directive "operationalize" paragraph, pre-flight step 1, and working-agreement item 1) were repointed, which resolves the contradiction where the same file both mandated it and said it was being retired. Nothing live was lost, verified not assumed: §5's test recipe duplicated root `CLAUDE.md`'s Build/Run/Test section, and §6's open-issues list is entirely resolved or stale — including its last apparently-live row ("Planetary Installations tab never appears, gated on dead `InstallationsDB`"), **REFUTED at HEAD**: `PlanetaryWindow.cs:102/218` gate on `ComponentInstancesDB`. Kept in place rather than archived so old line references stay resolvable. | 🗄 |
| `docs/ai/AI-BRAIN-BUILD-TRACKER.md` | **NEW 2026-07-11.** The standing build tracker for the NPC "brain" — the nine AI design docs turned into a verified, ordered, buildable plan along the **Planck→Brane ladder** (atomic scored-decision → Organism → Ecosystem → Galaxy → Brane, + cross-cutting Exploration). Built from a **six-agent feasibility survey** that checked every design claim against current branch source (file:line). Holds: the feasibility verdict (YES — mostly new logic + a few blobs on an already-built nervous system; build bottom-up); the ALREADY-BUILT substrate table; the NET-NEW table; the dead-code (don't-build-on) list; and the phased CI-gated wiring plan (Phase 0 foundations → the first traits → the Organism keystone → Ecosystem → Galaxy → Brane). The build authority; the `AI-*.md` docs are the rationale. | 📌 🟢 | Living — **the whole Planck→Brane ladder is built + CI-green** (top-matter reconciled to the detail tables 2026-07-13): Movement I foundations complete; Movement II Organism engine (2.1–2.8, incl. order emission + means-ends planner), Ecosystem (Phase 3), Galaxy+Crisis (Phase 4, `EnableGalaxyCrisis` live by default), and the Brane acceptance test (Phase 5) all engine-verified. All behaviour wires ship default-OFF behind gates (byte-identical until opted in). **Remaining tail: the Site Engine / Exploration (SE-2+) + local runtime play-tests** (CI can't run the client). As of 2026-07-13. Companion/tracker twin to TESTING-TRACKER + SYSTEMS-STATUS. | **⚠ 2026-07-28 (Ground Truth Phase C re-sweep): F-B1 is now BLOCKED on plan slice S1e.** F-A1 pointed the AI's eyes at `SignalStrength_kW` on the strength-only detection decision, but that field is a detection **MARGIN** (the receiver's own floor is subtracted, `SensorTools.cs:193,197`), so a ship at realistic range reports ~0 while a star reports 1.4 M kW. `ThreatAssessment.cs:39` sums it as "loudness … size proxy", so `CombatRisk.WouldEngage` never evaluates its risk band and every NPC decision reads "vs no threat".
| `docs/ai/AI-CAPABILITY-CATALOG.md` | **NEW 2026-07-12.** Phase A of the **utility-brain** (the "AI aware of ALL its options" engine). A **6-agent read-only scan** of the buildable catalog: every `*Atb : IComponentDesignAttribute` → its JSON `ComponentTemplate`(s) → the player NEED it serves (INTEL/OFFENSE/DEFENSE/ECONOMY/TECH/GROWTH/REACH/PEOPLE/INFLUENCE/GOVERNANCE), file:line-anchored. Holds the NEED→buildables catalog, the **4 GAME GAPS** (needs with NO buildable — INFLUENCE / DIPLOMACY-as-buildable / ESPIONAGE-as-buildable / GOVERNANCE-policy; root: `Factions/` has zero component attributes), 3 flagged data bugs (`spaceport` UniqueID collision; broken `CargoTransferAtbDB` type ref; inert `EmploymentAtbDB`), and the A-code→B→C build path (catalog class → needs model → scorer, one axis at a time). The parts manual the future utility scorer reads; also the to-do list for the missing player systems. | 📌 🟢 | Living — scan complete (knowledge artifact); CODE catalog + scorer not yet built. Feeds the utility-brain (side objective). As of 2026-07-12. |
| `docs/CLIENT-TEST-CHECKLIST.md` | This branch's client RUNTIME test scratchpad (what only the PC build can check) | ♻️ 🟢 | Refreshed 2026-07-27 (A5) — **new row: New Game with `Pulsar4x-Testing` TICKED**, which throws an NRE in the colony build today and is `[Ignore]`d in CI (`NewGameStartSmokeTests.cs:24`) so nothing is watching it; the mod ships and the mods page lists every discovered mod with a checkbox, so it is one tick from a player (off by default — no `DefaultEnabled` field). Prior refresh 2026-07-04 — **new G5 tactical-map block (units draw / click-to-move / click-to-place / terrain+hazards)** on top of the ground map (G3 planet view, G4 survey-reveal) + space stations (deploy/manage/pooled-materials); Fleet UX PASSED + prior confirmed-live section retained |
| `docs/DEVTEST-RUNTIME-FINDINGS.md` | The DevTest playtest **runtime-bug ledger** — every bug the everything-enabled sandbox surfaces, each with root cause + fix + **blast-radius** (is it a class? where else?) | ♻️ 🟢 | NEW 2026-07-14: two live classes flagged — **A** unguarded hard dict-index in a tick processor (froze the clock; engine twin of gotchas #10/#11/#14) · **B** faction-agnostic `Region.Surveyed` leaking a rival's survey to the player. Each has a standing sweep item |
| `docs/DEVTEST-AI-BUILD-LOG.md` | **NEW 2026-07-14.** The running BUILD DIARY for the DevTest "make the AI a real player" plan (Stage 0→6, batches B2→B17+): the plan table (CI gate + 🖥️ PC checkpoint per batch), the per-slice progress log, and — the point of it — **errors incurred + fixes, near misses, lessons learned, and the wide-implication (Prime-Directive) check for every change**. Distinct from `DEVTEST-RUNTIME-FINDINGS.md` (playtest bug ledger) and `docs/ai/AI-BRAIN-BUILD-TRACKER.md` (design→code spec): this is the diary of *what we did and what bit us*. | ♻️ 🟢 | Living — updated organically every slice. As of 2026-07-14: Stage 0 done, B4 largely built, **active target B5** (close the conquest loop). |
| `docs/FACTION-CREATION-GUIDE.md` | **NEW 2026-07-14.** The repeatable SOP for authoring a DevTest scenario faction — EVERY file, EVERY JSON node, and the two landmines, spelled out so a future session repeats it without re-deriving. Covers the faction JSON (all nodes: doctrine/personality/startingItems/componentDesigns/shipDesigns/colonies/stations/fleets/openingRelations/strain), the shared design files, the **gotcha-#10 four cross-checks + a runnable validation script**, LANDMINE #1 (off-world colonies MUST use `space-habitat` not `infrastructure` — tolerance-gated to 0), LANDMINE #2 (dialed Properties clamp to template Min/Max), colony-vs-station differences, the "real AI player" requirement list, and a numbered step-by-step. Built from the UMF/Kithrin buildouts. **Updated 2026-07-16: two new optional nodes documented — `fleetComposition` (§3i, the AI's fleet aspiration ladder) + `garrison` (§3j, the per-faction home-garrison mix).** | 📌 🟢 | CURRENT — the how-to companion to `DEVTEST-AI-BUILD-LOG.md` (the diary). Update when the faction-load path or a landmine changes. |
| `SESSION_STATE.md` | Session-close handoff ("read first after CLAUDE.md") + play-by-play + lessons | 🟢 | Refreshed 2026-07-16 — **LIVING OPPONENTS** session prepended (through-line/what-shipped/errors-corrected/lessons/open-flagged): the AI now masses what it builds (`FleetAssembly` sweeps the root-fleet park-bug), the home-reserve rule (the developer's correction), per-faction fleet+garrison composition, three new AI-usable armed hulls cradle-to-grave, the event ticker. Older sessions collapsed under a details fold |
| `OPERATION-GROUND-TRUTH-PROMPT.md` (repo root) | **NEW 2026-07-27 — the HANDOFF PROMPT for the doc-reconciliation + planning session.** Self-contained instructions for an ultracode session: verify every planetary/ground/combat doc against code + the developer's 27 rulings, run game-log forensics, consolidate/delete drifted docs, produce the functional/accessible/observable delta ledger + THE PLAN, and author the `close-planetary-delta` workflow (build execution awaits the developer's go). Summarizes all 27 rulings inline; #21 (capture transfer) marked OPEN — never decide it. | 🟢 | Execute once, then mark superseded by the plan doc it produces |
| `docs/archive/DESIGNER-AUDIT-2026-07-08/` (9 files) | 🗄 **ARCHIVED 2026-07-28 — do not follow as live, do not size work off it.** The 2026-07-08 seven-way parallel survey of *is the designer UNIVERSAL*. **Its numbers are stale** (written at 67 templates; there are now 96 — its own later revision said 89 and that is stale too; its mount-flag table **omits the `Station` flag entirely**). **Its DIAGNOSIS is not stale and was folded, with every figure corrected inline, into `docs/COMBAT-DESIGNER-GROUND-TRUTH-2026-07-28.md` §16** — the *"universality is real in the basement and lost on the main floor"* verdict, the **two locks** (mount flag vs processor reader), the **nine duplicated ability pairs**, and the **three proof-of-pattern** parts of the tree that already do it right. Every file carries an archive banner. | 🗄 | Historical record of how the diagnosis was reached. **Read §16 instead.** |

## 4. Design docs (each carries a build state)

| Doc | Purpose | Status | Build | Notes |
|-----|---------|--------|-------|-------|
| `docs/combat/WEAPONS-DESIGN.md` | Weapon design: the two-axis Nature×Delivery taxonomy (governing frame) + the dodge/weapon-triangle/saturation resolve depth | 🔒 | ✅ | **Consolidated 2026-07-13** from WEAPONS-AND-DODGE + WEAPON-TAXONOMY. Triangle + saturation built + gauged; taxonomy is the current frame |
| `docs/ground/GROUND-SURFACE-MAP-DESIGN.md` | **THE single design for the planet surface — all three zooms.** Region ring → operational (war) hex → **mini hex** (tactical + construction), plus what the map SHOWS and what you can NAME. Layers: 1 region ring · 2 region combat · 3 fine hex ground · 4 two-zoom war-map/city sub-grid (buildings) · **5 mini-hex TACTICAL grid** (units plot + fight on mini hexes at continuous real-metre distance; the "transitional hexes" edge-continuity fix; ~37 km/tile, lazy so a 590M-hex planet is sidestepped) · **6 display + naming** (hover-reveal model, player glyphs, population-based city marker, battalion stacks w/ detection fog, click-to-name floating labels, weather/hazards). Sibling: `docs/AUTO-RESOLVER-GROUND-TRUTH-2026-07-29.md §12` = the *rules* (real km ranges); this doc = the *board*. | 🔒 | 🚧 | **Consolidated 2026-07-13** from GROUND-COMBAT-MAP + GROUND-CITY-AND-WARMAP + GLOBAL-HEX-GRID + the hex-map half of HEX-GROUND-AND-ORDERS. **Consolidated again 2026-07-24** from MINI-HEX-TACTICAL-GRID-DESIGN (→ Layer 5) + the tile-naming spec orphaned in DEVTEST-CONQUEST-SANDBOX (→ Layer 6); **resolved a live contradiction** between two locked docs ("battles never per-mini-tile" was superseded 2026-07-22 — units DO fight on mini hexes). Build state: Layer-5 M1/M2/M3 + K1–K4 BUILT; **M4 (per-mini-tile terrain) NOT built** — the main remaining gap; naming NOT built; client draws compile-only (never run). **2026-07-24 developer rulings recorded:** fine terrain is load-bearing (affinity + bends the fight + costs more, never forbids — *"nothing is impossible its just costs"*, and a faction good at uncanny building pays less); weather/hazards live at BOTH zooms (regional = it exists, mini = where). Where the diversity COMES FROM moved to its own doc → `docs/environment/SYSTEM-GENERATION-AND-PERSISTENCE-DESIGN.md` | **⚠ CANON-OVERRIDDEN 2026-07-28:** top banner added; **Layer 1 (region ring) DEMOTED to a visual aid** (its `Neighbors` graph is no longer a movement graph, `CrossingTimeSeconds` no longer a transit clock); **⛔ Layer 2 (combat on the region ring) SUPERSEDED OUTRIGHT** — combat resolves at the mini-hex level on continuous distances. Layer 4's combat half + Layer 5 were already right and are CONFIRMED, not changed. New: transitional hexes (M5).
| `docs/ground/GROUND-GAMEPLAY-DECISIONS-2026-07-24.md` | **NEW 2026-07-24 — the developer's RULINGS on ground gameplay end to end**, from a two-pass survey (8 domain readers + 8 cross-system connection readers, file:line throughout). Survey headline: ground is **~85% built engine-side, ~30% player-reachable, 0% observable** — a stock New Game raises no troops/enemy/fleet, five behaviour flags are process statics set only on the New-Game path (so a LOADED save plays differently), and the whole GroundCombat folder emits zero logs/events/battle records. **22 of 27 questions ruled:** ONE designer for everything + prebuilt unit templates DELETED · penetration/energy are WEAPON properties · invalid designs blocked from saving · research cost scales with complexity · every hazard gets a counter and the component depends on the hazard · rally point is a building setting (planetary coordinate / orbit) · ground units cost PEOPLE with no return · nothing is free (costed tile queue only) · every building occupies ground · fund employment + power · tile bonuses are semantic ("what makes sense") · DELETE march-to-region, replace with a two-layer `(regional)(mini)` coordinate · mini-hex movement works like regional · show destination/distance/ETA/speed · targeting and engagement gated by DOCTRINE · battle scoped to REAL DISTANCE · battalions composed the same way as fleets · battle log first · fading last-known contacts · no default garrison/enemy. **ALL 27 now ruled except planet-capture transfer (open).** Late rulings: ONE RTS-style build queue for everything (the location "hook" question was dissolved — one path, every entry carries its destination, visible progress) · a fight is COMMITTED (ends only when one side is dead or calls RETREAT, which starts a ~1-2h break-away timer; PURSUIT is a doctrine) · fire rate is CALCULATED not lumped (a weapon carries damage/second; the resolver integrates it over the tick against available targets and works out who died) · hover tooltip AND Force Management for unit detail · flags move into the SAVE. **Names the next design item: the DOCTRINE CATALOG for planetary AND space, leader-modulated** — today's entire ground doctrine surface is 3 JSON stances with an attack mult, a damage-taken mult and a cooldown; it must grow to carry engage / target-priority / retreat-trigger / pursuit. **Flags a calibration problem the rate model creates:** the ground tick is 1 hour, so 10 dps = 36,000 damage per tick (180 kills from one machine gun) — needs a finer combat step, scaled rates, or an engagement-width cap. Also records a verified correction: **ground weapons had NO fire-rate field at all** (`GroundWeaponAtb` = Mass/Attack/Range/Mode/Range_m); `Attack` was a flat lump applied once per game hour by the processor tick — which is exactly what ruling #23 replaces. | 🔒 | 🚧 | Decisions recorded, build pending. Build order flagged: #2 (weapon dials) → #4 (research costs) → #1 (delete prebuilts), since the prebuilts are today the ONLY carrier of penetration/energy | **⭐ CANON EXTENDED 2026-07-28 — the MOVEMENT & GEOGRAPHY addendum (rulings M1–M12).** One movement layer not three · **regions are a VISUAL AID** · position is a two-part address (regional hex + mini hex) and the player orders to a specific mini hex · **transitional hexes at the regional-hex level** · combat proximity at the mini-hex level, starting at weapons range (RED border; sensors WHITE) · **capture is per-HEX** (victory = written deferral) · orders only from Force Management · fortification region-adjacency deleted · hazards per-hex from terrain+geography · hybrid AI brain. Plus the governing law it produced — **One Verb, Both Seats** (now in root `CLAUDE.md`). Records what is ALREADY BUILT (the two-part address IS the live distance function) and the ONE real blocker (the `byRegion` combat bucket).
| `docs/environment/SYSTEM-GENERATION-AND-PERSISTENCE-DESIGN.md` | **NEW 2026-07-24 — LOCKED.** Where planetary/system diversity comes from, and how a generated system becomes permanent. The developer's requirement: generating a system for the FIRST time produces everything from *a grassland with a resource node in it* out to *the asteroids in the star's Oort cloud*, then **writes a spec file**. **Five locked decisions:** ① **HYBRID** file — seed/recipe for untouched space, **frozen detail for anything observed** ② written **the first time** ③ per-save vs **shared library** is a **player choice at galaxy setup** ④ **hand-editable** ⑤ surfaces **materialize on demand**. Key insight: store the **recipe, not the cake** (terrain is deterministic, so ~590M hexes become a few hundred lines) — and the hybrid kills the failure mode a pure recipe has (*improving the generator silently rewrites the terrain of every system anyone visited*; observed ground is frozen, so what became yours stops being a dice roll). Holds the verified EXISTS/MISSING ledger + the gauge-first build order G1–G6. | 🔒 | ❌ | **NOT BUILT.** Two concrete gaps found: **no system writer exists anywhere** in `GameEngine/Galaxy/`; **procedural systems get NO asteroid belts** (`GenerateAsteroidBelt` reachable only from the authored `LoadFromBlueprint` path). The format + read path DO exist and are live (`SystemBlueprint`/Sol JSON), so the writer emits a shape the game already reads. G1 (writer + round-trip gauge) is the keystone; G6 (terrain from stellar/orbital physics) unblocks surface-map M4 |
| `docs/ground/SURFACE-FOG-AND-RECON-DESIGN.md` | **Per-faction ground fog + reconnaissance** — the LOCKED two-tier survey (orbit = parameters + deposit location/type un-assayed; ground scout = assay amount/rate + reveal hidden sites), the recon-as-a-component rule, and the finding that ONE fog layer feeds both survey AND honest landing intel. Holds the "easiest landing" score + beachhead/FOB + the 6-slice build plan (path A: fog first) | 🔒 | 🏗️ | NEW 2026-07-17. **Slices 1–4 BUILT (per-faction reveal, masked-deposit assay, space-survey + ground-scout wiring, hidden sites); slice 5 = `GroundThreat` (Earthfall G2.2); slice 6 (fog consumers) partially wired — the "easiest landing" score IS consumed (Audit M3, `MilitaryTarget.LandingEaseFactor`, 2026-07-22); client 2b/4b pending.** |
| `docs/ground/GROUND-ORDERS-CATALOG-DESIGN.md` | The ~60-order Aurora-style catalog for fleets AND ground formations (O1–O3 roadmap) | 🔒 | ⚫ | **Split out 2026-07-13** from the order half of HEX-GROUND-AND-ORDERS (the map half went to GROUND-SURFACE-MAP) | **⚠ CORRECTED 2026-07-28 (canon + a re-count):** its **O1 open decision is CLOSED** — the old lean (keep combat/doctrine/ground actions as DIRECT CALLS with UI-only descriptors, to preserve a mid-battle engagement-lock bypass) is now **FORBIDDEN** by **One Verb, Both Seats**: it is two parallel paths for one verb, and the AI issues orders through the queue, so a direct call is AI-invisible — the same defect as the click-to-march. Ruling: they JOIN the pipeline; mid-battle reaction is an order PRIORITY property, not a second execution path. Plus **M9**: ground orders come from Force Management and nowhere else. Also **re-counted its headline survey — 46 command classes exist and 23 distinct types are reachable in the client, not "~51 exist / ~5 reachable"**; the "~5" was true only of the fleet Issue Orders tab.
| `docs/ground/PLANETARY-GAMEPLAY-AUDIT-2026-07-24.md` | **NEW 2026-07-24 — holistic planetary-gameplay audit** across 8 subsystems (surface map · population/morale · infrastructure · surface minerals · ground combat · invasion · unit assembly · the connected loop), design-docs-vs-**real-code** with honest built/partial/dark grades + an adversarial verify pass. Headline: the cradle-to-grave loop (survey→settle→mine→build→invade→hold) is BUILT and CI-path-proven, but (1) **unreachable from the menu front door** (default game spawns no enemy/garrison/fleet), (2) **closes by bypassing bombardment** (orphaned from fleet combat + AI), (3) **runtime-unverified**. Holds the status board, the 4 named loop-breaks + 5 built-but-disconnected islands, and a **P1–P7 roadmap** (open the door → prove it runs live → close bombardment → make inert levers bite → fix client fog+assembler → locate organic growth → collapse the dual hex/mineral representations). | 🟢 📸 | ✅ | Point-in-time audit — **steer planetary work by this.** Also drives the doc-cleanup rows below (PLAY-TO-MARS / MVP / GROUND-UNIT-VARIABLES stale) |
| `docs/economy/OFF-WORLD-INFRASTRUCTURE-DESIGN.md` | Off-world infrastructure: the station as universal host + the colony-progression growth ladder | 🔒 | ➕ | **Consolidated 2026-07-13** from SPACE-STATIONS + COLONY-PROGRESSION. Stations cradle-to-grave built + green; ladder is the growth axis |
| `docs/ai/AI-DECISION-ENGINE-DESIGN.md` | The NPC decision engine: Part A the needs-ladder objective engine (destination) + Part B the means-ends planner (how to reach it) | 📝🔒 | ➕ | **Consolidated 2026-07-13** from AI-OBJECTIVE-ENGINE + AI-MEANS-ENDS-PLANNER. Objective front-end built+gated; planner act-half stubs |
| `docs/ai/AI-EMERGENT-POLITICS-AND-CRISIS-DESIGN.md` | Emergent inter-faction politics (Organism pointed outward) + the temporal arc & late-game crisis + the authoring/acceptance layer | 📝🔒 | ⚫ | **Consolidated 2026-07-13** from AI-ECOSYSTEM + AI-GALAXY-AND-CRISIS + AI-SUPERCLUSTER-AND-AUTHORING |
| `docs/ai/DEVTEST-CONQUEST-SANDBOX-DESIGN.md` | **NEW 2026-07-13.** The DevTest game mode (replaces Quickstart): start-from-nothing player vs. two authored AI factions (UMF kinetic/inner-system/at-war; Kithrin energy/outer-system/expansionist) racing to conquer Sol — the **integration test** for the whole game. Holds: the t=0 situation; the two factions' **materiel sheets** (all EXISTING designer templates as new DESIGNS, not new mechanics — the reusable "base for designing factions"); the consolidated AI **sense→decide→act** loop (perception + shared scorer + action library over the player's own order rail) with the ranked single-system action program (ground-war keystone → fuel → aggression → seeded-expansion); tile-naming; and the **few-but-thorough CI-batch build plan** (7 batches). Operationalizes the AI-BRAIN-BUILD-TRACKER / AI-DECISION-ENGINE against a concrete scenario harness. | 🔒 | ⚫ | Build not started; this doc is the plan. Batch 1 = this doc. |
|-----|---------|--------|-------|-------|
| `docs/environment/ENVIRONMENTS-DESIGN.md` | Space hazards + planetary terrain as ONE physics-driven system: exotic hazard catalog, physics→hazard generation, the 6 meta-mechanics | 🔒 | 🚧 | **INFRASTRUCTURE BUILT (E1–E3, 2026-07-04): region-hosted typed effects (reuse `HazardEffectType`) + the physics→hazard generator (gas-giant-gated) + per-tick attrition — so "more environments" is now DATA, not engine code. E4/E5/transience + the exotic-catalog sweep are the later work.** Design below the build status: Terrain = the ground twin of a space hazard (share the vocabulary, don't refactor the green engine); environments are exotic + generated from planet physics (temp/atmosphere/tectonics/body-type/orbit), **gas-giant-gated**. Holds the creative sweep (6 meta-mechanics + exotic catalog) + the E1–E5 build plan. Co-fixes the space side's flagged flat-RNG placement |
| `docs/economy/COMPONENT-DESIGNER-CATEGORIES.md` | **NEW 2026-07-08. DESIGN-LOCKED (categories) — the decision doc realizing the universal-assembly principle in the DESIGNER.** Collapses the 67 hand-authored component templates into **11 parametric categories / ~37 doors** (Weapons/Propulsion/Sensors/Power/Defense/Enhancers/Industrial/Logistical/Civic/Command/Chassis), where specific things (phaser, submarine, RTG, bunker, dreadnought hull) FALL OUT of sliders instead of being authored, and any component mounts on any Chassis that can carry+supply it. Adds two net-new capabilities (Electronic Warfare, Enhancers), makes ship hulls + station designs real, and deletes the parallel ground systems. Includes a **12-unit franchise stress test** (Star Trek / Wars / Gate) that maps ~80% cleanly and catalogues **12 holes** (H1 matter-teleport + H4 the innate-"magic"/People boundary are the load-bearing two). The rule: roles are DIALS, not doors. **+ WIRING-READY (2026-07-09): the map half of the two-doc wiring reference** — this doc holds the categories/boundary/stress-test/holes (all 12 holes now dispositioned + homed in a dossier); the per-category wiring lives in the `⚙ Wiring Dossier` of each `docs/economy/COMPONENT-DESIGNER-DIALS.md` section. Together the two are the ONLY reference needed to wire — the resolver anatomy + the five merged strategic docs are folded into the dossiers. | 🔒(categories) | 🟢 | **All 37 doors locked + stress-tested + wiring-ready.** Supersedes the per-template model + `WEAPON-UNIFICATION-DESIGN.md` (deleted). Realizes `docs/economy/UNIVERSAL-ASSEMBLY-DESIGN.md`. Next: land the shared prerequisites (resolver merge, §0b mass-cap, durable seats, refining feed, BonusCategory) → build Weapons as the pilot against dossier ⚙ 1. |
| `docs/economy/DESIGNER-NORTH-STAR.md` | ⭐ **🔒 LOCKED 2026-07-29 — THE DESIGNER'S NORTH STAR, in the developer's words: *"this is how the designer must be."*** The METHOD — **derive the doors from the values the sim actually reads**, never invent them — plus the first category worked end to end. **Weapons: 5 doors and 41 dial groups become 2 choices and 4 sliders.** Holds the ten variables the resolver reads (file:line), the five questions they group into, both axes (**how it gets there** × **what it's good against**) with the real shield-soak numbers, the two couplings (dps = per-shot × rate; **armour cares about shot size, shields do not**), the 🔑 **asymmetry** (shields: the attacker decides · armour: the DEFENDER decides · hull: nobody), compositional generic naming (1,073 names, no franchise IP), the proof that all **eleven** in-game weapons fall out, and what it kills. Live driveable reference linked inside. | 🔒 | ➕ | **Supersedes the Weapons structure** in CATEGORIES §2 and DIALS §1 (both bannered). **Part Two adds DEFENSE** (derived 2026-07-29): the four-layer onion (miss→shield→armour→structure), the 🔒 **zero-sum plate-tuning ruling**, and the finding that Hardening is not a combat defence at all, Fortification is infrastructure, and Evasion — the best defence in the game — is not sold at that door. **Part Three adds PROPULSION** (derived + corrected 2026-07-29): the category with the *opposite* problem — almost no dials at all, because thrust is linear in engine mass and a bigger engine is strictly better. Restores the real physics trade (`T · v = 2P`), and §23.2 records the correction that the trade is **THREE-way**: `SensorSignatureAtb`'s magnitude **is** the thrust, so shoving hard also buys you **being seen from farther away**, and detection decides who shoots first. §23.3 locks *the choice picks your dial set* (Reaction / Traction / Warp read different things); §23.4 lists the rejected candidates and the **missing symmetric drive-heat wire**. §25 closes the loop across three derivations: **Propulsion is the game's largest defensive purchase and nothing says so.** **Plus two developer rulings (2026-07-29): §1b 🔒 EVASION IS A MULTIPLIER** — there is no to-hit roll in either resolver (`EffToughness = Toughness ÷ landed`), so evasion is an effective-health multiplier capped at **×20 point-blank / ×50 at range** against armour's **×10**; the whole defence stack becomes `Health × evasion mult × armour mult` with shields as the one depleting pool, all three keyed to the two weapon axes (evasion→delivery, armour/shields→nature) — **the defence model is the weapon model in a mirror**. And **§23.2a 🔒 SIGNATURE MUST COST MORE**: detection range grows as **√signature**, so loud is nearly free today; the ruling makes suppression a **tax on the whole power budget** (`P_drive = P_total × (1 − suppression)`) that pays twice, and §23.2b names the cost already there — `FirstStrike_SeerWipesBlindEnemy_Unscathed` wipes an equal blind fleet **taking zero losses**. **And §23.3a 🔑 THE GROUPING** (developer: *"don't we have locomotion in another door, why not just group everything?"*) — the design already puts ground-locomotion under Propulsion, but the engine carries **two implementations of one verb sharing ZERO fields** (`NewtonionThrustAtb` vs `GroundLocomotionAtb`); they unify because **every family is one power budget spent two ways** — Reaction splits thrust↔exhaust velocity, **Traction splits speed↔tractive effort (the gearbox trade — `SpeedFactor` and `RoughHandling` are the same zero-sum split)**, Warp splits top-speed↔bubble-cost. Same two dials; the medium changes what they buy. Two **corrections** landed with it: **§23.3b `Amphibious` costs mass and is read by NOTHING** (ocean is hard-coded impassable to everyone — the clearest §1-law violation yet), and **§23.3c `Fluid` is not a family** (zero components anywhere — it is Reaction with the propellant scooped). **Three developer decisions closed 2026-07-29:** **§26 fuel type = a MULTIPLIER ON THE BUDGET** (not a door, not a slider position — `P_total = drive size × FuelGrade`, so it works for every family, where the old exhaust-velocity lookup only meant anything to Reaction); **the refinery rung already exists** (all four fuels are `IndustryTypeID: refining` today), but a pure multiplier is a dominance ladder so the counter-axis is `VolumePerUnit` — **performance × density × cost** — and **RP-1 is a strictly dominated dead RECIPE** (Methalox beats it on velocity *and* density at identical cost). **§26a `Amphibious` KEPT**, which means wiring it: `HexPathfinder.IsImpassable` becomes unit-aware (blast radius: an impassable hex is dropped from the graph entirely, so the graph becomes per-unit). Plus two explainers for pending calls: **§1c the TWO CEILINGS** (`EvasionCap` bites point-blank → ×20; `MinLandedFraction` bites at range → ×50; recommend making them agree at ×20) and **§15.2 THE ARMOUR BAND** (a graphic equaliser with fixed total power; needs both ends or combat becomes a pre-battle coin flip — **and it cannot be set until §15.1 moves the ship side to the ground's 1.0-neutral parameterisation, because `ArmourHardeningAtb` has no midpoint to lean around**). **§26b ✅ BUILT (2026-07-29) — two cheap additions** from asking *what does the sim already read that has no dial?*: **Warp gains `Startup vs Endurance`** — bubble CREATION gates departure and SUSTAIN is charged per second in transit, both live and neither dialled, so one slider multiplies creation and divides sustain (**product invariant** — zero-sum like `T·v = 2P`), range [0.4, 2.5] reciprocal-symmetric, 1.0 byte-identical, with a buildable `alcubierre-2k-endurance` and gauge `WarpBubbleTradeTests`. And **the ground frames' `Locomotion`/`CarryClass` dials become NAMED enum pickers** (they were raw integers showing *"Locomotion: 1"*) — ⚠ note the renderer builds `max − min` entries, so `MaxFormula` must be the enum COUNT or the last value is silently cut. Eight categories still owed. |
| `docs/economy/COMPONENT-DESIGNER-DIALS.md` | **NEW 2026-07-08 (living doc). The lowest-level designer spec — for each of the ~37 doors, the actual DIALS a player turns, the derived stats they produce, and the modellability audit (§0d Modelled/Wire/Defer).** Governing rule: a dial is real only if it moves a number the resolver reads (§0e number model + the per-category yardstick). **Weapons category done** (Energy/Ballistic/Melee/Guided 🔒 + Exotic 🟡). **Propulsion category COMPLETE (5/5 🔒 §2.1–2.5)** — Reaction/Traction/Fluid/Warp/Exotic, yardstick = the Newtonian move + closing model; the most Modelled-today category so far (Newtonian physics + ground-locomotion + warp-bubble/jump all live). Two prerequisites fell out: the air/altitude/depth combat layer (Fluid deep half) + the H8 gate-network / H1 teleport layers (Warp-gate, Exotic-teleport). **Sensors category COMPLETE (4/4 🔒 §3.1–3.4: Detection/Survey/Fire Control/Electronic Warfare)**. **+ §0f the multi-consumer rule** (a dial is real if ANY sim system acts on it — combat is one of five; components serve exploration/economy/colonization/navigation/research/first-contact and mount on facilities, not just warships — a standing lens for every followup category). Sensors is where it first flipped a verdict: **Survey went thin → RICH** (8 scanning modes across 5 consumers; surfaces two franchise-earning net-new science loops — the exploration mystery-box + xenoarchaeology). Detection = most-built (also serves navigation/SAR/observatory); Fire Control = purest CONNECT door (built-but-dead `Range`/`TrackingSpeed`/`FinalFireOnly`); EW = net-new on the detection engine's surface. **+ the HOLISTIC per-dial review format is now the STANDARD for every category** (developer's call): each dial presented as one unit — decision → justified options (anti-dominance) → wire strip (Modelled/Wire/Defer + exact resolver/consumer insertion + file:line) → numbers; rendered as an instrument-spec artifact per category (Sensors artifact published). **Power category COMPLETE (2/2 🔒 §4.1–4.2 Generation/Storage)** — yardstick = the supply gate (Power decides whether every other door functions; §0b "numbers force the build" lives here); honest gap flagged: under-supply hard-blocks where §0b wants a soft throttle. **Defense category COMPLETE (4/4 🔒 §5.1–5.4 Armor/Shields/Hardening/Fortification)** — Shields most-Modelled (pool+matchup live both domains); Armor holds the two best CONNECT wires (feed ship `Combatant.Armour` for swarm-bounce; add an armour NATURE dimension = the matchup-doubler); Hardening honestly not-a-combat-door (consumer = hazards); Fortification clean/ground-only. **Enhancers category COMPLETE (3/3 🔒 §6.1–6.3)** — the honest net-new force-multiplier category; Bio-augmentation mostly-Modelled (GroundAugmentAtb, ground-only), **Training/Doctrine reframed to the UNIT-CALIBER door** (a design-time `Quality` multiplier makes Space Marine ≠ Guardsman / Falcon ≠ freighter — the unit's own stat, not the commander; scarce talent-pool = anti-dominance), Systems essentially net-new; holds the gear-vs-being boundary (H4). **Industrial category COMPLETE (2/2 🔒 §7.1–7.2)** — the economy spine (mine→refine→build), mostly Modelled; #1 gap = the BROKEN refining feed (refinery works but inputs aren't auto-supplied → materials never made); adapted per developer with a NEW `unit-assembly` type (vehicle-foundry — the AT-M6 build path) + a generalized assembly-bay size gate. **Logistical category COMPLETE (2/2 🔒 §8.1–8.2)** — hold-and-move backbone, mostly Modelled (universal typed cargo hold + troop bay; manual transfer + a working automated freight market). Headline finding: the mothball/reserve ask exposed that the game has **NO fleet/army upkeep** (only stations) — so it's a two-part build (a ship/unit upkeep clock, then the reserve discount), and the upkeep clock is the missing economic pressure worth building on its own. **Civic category COMPLETE (2/2 🔒 §9.1–9.2)** — the human backbone (colony+population), mixed; Habitation load-bearing (infrastructure = a REAL economy multiplier; pop-support = carrying capacity), Development split (research/academy Modelled, but **terraforming/world-improvement MISSING** — the franchise gap); Civic is the hub (population→talent→Enhancers elites, research→every door). Two switched-off spots: sustenance inert, employment data-dead. **Command category COMPLETE (1/1 🔒 §10.1)** — the "play at your own altitude" delegation layer, the least-wired category: the seat substrate is Modelled + the right shape (one `AdminSpaceAtb` → colony HQ AND ship bridge, "one shape two altitudes"), but every consequence is stub/net-new (span unenforced, a seated officer gives no bonus, the delegation loop is designed-only). Sharp correction baked in: the funded-officer record `AdministratorDB` is **built-but-DEAD + duplicated** (zero consumers; research really runs on the twin `ResearcherDB` — the governance doc pointed at the corpse). **Chassis category COMPLETE (5/5 🔒 §11.1–11.5 Personnel/Vehicle/Hull/Structure/Mega, LOCKED 2026-07-09)** — the FINAL category (the container/frame that holds the §0b mass budget); keystone build = port the ground carry gate to ships+stations so §0b forces the build at ALL scales, not just ground. **All 37 doors now locked.** **+ 📎 Main-Merge Delta (2026-07-09):** the `ai-faction-perf-planning` merge (PR #77) is 100% design docs (0 `.cs`/`.json`) → **no Modelled/Wire/Defer verdict moves**; it only fills in DESIGN behind deferred doors — Survey's two ⏳ net-new loops (anomaly + xenoarchaeology) are now DESIGNED as one field-site loop (`docs/explore/EXPLORATION-CONTENT-DESIGN.md`); Command's stance/funding/competence/auto-runner/delegate-consolidation are designed (`docs/society/GOVERNANCE-AND-DELEGATION-DESIGN.md`) + a new **Contracts** dial + cabinet seats; the shared person-modifies-outcome wire is designed; `SignalQuality` is DESIGN-CUT so Detection Dial E + EW Jamming wires were corrected to strength-only; bioweapon enriches Weapons▸Exotic (no new door); Influence + Living-Galaxy are out of scope (strategic layers). **+ 🧭 Essence Extensions (2026-07-09, 🔒 LOCKED — first-class, wired alongside; +Weapons▸Exotic locked, so ALL base doors + extensions locked):** a creative pass on what the merged docs' ESSENCE demands be ADDED — keystone move = promote the `WeaponProfile` into a medium-agnostic **PROJECTOR⛓COUNTER** schema (a `pillar`+`skeleton-role` TAG + shared dial-spine Reach·Cadence·Selectivity·Delivery⛓Counter, graded per-pillar-resolver; zero new doors); **§0g** = three acceptance criteria (Reachable/Mirrored/Observable); authoring law = **count resolvers not doors** (37 doors = ~7 resolver loops + data). Per-pillar gear: leadership Command-Reach/Academy/Hardening dials + a **NEW C3 Relay door**; exploration field-lab/excavation/salvage/tow/counter-interference dials + a **Route Works** door; influence SWAY-effect + jamming/insulation counters; espionage ELINT-flavor (re-homes cut Resolution → Information Ledger) + counter-intel + covert-insertion. Convergence: the two new doors both fill hole **H8** (network infra); convert fills **H9** — the essence lands on the 2 open holes, doesn't grow the 11 categories. **+ ⚙ WIRING DOSSIERS (2026-07-09): every category now carries a self-contained `⚙ N — WIRING DOSSIER` at the end of its section** — the ONE reference to wire that category: the complete dial→derived-stat→engine-wire table (essence-ext dials folded in as first-class rows) · resolver/system insertion points (from the anatomy, adapted) · prerequisite fixes & dead stubs · the merged-doc design essence captured inline · the §0g Reachable/Mirrored/Observable stamp · cross-category shared state · owned holes — all VERIFIED against the live engine (file:line) by 11 parallel agents. The framework moves promoted to §0g/§0h/§0i. The 📎/🧭 appendices + `docs/AUTO-RESOLVER-GROUND-TRUTH-2026-07-29.md §14.1` are now origin/rationale; the dossiers are the wiring authority. **+ RECONCILED 2026-07-11: the §-level design-spec sections were flipped to match their own ⚙ dossiers now that the dials are WIRED** — §5 Defense headline #2 (armour nature-resistance ✅ both ground+ship), §6.2 caliber "Quality tier" + scarcity-cost (✅ `UnitCaliberAtb` + `ManpowerTools` talent draw), §6.3 automation (✅ `CrewAutomationAtb`), and the §6 build-list, all previously reading "net-new/to-wire" against already-shipped code (a five-agent audit found the dossiers current but these sections lagging). | 📌 🟢 | Living — **all 37 doors locked + wiring-ready; the caliber/automation/fire-control-range/armour-nature dials are now WIRED + CI-green (2026-07-11), and the design-spec sections reconciled to match.** Companion to `docs/economy/COMPONENT-DESIGNER-CATEGORIES.md` (the map). Progress table at top tracks all 37 doors. |
| `docs/economy/CAPABILITY-BUILD-PLAN.md` | **NEW 2026-07-12. THE unified step-by-step build plan — consolidates `docs/economy/COMPONENT-DESIGNER-DIAL-LEDGER.md` (what's built) + `docs/ai/AI-CAPABILITY-CATALOG.md` (what the AI can choose) into one minimally-sliced implementation plan.** Thesis: the NPC utility brain can only pick an option that is (a) buildable, (b) actually wired, (c) readable — so the dead-dial wires + the missing content systems (INFLUENCE/Embassy/Spy-net) are prerequisites, and the brain (catalog→needs→scorer) is the capstone. Produced from a 7-agent build→wire→test code audit (file:line). Holds: the dependency spine, the two ledger corrections (mass-cap already bites; reactor-heat already built), ~28 CI-gated slices across 6 tracks (each byte-identical-first or flagged RB), and the two critical paths — "convert their people instead of war" (INFLUENCE, 3 slices) and "AI aware of its options" (the scorer, after the wires it scores exist). **This is the doc we build from; the ledger's wire-plan + the catalog's build-path now point here.** | 📌 🟢 | Living plan — flip a slice's row here + its dial-ledger row in the same commit the code lands. |
| `docs/economy/COMPONENT-DESIGNER-DIAL-LEDGER.md` | **NEW 2026-07-12. The honest CODE-state companion to `docs/economy/COMPONENT-DESIGNER-DIALS.md`** — that doc is a DESIGN spec where every door reads 🔒 LOCKED (design decided, *not* built), which reads as "finished" when the code is a fraction of it. This ledger measures the code axis: for each of the 37 doors, which of its speced DIALS actually move a number a resolver reads (atb field + resolver read + test — verified file:line by a 7-agent audit), and for each unwired dial the exact wire+test to build it. Holds: the two-axis door table (25 BUILT / 7 PARTIAL / 5 DESIGN-ONLY, but dial-thin — Survey 3/10, Transfer 3/10, Hull 1/4), the per-door dial truth, a ranked wire-plan (headline: ship armour-penetration/per-shot-alpha is CI-green on the ground but DEAD in space — one `StepEngagementGroup` wire lights 3 weapon doors), and the spec-drift corrections (dials the spec over-claims as wired but are dead — Traction amphibious/motive/mass, Relay, Route Works; and one it under-claims — refining WORKS). | 📌 🟢 | Reference — the build tracker behind the design spec; flip a dial's row here in the same commit its code state changes. |
| `docs/economy/COMPONENT-DESIGNER-DIAL-AUDIT-2026-07-23.md` | **NEW 2026-07-23. Point-in-time audit of EVERY player DIAL of EVERY door** against the developer's universal-designer standard (wired · universal/Zerg-safe · non-redundant · costed · cradle-to-grave+DevTest). 27 of 34 doors done (7 pending after a session cap — Chassis ×5 + Command + Civic▸Development, Chassis first); 96 dials clean, **123 flagged**, collapsing to **7 patterns** — dominant = the FREE-WIN dial (the S9–S15/ground-Attack move), most important = the crew-source universality gap (one fix, ~12 doors). Holds the tiered slice backlog (Tier 0 landmines/crashes → Tier 1 byte-identical quick wins → Tier 2 rebalance-decisions → Tier 3 crew → Tier 4 wire-or-cut) + the developer decisions. Flagged items are raw audit reads (verify pass was cut off) — confirm against source before each fix. | 🟢 | Point-in-time findings; finish the 7 pending doors + verify pass, then fold in. Companion to the DIAL-LEDGER (code-state) + DIALS (design spec). |
| `docs/economy/UNIVERSAL-ASSEMBLY-DESIGN.md` | **NEW 2026-07-05. Cross-cutting PRINCIPLE — governs the whole build system.** Everything buildable (unit / ship / installation / station / super-weapon / world-ship) is the SAME kind of object: a **chassis (structural budget) + components (consume budget, contribute stats)**, stats emergent from the sum, gated by physics, at any scale, within reason (tech+scale caps). Differ by number & scale, not kind. Mostly UNIFICATION not rewrite — components are already universal (`CONVENTIONS §6`), ships are already assemblies, `IConstructableDesign` is the seam, and the ground assembler (`GroundUnitAssembly`) is the general-purpose core. Non-breaking convergence roadmap: record → harden on ground → generalize the assembler+gate → extend the scale ladder → converge the designer UX. **+ §0 ONE-DESIGNER decision (2026-07-14, dev's call): ONE designer for EVERY buildable (component/ship/ground-unit/station/installation) — no per-domain designers; the deleted `GROUND-UNIT-DESIGNER-DESIGN.md` (its durable unit model folded in here + implemented by `GroundUnitAssembly`) had its separate-window plan retired — assembly UX lands INSIDE the one Component Designer.** | 🔒(principle) | 🚧 | **Principle locked; realized at unit scale (ground G-D track). §0 ONE-DESIGNER locked. Survey + roadmap recorded.** |
| ~~`docs/combat/WEAPONS-DESIGN.md`~~ | **DELETED 2026-07-08 — superseded.** Its plan was "merge the two parallel weapon systems (`GroundWeaponAtb` ↔ space weapon atbs) into one `WeaponProfile`." The **parametric-families** direction (2026-07-08, see below) replaces that entirely: Weapons becomes 5 designer *families* (Energy / Ballistic / Melee / Guided / Exotic), each a slider-space from which specific weapons EMERGE, and **both** old systems get deleted for good rather than reconciled. Removed so it can't drag work back toward "reconcile" instead of "collapse + delete." | — | ⛔ | Superseded by the parametric-designer direction (weapons locked; propulsion next). |
| `docs/society/MORALE-AND-POPULATION-DESIGN.md` | Morale as the population-tank valve; people as a finite draw (M1–M5) | 🔒 | ✅ | M1–M5 built + green this session. Open = employment numbers + base-low **calibration** (PC) |
| `docs/society/GOVERNMENT-AND-POLITICS-DESIGN.md` | Government-as-modulator (dials) + internal politics (legitimacy/rebellion/demands) | 🔒 | ➕ | Government dials ✅ wired; legitimacy ✅ live; rebellion first rung ✅. Demands-engine + Interior Minister ⚫ (blocked on delegation). Parked "religion 5th dial" reframed → `docs/society/INFLUENCE-PILLAR-DESIGN.md` |
| `docs/society/INFLUENCE-PILLAR-DESIGN.md` | **NEW 2026-07-07 (concepts).** Influence as the **fourth conquest pillar** — conquest by belief/culture/ideology, no weapons: convert a rival's population until the world **secedes**. Same skeleton as Military/Espionage/Diplomacy (delegate + hands + target + projection + counter + kill), run by the leader pipeline. The **kill reuses the legitimacy/rebellion system** (+ one new external-belief-pressure input); **unifies with espionage `sow-unrest`** (covert twin, same target). Religion is flavor #1 (culture/ideology #2/#3); the govt dial sets capacity/style. Delivery = missionary-as-spy first, culture-field = 2.0. NPC mirror (counter-influence) is a co-requisite. Un-parks the religion dial into correct scope. | 🔒(concepts) | ⚫ | Pillar + kill-mechanic + espionage unification + flavors locked; build order set (belief-pressure input first). Open: pressure math, conversion-target rules, psionic frontier, culture-field 2.0 |
| `docs/society/DIPLOMACY-DESIGN.md` | External politics: relationship track, IFF, first-contact, treaties, casus belli, reactive engine | 🔒 | ➕ | Substrate + teeth + first-contact + commerce + reactive DRIFT ✅. **2026-07-07: the 3 keystone prereqs CLEARED/DISSOLVED** (GlobalManager fixed · `SignalQuality` cut→gradient in Ledger · hostility-from-`DiplomacyDB` done) — politics buildable now; **Foreign Minister seated in the leader pipeline** (empire→per-faction→Envoy/Ambassador) + FM MVP (per-faction FM + "Are we good?" loop + NPC counterparty + observation feeder). Commitment executor / treaty-proposal policy / negotiation UI ⚫ future | **Phase-B corrected 2026-07-27, two defects:** the blast-radius table row said *"hostility isn't diplomacy-driven"* while this doc's own banner ~460 lines above said keystone 3 was substantially done (row corrected — `AreHostile` reads `DiplomacyDB` both ways, and the precise shape is that diplomacy SUPPRESSES default hostility or FORCES war, never creates it from a score); and the claim that `SignalQuality` **"was CUT"** is **REFUTED** — it is live in 9 files gating survey reveal at 0.20/0.80, so only its hidden-info-gradient ROLE was cut. Read literally the old wording invited deleting live code.
| `docs/society/GOVERNANCE-AND-DELEGATION-DESIGN.md` | The delegate layer (ministers/governors/admirals) — "play at your own altitude" | 🔒 | ⚫ | **NOT built — the key blocker** for Foreign/Interior Ministers (and thus much of politics) |
| `docs/ai/AI-COMMAND-AND-COMMUNICATION-DESIGN.md` | **NEW 2026-07-10 (v0.3 DISCUSSION DRAFT).** The information/communication layer that ties the AI levels together — the twin of `docs/society/GOVERNANCE-AND-DELEGATION-DESIGN.md` (who the seats are) answering *how they work together*. Built on **mission command**: DOWN the chain flows a **MANDATE** (objective + priority + resource-share + stance/principles + constraints); UP flows a **REPORT** (status + progress + exception/escalation) — two data slots per boundary, read on each level's clock, no message bus for v1. Defines the **4 tiers** (Head of State / Cabinet / Operational / autonomous) with what each **SEES / REACTS-TO / DECIDES**, the **cadence pyramid** (rare cheap strategy → frequent cheap reactions = the affordability lever), and **identity propagation**: faction identity = dials (`DoctrineVector`) + character traits + government modulator, expressed as **WEIGHTS on scored choices** at every level (one AI, faction-supplied weights — "no special AI path" applied to *character*); officer competence = execution texture on top. Grounded in the 2026-07-10 five-agent survey (hands DONE, eyes half-wired, brain/goal-slot/delegate-behavior = the real build). | 📝(discussion) | ⚫ | **v0.3 strawman — NOT locked.** Settled: identity drift (state-vs-trait), officers-as-characters (faction outweighs officer until tenure/experience override), autonomy = HoS/player dial (NPCs run delegates free). **Trait vocabulary expanded 6→12** via a four-agent cross-franchise survey (Expanse/BSG/B5/Dune/Culture · Trek/Wars/ME/40k · Stellaris+Stargate/Halo/StarCraft/4X · hive-mind axis-hunt): added Zealotry/Guile/Collectivism/Authoritarianism/Altruism, each on a named lever + live-when-wired rule; deferred Patience/Adaptability/Venality/Isolationism; cut grudge→relationship track; faction-fingerprint gauge added. §5 still-open: ratify the 12, tier count, player report UI. §6 = the "Mars attacks Earth" first slice (goal slot → fill HoS stub → one military delegate → 2 gauges → watch it). |
| `docs/ai/AI-PERSONALITY-IMPLEMENTATION-SPEC.md` | **NEW 2026-07-10 (v1 IMPLEMENTATION SPEC).** The code-ready companion to the AI C&C design doc — *exactly what to code and where* for the 12-trait system, grounded in a five-agent audit of the real source (every file:line verified). Holds: the **acceptance audit** (§1 — per-trait NOW/LATER verdict table = which of the 12 are demonstrable now: **9 now, 3 sequenced behind espionage/commitment/exploration, 1 facet cut**); the `PersonalityDB` **data model** (host-agnostic blob, the all-0.5-neutral trap); the **scoring engine** (Feature dot-product, worked Klingon→warship/Ferengi→refinery proof); **drift** (mood-vs-trait, the `IdentityResistance` formula that solves the Klingon-loss puzzle, event→pressure table); the **blend** (`OverrideWeight` = tenure×experience, capped 0.45; the unset-tenure-fields gap to fix); the **`ModifiableValue` trait→number** pattern; the **demonstration methodology** (per-trait flip test + fingerprint test = the acceptance gate, since CI can't run the client); **reconciliations** (bombardment verified BUILT; Authoritarianism-vs-`GovernmentDB` split; no fleet-morale system); and the **build order**. | 📝 spec — data model BUILT, trait-wiring/drift/scoring pending | 🟡 | `PersonalityDB` BUILT + tested as a `Dictionary<PersonalityTrait,double>` at `Factions/PersonalityDB.cs` (scenario-attached, byte-identical at 0.5 neutral); `NPCDecisionProcessor.Tick` is a full engine, NOT a stub. Pending: §3 scoring, §4 drift, §5 blend, §7 demonstration — those files (`AIScoring.cs`/`PersonalityDrift.cs`/…) don't exist yet. |
| `docs/society/ESPIONAGE-AND-INTELLIGENCE-DESIGN.md` | Hidden-info engine: Information Ledger, agents, covert-action catalog, counter-intel (cluster D–H) | 🔒 | ➕ | **BUILT, DEFAULT-OFF (2026-07; runtime unverified — CI can't run the client).** The MVP gather/ledger spine landed: Information Ledger (`InformationLedger.cs`), covert-action catalog (`CovertActionCatalog.cs`), risk model (`CovertRisk.cs`), op resolver + tasking with grave-rung agent loss (`Espionage.cs`/`EspionageProcessor.cs`/`CovertOpDB.cs`), Intelligence-HQ component + agent recruitment (`IntelDirectorateAtb/DB/Processor.cs`), and the client `IntelligenceWindow.cs`. The two runtime gates default false (`NPCDecisionProcessor.EnableIntelLedger`/`EnableEspionageMirror`) so a default game runs no ledger updates and no spy mirror — the always-on mirror is aspirational at runtime. See the AS-BUILT banner at the doc top. **2026-07-07: seated in the leader pipeline** — agents = full leader-entities; Director→per-faction Spymaster→agents; counter-intel = same agent targeting self; `SignalQuality` CUT so passive feed is binary (gradient in the Ledger); MVP = Ledger+passive+seat+gather + **the NPC counterparty (detect+react, the mirror) as a co-requisite** (2nd vertical slice after Governor). **Agent delivery resolved = PHYSICAL** (troop-transport reuse; EMCON=stealth; "attach as infiltrator" the one new mechanic; delegate shortcut). Value is downstream of the NPC-delegate loop firing. **Covert-weapons/bioweapon slice added** (species research via abduction-or-distant-study → weapon designer → deliver by vessel/agent/proxy → colony-population/rebellion effect → WMD blowback + reputation; proxy = fund/incite rebellion; the intersection weapon) |
| `docs/combat/COMBAT-DESIGN.md` | Master space-combat design (11 systems) + fleet components/doctrine | 🔒 | ➕ | Auto-resolve spine + doctrine ✅; some systems partial |
| `docs/earthfall/CAMPAIGN-PLAN.md` (+ `findings/` A1-A6, R1-R4) | **NEW 2026-07-17 — OPERATION EARTHFALL: the parallel-lane fix/build campaign plan, AUTHORED NOT STARTED.** Born from the developer's real playthrough logs (main `fe72043`: freeze at 2050-04-22, the UMF abandoning its own invasion, the transport that never sailed, the "ghost" fleet, the idle Kithrin). Ten verified research ledgers (`findings/`: freeze=native blip render; ghost=honest 200Gm horizon+dishonest ring; flip=stale-morale echo→one-sample rebellion→180d Defend lock at the Survive floor; sealift=4× queue+fuel-less pad+uncharged hulls; ground campaign step-and-click gaps; faction development verdicts; player marine chain ~90% built; manager+rings anatomy; infra-combat semantics; 62 docs directives). The plan: 5 parallel BRANCH LANES with hard file fences (CORE=resolvers/legitimacy/umf.json · GROUND=GroundCombat+six-point JSON · CLIENT=all windows · DEV=Expand/stations/kithrin.json · TWOD=GroupPlane) + merge order + a campaign amendment to the dashboard rule (lane-notes files, settled at P8.2). Executable per-phase via the committed workflow `.claude/workflows/earthfall-campaign.js` (`Workflow({name:'earthfall-campaign', args:{phase:'P0'}})`) — fully self-contained for any fresh session/model. | 🔒 | ✅ | **EXECUTED + landed 2026-07-21 (all lanes merged CORE, CI-green each slice).** All five lanes shipped: P0/P3/P4/PW/P8 (CORE), G1-G4 (GROUND), C1-C5 (CLIENT), D1-D3 (DEV), T0-T3/S0-S2 (TWOD). Developer decisions answered in `EARTHFALL-DECISIONS.md`; the built invasion flow (both seats, step-by-step) is `docs/ground/EARTHFALL-CAMPAIGN-OPS.md`; what-shipped-per-lane is `docs/earthfall/CAMPAIGN-REPORT.md` |
| `docs/earthfall/CAMPAIGN-REPORT.md` | **NEW 2026-07-21 — Operation Earthfall final report.** What shipped per lane (CORE/GROUND/CLIENT/DEV/TWOD), the applied developer decisions (→ `EARTHFALL-DECISIONS.md`), the invasion on-switch gate-flip (`EnableGroundTacticalAI` + `AutoFormUp` ON for menu games, one-line revert), the deferred/tabled items (orbital-strike-at-a-hex; bombard re-fire cadence — both dev-tabled), and the `CLIENT-TEST-CHECKLIST` items awaiting the developer's local runtime pass. | 🟢 | — | The campaign close-out; pairs with the ops walkthrough |
| `docs/ground/EARTHFALL-CAMPAIGN-OPS.md` | **NEW 2026-07-21 — the developer's "each individual step and click" deliverable.** The complete invasion flow as BUILT, from BOTH seats: the AI's rung sequence (mass → sail → one-shot bombard → scored landing → form up → brain posture → advance → beachhead → per-hex infra → second wave → capture, with file anchors) and the PLAYER's actual windows/buttons for the same acts (design a unit → assemble → build → form battalion → stance/ROE → move → embark → land → raze/capture). The support loop (ammo/resupply/upkeep/fire support) + the honest LIMITS list, in plain English. Notes the developer's Space Marine is their OWN live-designed unit riding generic rails. | 🟢 📚 | ✅ | The as-built play-by-play; the honest ceiling for "you can take a planet" |
| `docs/earthfall/GROUND-TACTICAL-AI-DESIGN.md` | **NEW 2026-07-17 → BUILT 2026-07-20.** The ground tactical brain (Earthfall G2.2) — the answer to "is the AI smart enough to know when to be defensive vs offensive on the ground." The pure `GroundTactics.DecidePosture` decision model (odds via the fleet AI's own CombatRisk curve × personality × role × fortification/terrain × reserve × ammo, hysteresis + break-glass), the fog-honest `GroundThreat` read, and the `GroundTacticalBrain` wire behind `EnableGroundTacticalAI`. | 🔒 | ✅ | Design → engine BUILT (G2.2, `EfGroundTacticalBrainTests`); the six thresholds are FLAGGED for the developer (defaults stand) |
| `docs/earthfall/EARTHFALL-DECISIONS.md` | **NEW 2026-07-21 — the developer's answers to the campaign's queued decisions.** The record of the 5 build-shaping decisions (window name = Force Management; NO hard-coded Space Marine; player ships boot charged; bombard re-fire TABLED; the hex is the unit of everything) + the balance-dial defaults that stand (developer tunes from live play) + what's still deferred. | 🟢 | — | The authoritative decision log the PW/P8 run obeyed |
| `docs/earthfall/IMPLEMENTATION-AUDIT-2026-07-22.md` | **NEW 2026-07-22 — the plan-vs-code line-by-line audit of the whole Earthfall window (`92b51ae..a962475`, merged at `2cb6596`), the last verification before DEV-PC play-testing.** 26 audit units. Verdict: faithful to plan, **0 critical / 0 high / 4 medium / 15 low**. Crash-risk surface (six-point registration, save-safety, enum order, L9, merges) verified clean. The one pre-test-critical item: M1 — the P3 legitimacy fixes (`ReadCurrentMorale`/`EnableRebellionDebounce`) ship dormant (flags default-false, never flipped in `NewGameMenu`). | 🟢 | — | Pairs with the resolution plan; the findings drive it |
| `docs/earthfall/AUDIT-RESOLUTION-PLAN.md` | **NEW 2026-07-22 — the ordered fix plan for the audit findings.** 5 one-commit slices (M1 flag-activate [decision-gated] → M2 ground hysteresis binds → M3 wire the fog-honest landing score → low code batch → doc/hygiene batch) + M4 (beachhead hex-retrofit) tracked as a follow-on. Each slice cites file + acceptance gauge; CI-gated between slices. | 🟢 | 🔜 | The implementer's runbook; the Opus-4.8 prompt drives it |
| `docs/earthfall/LANE-*-NOTES.md` (5: CORE/GROUND/CLIENT/DEV/TWOD) | **NEW 2026-07-18→21 — the per-lane conflict-free notes files** (CAMPAIGN-PLAN §2.4): each lane's pending dashboard rows, cross-lane requests, developer decisions, and byte-identity claims, appended slice-by-slice so parallel branches never collided on the shared dashboards. **P8.2 swept every pending row into the real dashboards** (this file, TESTING-TRACKER, SYSTEM-CONNECTION-MAP, the subsystem CLAUDE.mds). | 🟢 🗄 | — | History/audit trail; the rows they carried are now LANDED in the live dashboards |
| `docs/combat/HOMEWORLD-SENSOR-HORIZON-MEMO.md` | **NEW 2026-07-18 (Earthfall C2.1) — the 200 Gm homeworld sensor-horizon design call.** Three options (KEEP + honest readouts [default, shipped] / signature-curve / EMCON-gate), each with its file:line dial and gameplay consequence. **No engine value changed** — a decision memo for the developer. | 🟢 | — | Decision-memo; C2.1 made the colony detection ring honest but changed no horizon value |
| `docs/combat/DETECTION-DESIGN.md` | Sensors/detection/fog-of-war/EMCON as a decision (dark-vs-loud) | 🔒 | ✅ | EMCON posture + fog + first-strike built. **2026-07-07: `SignalQuality` CUT** — detection = strength only (see-it/how-loud); retires the "detection-quality fix" prereq; re-point survey body-ID to survey-points | **⚠ CORRECTED 2026-07-28 (Ground Truth Phase C re-sweep) — its headline `SignalQuality` decision banner was a DECISION written as an accomplished FACT and was never executed in code.** The field is **live in 9 files** and still gates survey reveal at `> 0.20`/`> 0.80` (`SystemBodyInfoDB.cs:154-160`, `StarInfoDB.cs:130`), so "the field is deleted" read literally authorised breaking survey accuracy; and 2 of the 3 "bugs deleted by deletion" no longer argue for it (the byte-overflow was fixed 2026-06-28 and is CI-gauged). **The traced consequence is the big one:** this banner routed detection onto strength only, `AI-BRAIN-BUILD-TRACKER` F-B1 built the AI's eyes on strength, and `SignalStrength_kW` is a detection **MARGIN** not loudness (`SensorTools.cs:193,197` subtract the receiver floor) — which is why all 288 AI decisions in the real play log read "vs no threat." Correction banner added; plan slice **S1e** re-shaped from bug-fix to design decision.
| `docs/explore/EXPLORATION-CONTENT-DESIGN.md` | **NEW 2026-07-07 (concepts).** The field-site loop that gives exploration something to FIND and scientists a field career: survey→assign scientist(s)→yield→deplete/persist/blows-up, fed by a broad data-driven catalog (6 planetary + 5 space site types). **Three** scientist shapes (persistent study / one-shot expedition / **incident-response** — reactive, added 2026-07-11: a hazard/accident/outbreak degrades a held world on a clock, science resolves it); **multiple scientists per site with diminishing returns, no hard cap** (opportunity cost self-regulates); scarcity = hot spots. **Developer's bar (2026-07-11): content DENSITY is first-class — dense enough that a single home system fills 250 in-game years ("four seasons of Star Trek"); this IS science, not the reward for leaving home.** Space adds four specials: transient finds, system-scale prizes, **network-reshaping** (fixes flat topology / revives dead `CreateConnection`), hazards-as-treasure-map. Closes 3 surveyed gaps at once (nothing-to-find, scientists-don't-matter, undifferentiated planets). | 🔒(concepts) | ⚫ | Loop + catalog + scientist model locked; build order set (fix+wire ruins first). Open: DR curve, yield/rarity numbers, network-find interaction, event-chain depth. Notes the live ruins tautology bug |
| `docs/combat/INFORMATION-DELTA-DESIGN.md` | The gap between what the sim KNOWS and SHOWS; the readout backlog | 🔒 ♻️ | ➕ | Engagement/detection/Δv/ETA readouts built; more to wire |
| `docs/combat/CARRIER-DESIGN.md` | **NEW 2026-07-14, DECISIONS LOCKED same day.** Carriers as **standoff force projection** — design + Prime-Directive EXISTS/MISSING ledger. Key findings: NO new combat engine needed (launched strike craft fight as a **sub-fleet** in the one auto-resolve loop, cheap because it already buckets fire by class); strike craft are **real built entities** (just small ship designs — craft cradle-to-grave already exists); the **counter is already in the box** (flak/point-defense shred saturation-heavy evasive swarms). **Locked decisions (developer):** strike craft = real built ship-entities launched as a sub-fleet; carrier + wing doctrine SUPERSEDE the parent fleet; ONE Carrier/Standoff doctrine role; and — the keystone — **GENERALIZE the bay** (`GroundBayAtb → BayAtb` with a Bay-Class dial: troops/vehicles/strike-craft/passengers + a Cycle-Rate dial), so the flight deck = the troop bay = a passenger berth (CONVENTIONS §6, don't build parallel systems). Holds both cradle-to-grave chains, the generalized-bay component spec (Bay Class = troops/vehicles/strike-craft/passengers/**shuttles**), the pulse-loop combat operation, the connection map, the C1→C5 build sequence (C1 = generalize the bay byte-identically; carrier rides in C2–C4), and **§12 the full C1 blast radius** (8 engine files + 7 test files + 1 JSON FQN; client CLEAN; the `GroundCarryClass` chassis↔bay coupling; ordinal-stable enum; L3 save note). | 🔒 🔴 | **OFFICIAL — design-locked, ready to build; not yet started.** Blast radius mapped + verified tractable (byte-identical troop transport). Aligned with COMBAT-DESIGN (one loop + sub-fleet doctrine) + WEAPONS-DESIGN (triangle). Touches GroundCombat (the bay generalization). |
| `docs/economy/RESOURCES-AND-MATERIALS-DESIGN.md` | Full economy survey (minerals→materials→production→commerce→NPC econ) | 🔒 📚 | ➕ | Economy largely built; read before touching any econ part |
| `docs/archive/HAZARD-DISCOVERY-RESISTANCE-RESEARCH-DESIGN.md` | Hazard discovery → resistance research → armor loop (design) | 🗄 | ✅ | **SUPERSEDED (2026-07-13) — foundations built; banner added.** Kept for locked decisions; live status in `GameEngine/Hazards/CLAUDE.md`. To relocate to `docs/archive/` in the Wave-4 foldering |
| `docs/environment/STELLAR-ENVIRONMENTS-CATALOG.md` | Catalog of stellar/space environments | 📚 | — | Reference catalog |
| `docs/explore/SITE-ENGINE-DESIGN.md` | **NEW 2026-07-12 — the unifying mid-game SPINE.** One data-driven engine (located site → berth-seated leader works it → fought over → composable branches → yield) that every episode across every franchise is a *row* of; the Command Berth = the worker interface; combat/research/diplomacy/economy/crisis become its consumers. Grounded in a four-agent survey (most runtime exists). Parent of `docs/explore/EXPLORATION-CONTENT-DESIGN.md` + the Command-Berth half of `docs/society/GOVERNANCE-AND-DELEGATION-DESIGN.md`. | 🔒 | ➕ | **BUILT SE-1→SE-6 (2026-07-13, merged PR #81; runtime unverified — CI can't run the client), gated off by `AutoSpawnSites` default-false.** `GameEngine/Sites/` (FieldSiteDB / SiteMachine / CommandBerthDB / SiteWorkProcessor / SiteIncidentProcessor) + client `SiteWindow` + ~20 tests. Anomaly-first spine done; surface/incident/branch/yield slices landed |
| `docs/explore/EXPLORATION-CONTENT-DESIGN.md` | The field-site loop + the sci-fi episode catalog (six planetary + five space + incidents) — now a **catalog chapter of the Site Engine** | 🔒 | ➕ | X.1 ruins-tautology fix shipped (ruins generate); the field-site consumer is the Site Engine build |
| `docs/showcase/FRANCHISE-LITMUS-TEST.md` | **NEW 2026-07-21 — the franchise litmus reading.** Master verdict: can the Component Designer + Entity Assembler BUILD (and does the auto-resolver correctly FIGHT) iconic units from four battles? Reconciled from the four battle specs + adversarial verification. Verdict: ~89% of 66 units buildable today, ~92% with cheap JSON, ~8% engine-work; the top-3 shared fixes (ground-weapon-read / ground troop bay / ground command aura) + the one big engine job (weaponised-station combatant) + the air/altitude gap. | 🟢 ♻️ | ➕ | The authority where the battle drafts differ; drives the build backlog |
| `docs/showcase/LITMUS-{GEONOSIS,DS9,ORI-SUPERGATE,COLD-STEEL-RIDGE}.md` | Per-battle cradle-to-grave build specs (unit-by-unit chassis+components vs the real templates + EXISTS/MISSING/NEEDS ledger). SW Geonosis · Trek DS9 (Dominion War) · Stargate Ori Supergate · 40K Macragge/Cold Steel Ridge. | 🟡 ♻️ | ➕ | **SURVEY DRAFTS** — adversarially verified; load-bearing errata reconciled in the master doc, fixed at build time |

## 5. Aurora 4X design reference (static external spec — `docs/aurora/`)

| Doc | Purpose | Status |
|-----|---------|--------|
| `docs/aurora/INDEX.md` | Index + the "reference not spec; verify constants" caveat | 📚 |
| `GROUND-COMBAT.md` · `PLANETARY-INFRASTRUCTURE.md` · `SPACE-COMBAT-BENCHMARK.md` · `SHIP-DESIGN.md` · `MISSILES-AND-FIRE-CONTROL.md` · `SENSORS-AND-DETECTION.md` · `RESEARCH-AND-TECH.md` · `FLEETS-AND-SHIPYARDS.md` · `LOGISTICS.md` · `EXPLORATION-AND-SURVEY.md` · `COMMANDERS-AND-OFFICERS.md` · `COLONY-ENVIRONMENT-AND-POPULATION.md` · `DIPLOMACY-AND-INTEL.md` | Aurora's mechanics captured as the design spec for systems Pulsar lacks/deepens | 📚 (all) |

> These are **static reference**, not tracked for "build status" — they're the external spec we diff against. The *shape* is reliable; specific *constants* are approximate (verify before hard-coding).

## 6. Superseded / historical (kept for context, do not follow as live)

| Doc | Purpose | Status | Notes |
|-----|---------|--------|-------|
| `docs/archive/PLAN.md` | The original pre-rework development plan | 🗄 | **SUPERSEDED (2026-06-26) → `docs/MVP.md`** (already carries a banner) |
| `docs/archive/AURORA-GAP-ANALYSIS.md` | 2026-06-21 snapshot of Pulsar vs Aurora depth | 🗄 📚 | Historical; the live system map is `SYSTEMS-STATUS-AND-TEST-PLAN.md` |

---

## Known doc-debt (the honest to-do, so it isn't lost)

1. **`SYSTEMS-STATUS-AND-TEST-PLAN.md` (🟡)** — refresh the rows for morale/population, government, diplomacy, crew/manpower, stations to their built state (they landed after 2026-06-29). Its connection graph was extracted to `SYSTEM-CONNECTION-MAP.md` (2026-07-13); the doc is slated for retirement once its build-status content is absorbed here + into TESTING-TRACKER.
2. When **delegation** or **espionage** starts, flip their Build column here 🏗 and add their systems to the systems map.
3. **Docs-audit follow-ups (2026-07-13):** (a) physically folder `docs/` into subject subfolders (`combat/ ground/ economy/ society/ ai/ environment/ explore/`) per `DOCS-AUDIT-2026-07-13.md` §3a — the folder move happened, but **the `.cs` half of the link sweep was never done and rotted badly: 327 `Design: docs/…md` pointers across 249 code files led to 404s** (over half of the 48 distinct doc paths cited from code were dead — 13 moved by the reorg, 10 consolidated into a survivor, 2 superseded outright). ✅ **SWEPT + CLOSED 2026-07-27** (OPERATION GROUND TRUTH, `docs/DOCS-AUDIT-2026-07-27.md` §7): every pointer repointed to its live doc, verified by re-grep to zero, provably comment-only (327 in / 327 out, no non-comment line touched). **Standing gauge for the next session — this is the residual-grep the audit process asks for, run it after any doc move:** `grep -rhoE "docs/[A-Za-z0-9_/-]+\.md" Pulsar4X/ --include=*.cs | sort -u | while read p; do [ -f "$p" ] || echo "DEAD: $p"; done`; (b) trim the mega-cells in this file (COMPONENT-DESIGNER-DIALS, the AI-suite prose) to one-line blurbs; ~~(c) relocate the 3 bannered superseded docs (`PLAN`, `AURORA-GAP-ANALYSIS`, `HAZARD-DISCOVERY`) into `docs/archive/` when (a) runs.~~ ✅ **CLOSED — all three are in `docs/archive/`**, and `SYSTEMS-STATUS-AND-TEST-PLAN.md` joined them 2026-07-27 (23 reference sites across 17 `.md`/`.cs` files repointed; residual grep 0).

---

**As of 2026-07-13 (docs-vs-code audit executed — branch `claude/docs-codebase-audit-xnzwqn`):** ran the full audit in `DOCS-AUDIT-PROCESS.md`. W1 flipped ~60 stale "not built" claims to truth across 39 design + subsystem docs (three-state vocabulary; nothing claimed to "work" at runtime). W2 bannered the superseded snapshots. W3 extracted `SYSTEM-CONNECTION-MAP.md` and set the one-status-owner split. W4 consolidated 12→6 AI docs, 5→3 ground, 6→4 combat, + economy/designer folds (fold-verified, zero broken links) — `docs/` went 55→47. Remaining: the doc-debt items above (foldering + mega-cell trim).
