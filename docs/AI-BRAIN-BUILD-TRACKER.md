# AI Brain — Build Tracker (Planck → Brane)

> **Consolidated 2026-07-13 from: `docs/AI-BRAIN-BUILD-TRACKER.md` + `docs/AI-BRAIN-BUILD-TRACKER.md`.** The old wiring-map (the design→code bridge + its §4 socket-verification sweep) is now folded in below as the section **"Design→Code Wiring Map & Socket Verification."** This tracker is the ONE hub for both the *build state* (the Planck→Brane ladder, the slice tables) and the *design→code mapping* (which designed element lives at which file:line, and whether its socket exists). Where the two ever disagreed, the reconciliation is toward this tracker's current (Wave-1-corrected) state.

**What this is:** the single standing tracker for building the NPC "brain" — the faction-AI design suite (the nine `AI-*.md` + `EXPLORATION-CONTENT-DESIGN.md` docs) turned into a **verified, ordered, buildable plan**. It answers, for every rung of the ladder: *what does the design say to build · where does it plug into the real code (file:line) · is it possible today · what's the gauge · what's its status right now.* It is the doc-level twin of `docs/TESTING-TRACKER.md` (tests) and `docs/SYSTEMS-STATUS-AND-TEST-PLAN.md` (systems), pointed at one campaign.

> **As of 2026-07-13.** Built from a **six-agent feasibility survey** (one per doc-group) that verified every design claim against the *current* branch source (`claude/sol-playtest-earth-map-8r59j6`), not the `main` baseline the docs were written against. Every file:line below was checked in code. Companion design docs: the nine `docs/AI-*.md`. This tracker is the build authority; the design docs are the rationale. **Top-matter reconciled 2026-07-13 to match the per-slice detail tables below: the whole Planck→Brane ladder is now built and CI-green — the Organism engine, the Ecosystem (Phase 3), the Galaxy+Crisis (Phase 4), and the Brane acceptance test (Phase 5) — all engine-verified in CI. All the behaviour-changing wires ship default-OFF behind gates (`EnableOrderEmission`/`EnableDiplomaticProposals`/`EnableIntelLedger`/`EnableEspionageMirror`), so New Game is byte-identical until a client/test flips one; the ONE exception is the galaxy crisis (`EnableGalaxyCrisis` defaults true — live in a real game). "Built + CI-green" means the engine compiles and its gauges pass; it does NOT mean a live play-test has read it — that remains the developer's local-runtime job (only the Site Engine / Exploration rung has runtime tail work left).

---

## The headline — is it possible? YES, and mostly on rails already laid

**The brain is not a new nervous system — it is a mind plugged into one that exists.** The six surveys agree: the hard infrastructure the design needs is **already built and, on this branch, better than the docs assume.** What's left is **new *logic* (scoring / objective / transition engine) + a few new *data blobs* (personality, objective, threat-estimate) + wiring** — not a rebuild.

**The one load-bearing sequencing fact:** the upper rungs (Ecosystem, Galaxy, Brane) genuinely *compose* the lower engine — but they sit on the **Organism engine (needs-ladder + traits + mood + transition), which is design-only today.** So the build is **bottom-up**: the atomic scored-decision and the single-faction Organism come first; everything above is composition once they exist.

**The keystone gate — NOW OPEN (2026-07-11).** `NPCDecisionProcessor.Tick` (`Factions/NPCDecisionProcessor.cs`) fires monthly per-NPC and used to leave the axis→orders translation an empty `// TODO`. That stub is **filled (Movement II Phase 2.4b):** the Tick now reads the needs-ladder, selects a personality-shaped objective, commits it through the hysteresis engine, and stores a `StrategicObjectiveDB` on the NPC. **Both halves are now built + CI-green:** the DECISION loop AND the ACT — `EmitOrders` (`NPCDecisionProcessor.cs:176`) turns the stored objective into real orders through the six-resolver means-ends planner (Phase 2.4c → 2.8). It ships behind the default-off `EnableOrderEmission` gate (`NPCDecisionProcessor.cs:42`), so it's byte-identical until a client/test opts in. The tiny blobs it reads (`PersonalityDB`, `StrategicObjectiveDB`) are built.

**The chosen strategy — FOUNDATIONS FIRST (2026-07-11):** the six surveys sort the design into three tiers — buildable-now, net-new-but-unblocked, and blocked-behind-a-subsystem (espionage, trade-money, bloc-demands, gate-network, capability-tech). The design would let those last ones ship as inert "labeled sockets." **The developer chose to build the missing pillars FIRST**, so that when the brain goes in, *every* trait/role/rung wires into a live system — nothing on the finished brain is a dark socket. That splits the campaign into **Movement I (Foundations — level the ground)** and **Movement II (Build Up — the brain)**, both detailed below. Honest scope: Movement I is the bulk of the remaining work, because three of its tracks are full subsystems (each already has a design doc — it's *build*, not *design*).

---

## The ladder — Planck → Brane (the tracker's spine)

The design frames itself, unironically, as *"Planck-length → multiversal-branes"*: the atomic scored-decision at the bottom, an instantiable universe at the top. Build bottom-up.

| Rung | What lives here | Depends on | Status |
|---|---|---|---|
| **⚛ Planck** — the atomic scored decision | the one trait-weighted "score the options, pick one" helper every level reuses | — (foundation) | 🟢 REALIZED (in effect) — the atomic trait-weighted decision is live as `NeedsLadder` + `ObjectiveSelector` + the personality wires; a standalone `DecisionScorer` (Phase 0.2) was folded into these rather than built separately |
| **🧬 Organism** — one faction's mind | needs-ladder (what it wants) · 12-trait `PersonalityDB` · mood · the Tick decision body · transition engine · officer characters | ⚛ Planck + the gauges | 🟢 BUILT + CI-green (gated off) — `PersonalityDB` + the four first-trait wires (Phase 1) + the full decision engine (needs-ladder → objective → hysteresis → the filled Tick, 2.1–2.4b) + order emission via the six-resolver means-ends planner (2.4c–2.8) + Ambition cadence (2.5) + eyes→Risk (2.6) + officer-trait blend attached to a live decision (2.7 / 2.7-attach) all built byte-identically. Acting rides the default-off `EnableOrderEmission`. Runtime (a play-test) not yet read |
| **🌐 Ecosystem** — factions vs factions | rival-intel fog · scored stance-selection · NPC treaty policy · coalitions/betrayal emerge | 🧬 Organism + rival-intel | 🟢 BUILT + CI-green (gated off) — Phase 3 done: the persistent Information Ledger (3.1) + structural/rising-threat read (3.2) + NPC treaty policy calling `Treaties.Propose` (3.3) + coalitions form/crack (3.4). Ships behind default-off `EnableDiplomaticProposals`/`EnableIntelLedger` → byte-identical until opted in. Runtime not yet read |
| **🌌 Galaxy** — the arc + the crisis | early/mid/late emerges · late-game crisis = an existing faction's runaway ascension | 🌐 Ecosystem + capability-tech | 🟢 BUILT + LIVE — Phase 4 done: capability-tech (4.1, a real `tech-ascension` producer in the base mod) + the ascension seed → NPC coalition (4.2). `EnableGalaxyCrisis` defaults **true** (the crisis actually triggers in a real game); still byte-identical across the suite because nothing there researches the tech. Reachability + coalition gauged in CI; a full played run not yet read |
| **🪐 Supercluster / Brane** — authoring a universe | stage a franchise from JSON · the north-star acceptance test | all rungs + JSON schema | 🟢 BUILT + CI-green — Phase 5 done: personality + opening-diplomacy authored as JSON (5.1) + decision-log gauges (5.2) + the `NorthStarAcceptanceTests` acceptance test that composes the whole stack (5.3). Engine-verified in CI; a played franchise-staging run remains the developer's local-runtime check |
| **🔭 Exploration** *(cross-cutting)* | field sites to find · scientists with a career — feeds the Organism's objectives | independent; shares the competence prereq | 🟢 **SE-1 DONE (CI-green 2026-07-13)** — the scientist career (X.0) + ruins generate (X.1) + the **Site Engine anomaly spine (SE-1a/b/c)** are all CI-green (`docs/SITE-ENGINE-DESIGN.md`, architecture LOCKED). SE-1 = the whole engine on a space anomaly (record + state machine + factory + presence + accrue + resolve + research yield). **Next: SE-2 (Command Berth) → SE-3+ (surface/incident/crisis/content).** |

---

## The nervous system — ALREADY BUILT (what the brain plugs into)

Verified live on this branch. **Do not rebuild any of these — wire to them.**

| Substrate | Verified home (file:line) | Note |
|---|---|---|
| **The clock** — monthly per-NPC decision loop | `Factions/NPCDecisionProcessor.cs:17,19,42`; `GlobalManager` iterated `Engine/MasterTimePulse.cs:348`; liveness gauge `TickCount:29` | Fires live (proven by `FactionEconomyTests`). Body now runs drift + `UpdateStrategicObjective` (the filled decision loop, 2.4b) — the axis→orders TODO is gone; only the ACT (order emission, 2.4c) remains. Its header comment was corrected in the 2.4b pass (the old "not iterated" STALE note is retired). |
| **The hands** — headless order rail | `Engine/Orders/IOrderHandler.cs:10`; `StandAloneOrderHandler.cs`; ~15 sim-thread callers | A delegate issues the *same* orders a player does — no separate AI code path. |
| **The hands** — direct-call APIs | `Combat/FleetDoctrine.cs:47,34`; `Sensors/Emcon/FleetEmcon.cs:55`; `Factions/Diplomacy.cs:19`; `Factions/Treaties.cs:32,66,37`; `Combat/CombatEngagement.cs:485` (OrderAttack); `GroundCombat/GroundForcesDB.cs:388` | Doctrine/EMCON stay live mid-battle. Diplomacy/Treaties are static utils (bypass OrderHandler — no order record for Advise-mode; a real gap for later). |
| **The event bus** — pub/sub, ~250 kinds | `Engine/Events/EventTypes.cs:21-443`; `EventManager`; `Tech/ResearchProcessor.cs:28-33` reacts (not polls); every faction subscribes `Factions/FactionEventLog.cs:30-34` | The reactive spine: subscribe the brain the way ResearchProcessor already does. `CombatStarted` event not yet published (net-new). |
| **The delegate chassis** — durable officer-in-seat | `People/AdminSpaceAtb.cs:38`; `AdminSpaceProcessor.cs:47` `ReconcileSeats` (durable), `:106`/`:129` decapitation grave-rung; gauge `AdminSpaceSeatReconcileTests` | **Our branch — docs call this broken/"fix first"; it's already fixed.** Seats persist across recalc, survive save, empty on officer/component death. |
| **The competence→outcome wire** (proven TWICE) | research: `Tech/ResearchProcessor.cs:246` `RefreshPointModifiers` + `ResearcherDB.cs:16`; combat: `People/CommanderBonuses.cs:17,28` folded at `Combat/CombatEngagement.cs:1485-1501`, generator `NavalAcademyProcessor.cs:57`; gauges `CommanderCombatBonusTests` | **Our branch — the doc's roster #10 "STUB, no commander link" is STALE.** Flagship competence scales the fleet, academy→skill→fight end-to-end. |
| **The diplomacy substrate** | `Factions/DiplomacyDB.cs:21,48,60`; `RelationshipState.cs:35`; IFF reads it `Combat/CombatEngagement.cs:178…521,756`; first-contact from sensor loop `Sensors/SensorRecever/SensorScan.cs:120`→`Factions/FirstContact.cs:40`; `Treaties.cs`; `CasusBelli.cs`; `ReactiveDiplomacy.cs:58` | The tools the Ecosystem points outward — all real, all wired. Nothing calls `Treaties.Propose` yet (no NPC proposal policy). |
| **The planet-rung gauges** (needs-ladder inputs) | morale `Colonies/ColonyMoraleDB.cs:54`; legitimacy `Colonies/LegitimacyDB.cs:45`, collapse `:107`; rebellion `Colonies/RebellionDB.cs:37`; money `Factions/FactionInfoDB.cs:38`; own ship value `Combat/ShipCombatValueDB.cs`; war-standing `DiplomacyDB` | Every colony carries morale+legitimacy+rebellion, recomputed monthly — the **planet rung is pre-wired**. |
| **Scenario-as-JSON** (the authoring floor) | `Engine/Factories/DefaultStartFactory.cs:61` `LoadFromJson`; `Factions/FactionFactory.cs:57,71-81` (doctrine-vector parse); `Galaxy/StarSystemFactory` | A universe already loads from a JSON bundle: systems + jump-net + factions + designs. Real for geography/factions; aspirational for traits/ambition. |
| **The survey→reveal spine** (exploration) | `GeoSurveys/GeoSurveyProcessor.cs:25-83`; `Galaxy/PlanetRegionsDB.cs:142` `RevealRegion`; fires `EventType.GeoSurveyCompleted` | Richer than the doc knew — completion reveals regions + grants per-faction mineral access. The field-site loop hooks the same point. |
| **The PersonalityDB host** | `Factions/FactionInfoDB.cs:125` (Doctrine already lives here); `People/CommanderDB.cs` | The faction blob already seats identity data — the new blobs attach the same way. |

---

## The net-new to build (the brain itself)

None of these exist in source yet (grep-confirmed: they appear only under `docs/`). All have proven attach hosts or copyable patterns.

| Rung | Net-new piece | Kind | Host / pattern to copy |
|---|---|---|---|
| ⚛ | **`DecisionScorer`** — score options × identity weights, pick one | pure helper (no host) | CI-testable function; inputs = `PersonalityDB` + `DoctrineVector` |
| 🧬 | **`PersonalityDB`** — 12 traits (0–1) + mood dict + drift | blob | ✅ BUILT (M2-0a) — `Factions/PersonalityDB.cs`, 12 traits @ 0.5 neutral. Mood dict + drift = a later slice |
| 🧬 | **`StrategicObjectiveDB`** — the goal slot / mandate the Tick writes | blob | ✅ BUILT (2.1) — `Factions/StrategicObjectiveDB.cs`, written by the Tick (2.4b) |
| 🧬 | **Needs-tier field** (fractal: planet/system/empire) | field | ✅ REALIZED at the empire tier (2.2, `NeedsLadder` → `StrategicObjectiveDB.Tier`); the planet/system-seat fractal is a later slice |
| 🧬 | **Trait-weighted validity scorer + transition engine** (commit + hysteresis) | pure logic | ✅ BUILT (2.3, `ObjectiveTransition`) — commit + hysteresis; the objective scorer is `ObjectiveSelector` (2.4a) |
| 🧬 | **The Tick body** — read gauges → score objective → emit orders | logic | ✅ DECISION half built (2.4b, `UpdateStrategicObjective`); the emit-orders half is 2.4c |
| 🧬 | **Faction roll-up gauges** — sum colony morale/economy + fleet strength to empire tier | cheap helpers | ✅ BUILT (F-A3 + F-B1a) — `Factions/FactionRollup.cs` (Balance/Population/Morale/Legitimacy/MilitaryStrength) |
| 🧬 | **`ThreatEstimate`** — fog-limited enemy strength ("the eyes") | logic + slot | ✅ BUILT (F-B1b) — `ThreatAssessment.DetectedStrengthOf` (fog-limited) + `IntelAssessment` (Confirmed→true strength). Wiring it to the Risk trait is 2.6 |
| 🧬 | **Officer traits + tenure-weighted blend + shared drift** | fields + logic | trait fields on `CommanderDB`; tenure inputs already stored (`CommissionedOn`/`Experience`) |
| 🌐 | **Rival-intel fog store** — "what I think faction X's power/intent is" | blob | net-new on the sensor-contact model (`SensorContact.cs:42`, `FactionSystemInfoDB.cs:12`) |
| 🌐 | **Structural/rising-threat read** — fear rising power, not just attacks | logic | consumes rival-intel + `ThreatEstimate` |
| 🌐 | **NPC treaty/stance policy** — actually call `Treaties.Propose` | logic | the direct-call exists; the *policy* is new |
| 🌌 | **Capability-tech concept** — a tech that grants a capability, not a component | tech-model | today `Tech.Unlocks` moves item-IDs only — the crisis's single genuinely-new mechanic |
| 🌌 | **Ascension seed + crisis trigger** | data + logic | trigger enum exists (`EventTypes.cs:122 AnomalyDiscovered`) with no producer |
| 🪐 | **Author traits/ambition/mood/opening-diplomacy as JSON** | schema | opening-diplomacy exists in C# (`GameStageFactory.SetRelation:169`) — expose as data |
| 🪐 | **Decision-log gauges** (the emergence acceptance test) | readout | inert until the engine + logs exist |
| 🔭 | **`FieldSiteDB`** (type · shape {persistent/one-shot/incident} · yield · per-faction fog · assigned scientists) | blob | net-new; the one new data structure |
| 🔭 | **`FieldSiteProcessor`** (yield-over-time + one-shot resolve) | processor | reuses `ResearcherDB` funding dial + `AssignScientistOrder` verbatim |
| 🔭 | **Site catalog** (6 planetary + 5 space types) | JSON | mirror `CombatDoctrines`/`GroundStances` catalogs |

---

## Dead code — DO NOT build on (Landmine L1)

| Corpse | Verified (file:line) | Build on this instead |
|---|---|---|
| **`AdministratorDB`** — orphan stub, verbatim research-flavored copy | `People/AdministratorDB.cs:10-72`; `new AdministratorDB` = **zero** matches; zero `.cs` consumers | the live `ResearcherDB` + `CommanderBonuses` competence pattern |
| **`RuinsDB`** — never generates + nothing reads it | tautology bug `Galaxy/SystemBodyFactory.cs:1419-1420` (`!=A \|\| !=B` always true → early-return); only consumer is inside `GenerateRuins` itself | a new `FieldSiteDB` (reuse RuinsDB's size/quality enums if wanted) |
| **`ServeyAnomalyAction`** — throws | `Fleets/ServeyAnomalyAction.cs:14-32` `Execute`/`IsValidCommand`/`Clone` all `throw new NotImplementedException()` | the `FieldSiteProcessor` loop |

---

## The strategy — LEVEL THE GROUND FIRST, then build up (developer's call, 2026-07-11)

The design's "live-when-wired" pattern would let us ship the brain with several dials dark (espionage, trade, bloc-demands, gate-network, the crisis) as **labeled sockets** — present but inert until their subsystem lands. The developer chose the opposite, deliberately: **build the missing pillars FIRST so every trait, role, and rung wires into a LIVE system — nothing on the finished brain is a dark socket.** Then build up.

So the campaign is two movements:

- **Movement I — Foundations (level the ground).** Build the Tier-2/Tier-3 pieces that don't exist yet: the shared prerequisites, the "eyes," and the dark subsystems (espionage, trade-money, popular-demands, gate-network, capability-tech). Most already have a design doc — this is *build*, not *design*. **Honest scope: this is the bulk of the remaining work** — three of these (espionage, the trade economy, popular-demands) are each a subsystem in their own right.
- **Movement II — Build Up (the brain, on level ground).** The bottom-up brain phases below — but now every slice wires into a live pillar, no sockets left dark. Ends at the Brane acceptance test = *finish EVERYTHING.*

---

## Movement I — Foundations First (level the ground)

Ordered by dependency and cheapness. Each is a build track; the "Unblocks" column ties it to the Movement-II slice(s) it lights up.

> **✅ MOVEMENT I COMPLETE (2026-07-11), except two deliberate deferrals.** All foundations are CI-green: the eyes (F-B1a/b), competence-researchable (F-A2), the faction roll-ups (F-A3), trade-as-money (F-C1), popular-demands (F-C2), the espionage core (F-C3), and capability-tech (F-D2). **Deferred by design:** F-D1 (gate-network — a late-game tech, developer's call) and F-B1c (the persistent Information Ledger with time-decay — the per-facet blob exists; the live monthly decay wire is a later slice). The ground is level: every Movement-II slice below wires into a live pillar.

### Group A — cheap shared prerequisites (do first; small, each unblocks several things)
| # | Foundation | Verified gap / plug point | Design doc | Unblocks | Status |
|---|---|---|---|---|---|
| **F-A1** | ~~Fix the degenerate detection-quality~~ — **SATISFIED / SUPERSEDED (2026-07-11)** | the detection-quality bug was already FIXED (`Sensors/CLAUDE.md:160`, 2026-06-28); only a bounded multi-band quirk remains (a tidy standalone follow-up, not on the critical path). AND the AI merge **DESIGN-CUT `SignalQuality`** → detection is strength-only, so the eyes (F-B1) build on **signal STRENGTH + range**, not quality | `docs/DETECTION-DESIGN.md` | folded into F-B1 | ✅ superseded |
| **F-A2** | **Make competence researchable** — scientists ship with an EMPTY `BonusesDB`, so the live reader (`RefreshPointModifiers`) had nothing to fold; add the missing GENERATOR | built: `People/CommanderBonuses.cs` `RollResearchCompetence(cap, techCategory)` — the research twin of `RollCombatCompetence`, shaped for the reader (FilterId = tech category, Percentage); gauge `CommanderResearchBonusTests` (generator + real generate→read round-trip through `RefreshPointModifiers`, matching-category only; empty scientist unchanged → byte-identical). *Wiring it onto a live scientist path is a later slice (the deliberate rebalance) — this makes competence available.* | — | Organism 2.7 (officer traits), Exploration X.0 (field career) | 🟡 WIRED (CI-green `a0b3f17`) |
| **F-A3** | **Faction roll-up gauges** — sum colony population/morale/legitimacy + the ledger to the empire tier | built: `Factions/FactionRollup.cs` (`Balance`/`TotalPopulation`/`ColonyCount`/`MeanMorale`/`MeanLegitimacy`, population-weighted, read-only → byte-identical); gauge `FactionRollupTests`. *Military-strength roll-up deferred to F-B1 (needs cross-system ship enumeration).* | (this tracker) | Organism 2.2 (needs-ladder), F-B1 | 🟡 WIRED (CI-green `3855daf`) |

### Group B — the Eyes (rival perception; the linchpin, shared with espionage)
| # | Foundation | Verified gap / plug point | Design doc | Unblocks | Status |
|---|---|---|---|---|---|
| **F-B1** | **`ThreatEstimate` + the rival-intel "Information Ledger"** — own + fog-limited-enemy strength; a per-rival estimate that sharpens with detection and decays to Stale. The ONE true net-new *gauge*; it is BOTH the Ecosystem's missing input AND espionage's intel arm. **Built on signal STRENGTH + range** (F-A1's quality path was design-cut). | **F-B1a (own strength) built:** `Factions/FactionRollup.cs` `MilitaryStrength(faction)` sums own ships' `ShipCombatValueDB` Firepower+Toughness across systems; gauge `FactionRollupTests.MilitaryStrength_*` (excludes rivals). **F-B1b (fog-limited enemy estimate + per-rival store) pending** — contacts carry position/signal not combat value (`EntityManager.cs:638`, `SensorContact.cs:42`); no faction-level intel store yet | `docs/ESPIONAGE-AND-INTELLIGENCE-DESIGN.md` (Information Ledger), `docs/DETECTION-DESIGN.md` | Organism 2.6 (Risk), Ecosystem 3.1–3.4, Galaxy 4.2 | 🟡 F-B1a CI-green (`9ec430b`); F-B1b CI-green (`38fee3f`); F-B1c (persistent Ledger + decay) later |

### Group C — the dark economic / political pillars (each a subsystem; already designed)
| # | Foundation | Verified gap / plug point | Design doc | Unblocks | Status |
|---|---|---|---|---|---|
| **F-C1** | **Trade-as-money** — a Trade `TransactionCategory` + income from standing trade agreements, so a trade agreement earns money | **F-C1a built:** `Factions/Ledger.cs` gains the `Trade` category + `Factions/TradeIncome.cs` `MonthlyIncomeFor(faction)` (sums standing `TradeAgreement`s × a per-agreement value); pure read → byte-identical; gauge `TradeIncomeTests`. **F-C1b built:** `Factions/TradeIncomeProcessor.cs` — a monthly `IHotloopProcessor` keyed on `DiplomacyDB` (L9-clean, L4-safe) that books `MonthlyIncomeFor` into the ledger under `Trade`, gated behind default-off `EnablePayout` (→ byte-identical); gauge `TradeIncomeTests.PayoutProcessor_*` (on → ledger grows; off → no trade txn). | `docs/DIPLOMACY-DESIGN.md` (commitment/exchange), `docs/RESOURCES-AND-MATERIALS-DESIGN.md` | role #6 Trade Minister; commerce diplomacy; Altruism (Ledger gift) | ✅ F-C1a (`3e591e5`) + F-C1b (`b66ac85`) CI-green |
| **F-C2** | **Internal popular-demands / bloc layer** — the Stellaris-parties demand engine on the morale/legitimacy substrate; an unmet demand pressures legitimacy | building per `docs/GOVERNMENT-AND-POLITICS-DESIGN.md`. **F-C2a built:** `Factions/PoliticalBlocs.cs` — the 5 fixed blocs (Labor/Merchants/Militarists/Liberty/Order) + derived `Loudness(gov, bloc)` (the government dials bias which blocs organise); pure → byte-identical; gauge `PoliticalBlocsTests`. **F-C2b built:** `Factions/DemandEngine.cs` — `SurfaceDemands(moraleFactors, gov, atWar)` emits a `PoliticalDemand` per morale factor bad past a threshold + a war demand that flips on militarism; pressure = badness × bloc loudness; gauge `DemandEngineTests`. **F-C2c built:** `Factions/DemandResolution.cs` — the enact/refuse TEETH: `LegitimacyDelta(demand, response, gov)` (Enact → +gain; Refuse → a loss scaling with pressure × the regime's refusal hardness — a consent regime bleeds more than a command one); pure → byte-identical; gauge `DemandResolutionTests`. **Popular-demands core complete (blocs → demands → teeth→legitimacy); wiring it onto a live processor + the Interior-Minister delegate is the follow-on.** | `docs/GOVERNMENT-AND-POLITICS-DESIGN.md` (popular-demands) | role #3 Interior Minister; Authoritarianism (full) | ✅ **DONE** — F-C2a (`02d0369`) + F-C2b (`192777a`) + F-C2c (`a9b2d3e`) all CI-green |
| **F-C3** | **Espionage engine** — the Information Ledger + agents as taskable operatives (Spymaster delegate + covert-action catalog + the risk/reward detection bet) | building per `docs/ESPIONAGE-AND-INTELLIGENCE-DESIGN.md` (F-B1 eyes done; F-A1 superseded). **F-C3a built:** `Factions/InformationLedger.cs` — `InformationLedgerDB` per-rival per-facet (Disposition/Military/Economy/InternalPolitics/Secrets) intel with `Inferred → Confirmed → Stale` bands (`LevelOf`/`Confirm`/`DecayStale`, deep Clone); a new blob NOT yet attached → byte-identical; gauge `InformationLedgerTests`. **F-C3b built:** `Factions/IntelAssessment.cs` — `EstimatedMilitaryStrength(observer, rivalId, ledger)`: Confirmed Military intel → the rival's TRUE strength (`FactionRollup.MilitaryStrength`), else → the fog-limited `ThreatAssessment.DetectedStrengthOf`; pure → byte-identical; gauge `IntelAssessmentTests`. **F-C3c built:** `Factions/CovertActionCatalog.cs` — the data-driven covert-action menu (`CovertAction` gather/steal-tech/steal-funds/sabotage/sow-unrest/turn-or-assassinate/disinformation/counter-intel; each a `CovertActionDef` with its facet + baseline detection risk), mirroring `ExchangeCatalog`; gauge `CovertActionCatalogTests`. **F-C3d built:** `Factions/CovertRisk.cs` — the graduated risk model: `Resolve(baseRisk, agentSkill, targetCounterIntel, roll) → Clean/Traced/Caught` (deterministic, roll passed in) + `SuspicionAfter` (per-rival suspicion accrual); gauge `CovertRiskTests`. **Espionage CORE complete (Information Ledger + intel-resolve + covert-action catalog + risk/suspicion); the live agent-as-component + Spymaster delegate + the mirror are the follow-on (same shape as popular-demands' live-processor follow-on).** | `docs/ESPIONAGE-AND-INTELLIGENCE-DESIGN.md` | Guile (full facet); roles #4 Spymaster, #16 Agent; the mirror (NPCs spy on you) | ✅ F-C3a + F-C3b + F-C3c + F-C3d all CI-green (`2c4782d`) — espionage core complete. **✅ LIVE AGENT complete (E1–E6, 2026-07-12 — see Update 3): buildable directorate → recruited operative → taskable covert op (gather + steal-funds) → grave rung → client UI button → NPC mirror.** Deferred: steal-tech/sabotage/sow-unrest/disinformation effects, Spymaster delegate, casus-belli-for-them |

### Group D — the world / tech-model gaps
| # | Foundation | Verified gap / plug point | Design doc | Unblocks | Status |
|---|---|---|---|---|---|
| **F-D1** | **Jump-network rework / gate-network** — implement the dead `CreateConnection` stub so a discovered/built connection links two systems | `JumpPoints/JPFactory.cs:124` `CreateConnection` is an empty `// FIXME` stub | (the exploration + galaxy docs; hole H8) | Exploration network finds (gate/wormhole); hole H8 | ⏸ DEFERRED (developer's call 2026-07-11 — a late-game tech; lands with the exploration content it's tied to) |
| **F-D2** | **Capability-tech model** — a tech unlock KIND that grants a capability, not just moves an item-ID | built: `Factions/FactionDataStore.cs` gains a `Capabilities` set + `HasCapability(id)` + a `capability-` prefix branch in `Unlock` (a tech whose Unlocks lists a `capability-xxx` id grants the ability instead of no-opping); empty by default → byte-identical; gauge `FactionCapabilityTests`. | `docs/AI-EMERGENT-POLITICS-AND-CRISIS-DESIGN.md` | Galaxy 4.1–4.2 (the crisis ascension) | ✅ CI-green (`ce95e75`) |

**Foundation build order:** Group A (cheap, parallelizable) → F-B1 (the eyes) → Groups C & D (independent subsystems, any order; F-C3 after F-A1+F-B1). Each track is itself CI-gated and byte-identical-first, and each has a live gauge before it's called done. When Movement I is green, the ground is level: every Movement-II trait/role/rung has a real system under it.

---

## Movement II — Build Up (the brain, on level ground)

Same discipline that kept the whole campaign clean: **one slice at a time, a new field/blob defaults neutral so existing fixtures stay green, the payoff lands via a new example, push, WAIT for CI green before the next slice.** Each slice names its plug point and its gauge. **Assumes the Movement-I foundation each slice needs is green** (the "Unblocks" links above are the map).

> **Progress (2026-07-11):** Phase 0 ✅ · Phase 1 (four first-trait wires) ✅ **CI-green** (`cb1c4b5` / `41abdcb` / `a2a0a33` / `107523e`, all verified `success`) · Phase 2 the **DECISION loop** ✅ **CI-green** end-to-end (2.1 `9e5ebd8` → 2.2 `ced4309` → 2.3 `2b9241d` → 2.4a `2842532` → 2.4b the filled Tick `1095045`, all verified `success`). The **first ACT** is in and green: **2.4c** (CI-green `b2fb85b`) — a GrowEconomy objective queues an industry job behind the default-OFF `EnableOrderEmission` gate. **The reactive Organism brain is now COMPLETE:** the decision loop (2.1–2.4b) + the first emitter (2.4c) + all the trait/decision wires — 2.5 (parked as emit-adjacent), **2.6 eyes→Risk**, **2.7 officer character** — are in (2.6/2.7 pure, byte-identical). **The means-ends PLANNER (row 2.8) is now BUILDING, interleaved** (the survey's fit verdict: the economy planner is the honest version of 2.4c, so we make it correct rather than build 2.4d shallow): **P0-a `FactionState` snapshot ✅ committed**; P0-b (resolver skeleton + the `AutoAddSubJobs` fix + `EmitOrders` rewire) next on P0-a green. The reactive **Conquer** emitter is deliberately NOT built (premature — its reachability/router/fuel sub-subsystem is the deferred P-3). Phases 3–5 + Exploration remain.
>
> **Update (2026-07-12):** Since that note, **Phase 2.8 (means-ends planner) landed complete** (all six resolvers CI-green), **Phase 4 (Galaxy+Crisis) and Phase 5 (Brane acceptance test) are DONE**, and **Exploration's prereqs shipped** (X.0 scientist career + X.1 ruins, CI-green) with the field-site loop reframed as the **Site Engine** (`docs/SITE-ENGINE-DESIGN.md`). **The verified remaining tail (grep-confirmed against HEAD, not just the status marks):** (1) **Exploration** → build the Site Engine (~20 CI slices, playable after ~3; SE-5 also closes the deferred F-D1 gate-network); (2) **Phase 3.1 — the persistent rival-intel LEDGER read.** `InformationLedgerDB` exists (decay logic + `Confirm`/`DecayStale`) but is an **inert shell — never attached to a faction, never written by any processor, and there is NO rising-over-time/trend read** (the 3.1 deliverable). Confirmed incomplete. (3) **Phase 2.5 — Ambition CADENCE.** `Ambition` steers *which* objective (tier selection, 2.4a ✅) but **nothing scales the cadence/timing of expansion by it** — the 2.5 deliverable is absent. Confirmed incomplete. (4) **Phase 2.7-attach.** `OfficerCharacter.cs` (blend/drift math) is ✅ built but has **ZERO live consumers**, and `CommanderDB` carries **no officer-trait fields** — attaching traits + wiring one into a live decision is unbuilt. Confirmed incomplete. **Net: after the Site Engine + this ~3-item tail (3.1, 2.5, 2.7-attach), the Planck→Brane ladder is genuinely done** (the Brane acceptance test already passes).
>
> **Update 2 (2026-07-12, later — the tail CLOSED + a codebase audit):** the ~3-item brain tail is **DONE + CI-green**: **3.1** (`357649e` — the Information Ledger is live + a rising-over-time read), **2.5** (`14f4785` — Ambition scales the expansion commit cadence), **2.7-attach** (`5eb62f9`+`8f9ca92` — `CommanderDB.Personality` + the retreat-nerve officer blend). A four-agent audit + three parallel implementers also closed the **dark-socket** items surfaced along the way (all CI-green `9ab9a42`): **popular-demands** now has a live consumer (`4375093` — unmet demands erode legitimacy via `LegitimacyProcessor`, gated), and **`MilitaryReach`** (`9ab9a42`) is built — the last deferred P-3 military piece (the Conquer strike now gates on a real same-system/known-jump reachability + fuel read). **Two audit CORRECTIONS to stale claims:** (a) the **Conquer STRIKE is already built** (`MilitaryTarget`/`MilitaryComposition` + `ConquerResolver` sails a massed fleet, gated off) — earlier text calling it "deferred P-3" was under-claiming; only the multi-jump auto-router polish remains inside `MilitaryReach`'s documented v1 bound; (b) **the "level ground / every pillar live" claim was overstated** — the Movement-I pillars (espionage, trade, popular-demands) were built as pure logic; popular-demands + trade are now genuinely wired, but the **espionage covert-action pillar still has no live agent** (its logic — `CovertActionCatalog`/`CovertRisk` — has no taskable operative/processor). Also corrected: `TradeIncomeProcessor` was NOT a dark socket (it auto-discovers + fires via `DiplomacyDB`, gated by `EnablePayout`); it only lacked observability, now fixed with a `FireCount` gauge (`36c8afe`). **The genuine remaining tail is now just TWO items: (1) the espionage live agent/operative, and (2) the Site Engine (Exploration).** After those, the ladder is closed.
>
> **Update 3 (2026-07-12, later — the ESPIONAGE LIVE AGENT is DONE, tail item #1 closed).** The espionage covert-action pillar now has a full, player-reachable cradle-to-grave agent — built as six CI-gated slices (E1–E6), all green:
> - **E1 (`ed054aa`) — the gear/seat.** `IntelDirectorateDB` + `IntelDirectorateAtb` + the base-mod `intelligence-directorate` installation (buildable on Earth from turn one). Op capacity + counter-intel rating. Gauge `IntelDirectorateBaseModTests`.
> - **E2 (`6306ce3`) — the operative.** `CommanderTypes.Intelligence` + `BonusCategory.Espionage` tradecraft (`CommanderBonuses.RollEspionageCompetence`/`EspionageSkill01`) + `IntelDirectorateProcessor` recruiting agents up to capacity. Gauge `IntelOperativeTests`.
> - **E3 (`930a368`) — the op + grave rung.** `Espionage.TaskAgent` + `CovertOpDB` + `EspionageProcessor` (the live consumer that finally runs `CovertActionCatalog`/`CovertRisk`/the ledger): seeded-RNG detection roll → GatherIntel confirms the actor's intel, caught sours the target's relation + spikes `RelationshipState.Suspicion` + LOSES the agent. Gauge `CovertOpTests`.
> - **E4 (`c0f8fc8`) — the button.** The client `IntelligenceWindow` (toolbar) — agents, assign a covert op, and the intel ledger. The "access / assign / ready-up" UI.
> - **E5 (`1a4a006` + fix `e576e6b`) — the mirror.** `NPCDecisionProcessor.RunEspionageMirror` (gated `EnableEspionageMirror`): a hostile NPC spies back on the player, so counter-intel is a standing decision. Gauge `NPCEspionageMirrorTests`.
> - **E6 (`e384df6`) — broadened effects.** `EspionageProcessor.ApplyEffect` now a switch: GatherIntel + **StealFunds** (siphons `StealFundsFraction` of a rival's treasury via the `Ledger`, new `TransactionCategory.Espionage`). Gauge `CovertEffectsTests`.
>
> **Deferred as named next-slices (deliberate gaps, not silent ones):** the deeper covert effects — **steal-tech** (tech-store unlock chain), **sabotage** (Damage system), **sow-unrest** (a *persistent* legitimacy factor, or a direct drop is recomputed away), **disinformation** (plant a false record in the target's ledger); the **Spymaster delegate** (auto-run the portfolio); **passive ambassador intel**; and the **casus-belli-for-them** wiring (a caught spy hands the rival a justification for war — the highest-value connection into the war brain). **Net: tail item #1 (espionage live agent) is CLOSED. The only remaining AI-brain tail item is (2) the Site Engine (Exploration / SE-1).**

> **Update 4 (2026-07-13) — SE-1 (the Site Engine anomaly spine) is DONE + CI-green.** The last AI-brain tail item's FIRST engine is built across three byte-identical slices, all green: **SE-1a** (`658eb50` — `FieldSiteDB` the site record + `SiteMachine` the pure agency-preserving state machine: work banks Progress + Understanding, understanding UNLOCKS the resolve branch, never a timer), **SE-1b** (`cc1a380` — `FieldSiteFactory` drops a neutral point-in-space anomaly like a jump point + `SiteWorkProcessor`, a daily hotloop that banks work while a faction ship is parked within 1000 km, recording the working faction), **SE-1c** (`431a46c` — resolve + the YIELD ROUTER `SiteYields.DeliverResearch`: a resolved Science anomaly pays its banked Progress into the working faction's nearest-breakthrough tech, exactly once). Gauges: `FieldSiteTests`, `SiteWorkTests`, `SiteYieldTests` (15 tests). **Byte-identical** — nothing spawns an anomaly in the live New-Game path yet, so the game is unchanged until an exploration-discovery slice wires spawning in. **Next on the Site Engine: SE-2** (the Command Berth as a real researched/built component with a seated leader whose life is at risk — replacing SE-1b's "any ship parked here" placeholder worker), then SE-3+ (surface sites, incident shape, branches/crisis, content). See `GameEngine/Sites/CLAUDE.md` for the build-sequence table.

### Phase 0 — ⚛ Foundations (unblock everything)
| # | Slice | Plug point | Gauge | Status |
|---|---|---|---|---|
| 0.1 | `PersonalityDB` blob (12 traits, 0.5 default) + JSON parse | new blob on `FactionInfoDB`; mirror `FactionFactory.cs:71-81` | load/clone/parse round-trip test | ✅ **M2-0a** (CI-green `cb1c4b5`) — `Factions/PersonalityDB.cs` (the 12-trait blob, `TraitOf` defaults to `Neutral`=0.5, `SetTrait` clamps 0..1, deep Clone); a new blob NOT attached to any faction → byte-identical; gauge `PersonalityDBTests`. JSON-parse-onto-faction is the later authoring slice. |
| 0.2 | `DecisionScorer` pure helper (score × identity weights) | new pure class | scorer unit test (weights bias the pick) | ✅ FOLDED — no standalone class; the trait-weighted decision is realized directly in `NeedsLadder` (2.2) + `ObjectiveSelector` (2.4a) + the Phase-1 personality wires. Revisit only if a shared scorer is needed for Phase 3+. |
| 0.3 | Faction roll-up gauges (`FactionEconomySnapshot`, `FactionStrengthRollup`) | sum over `Colonies`/fleets | rollup matches hand-sum | ✅ BUILT as **F-A3 + F-B1a** — `Factions/FactionRollup.cs` (Balance/TotalPopulation/ColonyCount/MeanMorale/MeanLegitimacy/MilitaryStrength); gauge `FactionRollupTests`. |

### Phase 1 — ⚛→🧬 The first traits (proof-of-concept; no Tick body needed)
| # | Slice | Plug point | Gauge | Status |
|---|---|---|---|---|
| 1.1 | **Zealotry/Xenophobia → treaty acceptance** *(the cheapest wire — start here)* | `Factions/Treaties.cs:66` `WouldAccept` (+`RequiredScore:37`) | accept-good-treaty at trait 0 vs refuse at trait 1 (two `RelationshipState`s, no processor) | ✅ **M2-1a** (CI-green `cb1c4b5`) — `Factions/Treaties.cs` gains `RequiredScoreWith(t, decider)` (base `RequiredScore` + centered Xenophobia/Zealotry penalties) + 3-arg `WouldAccept` + optional `targetPersonality` on `Propose`; the 2-arg overloads delegate to null and the entity `Propose` reads the target's `PersonalityDB` → neutral/absent is byte-identical, a xenophobe refuses a deal a neutral signs. Gauge `PersonalityTreatyTests`. |
| 1.2 | Collectivism → retreat threshold | `Combat/CombatEngagement.cs:47` const + `:1401` `ShouldRetreat` | flees at 25% loss (0) vs fights to wipe (1) | ✅ **M2-1b** (CI-green `41abdcb`) — `CombatEngagement.RetreatThresholdFor(personality)` tilts the break-off casualty fraction by Collectivism (centered on neutral, clamped 0.05..0.95); `PersonalityOf(fleet)` resolves the owning faction's `PersonalityDB` (defensive like `AtPeace`), and `ShouldRetreat` reads it. Null/neutral → the flat `RetreatCasualtyThreshold` → byte-identical (no faction carries one yet; the combat fixtures are the tripwire). Gauge `PersonalityRetreatTests`. |
| 1.3 | Honor → keep-faith / renege | keep-faith live at `CombatEngagement.cs:1504/1529`; +new ~12-line `Diplomacy.BreakTreaty` | pact-intact (1) vs pact-broken (0) | ✅ **M2-1c** (CI-green `a2a0a33`) — `Treaties.WouldKeepFaith(breaker, payoff)` (Honor resists temptation: high keeps faith under a rich payoff, low reneges; null→neutral) + `Diplomacy.BreakTreaty(...)` (voids the pact flag on BOTH rows + craters the victim's trust by `BetrayalScorePenalty`, fires an event; no-op on unsigned/unmet). Nothing calls either autonomously → byte-identical. Gauge `PersonalityHonorTests`. |
| 1.4 | Authoritarianism → tax-under-unrest | `GovernmentDB.cs:68` `TaxCeiling` + `RebellionDB:37`/`LegitimacyDB:107` | high-Auth holds tax under unrest, low-Auth cuts to appease | ✅ **M2-1d** (CI-green `107523e`) — `TaxPolicy.TaxRateUnderUnrest(personality, taxCeiling, unrest)`: no unrest → the `GovernmentDB.TaxCeiling` for everyone (byte-identical); unrest pulls the rate down to appease, resisted by Authoritarianism (high holds/suppresses, low cuts, neutral halves). Pure decision helper — nothing fires it yet → live behaviour unchanged. Gauge `PersonalityTaxTests`. |

### Phase 2 — 🧬 The Organism engine (the keystone)
| # | Slice | Plug point | Gauge | Status |
|---|---|---|---|---|
| 2.1 | `StrategicObjectiveDB` goal slot | new blob on faction | write/read/clone | ✅ **M2-2.1** (CI-green `9e5ebd8`) — `Factions/StrategicObjectiveDB.cs`: the goal slot (`NeedTier` Survive→Stabilize→Thrive→Ambition + `StrategicObjective` None/Defend/Consolidate/GrowEconomy/AdvanceTech/Expand/Conquer + `TargetFactionId` + `CommittedUntil` for 2.3 hysteresis) with deep Clone. A new blob attached to nothing → byte-identical. Gauge `StrategicObjectiveTests`. |
| 2.2 | Needs-ladder read (Survive→Stabilize→Thrive→Ambition) from planet gauges + rollups | new logic over the built gauges | tier flips as morale/money/war-standing move | ✅ **M2-2.2** (CI-green `ced4309`) — `Factions/NeedsLadder.cs`: pure `AssessTier(atWar, own/enemy strength, morale, legitimacy, balance, inRebellion)` settles the lowest unmet tier (rebellion/collapse/losing-war → Survive; any war or internal trouble → Stabilize; dominant-secure-rich → Ambition; else Thrive), + an entity gatherer over `FactionRollup` + `DiplomacyDB` + `RebellionDB`. Read-only → byte-identical. Gauge `NeedsLadderTests` (tier boundaries + a fresh start faction reads Thrive). |
| 2.3 | Transition engine (commit + hysteresis) | new pure logic | no thrash under noise; re-plans on real change | ✅ **M2-2.3** (CI-green `2b9241d`) — `Factions/ObjectiveTransition.cs`: `ShouldReplan(currentTier, committedUntil, proposedTier, now)` (a more-urgent tier preempts; else re-plan only once the commitment expires) + `Advance(objective, …)` stamps the new plan + re-arms `CommittedUntil`. Anti-thrash: holds a committed objective under noise, drops it for an emergency. Pure logic; the only mutation is a `StrategicObjectiveDB` nothing attaches yet → byte-identical. Gauge `ObjectiveTransitionTests`. |
| 2.4 | **Fill `NPCDecisionProcessor.Tick`** — score objective → emit orders | `NPCDecisionProcessor.cs:79-83` | an NPC colonizes/builds/attacks per its objective | 🟡 **2.4a + 2.4b CI-green; 2.4c committed (first ACT); 2.4d+ next.** **M2-2.4a** (CI-green `2842532`) — `Factions/ObjectiveSelector.cs`: pure `SelectObjective(tier, doctrine, personality)`. Gauge `ObjectiveSelectorTests`. **M2-2.4b** (CI-green `1095045`) — `NPCDecisionProcessor.UpdateStrategicObjective`: the Tick assesses tier (`NeedsLadder`) → selects (`ObjectiveSelector`) → commits (`ObjectiveTransition`) → stores a `StrategicObjectiveDB`. The **decision loop is live** (the ~4-year-old Tick stub is filled). Gauge `NPCObjectiveTickTests`. **M2-2.4c** (CI-green `b2fb85b`) — the first ORDER emission: a new default-OFF `NPCDecisionProcessor.EnableOrderEmission` gate; when on, `EmitOrders` routes a **GrowEconomy** objective to `TryQueueEconomyJob` — queue an industry job on a free production line (the same lever a player pulls). Flag off → byte-identical (every existing test); other objectives no-op here. Gauge `NPCOrderEmissionTests` (default-off; GrowEconomy queues one job; routing by objective). **Known shallowness (fills at 2.8):** it queues a build BLIND to inputs — if the colony lacks the materials the job parks at `MissingResources` and nothing grows; it doesn't mine/refine/ship/survey to feed itself. That's the means-ends-planner gap (row 2.8). **2.4d+ (next):** the remaining objectives (Expand→survey/colonize, Conquer→build+attack, Defend/Consolidate). |
| 2.5 | Ambition/Aggression traits become live (ride the Tick) | the Tick body | expansion cadence tracks Ambition | 🟢 **DONE + CI-green (2026-07-12, `14f4785`).** Ambition already steered *which* objective (`ObjectiveSelector.AmbitionAim`, 2.4a); now it also drives the **CADENCE** — `ObjectiveTransition.CommitFor(objective, personality)` scales an expansion-family aim's (Expand/Conquer) commit dwell by Ambition (`1.0 + AmbitionCadenceSpread·(Neutral−ambition)`), so a high-Ambition faction re-commits to the push sooner; threaded through `NPCDecisionProcessor.UpdateStrategicObjective`. Byte-identical: at neutral Ambition the factor is exactly 1.0 → bit-for-bit `DefaultCommitFor`; non-expansion objectives never scaled. Gauge `AmbitionCadenceTests`. |
| 2.6 | `ThreatEstimate` (the eyes) → Risk trait | new logic over `GetSensorContacts` (`EntityManager.cs:638`) | engages at parity (Risk 1) vs demands 2× (Risk 0) | ✅ **M2-2.6** (CI-green `ed46aed`) — `Factions/CombatRisk.cs`: `RequiredStrengthRatio(risk)` scales the own-vs-enemy bar from 2× (Risk 0, overwhelming) to 1× (Risk 1, parity); `WouldEngage(own, enemy, risk)` decides, + an entity convenience that reads own strength (`FactionRollup.MilitaryStrength`) vs the fog-limited/intel-sharpened enemy estimate (`ThreatAssessment.DetectedStrengthOf` / `IntelAssessment`) and the faction's Risk trait. The eyes now decide willingness to fight. Pure/read-only, nothing calls it yet → byte-identical. Gauge `CombatRiskTests`. |
| 2.7 | Officer traits + tenure blend + drift | fields on `CommanderDB` | officer character shifts a decision; drifts with tenure | ✅ **M2-2.7** (CI-green `19238d1`) — `People/OfficerCharacter.cs`: the pure blend/drift MATH — `TenureWeight(experience, cap)` (green→0, seasoned→1) + `Blend(officer, faction, tenure)` (a green officer follows doctrine, a veteran runs on their own character) + `Drift(trait, target, rate)`. Pure/byte-identical; attaching officer traits to `CommanderDB` + wiring a blended trait into a live decision is the follow-on micro-slice (Phase-1 pattern). Gauge `OfficerCharacterTests`. **2.7-attach DONE + CI-green (2026-07-12, `5eb62f9`+`8f9ca92`):** `CommanderDB` now carries a `[JsonProperty] PersonalityDB Personality` (default all-neutral, deep-copied on clone → save-safe), and the fleet **retreat decision** (`CombatEngagement.ShouldRetreat`) reads the flagship officer's own Collectivism **blended** toward the faction's doctrine by tenure (`OfficerCharacter.Blend` over `TenureWeight`, via `FlagshipCommanderOf`). Byte-identical: a green/neutral/absent officer → the faction value exactly (and no faction carries a PersonalityDB today). Gauge `OfficerAttachTests`. **The blend math is now wired end-to-end.** |
| **2.8** | **The MEANS-ENDS PLANNER — "I know what I have · I know where I want to be · HOW DO I GET THERE?"** — **MAPPED (`docs/AI-DECISION-ENGINE-DESIGN.md`, 2026-07-11)**; a deliberative layer BETWEEN objective-selection (2.4a) and order-emission (2.4c) | slots into `NPCDecisionProcessor.EmitOrders`; per-objective backward-chaining resolvers on a shared state/feasibility substrate, riding the monthly-hysteresis loop (2.3) | an NPC whose colony lacks the INPUTS for its build first queues the mine/survey/logistics — the goal becomes REACHABLE, not just stated | 🟢 **BUILT & CI-GREEN — all six objective resolvers landed** (GrowEconomy/Consolidate/Defend/Expand/AdvanceTech/Conquer; see the "Planner build progress" note below for per-slice SHAs). **Scope:** BOUNDED (~13-18 slices, no new orders invented); the engine resolves the *buildable* prereq tree (`AutoAddSubJobs`), the planner owns below the **mineral floor** + plan-time feasibility. **FIT VERDICT: INTERLEAVE** — the economy planner is the honest continuation of the reactive work (make 2.4c *correct*, don't build 2.4d shallow); REPLACE each `EmitOrders` arm (don't wrap), keep the single `EnableOrderEmission` gate. **Build spec in `docs/AI-DECISION-ENGINE-DESIGN.md`:** 5 new classes (`FactionState`/`PlannerAction`/`IObjectiveResolver`+registry/`GrowEconomyResolver`/`FeasibilityOracle`), the `CanActuallyBuild` 6-check oracle (mirrors `ConstructStuff`), the 6-step mineral-floor bridge, the test plan (progress-not-completion + a pinning cross-check) + the **visibility readout** (a deliverable — failures are silent), and the slice order **P0-a→P1-e**. GrowEconomy 🟢 first (interleave now) · Expand 🟡 build-as-resolver (skip blind) · Conquer/Defend 🔴 last (reachability/router/fuel = the one deferrable sub-subsystem). |

> **⛏ The means-ends planner — the systemic missing link (developer's insight, 2026-07-11).** The Organism brain now PERCEIVES ("what I have" — the needs-ladder) and picks a GOAL ("where I want to be" — the objective), then fires a single primitive order (2.4c). The **missing middle is planning: "how do I get there?"** — walking a goal *backward* through its prerequisites until it reaches something doable this cycle. **It is systemic, not economy-only** — every objective has the same shape: *Conquer* needs a fleet-in-range needs warships needs materials needs minerals needs a survey **and** fuel+movement; *Expand* needs a colony ship needs a *found* habitable body needs a survey sent; *Defend* needs firepower *where the threat is* (a build chain **and** a move chain); *GrowEconomy* needs the mine→refine→build supply chain. Today the NPC would set a fine goal then flail, because it doesn't reason along the chain the game already enforces on the player. **Two things de-risk it:** (1) the engine ALREADY holds the dependency knowledge — `IndustryTools.AutoAddSubJobs` auto-queues a build's sub-jobs, and the cradle-to-grave law (`CLAUDE.md`) is the same chain written down — so the planner READS an existing graph, it doesn't invent one; (2) the monthly re-plan-with-**hysteresis** loop (2.3) is already the right substrate — the planner takes **one step toward the goal per cycle** (queue the mine now, the refinery once ore flows, the build after), i.e. least-commitment planning that fits the Tick. **Scope (MAPPED 2026-07-11 — `docs/AI-DECISION-ENGINE-DESIGN.md`, from a 4-agent read-only survey):** it is BOUNDED, not open-ended. The engine ALREADY resolves the *buildable* prerequisite tree recursively (`IndustryTools.AutoAddSubJobs`, arbitrary depth) and stops only at the **mineral floor** — so the planner delegates component→material→material for free and owns (a) below the mineral line (mine/survey/logistics), (b) plan-time feasibility (money/crew/tech/capacity — the engine checks these only at execution, SILENTLY), and (c) selection/scoring (which rival, which body, what to mine — none exist). It invents NO new orders (every rung has a player-equivalent lever) and rides the monthly-hysteresis loop (one step/cycle = least-commitment planning, no big up-front solve). **~13-18 slices, sequenced cheapest-first (P-0 shared substrate → P-1 GrowEconomy reconciliation → P-2 Expand → P-3 Conquer/Defend foundations).** The military routing (reachability read + multi-jump auto-router + fuel-readiness) is the one genuine sub-subsystem and can be funded or deferred like the gate-network was. Related law: the Prime Directive's cradle-to-grave (`CLAUDE.md`).
>
> **Planner build progress:** **P0-a ✅ CI-green `5d5581c`** — `FactionState` snapshot (the gather-once "what I have"; gauge `FactionStateTests`). **P0-b ✅ CI-green `2468d20`** — `PlannerAction`/`IObjectiveResolver`+registry/`FeasibilityOracle` (capacity+tech) / `GrowEconomyResolver` Rung C; `EmitOrders` rewired to snapshot→registry→resolve→execute (REPLACE the GrowEconomy arm — `TryQueueEconomyJob` superseded). **The build now routes through `AutoAddSubJobs` → the economy brain is material-aware** (the fix for 2.4c's blind-build shallowness). **P1-a ✅ CI-green `cd14522`** — `FactionState.StalledJobs()` + `MineralShortfalls()` (the mineral-floor detector) + the `MineralShortfall` struct. **P1-b ✅ CI-green `1530e04`** — `GrowEconomyResolver` Rung B (the MINE case): a stalled build starved for a mineral that's **present-but-unmined** on the home body → `Kind="QueueMine"` (build a Mine to feed it). The string→int mineral bridge (`CargoGoods.GetMineral(id).ID`), the deposit read (`PlanetMinerals.Minerals[mid].Accessibility`), and the capacity read (`Mining.ActualMiningRate[mid]`) are all wired; the mine design is found generically via `ComponentDesign.HasAttribute<MineResourcesAtbDB>()`. **The AI now reaches BELOW the mineral floor** — the deepest rung of "how do I get there." Byte-identical off; gauge `GrowEconomyResolverTests.GrowEconomy_StalledOnUnminedMineral_QueuesAMine`. **Visibility readout ✅ CI-green `d2efcf7`** — `Factions/PlanReadout.cs` (a `SocietyReadout`-style line: `obj GrowEconomy/Thrive | last: QueueMine — build a Mine…`) + `StrategicObjectiveDB.LastActionKind/Detail` recorded in `EmitOrders`, so a stuck NPC is never a silent black box (the Visibility Gate — a first-class deliverable since every planner failure is otherwise silent). Gauge `PlanReadoutTests`. **REORDER (2026-07-11, research-driven):** P1-c (logistics) + P1-d (survey) are **DEFERRED** — research found logistics is `LogiBaseDB`-gated (only a colony with a logi-base component has one; `SetLogisticsOrder` no-ops without it) and needs a multi-colony empire, and survey-for-economy bleeds into Expand — both are lower near-term value + higher complexity than the single-colony work. **P1-e ✅ CI-green `0444888`** — the `FeasibilityOracle` teeth: `CanQueue` now mirrors `ConstructStuff`'s execution gates at PLAN time — the crew gate (ship hulls only) + the infra-scaled capacity gate (a line that turns over <1 pt/tick would silently stall → refuse up front). NO money gate (not in `ConstructStuff` — mirror, not superset), so byte-identical for GrowEconomy's refining/installation builds; gauge `FeasibilityOracleTests` (passes the real start build, bites a zeroed-rate line). **✅ The single-colony GrowEconomy planner is COMPLETE** (snapshot → resolve → material-aware build via `AutoAddSubJobs` → mineral-floor sensor → queue-a-mine → plan-time feasibility → observable via `PlanReadout`). **✅ THE CRISIS RESOLVERS ARE IN — parking-lot gap G1 CLOSED (Consolidate CI-green, Defend committed).** The NPC used to act only in good times and FREEZE in a crisis; both crisis tiers now resolve.
> **CONSOLIDATE ✅ CI-green `54df2b3`** (Stabilize tier) — `Factions/ConsolidateResolver.cs` EASES TAX on its most-restless colony. Low `ColonyMoraleDB.Morale` (below the neutral 50) is the unrest signal; it sets `ColonyEconomyDB.TaxRate` down to `TaxPolicy.TaxRateUnderUnrest(personality, taxCeiling, unrest)` (the M2-1d helper — an authoritarian regime holds the line, a permissive one cuts hard), which feeds straight back into morale and the treasury. One internal lever, no military reach. `FactionState.ColonyState` gains `Morale`/`Economy` reads (additive). Gauge `ConsolidateResolverTests`. (Follow-on refinements: a QueueComfort fallback for the default-0.0-tax case; raise-morale / quell-`RebellionDB`.)
> **DEFEND ✅ CI-green `8c2f281`** (Survive tier) — `Factions/DefendResolver.cs` acts AT HOME: Rung A queues a WARSHIP at a colony with a free shipyard line (an armed `ShipDesign`, detected via `TryGetComponentsByAttribute<weapon-atb>`, oracle-gated on the existing ShipDesign crew/talent branch, `AutoAddSubJobs`-routed); Rung B postures an owned fleet to the `defensive-line` catalog doctrine (`FleetDoctrine.TrySetDoctrine`) when no warship can be built — the always-available fallback so Defend is never a no-op. `FactionState.OwnedFleets()` added (enumerate faction-owned fleets across systems). The "sail force to the border" half (reachability/router/fuel/target) is the **deferred P-3** military sub-subsystem — a deliberate deferral, not a gap. Byte-identical off; gauge `DefendResolverTests`.

**EXPAND ✅ CI-green `4ff3b08`** (Ambition tier, P-2 FOUND leg) — `Factions/ExpandResolver.cs` settles new worlds. It reads colonizability the SAME way the player's colonize UI does (`SystemWindow.cs`: a body carrying a `ColonizeableDB` whose geo-survey is complete) — cradle-to-grave correct, the AI and the player agree on what's settleable. Among the colonizeable, surveyed, UNCOLONIZED bodies in a system where the faction already has a colony (v1 near-home; `KnownSystems` widens the pool later), it picks the most habitable (lowest `SpeciesDB.ColonyCost`) and FOUNDs a colony there — an instant `CreateColonyOrder` (no colony ship). `FactionState` unchanged (the resolver enumerates the colony systems). When the best world still needs a survey it SAYS so (Visibility Gate) rather than founding blind — the survey→move sub-chain is the follow-on slice. Byte-identical off; gauge `ExpandResolverTests`. **Landmine found + dodged:** `SystemBodyInfoDB.SupportsPopulations` is NOT set on blueprint-loaded bodies (only the procedural/hardcoded gen paths set it), so the design's "filter on SupportsPopulations" would have found ZERO candidates — the AI uses the player's real marker (`ColonizeableDB`) instead.

**ADVANCETECH ✅ CI-green `00377e8`** (Thrive tier, tech-led) — `Factions/AdvanceTechResolver.cs` pushes research: it BUILDS research capacity — a design carrying `ResearchPointsAtbDB` (the base-mod `default-design-research-lab`; more labs → more research points), queued through the shared oracle-gated / `AutoAddSubJobs` machinery. v1 lever = queue a lab; assigning idle scientists / raising funding / steering the tech queue are finer levers a later slice can add. Byte-identical off; gauge `AdvanceTechResolverTests`.

**CONQUER ✅ CI-green `59f3456`** (Ambition tier, P-3) — `Factions/ConquerResolver.cs`, the SIXTH and last objective. v1 does the first rung: MASS a war fleet (`QueueWarship` on a free ship line — an armed `ShipDesign`, oracle-gated, `AutoAddSubJobs`-routed, mirroring Defend's Rung A; you can't conquer without a fleet). The rest of the chain — picking WHICH rival, checking REACH, fuelling the empty-spawned ships, composing the strike group, and the STRIKE — is the deferred P-3 military sub-subsystem (needs rival factions to exercise + 3 new helpers `MilitaryTarget`/`MilitaryComposition`/`MilitaryReach` + a multi-jump router). Also updated the stale `Registry_ResolvesGrowEconomyOnly` gauge (it asserted "no Conquer resolver yet" — now every concrete objective resolves, so it checks `None` never resolves instead). Byte-identical off; gauge `ConquerResolverTests`.

## 🧠 THE MEANS-ENDS PLANNER IS COMPLETE — all six strategic objectives resolve

GrowEconomy · Consolidate · Defend · Expand · AdvanceTech · Conquer each have a registered `IObjectiveResolver` that names ONE real, player-equivalent step toward the goal per monthly cycle, on the shared `FactionState` snapshot / `FeasibilityOracle` / `PlannerAction` substrate, surfaced through `PlanReadout` (the Visibility Gate). The NPC "knows what it has, knows where it wants to be, and knows the next move to get there." All CI-green, all byte-identical behind the default-off `EnableOrderEmission` gate (flip it on for a play-test). **Labelled deferrals (not gaps):** the P-3 military REACH (sail-a-fleet-to-the-border for Conquer/Defend), Expand's survey→move leg, and the Consolidate QueueComfort fallback. **Next (refinement, optional):** Consolidate QueueComfort (a comfort-infra fallback for the default-0.0-tax case, buildable `default-design-infrastructure`/`HousingAtbDB`), then the P-3 military reach when the developer wants NPCs that actually attack. **For playability, the switch is `EnableOrderEmission` — a live play-test (T0) is the next gauge, and only the developer's local Windows build can read it.**

### Phase 3 — 🌐 The Ecosystem
| # | Slice | Plug point | Gauge | Status |
|---|---|---|---|---|
| 3.1 | Rival-intel fog store + the **rising-over-time** read | new blob on the sensor model | intel decays; a riser is seen rising; ignorance is a real gauge | 🟢 **DONE + CI-green (2026-07-12, `357649e`).** The `InformationLedgerDB` shell is now LIVE: `FactionFactory` attaches it to every faction (like `DiplomacyDB`), and each monthly `NPCDecisionProcessor` Tick (behind a default-off `EnableIntelLedger` gate) Confirms+samples the rivals a faction currently `DetectedStrengthOf` and decays the rest to Stale. Added the trend fields + `RecordSample` + a pure `IsRising` (record/ledger/`ThreatAssessment.IsRising` — the "who is GROWING" read). Byte-identical: the ledger is empty+inert until the gate flips; all new intel APIs additive; no `Confirm` signature changed. Gauge `InformationLedgerLiveTests`. |
| 3.2 | Structural/rising-threat read | logic over 3.1 + `ThreatEstimate` | a riser is feared before it attacks | 🟡 **CI-green `6c2ae76`** — `ThreatAssessment.GreatestThreatTo(observer)` names the strongest DETECTED rival that out-muscles the observer (fog-limited via `DetectedStrengthOf`, own via `FactionRollup.MilitaryStrength`); ranking split into a pure `PickGreatestThreat(dict, own)` so it's testable without a sensor scenario. Pure read → byte-identical (nothing consumes it yet; the 3.3 "propose a DefensivePact against a shared threat" sharpening is next). **v1 is CURRENT-state** ("strongest now"); the RISING-over-time read rides 3.1's persistent Information Ledger (F-B1c). Gauge added to `ThreatAssessmentTests` (pure ranking + a wired huge-rival end-to-end). |
| 3.3 | NPC treaty/stance policy (call `Treaties.Propose`) | `Treaties.cs:32` | an NPC proposes a pact against a shared threat | 🟡 **CI-green `760c7ad`** — `NPCDecisionProcessor.RunTreatyPolicy` proposes a **NonAggression** pact to the first met, not-at-war rival whose standing clears the bar (−25), wired into the monthly `Tick` behind the new default-off `EnableDiplomaticProposals` gate (sibling of `EnableOrderEmission`, so diplomacy/order tests don't couple). Turns the built-but-uncalled `Treaties.Propose` into live behaviour; skip-if-already-signed guards re-warm churn. auto-resolves via `WouldAccept` (a "player answers the offer" inbox is later). Byte-identical off; gauge `NPCTreatyPolicyTests`. **+ SHARED-THREAT sharpening CI-green `d721cc2` — wires 3.2 in:** `RunTreatyPolicy` now runs two passes — Pass 1, when `ThreatAssessment.GreatestThreatTo` names a rival it fears, seeks a **DefensivePact** with a TRUSTED (Allied-75) neighbour who isn't that threat (ally against a common enemy — the diplomatic seed 3.4's coalitions grow from); Pass 2 is the plain NonAggression détente. Byte-identical off (no threat / no Allied-75 partner → Pass 1 no-ops → identical to the 3.3-only behaviour). Gauge `RunTreatyPolicy_WithASharedThreat_ProposesADefensivePactToATrustedAlly`. |
| 3.4 | Coalitions/betrayal emerge (Honor×Guile) | composition of 3.1–3.3 | alliance forms vs a riser, cracks when it dies | 🟢 **CI-green `3625db6`** — built in three composed slices. **3.4a `b75677a`** (combat teeth): `CombatEngagement.AreHostile` now checks a declared-war latch (`IsAtWar`, mirroring `AtPeace`) BEFORE the peace-suppression — a declared war overrides any Friendly/Allied stance or signed pact, so allies who go to war CAN shoot each other (the developer's rule) and a broken pact instantly re-arms. Byte-identical (no fixture had a war+pact combo). Gauge `DiplomacyIffTests.DeclaredWar_OverridesASignedPact`. **3.4b `d782cf4`** (the coalition forms): `RunTreatyPolicy` Pass 0 — if a DefensivePact ally is AtWar with X, we `Diplomacy.DeclareWar` on X (casus belli AllyDefense); two pacts vs a shared enemy become one coalition whose guns fire (via 3.4a). Gauge `RunTreatyPolicy_JoinsAPactAllysWar_AgainstTheirEnemy`. **3.4c** (the crack): Pass C — once the shared threat is gone (`GreatestThreatTo == -1`), a LOW-Honour faction breaks the pact (`Diplomacy.BreakTreaty` gated by `Treaties.WouldKeepFaith` vs `PactAbandonTemptation` 0.5); high-Honour keeps faith; breaking re-arms the former allies (3.4a). Gauge `RunTreatyPolicy_WhenThreatIsGone_LowHonourCracksThePact_HighHonourKeepsFaith`. All behind the default-off `EnableDiplomaticProposals` gate → byte-identical off. |

### Phase 4 — 🌌 The Galaxy + Crisis
| # | Slice | Plug point | Gauge | Status |
|---|---|---|---|---|
| 4.1 | Capability-tech concept (tech grants a capability, not a component) | extend `Tech.Unlocks` model | a tech flips a faction capability flag | 🟢 **DONE + REACHABLE** — the mechanism (F-D2: `FactionDataStore` routes any `capability-` unlock id to `Capabilities`/`HasCapability`) now has a REAL base-mod producer: **`tech-ascension` ("Transcendence")** in `techs.json`, `tech-category-stellar-science`, whose `Unlocks` grants `capability-ascension`. Reachable in a played game — gated behind the endgame Alcubierre warp branch (`tech-alcubierre-collapse-efficiency` unlocks it; cost/placement a first cut). So a faction can actually research its way to ascension, not just have it forced in a test. Gauges `FactionCapabilityTests` + `GalaxyCrisisReachabilityTests` (research the real tech → capability) + `BaseModIntegrityTests` (the tech loads). |
| 4.2 | Ascension seed + crisis trigger → coalition | producer for `EventTypes.cs:122`; reuse 3.x | one faction ascends → galaxy coalitions vs it | 🟢 **DONE + LIVE** — **4.2a** `GalaxyCrisis.Ascendant(game)`/`IsCrisisActive` name the faction holding the ascension capability. **4.2b** `GalaxyCrisis.FormCoalitionAgainstAscendant(game, when)` — the COALITION: every other NPC declares war on the ascendant (reuse Phase-3.4 `Diplomacy.DeclareWar`), fires a one-time `EventType.GalacticCrisis`, idempotent. **The player keeps agency** (only NPCs auto-join; if the PLAYER ascends, every NPC turns on them). **Phase-4 FINISH (2026-07-12):** `EnableGalaxyCrisis` now defaults **true** (the crisis actually triggers in a real game), still byte-identical across the suite (nothing there researches `tech-ascension`, so no faction holds the capability → the coalition call returns 0). Gauges `GalaxyCrisisTests` (NPCs unite; player not dragged in; idempotent) + `GalaxyCrisisReachabilityTests` (the real research→ascend→coalition chain). **Phase 4 is complete: wired, reachable, live, and gauged.** |

### Phase 5 — 🪐 The Brane (authoring / acceptance test)
| # | Slice | Plug point | Gauge | Status |
|---|---|---|---|---|
| 5.1 | Author traits/ambition/mood/opening-diplomacy as JSON | extend `FactionFactory` schema; expose `GameStageFactory.SetRelation:169` as data | a scenario JSON sets a faction's personality + opening war | 🟡 **5.1a BUILT, CI-verifying** — **personality authoring**: `FactionFactory.LoadFromJson` now reads a `"personality"` JSON node (trait-name → 0..1, case-insensitive; `FactionFactory.PersonalityFromJson`) and attaches a `PersonalityDB` — so a scenario hands a faction its 12-trait identity (aggression/honour/xenophobia/…) that the whole brain already reads. Byte-identical: no node → no blob → every trait falls back to Neutral (0.5) exactly as today; no existing scenario file has the node. Gauge `PersonalityAuthoringTests`. **5.1b BUILT (2026-07-12):** opening diplomacy — `FactionFactory.ApplyOpeningRelations(game, faction, node, when)` reads an `"openingRelations"` JSON array (target by name/abbreviation → score + `atWar`), resolves targets against `game.Factions`, and applies via the real `DiplomacyDB`/`Diplomacy.DeclareWar`. Wired as a SECOND PASS in `DefaultStartFactory` (after every faction loads, so cross-faction names resolve). Byte-identical (no `openingRelations` node → no-op; unknown target skipped, never throws). Gauge `OpeningDiplomacyTests`. **5.1 (author personality + opening war as JSON) is DONE.** Next: 5.2 decision-log gauges. |
| 5.2 | Decision-log gauges (emergence checkable) | readout over the engine | a run's decisions are legible + trace to inputs | 🟢 **BUILT** — `ObjectiveSelector.SelectWithReason` returns the objective AND a legible REASON tracing it to the driving input (the tier + the winning doctrine axis / personality trait, e.g. "Ambition tier: Aggression 0.90 > neutral → Conquer"). `NPCDecisionProcessor.UpdateStrategicObjective` captures it onto `StrategicObjectiveDB.DecisionReason` (noting hysteresis when the brain HELD a prior objective), and `PlanReadout.Faction` surfaces it (`obj X/Tier \| why: … \| last: …`). So an authored personality's fingerprint is CHECKABLE in what its brain decides. Byte-identical: `SelectObjective` still returns the same objective (discards the reason), guarded by `ObjectiveSelectorTests`. Gauge `DecisionLogTests`. |
| 5.3 | **Acceptance test** — stage one aspect of a franchise, it plays believably | the whole stack | the north-star test (`docs/NORTH-STAR-VISION.md`) | 🟢 **BUILT** — `NorthStarAcceptanceTests` stages a galaxy and composes the WHOLE stack: (1) **authored powers behave like themselves** — three NPCs with distinct doctrines settle DISTINCT character-appropriate objectives (Economic→GrowEconomy, Tech→AdvanceTech, Expansion→Expand) through the real `NPCDecisionProcessor`, and each `DecisionReason` **traces to its authored input** (5.2), while the **player settles nothing** (agency); (2) an **authored opening war** (5.1b) persists, and when a rival **researches `tech-ascension` and ASCENDS** the galaxy unites against it through the **LIVE decision tick** (Phase-4 crisis, `EnableGalaxyCrisis` on) — old enemies join the coalition — **while the player keeps agency** (not auto-committed). Distinct powers behaving legibly in a galaxy that reacts = the vision's acceptance bar. Byte-identical (a new test only). **Phase 5 (🪐 The Brane) is COMPLETE.** |

### 🔭 Cross-cutting — Exploration (independent; can run in parallel)
| # | Slice | Plug point | Gauge | Status |
|---|---|---|---|---|
| X.0 | **Scientist competence researchable + a field CAREER** (was: scientist ships with empty `BonusesDB`) | `People/CommanderBonuses.cs` + academy | a trained scientist scales research/field yield | 🟢 **DONE + CI-green (2026-07-12)** — three slices: **X.0-1** `ResearchAcademyDB`/`ResearchAcademyProcessor` (graduate scientists whose competence scales with school quality, the scientist twin of `NavalAcademyProcessor`, `0ebefb0`); **X.0-2** the base-mod buildable Research Academy (`ResearchAcademyAtb` + JSON, six-point reg, save/load-safe, `fea7ba7`+`28bf2ec`); **X.0-3** experience-over-time (`ResearchProcessor.GrowScientistExperience` — a scientist improves the longer they work a lab, `70c87aa`). The F-A2 generator (`RollResearchCompetence`) is now reachable through a real career. Gauges `ResearchAcademyTests`/`ResearchAcademyBaseModTests`/`ResearchExperienceTests`. |
| X.1 | Fix the ruins tautology (so something spawns to find) | `Galaxy/SystemBodyFactory.cs:1419` | ruins generate on qualifying bodies | 🟢 **DONE + CI-green (2026-07-12, `957bbc5`+`72c7a88`)** — two latent bugs fixed: the always-true tautology (`!=A \|\| !=B`, now the pure tested `CanBodyHaveRuins`) AND a `WeightedList.Select` int-vs-fraction overshoot the tautology had hidden. Atmosphere gate dropped (Mars/Luna qualify — ruins need ground not air). Rolled from a **dedicated per-body RNG** so the shared stream + `SystemGenTests` golden master stay exact. Ruins now generate; **still latent (no consumer)** — the consumer is the Site Engine. Gauge `RuinsGenerationTests` + the universe-gen smoke tests. |
| X.2 / X.3 | `FieldSiteDB` + `FieldSiteProcessor` + the site catalog | — | assign a worker → site yields/depletes; the loop runs from data | 🔁 **SUBSUMED by `docs/SITE-ENGINE-DESIGN.md` (2026-07-12).** Three player walkthroughs generalized the field-site loop into ONE **Site Engine** (a located site → a berth-seated leader works it → fought over at that location → composable branches → yield). X.2 = SE-1/SE-3 (`FieldSiteDB` + the site state machine + the Command-Berth worker); X.3 = SE-6 content (catalog rows). Build there, not here. |

> **X.0 was shared with the Organism:** the same "empty `BonusesDB` → make competence earnable" fix serves both the field-scientist career (done, above) and the officer-competence blend (2.7-attach — still open). Built once (F-A2 + X.0).
> **The Exploration rung is now the Site Engine.** `docs/SITE-ENGINE-DESIGN.md` is the parent; it completes 🔭 Exploration and overshoots it (Command Berth = the delegate worker; located combat; the crisis-rupture edge). Anomaly-first build: SE-1 (the engine on a space anomaly) → SE-2 (Command Berth) → SE-3 (surface + ground combat) → SE-4 (incident) → SE-5 (branches/crisis, which also builds the deferred F-D1 gate-network) → SE-6 (content). ~20 CI slices to the full engine, playable after ~3.

---

## Feasibility scoreboard (from the six surveys)

- **Traits (12):** 5 wired live (Phase 1: Xenophobia/Zealotry→treaty, Collectivism→retreat, Honor→renege, Authoritarianism→tax; + Aggression flips Conquer at the Ambition tier in 2.4a) · the rest ride the now-built decision loop (Ambition cadence 2.5, Risk 2.6, officer traits 2.7) or a down-payment already made (Guile→EMCON, Altruism→Ledger gift, Curiosity→exploration).
- **19-role delegate roster:** ~3 ride a built consumer today · 1 wired-but-unfed slot (Governor) · ~15 net-new seats — but their dark pillars are now BUILT (espionage core, popular-demands, trade-money all CI-green in Movement I), so the seats are no longer hard-blocked.
- **Wiring-map's 3 "already there" claims:** clock ✅ · event bus ✅ · order rail ✅ — all VERIFIED. Plus two branch-diff bonuses the docs miss: durable seats + commander combat-competence are already built.
- **The recurring gap across rungs — NOW CLOSED:** the **"eyes"** (a fog-limited estimate of a rival's strength) was the one true net-new gauge gating Risk, structural-threat, coalitions, and the crisis. Built in Movement I (F-B1: `FactionRollup.MilitaryStrength` own-strength + `ThreatAssessment.DetectedStrengthOf` fog-limited + `IntelAssessment` confirmed-intel sharpening). 2.6 wires it to the Risk trait.

---

## 🕳 Known gaps / parking lot — the NEXT holes (named 2026-07-11, the developer's "any more holes?" pass)

Beyond the means-ends planner (2.8), these systemic gaps are named NOW so they get built or deferred **deliberately**, not rediscovered by surprise (the same discipline that turned "how do I get there?" into row 2.8). Tagged **NEW** (a genuine gap) vs **DELIBERATE** (a v1 simplification, listed so it stays a conscious choice). Ranked by how much each makes the AI feel dumb.

| # | Gap | Why it matters | Tag / likely shape |
|---|---|---|---|
| **G1** | **Crisis paralysis** — ✅ CLOSED: **both crisis resolvers built** — `ConsolidateResolver` (Stabilize, eases tax, CI-green `54df2b3`) + `DefendResolver` (Survive, builds a warship / postures a fleet AT HOME, committed) | the NPC no longer freezes in a crisis — it eases unrest when restless and builds/postures defense when threatened | ✅ **DONE** (v1) — the "sail force to the border" half of Defend rides the deferred P-3 military reach (a labelled deferral, tracked below), but the freeze is gone |
| **G2** | **No memory / no grudges** — every cycle re-decides from *current* state; no experiential history | can't learn from a crushed invasion, hold a betrayal grudge, or adapt — the biggest **character** gap (the intel ledger is about rivals, not the NPC's own past) | **NEW** — a `FactionMemoryDB` (recent outcomes + per-rival grudge/trust drift) |
| **G3** | **One objective at a time** — needs-ladder → one tier → one thread, one step/cycle | a real empire grows + researches + defends + expands in parallel; the structural form of the "plodding" risk | **PARTLY DELIBERATE** (least-commitment = anti-thrash) — revisit once resolvers exist: per-colony / per-fleet sub-objectives |
| **G4** | **Picks "first valid," not "best"** — no valuing of options | "does something legal" vs "does the *smart* thing" — no ranking of which world to settle / which rival to hit / what to build | **NEW** — an opportunity-scoring layer over the resolvers (Expand's habitability scorer is the first instance) |
| **G5** | **Diplomacy is weather, not a weapon** — reactive drift only, never instrumental | the NPC never allies-to-survive or bribes-to-delay-a-war; diplomacy should be a planner **lever**, not just ambient drift | **NEW** — a diplomacy resolver driving `Treaties.Propose` / the `ExchangeCatalog` commitment model |
| **G6** | **AI can't tell the PLAYER what it's doing** — the plan/visibility readout is dev-facing only | invisible intent reads as random even when clever; the Visibility Gate pointed at the **player**, not the developer (this codebase's recurring theme) | **NEW** — an in-game intent/reaction event feed ("The Directorate masses a fleet on your border") |
| **G7** | **No long-horizon investment / no player-modeling** — greedy-nearest-step; treats the player like any faction | won't endure short-term pain for a big payoff; won't adapt to *how you* play | **DEFERRED (advanced)** — after the reactive + planner layers prove out |

*Rank for "makes it feel dumb": G1 (freezes in crisis, fixable now) → G2 (goldfish memory) → G6 (can't communicate intent). G3/G7 are partly-deliberate trade-offs, not oversights.*

---

## Design→Code Wiring Map & Socket Verification

**What this section is (folded in from the old `AI-BRAIN-BUILD-TRACKER.md`, 2026-07-13):** the **design→code bridge** — it takes every piece of the AI design suite and names its **code home (file:line)**, its **kind** (wire-only / new-logic / new-mechanic), and whether a **socket already exists** for it. Think of it as the wiring diagram behind the slice tables above: the tables track *what's built*; this maps *where each designed thing plugs in and whether the plug is real.* It was written 2026-07-10 as the plan-before-the-first-commit; most of it is now BUILT (see the slice tables above — that's the live authority). It's kept here as the historical wiring record + the still-load-bearing **§4 socket-verification sweep** (the "does every designed thing have a home?" audit). Most of what shipped is **flag-gated OFF by default** (order emission, diplomatic proposals, espionage, intel ledger) so a New Game is byte-identical until a flag is flipped — code exists and is wired, but is not live-in-a-default-game and its runtime is unverified here (CI can't run the client).

**Legend for the layer items below:** **✅ built (wire only)** · **🔧 new logic (no new mechanic)** · **🧱 new order/mechanic to build**. Where an item says "to fill" or "the stub," read it as a past-tense record of the plan — the slice tables above hold the current state.

### Headline — the hard infrastructure is ALREADY there

We are not building a nervous system; we're plugging a brain into one that exists. (Same finding as "The nervous system — ALREADY BUILT" table above; stated here as the three load-bearing claims the wiring-map opened with.)

- **The clock fires.** `NPCDecisionProcessor` is an `IHotloopProcessor`, runs **monthly per NPC faction**, and the `GlobalManager` is now iterated so it genuinely fires. ✅ **(BUILT since this was written:** its `Tick` body — once the empty `// TODO` — is now fully implemented at `Factions/NPCDecisionProcessor.cs:127`: it reads the strategic objective (`UpdateStrategicObjective`, :143), runs the galaxy-crisis check (`:157-161`), and emits orders (`EmitOrders`, :167). The order-emission and diplomatic-proposal arms are **flag-gated OFF by default** — `EnableOrderEmission`/`EnableDiplomaticProposals` default `false` — so a default game is byte-identical; the crisis check `EnableGalaxyCrisis` defaults `true`. Runtime behavior is unverified here.)
- **The event bus works.** `EventManager` / `EventType` (~250 kinds) is a live publish/subscribe bus, **proven** — `ResearchProcessor` already *reacts to events* (rebuilds on `TechDiscovered`) rather than polling, and every faction already subscribes to the full firehose via `FactionEventLog`. ✅
- **The order rail works.** `Game.OrderHandler.HandleOrder(cmd)` is headless, UI-independent, already driven from the background sim thread; plus the direct-call APIs (`FleetDoctrine`/`FleetEmcon`/`CombatEngagement`/`Diplomacy`/`Treaties`/`GroundForces`). ✅

**So the AI = mostly NEW LOGIC (the scoring/objective/transition engine) + a few NEW ORDERS (the tool punch-list) + WIRING (subscribe the brain to the events).** *(This was the plan; the bulk of that new logic has since been built and wired — see the slice tables. It ships flag-gated off by default.)*

### Layer-by-layer wiring (design concept → code home → status)

**A. The brain's clock & reactivity (the cadence pyramid)**
- **Monthly PLAN** → fill `NPCDecisionProcessor.Tick` (the stub). ✅ fires · ✅ **body now BUILT** (`Factions/NPCDecisionProcessor.cs:127`; order-emission arm flag-gated off).
- **Event REACT** → in `NPCDecisionProcessor.Init`, subscribe to the interrupt `EventType`s (`ShipDestroyed`, `NewHostileContact`, `PopulationBombarded`, `UnrestIncreased`, `TreatyAgreed`, hazard…) exactly as `ResearchProcessor.Init` already does → a fast "re-evaluate this seat" path alongside the monthly plan. 🔧 wire.
- **Publish the missing interrupt** → have `CombatEngagement.EnsureInCombat` emit a new `CombatStarted` `EventType` (today combat is a 5 s poll nobody subscribes to). 🧱 tiny.

**B. The data model (the AI's state)**
- **`PersonalityDB`** (12 traits + mood dict + drift-factor gauge) — a host-agnostic DataBlob on the **faction entity AND commander entities**. Mirrors `DoctrineVector` (a struct field on `FactionInfoDB`) + the `LegitimacyDB`/`ColonyMoraleDB` host-agnostic pattern. **Default 0.5-neutral** (the all-zero = all-extremes trap). Full spec in `AI-PERSONALITY-IMPLEMENTATION-SPEC.md`. 🔧 new. *(BUILT — `Factions/PersonalityDB.cs`, Phase 0.1; attached to `CommanderDB.Personality` in 2.7-attach.)*
- **`StrategicObjectiveDB`** (tiny) on the faction — the **goal slot** (the mandate the HoS writes, the delegates read). 🔧 new. *(BUILT — Phase 2.1.)*
- **The needs-tier** (fractal) — a small field per level (planet/system/faction), each read from its own aggregate. 🔧 new. *(REALIZED at the empire tier — `NeedsLadder`, Phase 2.2; the planet/system fractal is a later slice.)*
- **Load from scenario JSON** — mirror `FactionFactory`'s `DoctrineVector` parse for a `"personality"` + `"ambition"` node. 🔧 wire. *(BUILT — Phase 5.1a/5.1b.)*

**C. The eyes (perception → gauges)**
- **Readable now:** money (`FactionInfoDB.Money`/`Ledger`), fog-correct sensor contacts (`EntityManager.GetSensorContacts`), diplomacy (`DiplomacyDB`), per-fleet strength (`FleetCombat`). ✅
- **Cheap wiring:** a faction-strength rollup + a faction-economy snapshot (sum over `FactionInfoDB.Colonies`/fleets — no helper today). 🔧 *(BUILT — `FactionRollup`, F-A3/F-B1a.)*
- **Real build:** an **enemy-strength estimate** (fog-limited — contacts carry position/signal, not combat value) + target value/defense. 🧱 *(BUILT — `ThreatAssessment.DetectedStrengthOf` + `IntelAssessment`, F-B1b.)*

**D. The scorer (the ONE engine)**
- New `Factions/AIScoring.cs`: `Feature` enum + `IScoredOption` + `PersonalityWeights` map + `DecisionScorer` (feature-dot-product; `PickBest`/`PickWeighted`). **Pure, CI-testable.** 🔧 *(FOLDED — no standalone `DecisionScorer` class was built; the trait-weighted decision is realized directly in `NeedsLadder` + `ObjectiveSelector` + the Phase-1 personality wires. Revisit only if a shared scorer is needed for Phase 3+ — see Planck rung + Phase 0.2 above.)*
- **Reused by ALL selection** — objective pick, action-plan pick, stance pick, inter-faction stance. Build once. (The "one engine, no special cases" principle, in code.)
- **Blend** (`PersonalityBlend`, tenure-weighted) + **Drift** (`PersonalityDrift.ApplyEvent`, the `IdentityResistance` formula, the `EventKind→Pressure` table). 🔧 *(Blend/drift MATH built — `People/OfficerCharacter.cs`, Phase 2.7.)*

**E. The transition engine (objective → coordinated action)**
- **Tier advancement objectives** + the **action-plan catalog** (data-driven, the *situation-type × archetype* matrix). Plans **compose atomic leader-tasks** = the tool vocabulary (layer F). 🔧 + data. *(The tier→objective selection is `ObjectiveSelector` 2.4a; the action-plan catalog became the means-ends PLANNER — the six `IObjectiveResolver`s, Phase 2.8.)*
- **Phase-gates** — a short linear list, each gated by a completion gauge that **reuses the tier-reading gauges** (C). 🔧
- **Commit-and-hysteresis** (re-plan only on success / material blocker / big gauge-shift; action-momentum; smoothed reads; cost-of-reversal) — the anti-thrash logic. 🔧 *(BUILT — `ObjectiveTransition`, Phase 2.3.)*

**F. The hands (task → game action)**
- **Built + headless:** `Game.OrderHandler.HandleOrder` + `FleetDoctrine`/`FleetEmcon`/`CombatEngagement.OrderAttack`/`Diplomacy`/`Treaties`/`GroundForces`/`IndustryOrder2`/`CreateColonyOrder`/survey/`DeployStationOrder`/`PlaceInstallation*`. ✅
- **The tool PUNCH-LIST (new orders the plans need):** `SetColonyPolicyOrder` (tax + stockpile + specialization), `RefitOrder`, a `ThreatCondition : ICondition` (so reactive standing orders can say "enemy in range"), the empty `RefuelAction`/`ResupplyAction.Execute` bodies, scrap/mothball, `SetGovernmentOrder`, wrap the diplomacy static calls as `EntityCommand`s (for Advise-mode). 🧱

**G. The seats & delegates**
- **v1: the delegate is a PLAIN FUNCTION** the `Tick` calls (per design §6) — no seat formalism yet. Reads the objective, emits orders through F. 🔧
- **Later: the officer-in-a-seat model** — the `AdminSpace*` seats + `CommanderDB`, with the **seat-durability bug now FIXED** (✅ `AdminSpaceProcessor.ReconcileSeats` carries seats over by reference — see §4 keystone) + a `LeaderLost` handler + the competence generator + skill fields on `CommanderDB`. The **three-mode dial** (Delegate/Advise/Hand-fly) = a field on the seat record; **Advise** needs a `ProposedOrder` staging state. 🧱
- **Character-assignment (HR)** = a HoS best-fit matching function. 🔧

**H. The mandate/report protocol**
- **Two data slots per boundary** — a mandate the higher seat writes, a report the lower seat writes — read on each level's clock. **No message bus for v1.** The **fractal needs-tier IS the report/mandate content** (report "my tier" up; get tier-appropriate objectives down). 🔧

**I. The ecosystem & galaxy (EMERGENT — no new engine)**
- **Same scorer, rivals as inputs.** Perception = fog-correct sensor contacts + `DiplomacyDB` ✅; inter-faction stance = the scorer (D) 🔧. *(BUILT — the Ecosystem, Phase 3.)*
- **Reputation** = a fog-gated accumulator on `DiplomacyDB` (a 3rd party must *observe* the deed). 🔧
- **Alliances** = warming `DiplomacyDB` relationships (already drift) + a pact object. 🔧 *(BUILT — coalitions form/crack, Phase 3.4.)*
- **The crisis** = a faction crossing an **ascension-seed** threshold (a game-changing tech/discovery), responded to via the same ecosystem coalition machinery — **no galaxy-AI, no new engine.** ✅ **BUILT** (`Factions/GalaxyCrisis.cs` + the `tech-ascension`→`capability-ascension` chain in `techs.json`; `EnableGalaxyCrisis` defaults on — see §4 and Phase 4). Ongoing: richer ascension-seed content via the Site Engine.

### The build / wiring order (each slice CI-green before the next)

> **Reconciliation note (2026-07-13): this ordered plan is now largely EXECUTED.** Steps 1–4 and much of 5–7 are built and wired (the scorer, personality core, the eyes, the filled `Tick`, the reactive spine, many punch-list orders, the seat-durability fix, and the crisis/ecosystem layer). Read it as the *record of the plan that was followed*, and see the slice tables above for exactly which slices are done vs. still open and which are flag-gated off. Most shipped slices are gated OFF by default; runtime is unverified here.

1. **Personality Core** (D: data + scorer + drift, pure, CI-testable) — the whole trait system *provable before any live wiring*. The foundation.
2. **The eyes** (C: the faction rollups + a rough enemy-strength read).
3. **Fill `NPCDecisionProcessor.Tick`** (A/B/E monthly plan): read tier → pick objective → write the goal-slot → a plain military delegate emits orders. **First visible AI** (Mars builds + attacks) in the Earth-Mars-alien scenario.
4. **The reactive spine** (A: subscribe to events + publish `CombatStarted`) — the AI *reacts*, not just plans.
5. **The tool punch-list** (F) — build each order as the archetype plans need it.
6. **The seat/officer formalism** (G: durability fix + skill fields + the three-mode dial) — later.
7. **The ecosystem layer** (I: reputation, alliances, the reading game) + **the crisis** — later.

### The three wiring principles

- **One engine, reused.** Build the scorer (D) once; objective / action-plan / stance / ecosystem selection all call it. No per-domain AI code.
- **De-risk by structure.** Push logic into the **CI-tested engine**, never the client; the trait/scoring/drift core is *pure and provable first* (the whole demonstration gate green before anything touches the live loop). The client only ever gets a thin readout.
- **Data-driven.** Traits, ambitions, action-plans, ascension-seeds are **authored JSON over a fixed engine** (mirror the base-mod pattern) — the engine is code, the content is data.

### §4 — SOCKET VERIFICATION — the "does every designed thing have a home?" sweep (3-agent adversarial pass, 2026-07-10)

**Bottom line: NO core design gap — nothing in the AI machinery (traits · scorer · objectives · command · the 21-seat roster) is homeless.** Every element is socket-exists or a clean must-build with a *proven* home. The adversarial sweep caught **two load-bearing NO-SOCKETs** (core mechanics the design leans on — NOT dark pillars) and **one keystone hostile-socket**. **Reconciliation (2026-07-13): of those three, the keystone (seat durability) and the crisis-ascension breakthrough have since been BUILT (marked ✅ below); the fleet-commander situational read is now built too (`ThreatAssessment` + `CombatRisk`, Phase 2.6 — the original "still open" note is superseded).** Verified `file:line` by three agents (original), re-verified against current source (reconciliation).

**🔴 The REAL gaps — all three now built**

1. **The fleet commander's SITUATIONAL READ — was NO-SOCKET, and it's load-bearing.** This is bigger than "how strong is *that* enemy fleet" (developer's framing, 2026-07-11): a commander doesn't stare at one contact — it **synthesizes everything it can see** (its own force, the *estimated* enemy force, whether escape is even possible, what's at stake behind it) into one of a handful of decisions — ***"I can take him"* · *"nevermind — run silent, opposite heading"* · *"we lost everything but we're going out swinging"* (a last stand).** That's a **scored engagement decision**, and it's exactly where personality bites: the last-stand vs. the retreat is the *same* board read filtered through **Honor / Zealotry / Risk** (a Klingon swings; a Ferengi runs). So the socket is two layers:
   - *The gauge (the missing input):* an **enemy-strength ESTIMATE**, fog-limited. `EntityManager.GetSensorContacts` (`Entities/EntityManager.cs:638`) returns **position/signal, not combat value** (`ShipCombatValueDB` lives on your OWN ships). **Build a `ThreatEstimate`** — infer strength from contact count/signal/known designs, degraded by fog (a dark contact you can't size is *itself* a decision — engage-to-identify vs. avoid). *(BUILT — `ThreatAssessment.DetectedStrengthOf`, F-B1b.)*
   - *The decision (what reads the gauge):* an **engagement scorer** that weighs own-strength ÷ threat-estimate × escape-viability × strategic-value-of-holding, **through the seated commander's traits**, and emits fight / disengage / last-stand. Reuses the same `DecisionScorer` as everything else (`AI-PERSONALITY-IMPLEMENTATION-SPEC §3`) — one engine, combat-flavored features. *(BUILT — `CombatRisk.WouldEngage` scales the own-vs-enemy bar by the Risk trait, Phase 2.6; the fuller escape-viability × strategic-value last-stand scorer is a later refinement.)*
   *Cannot be hand-waved* — with **no rubber-band** to forgive a miss, a bad read is an unrecoverable snowball loss; this is "the ONE thing we must get right" (`AI-EMERGENT-POLITICS-AND-CRISIS-DESIGN` Part 1 §1) at the tactical scale.
2. **The CRISIS ascension-seed — was NO-SOCKET on BOTH halves; ✅ the breakthrough half is now BUILT (2026-07-13).**
   - *Activation (partial):* the explore→discover→reward pipeline now has a home — the **Site Engine** (`GameEngine/Sites/`; SE-1 done, SE-2+ ahead per `Sites/CLAUDE.md`) is the located-episode/exploration-content system that fills what was called the "unbuilt exploration-content system." Demo sites are **flag-gated OFF** (`SiteScenario.AutoSpawnSites` default `false`) so a New Game is byte-identical. `ServeyAnomalyAction` still exists (`Fleets/ServeyAnomalyAction.cs`); the discovery-content authoring is the ongoing SE-6 work.
   - *The breakthrough itself — ✅ BUILT.* The "a capability, not a component" tech concept the original plan called for now exists: `Factions/GalaxyCrisis.cs` defines `AscensionCapability = "capability-ascension"` and a faction that `HasCapability` it (`:42`) is the galaxy crisis; the `tech-ascension` "Transcendence" research in `GameData/basemod/TemplateFiles/techs.json` grants that capability (a tech that grants a capability rather than unlocking a component — `Factions/FactionDataStore.cs:48`). The crisis is LIVE by default (`NPCDecisionProcessor.EnableGalaxyCrisis` defaults `true`); when a faction ascends, `GalaxyCrisis.FormCoalitionAgainstAscendant` (`:60`, called from `NPCDecisionProcessor.cs:161`) declares a coalition war. Runtime behavior is unverified here (CI can't run the client). *(See Phase 4 above.)*

**⚠ The KEYSTONE hostile-socket — ✅ NOW FIXED** (was: fix FIRST before ANY seat/officer work; developer: "paramount, or else nothing can be built," 2026-07-11)
**Seat durability — BUILT (2026-07-13).** The bug (`CalcEntityAdminSpace` doing `CommanderSeats = new List<>()` every pass, silently wiping any field on a seat) is fixed: `People/AdminSpaceProcessor.cs:47` now calls `ReconcileSeats(adminSpaceDB.CommanderSeats, current)` (`:57`), which **carries over each existing seat — and the commander sitting in it — BY REFERENCE so a seated commander survives the recalc** (doc-comment `:52-55`); a seat whose component is gone is dropped and its occupant's `CommanderDB.AssignedTo` is cleared (the decapitation grave-rung). Adding a field to the seat record is now safe. This unblocks the whole officer/seat delegate layer. *(Original text preserved for context: the plain-class `AdminSpaceAbilityState` and the two smaller hostile sockets — `InstallComponentInstanceOrder.Clone()` and `ServeyAnomalyAction` — should be re-verified against current source before relying on them.)*

**🟡 MUST-BUILD (clean home, just write it) — the expected new logic + the punch-list**
The whole scorer/`PersonalityDB`/objective/tier/mood/ambition core (none exists — the expected new logic; homes proven) · the goal slot (`StrategicObjectiveDB`) + mandate/report slots (proven attach-blob-to-faction pattern) · `CombatStarted` event + publish at `CombatEngagement.cs:517` · faction rollups (`FactionEconomySnapshot`/`FactionStrengthRollup` — per-unit sources exist) · reputation accumulator (clean home on `RelationshipState`) + observation-propagation · alliance-DEPTH accumulator (shared-adversity deeds; drift today reads only militarism/treaties) · the punch-list orders (`SetColonyPolicyOrder` — tax + stockpile MUST-BUILD, **specialization = NO-SOCKET/no substrate**; `RefitOrder`; `ThreatCondition`; the empty refuel/resupply `Execute` bodies; `SetGovernmentOrder`; wrap-diplomacy-statics-as-orders for Advise mode; the ground **load→move→land** order — the Generals can't project force between worlds without it) · the `AssignedOn` tenure date.

**⚫ DARK PILLARS — owner assigned, WHOLE mechanic must be built (each has its own design doc)**
Espionage (Spymaster/Agent — zero code, `ESPIONAGE-AND-INTELLIGENCE-DESIGN.md`) · bloc-demands (Interior Minister — no engine, `GOVERNMENT-AND-POLITICS-DESIGN.md`) · trade-money (Trade Minister — no `Trade` `Ledger` category, `RESOURCES-AND-MATERIALS-DESIGN.md`). Not homeless-in-design — the seats correctly own decisions over systems that don't exist yet. *(Reconciliation 2026-07-13: all three dark pillars have since been BUILT as CI-green Movement-I foundations — espionage core + live agent E1–E6, popular-demands F-C2, trade-money F-C1 — see the Movement I tables above.)*

**🟢 SOCKET-EXISTS (confirmed real)**
`PersonalityDB` homes (faction `FactionInfoDB.cs:125` + officer `CommanderDB.cs:10`) · the `ModifiableValue` trait→number wire (`ResearchProcessor.cs:246`, with per-id `RemoveModifier`) · the event bus + all six named interrupt EventTypes · scenario-JSON parse (`FactionFactory.cs:71-81`) + the moddable-catalog pipeline (`combatDoctrines.json` pattern) · the gauge sources (`Ledger` / `ColonyMoraleDB` / `LegitimacyDB` / `DiplomacyDB` / fog-correct `GetSensorContacts`) · the order rail + most seat levers · alliance warming→pact (already drifting, `RunDiplomaticDrift:97`) · the emergent arc (no socket needed by design).

**Spec correction (applied)**
`AI-PERSONALITY-IMPLEMENTATION-SPEC.md §5` claimed the officer tenure fields (`CommissionedOn`/`RankedOn`) are "never set." **FALSE** — they're stamped in four creation paths (`DefaultStartFactory.cs:284`, `FactionFactory.cs:238`, `ColonyFactory.cs:169`, `NavalAcademyProcessor.cs:24`). The only real tenure gap is the missing `AssignedOn` date.

*Companions to this wiring map: `AI-PERSONALITY-IMPLEMENTATION-SPEC.md` (the trait core, code-ready), `GOVERNANCE-AND-DELEGATION-DESIGN.md` (roster + parity + completeness punch-list), and the design docs `AI-COMMAND-AND-COMMUNICATION` / `AI-DECISION-ENGINE-DESIGN` (objective+planner) / `AI-EMERGENT-POLITICS-AND-CRISIS-DESIGN` (ecosystem+galaxy+authoring). This section is the bridge from those to the code — written as the plan before the first commit, kept as the historical wiring record now that the brain is largely built. For live build status, the slice tables above this section are the authority.*

---

## How this tracker stays true

1. **Build a slice → flip its row** (⚫ NOT-STARTED → 🟡 WIRED-CI-GREEN → 🟢 verified-live), record the gauge reading, in the same commit as the code.
2. **A slice isn't done until CI is green** (both `test` + `build-client`), byte-identity held where claimed.
3. **Correct the design docs as you build:** the surveys found stale line numbers (`ShouldRetreat` is `:1401` not `:1158`; `AreHostile/AtPeace` `:1504/1529` not `:1271`; `FirstContact.OnDetection` `:40` not `:57`) and stale "unbuilt" claims (durable seats, commander competence). Fix the doc rung you're wiring in the same commit.
4. **Bottom-up or it doesn't work:** never start an Ecosystem/Galaxy/Brane slice before the Organism rung it composes is green.
5. **Foundations before the brain:** a Movement-II slice may not start until the Movement-I foundation in its "Unblocks" chain is green — that's the whole point of leveling the ground first. When you finish a foundation, flip its row AND note which Movement-II slices it just unblocked.

**Status legend:** ⚫ NOT-STARTED · 🟡 WIRED (CI-green, engine-verified) · 🟢 VERIFIED-LIVE (the developer's local runtime) · ⏸ BLOCKED (name the blocker) · 🔧 needs a named prereq first.
